# S17 context

Base: `6e85eeec2686c59ae3c0d9e7dcb0baf2d7f2dcd8`, the delivered S16b merge through PR #34. S16b PR/main CI each passed six jobs, its required Aura proof passed, and its public delivery receipt supersedes its pre-PR record capture.

Branch: `slice/s17-interactive-graph-view`. The four unrelated S14 hygiene WIP files remain in the primary worktree and are excluded. Prior deliveries, histories, old projections and Aura projection `GRAPH-SAU-2026-09-12-fbc93afb3c6e` remain preserved.

S17 extends the existing fixed graph API and GenUI registry with custom SVG, native keyboard controls and stored-evidence drill-down. It does not add S18 Executive Mode, a new graph library, a new backend evidence endpoint, acquisitions or route calculations.

The current governed route-blocking views are empty; the UI must say so honestly. Nonempty blocker demonstration is explicitly test-only, simulated and Class D. S17 live proof uses the owned loopback mirror, not an unapproved Aura refresh.
