# S17 approved plan record

Approved complete plan body: `421515e85aedc027d6b2d06bff3215dafe473ffd1e5c7a573bda564c100f84a5`, S17 PLAN v1 amendment 1. This tracked record summarizes its project-specific obligations; the exact body and approval are retained in the established local workflow records. `S17-DEP-AM1` is the subsequently reviewed narrow serializer exception.

## Scope and interfaces

- Append `graph_view` after `decision_actions`, preserving manifest version 1.0 and prior descriptors. Props are the analysis opportunity ID and evidence mode.
- Collapsed initially, with no graph requests until opened. Expose only adjacency, route blocking, shared enabler and evidence-to-change views.
- Deterministic custom SVG, bilingual labels, native keyboard controls, usable pointer targets and explicit synthetic/derived status; no CDN, force layout, pan/zoom system or additional library.
- Bind asynchronous graph/evidence results to opportunity, mode, view, epoch and selection identity. Clear stale content synchronously and preserve keyboard focus without stealing it from outside the graph.
- Resolve node/edge references only against stored evidence records through existing analysis endpoints for visible portfolio products. Missing fields/references remain unavailable; document addresses are reference text, not file-serving paths.
- Preserve the live API's typed unavailable boundary. Fixture-backed UI proof never becomes a production fallback.

## Authority and ownership

UI catalogue 1.4.0 advances additively to 1.5.0; exact prior bytes are retained. Core 02/03/09 map the change under ADR-027. Thresholds, narratives, sector/evidence policies, current scenarios, public/golden data and numerical outcomes remain frozen.

The bounded dependency exception permits only evidence-support edge branch filtering and node-plus-edge disclosure aggregation in `graph/service.py`; shared-enabler cross-scenario edges and query/API shapes remain unchanged.

The implementer owns the 58 enumerated manual source/test/document paths. The owner retains history and inert-provisions the 18 reviewed new manual files. All ten slice records and `.workflow/state.json` are OWNER_ONLY; inert provisioning is not Sol native-creation evidence.

## Required proof and sequence

1. Meaningful source RED/GREEN, default-dev import/collection negatives and complete dependent contracts.
2. Source/Core/catalogue completion, then one graph and one product-manifest generation with literal identity binding.
3. Functional browser proof; real loopback graph-UI proof in the existing graph gate before shutdown; stopped-service checks. Keep six hosted CI jobs.
4. Exactly 16 added graph captures across four views, two locales and two viewports, retaining the existing 96 paths. Preserve 600 KiB/file, 16 MiB total, RGB/lossless encoding and existing comparison tolerances. Measure preflight; do not assume capacity.
5. One canonical visual allocation, reviewed actual differences, narrowly bound visual pins, exact clean-candidate integrated and portability checks.
6. Independent implementation review, correction/re-review only as needed, separate owner acceptance, then sequential PR/exact-head CI/merge/main verification and receipt.

No optional comparison framework, fixture expectation laundering, repeated unchanged stale-input suite, Aura clear or unapproved source expansion is authorized. Valid evidence and closed findings are reused; remaining proof is not claimed in advance.
