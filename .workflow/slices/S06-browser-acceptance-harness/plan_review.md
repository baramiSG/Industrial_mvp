# Supervisor Plan Review — S06 Real-Browser Acceptance Harness

Reviewer: Supervisor (Claude, `claude-fable-5-1-thinking-max`). Planner: agent e60f0b0d (GPT-5.6 Sol, `gpt-5.6-sol-max`). Plan read: `plan.md` §1–§28 (1,039 lines) against `SLICE_GRAPH.md` S06 (approved with amendments), owner ruling R-4, Core 01 §5/§7, Core 09 §2.7/Gate G, UX spec §7/§9/§10/§11, ADR-004/007/010, the v0.2.0 frontend and toolchain files I had read in the ritual.

## Verification performed (2026-09-02)

- Scope: the plan builds only the browser gate, its tooling, CI job, Make targets, documentary reference set, tests and documentation; UI edits are conditional on an observed defect with paired tests (§6.3). No governed artifact is touched; no manifest generator runs. Consistent with S06 scope and ADR-010.
- R-4: references are stored under the slice record, never compared, with an explicit non-oracle contract and a static test forbidding `to_have_screenshot`/pixel-diff APIs (§7.8, §17.1). Consistent with the owner ruling.
- Failure hooks: console error, `pageerror`, `requestfailed`, app-origin HTTP ≥ 400, and any non-app HTTP(S) origin fail the journey; no exclusion API on ordinary journeys; a separate self-test proves all channels (§7.3, test #17). Meets A5.
- Determinism: bound-FD uvicorn launch (no port race), health-state polling with process poll, no sleeps, state-based waits, one context per node, no xdist (§7.1, §9.1, §13). Sound.
- Coverage: 17 tests / 62 nodes cover A1–A6 — both cases, both modes, every existing selector path, popup, clipboard, print media, PDF structure, Tab order over the 15 current controls with `:focus-visible` evidence, axe WCAG 2.1 A/AA with no exclusions, RTL direction plus fontconfig and canvas non-tofu proof, four viewports with overflow/actionability checks (§8). The 15-control inventory matches my own count from `index.html` and `app.js`.
- External pins independently verified by me: PyPI `playwright==1.62.0` (current latest, Apache-2.0, `>=3.10`), `pytest-playwright==0.9.0`; npm `axe-core@4.13.0` MPL-2.0 with the SRI quoted in §5.2; GitHub tags `actions/cache@v6.1.0` and `actions/upload-artifact@v7.0.1` exist and are the latest releases; `greenlet` (Playwright dependency) publishes cp314 wheels. The prohibited-file scanner's four secret regexes are specific enough that vendored `axe.min.js` is unlikely to trip them; the staged scan remains mandatory (§12).
- Existing-test impact: `test_ci_contract.py` extension keeps all legacy assertions and allows exactly one `if: ${{ failure() }}` step (§15.2); `test_packaging.py` unchanged (git-tracked packaging picks up new files); `final_acceptance.sh` copy made truthful with paired contract test (§17.1, §19).
- Records and traceability: V3-A1..A6 rows promoted no higher than `TESTED` before delivery; KL-22 closure provisional until merge; ADR-007 enumeration amended to five checks; CHANGELOG `Unreleased` (§19).

## Round 1 findings and rulings

| ID | Severity | Requirement | Evidence | Problem | Required correction |
|---|---|---|---|---|---|
| PR-01 | MEDIUM (RULING) | ADR-004 ("keep `pip install -e .[dev]` … functional and documented"; `pyproject.toml` remains the single dependency source); NFR-008; risk control for the uv 3.12/3.14 matrix | Plan §5, §14.1 put `playwright==1.62.0` and `pytest-playwright==0.9.0` into the `dev` extra; §15.1 legacy jobs then install a browser driver package they never use; the documented pip development path changes its dependency set. | Adding the browser toolchain to `dev` widens every existing install path (pip job, uv 3.12/3.14 jobs, operator `START_DEMO_WSL.sh`) for a gate only the browser job and `make e2e` run, and couples the legacy matrix to Playwright's wheel/driver availability. | Declare a separate optional extra `e2e = ["playwright==1.62.0", "pytest-playwright==0.9.0"]` and leave `dev` unchanged. The browser job syncs `uv sync --locked --extra dev --extra e2e`; `make e2e` and the e2e tail of `make ci` use `$(UV) run --locked --extra dev --extra e2e`; legacy jobs, the pip path and `UV_RUN` stay exactly as today. `uv lock` still resolves the `e2e` extra for all supported Python versions. Update §5.1, §14.1, §15.1, §16, §17.1 (`test_browser_harness_contract.py` asserts the pins under `[project.optional-dependencies].e2e`), §18 AC-01/AC-03, §19 (DEVELOPMENT_GUIDE install command), §24 (`uv sync --locked --extra dev --extra e2e --python 3.12`). |
| PR-02 | LOW (RULING) | §7.1 determinism/portability | Plan §7.1 launches `PYTHONPATH=src python -m uvicorn … --fd <fd>` with an allow-listed child environment (`PATH`, `HOME`, `LANG`, `PYTHONPATH`, `PYTHONUNBUFFERED`). | With `PATH` reduced and no interpreter path stated, "python" may resolve to a system interpreter without the project's packages. | State that the child is launched with `sys.executable` (the interpreter running pytest inside the uv environment) rather than a bare `python`; keep the allow-listed environment. One sentence in §7.1 and the corresponding unit test in §17.1/Task 3 (child command uses `sys.executable`). |
| PR-03 | LOW (OBSERVATION, no plan change beyond one sentence) | NFR-004 / UX spec §11 (offline demo, no CDN) | `index.html:48` links "API" to `/docs`; FastAPI's default Swagger UI loads its assets from a public CDN. Not in S06 scope (no S06 test navigates to `/docs`). | An offline Ministry room would show a broken API page if the link is clicked. The S06 external-origin blocker would fail any test that followed it, which is correct behaviour. | Add one sentence to §8/§9 stating the `/docs` link is focused by the keyboard test but deliberately never activated in S06, and to §21 that offline API-docs assets (or removal of the link) are carried to S07 as a candidate defect. Record the observation in `docs/KNOWN_LIMITATIONS.md` as a new open item for S07 (not accepted, scheduled). |

No BLOCKER or HIGH findings. Tooling family, viewport matrix, oracle boundary, failure channels and the TDD/characterization sequence are approved as planned.

## Round 2

Planner (same agent) revised `plan.md`. Supervisor verified in the text: PR-01 — `[project.optional-dependencies].e2e` holds the two pins, `dev` unchanged (§14.1 TOML at lines 485–494); `UV_RUN_E2E = $(UV) run --locked --extra dev --extra e2e` separate from `UV_RUN` (§16 line 611); browser job `uv sync --locked --extra dev --extra e2e --python "3.12"` (§15.1 line 558); contract test asserts the `e2e` pins and the unchanged two-entry `dev` (§17.1 lines 655–658, Task 1 line 815); §24 proof commands updated (lines 959–962). PR-02 — `sys.executable` child launch stated (§7.1 lines 204, 216) with the matching unit assertion (§17.1 line 660, Task 3 line 847). PR-03 — `/docs` link focused but never activated (§8 line 367, §9 line 401), non-goal (§21 line 791), new OPEN row KL-31 scheduled for S07 (§19 line 736).

Round 2 added one record-integrity correction (RI-01): §28 claimed "mechanically checked" Sanad totals (48/100/22/100/0) that did not match the file; the planner recounted with `rg -o '\[(VERIFIED|SPECIFIED|DERIVED|PROPOSED|OPEN)\]' … | sort | uniq -c` and corrected §28 to 48/106/22/141/0, which the Supervisor reproduced independently with `grep -o | wc -l`.

Local environment facts recorded for the Implementer: `fc-match :lang=ar` resolves to DejaVu Sans (Arabic coverage present locally); `sudo` requires interactive authentication, so `playwright install --with-deps` must not be used locally (plain `playwright install chromium` only; system libraries were previously validated for the cached Chromium); 702 GB free.

Unresolved findings: **0**.

## Decision

**PLAN_APPROVED** — 2026-09-02, Supervisor. Implementation may begin on `slice/S06-browser-acceptance-harness` from base `d338f5d9ed49457d3a595b2d1e6b4f0bb7683efc`, executing plan Tasks 0–10 in order (toolchain/CI/Make/vendor contract RED→GREEN; prerequisite checker; server/collector; journey characterization; dossier/clipboard/print/PDF; accessibility/keyboard/RTL; responsive matrix and the untouched v0.2.0 reference set captured BEFORE any UI fix; minimal recorded defect remediation only if observed; truthful CI/final-acceptance copy and documentation; full verification and handoff records). The candidate stays uncommitted; the Supervisor stages exact paths, reruns the scans and owns commit/push/PR/merge. No `config/**`, `data/**`, `docs/core/**`, DOCX, golden or hash change; no `build_manifests.py`.
