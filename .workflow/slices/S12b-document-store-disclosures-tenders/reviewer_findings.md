# Independent reviewer findings — S12b Document Store, Disclosures and Tenders

**Plan review:** `reviewer-grok` (cursor-grok-4.6-xhigh) — plan-1 (SHA-256 `c70e9065…`) **APPROVE**, zero findings, six advisories transferred to IAC-7…IAC-12 (`.autonomous-workflow/evidence/s12b-document-store-disclosures-tenders/plan-1-review.json`). A first dispatch correctly refused an intermediate, still-being-written copy of the plan on identity mismatch.

**Implementation review 1:** `reviewer-grok` on candidate `87084ed2…` (194 files; composer slot 3) — **REJECT**: S12B-IR3-F01 (HIGH) six SASO records store Arabic in visual (reversed) order while declaring `languages: ["en"]`, overstating the logical-order contract; S12B-IR3-F02 (HIGH) RunReport `requests_made` 27/21 presented as transport counts without naming the aggregate-count artefact; advisories A01–A07 (`implementation-review-slot-3.json`).

**Owner seat override (OR-3, 2026-09-12 07:04Z):** the owner directed that GPT-5.6 Sol perform the correction round and Fable review it. Disclosure carried in every later record: the slot-4 correction was authored by an `implementer-fable` session (same model as the later reviewer, different session); the final candidate was swept, corrected and re-owned by `implementer-sol` (slot 5), whose sweep found and fixed three further record-contract defects (per-page hash rule, dropped empty pages / re-indexed page numbers, empty `superseded_run_ids`) under OD-13.

**Implementation review 2:** Fable reviewer session (claude-fable-5-1-thinking-max, OR-3 seat) on candidate `94eb2f4e…` (196 files) — **REJECT**: S12B-IR5-F01 (MEDIUM) `HTML_TEXT_LAYER_STDLIB 1.0.0` deviated from the literal DD-5 algorithm (`td`/`th` boundaries, nested-block end boundary) and its test pinned no exact lines; S12B-IR5-F02 (LOW) DD-10 path-segment / `list_id` validation unimplemented; advisories A01–A10. The reviewer independently verified all twelve records (schema, DOCUMENT_ID_V1, raw hashes, pypdf physical page counts incl. 21/21 with seven empty pages retained, `sha256("\n".join(lines))`, sorted superseded runs, byte-exact reconstruction, complete passports, truthful languages, zero Arabic presentation forms), the four manifest runs and §11 mirror, every frozen path and cross-process determinism on real PDFs (`implementation-review-slot-5.json`).

**Implementation review 3 (final):** the same Fable reviewer session on candidate **`5258e7d4dbc144d940792dcd57981e29d6faa1a301056fb9b26be0a00f3f674b`** (196 files, base `cdfd4ba4…`) — **APPROVE, zero findings**. Verified: the delta from `94eb2f4e…` is exactly the eleven owner-named non-hashed files; F01 corrected to the literal DD-5 algorithm (independent parser identical on eight probes and the fixture; exact 35-line tuple incl. U+200F pinned; three hand-derived probe tuples pinned); F02 fails closed in store, pipeline and CLI with zero fetches on ten unsafe ids; advisories dispositioned per OD-14; `pytest -q` 1928 passed; plan gates [0], [1], [3]–[20], [22]–[26] + corrected [25] green; integrity, smoke (frozen outcomes exact), reconstruction (1 snapshot / 4 artifacts + 12 records / 12 artifacts); the twelve records byte-identical to those verified in review 2; no fifth manifest run. Two INFO advisories: S12B-IR5B-A11 (uniform segment validation inside `reconstruct_document`/`iter_records`, unreachable from the CLI) and S12B-IR5B-A12 (`a<br>b` vs `a<br/>b` void-element asymmetry under literal DD-5 — conformant; worth pinning). Record: `implementation-review-slot-5b.json`.

**Stated limits of the final review:** plan [2] (`uv sync`) and [21] (`make ci`, incl. the browser gates) were not run by the reviewer (prohibited for the read-only seat); the owner lead agent ran `make ci` on the identical tree with exit 0 (1928 tests; 118 functional + 4 visual) and hosted CI runs the full matrix on the PR head.
