# S13a plan — universe acquisition and screening engine (planner-fable, attempt 1)

Full plan: `.autonomous-workflow/plans/s13-public-universe-screening/cycle-1/plan-1-s13a.json` (schema as s12c; 18 DDs, 13 TDD steps, 23 verification commands, 12 ACs, 8 ODs). Decomposition: `…/cycle-1/decomposition-1.json` (two children confirmed; s13a → s13b). Base `81eac4f2` = origin/main; branch `slice/s13a-universe-acquisition-and-screening-engine`.

**Objective.** Replace the S11 `un_comtrade` contract with the official UN Comtrade v1 API under OR-4 (header `Ocp-Apim-Subscription-Key` from the env-var NAME `IOR_COMTRADE_SUBSCRIPTION_KEY`; value never stored/logged/tested), acquire the Saudi HS6 universe (2021–2024 × M,X × partner world; HS revision per row from `classificationCode`) and stage-2 partner detail for engine-emitted survivors in guarded windows, build the screening engine by reusing the governed rule/signal/exclusion functions, emit typed dispositions and five route-specific Pareto queues, write a hashed reconstructible screening snapshot under `data/screening/`, and deliver a mounted-ready `/api/screening` router — with every top-level `src/ior_mvp/*.py`, the visual-pinned configs and `browser_tests/**` byte-identical and both public goldens exact.

**Design in brief.** DD-1/2 official contract in acquisition_sources 1.3.0 with an optional `credential_header` (Bearer default unchanged). DD-3 the universe is COMPLETE only when the body is the JSON dataset with `count == rows`, no `error`, reporter 682, contract period/flow/partner, six-digit HS6, one classificationCode per unit, no duplicates (HTML landing pages never become a universe). DD-4 per-row HS revision; universe kind 1.3.0; partners kind untouched. DD-5/6 windows W0 (documentation observation: limits, terms, pagination, partner token; ZATCA endpoint — the stored ZATCA pages are verified `PageNotFound.aspx`), W1 universe (`MAX_REQUESTS=9`), W2 lexicographic survivor batches (`R2 FULL fired`; KL-40 closed), W3 ZATCA only if a documented endpoint exists; honest UNAVAILABLE is a complete outcome. DD-7 new `ior_mvp.screening` subpackage; runtime never imports `acquisition.transport`. DD-8/9/10 per-HS6 projection into the governed case shape; R0 tariff-tree validity (DEGRADED without tree), R1-F/R1-D, R2, R3, R4-D, R5, R9-S (S12c artifact + cited Class-C plant-family links + `product_families.v1.yaml`), R10, R11 plus a distinct export/import ratio warning; codes not prose; dispositions via `screen_candidate` with reason codes, non-formal `indicated_state` (SCREENED_OUT→REJECT; R3-only→MONITOR with named supplier-concentration trigger; material→INVESTIGATE), needs from the governed code vocabulary. DD-12 queues (a)–(e) with YAML parameters, multi-membership, Pareto ranks, no global rank; persistence-only candidates counted unqueued; §8.2(c) recorded NOT_CALCULABLE. DD-13 `SCREENING-SAU-<as_of>-<inputs_hash12>` write-once snapshot, standalone validator, reconstruction pass. DD-14/15 API contract on a test-composed app, NFR-005 performance, size budgets. DD-17/18 manifests, scripts, Core 01/02/03/04/05/07/09 v2, ADR-019, KLs, split record.

**Gates.** Byte-identical set (verification [8]), visual manifest valid without regeneration ([9]), goldens ([1][3][10][18]), reconstruction incl. screening pass ([4][12]), threshold-literal and prohibited-file scans ([6][7]), credential name-only scan ([15]), runtime import boundary ([16]), `make ci` ([19]); ADR-019 and Core text before exactly one `build_manifests.py` run with the §11 mirror.

**Owner-lead rulings requested.** OD-1 mount `/api/screening` in s13b (default) vs s13a with a visual regeneration; OD-2 KL-34 in s13b (default); OD-3 §8.2(c) greenfield queue NOT_CALCULABLE at screening; OD-5 stage-2 scope; OD-6 product-family memberships beyond 7210/3902; OD-7/OD-8 only if triggered (size budget; missing terms URL). OD-4 (R1 materiality floor) is a restated deferred owner policy value.

**Seats.** implementer-sol first (mechanical TDD steps T0–T12); reviewer-grok reviews; stop uncommitted for review; no commit before APPROVE.
