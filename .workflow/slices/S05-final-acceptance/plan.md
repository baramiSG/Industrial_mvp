# S05 Final Acceptance Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task under Supervisor checkpoints. The Implementer must not dispatch another subagent unless the Supervisor explicitly directs it. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce fresh, reproducible evidence that the complete governed MVP satisfies every applicable requirement, close the specifically carried S02–S04 residuals without changing domain authority, publish the final operating/deployment/development records, and prepare application release `0.2.0` for a different-model holistic review and Supervisor-only tag.

**Architecture:** Keep governed domain/data/config bytes unchanged except the unhashed `config/project.yaml` release identity field. Add one tracked, Linux-native `scripts/final_acceptance.sh` orchestrator that records every subprocess exit code, clean-install proof, Docker/runtime journey, timing, failure, reversal, scan, documentation, and packaging result in ignored logs and generates one sanitized `acceptance_results.md`. Small residual changes are test-first; static/API and live HTTP evidence are explicitly distinguished from browser evidence.

**Tech stack:** Bash, Python 3.11+ standard library, FastAPI/uvicorn, pytest/TestClient, PyYAML, `uv --locked --extra dev`, GNU Make, Node syntax check, curl, Docker, Git/GitHub Actions.

## Global constraints

- Data classification is `confidential_demo` (`config/project.yaml:1-7`). Only frozen public snapshots and explicit Class-D demo scenarios may be read. No secret value, credential, personal data, unrestricted client dataset, live industrial source, uploaded Ministry record, or sensitive log may be introduced or printed.
- Session persona is **Senior QA / Verification and Release Engineer**. The stance is skeptical requirement-by-requirement acceptance, release reproducibility, and evidence before claims (`.workflow/slices/S05-final-acceptance/persona.md:1-15`).
- Authority order remains methodology DOCX → frozen Core 01–09 → versioned configuration → hashed evidence → implementation/tests (`docs/authority/00_AUTHORITY_MANIFEST.md:16-27`). The extracted mirror is a search aid, not authority (`:133-136`).
- Preserve public steel `INVESTIGATE` and public polypropylene `REJECT`, steel simulated `ADVANCE` route 5, and PP simulated `REJECT` route 0 (`AGENTS.md:23-35`; Core 09 `docs/core/09_TEST_ACCEPTANCE_AND_GOLDEN_CASES.md:45-105`).
- Do not edit `config/**` except `config/project.yaml` `project.version`, or edit `data/**`, `docs/core/**`, either methodology file, any golden expectation, either manifest, or any authority hash. Do not run `scripts/build_manifests.py`; `config/project.yaml` is unhashed and S05 has no governed-byte change.
- Synthetic inputs remain Class D, `source=DEMO_GENERATOR`, explicitly flagged/scenario-linked/disclosed, and unable to mutate `real_decision`. Unknown states do not become zero/false and do not improve D*.
- Domain threshold values remain versioned YAML. The only literal `250 ms` in new code is the Core 01 NFR-005 acceptance bound in a test/harness, not a domain decision threshold.
- Release identity moves from `0.1.0` to `0.2.0` in `pyproject.toml`, `src/ior_mvp/__init__.py`, `config/project.yaml` `project.version`, `CHANGELOG.md`, and the root editable-package stanza regenerated by `uv lock`. `config/project.yaml` is unhashed; `as_of_date` and every other project field remain unchanged.
- `uv lock` runs without `--upgrade`. Its permitted diff is exactly the editable root package version `0.1.0` → `0.2.0`; no direct/transitive dependency name, version, URL, hash, marker, or extra may change.
- Test-first is mandatory for behavior changes. The list-route and recursive-scan tests must be observed RED for the intended reason before production edits. GenUI PP `{}`, exact zero-synthetic HTML, and cached-scenario immutability are characterization/proof tests and must not manufacture a false RED. NFR-005 is a measured acceptance test with no production tuning unless it genuinely fails.
- Every command uses the detached-script + complete-log-read protocol in §20. A command launch is not evidence; the full log, final sentinel, and each internal step code are read before any claim.
- The final reviewer uses a model different from the Implementer and Supervisor, reads the methodology mirror in full, reviews the entire repository holistically, and is read-only. Planner, Implementer, and Reviewer cannot approve, commit, push, merge, tag, or authorize public support.
- No self-approval. “Locally green,” “reviewer zero findings,” “PR CI green,” “merged,” “default-branch CI green,” `COMPLETE`, and “tagged” are separate evidence events.

---

## 1. Objective and acceptance boundary

S05 is an acceptance/release slice, not feature development. Execution must:

1. add regression coverage for the carried residuals and make only three implementation-preserving code edits: list-route 404 mapping, recursive threshold scanning, and removal of one dead local;
2. measure NFR-005 on every loaded analysis route with one excluded warm-up and a median of five local calls below 250 ms;
3. exercise Core 01 journeys A–E for both opportunities and both modes over a live HTTP server, including list, detail, UI manifest, dossier JSON, dossier HTML, extraction, threshold, and project contracts;
4. execute all owner-mandate §27 steps, marking migrations/backup/external integrations `NOT_APPLICABLE` only with repository evidence;
5. generate fresh local acceptance evidence with per-step codes, then undergo Supervisor review, different-model final review, remediation, and a complete rerun;
6. promote evidence honestly: current `IMPLEMENTED` rows become `TESTED` only after observed execution; current/new `TESTED` rows become `COMPLETE` only after the S05 PR is merged and its default-branch CI is green;
7. publish the §28 documents and version `0.2.0`; and
8. leave the Supervisor, not this worker, to merge and create annotated tag `v0.2.0`.

Browser interaction is not claimed. Gate G/TL-07 are accepted at live HTTP payload plus static frontend-contract level, with the absent Playwright/real-browser layer explicitly retained as KL-22.

## 2. Governing requirement IDs

### 2.1 Core 09 Gates A–H

| ID | Requirement | Governing lines | S05 proof |
|---|---|---|---|
| GATE-A | Methodology/core present, external machinery referenced, governed hashes pass | Core 09 `:177-184`; Manifest `:16-27`, `:122-130` | integrity in `make ci`, clean pip path, and final scan; protected-path diff empty |
| GATE-B | Snapshots/units/source/hashes valid; scenarios reconcile and back-test | Core 09 `:185-191`; Core 06 `:226-237` | `validate_scenarios.py` in both install paths and final post-reversal scan |
| GATE-C | Every R-rule visible; thresholds config-sourced; steel/PP expectations pass | Core 09 `:192-197` | golden/rule suites, live detail contracts, recursive literal scan |
| GATE-D | Effective capacity/unknown penalty/D* gates exact | Core 09 `:198-204` | capability unit/golden/live contracts |
| GATE-E | Unsupported economics first; S* minimal; national value/competition visible; PP gets no support | Core 09 `:205-211` | economics/golden/live contracts |
| GATE-F | Zero synthetic leakage; dual states; every synthetic row labelled | Core 09 `:212-217` | isolation/dossier/live contracts and exact disclosure assertions |
| GATE-G | Responsive frontend contract, case/mode controls, adaptive manifest, printable dossier, Arabic | Core 09 `:218-225` | static source tests + live API/dossier contracts only; browser interaction remains KL-22 |
| GATE-H | Authorized manifest policy, integrity, pytest, smoke, release evidence | Core 09 `:226-235` | no generator in S05; fresh `make ci`, clean pip gates, Docker, PR/default-branch CI |

### 2.2 Core 09 §7 packaged-MVP definition of done

These IDs are added to traceability in S05.

| ID | Requirement | Governing lines | S05 proof |
|---|---|---|---|
| DOD-01 | Starts from a clean Python environment with documented commands | Core 09 `:238-243` | `python3 -m venv --clear`, editable dev install, all gates, uvicorn |
| DOD-02 | No external key required | Core 09 `:241-244`; Core 01 NFR-004 | clean environment, no key read, offline runtime |
| DOD-03 | All tests pass | Core 09 `:242-245` | `make ci`, clean-venv pytest, final rerun, hosted CI |
| DOD-04 | Integrity passes | Core 09 `:242-245` | integrity in both paths and final scan |
| DOD-05 | Two public golden outcomes are exact | Core 09 `:244-247` | unit goldens + live HTTP states |
| DOD-06 | Steel simulated transition is exact and disclosed | Core 09 `:245-248` | live detail/manifest/dossier JSON/HTML |
| DOD-07 | PP simulation still rejects support | Core 09 `:246-249` | live detail/manifest/dossier |
| DOD-08 | Interface usable at desktop/tablet widths | Core 09 `:247-250` | static responsive/semantic contract only; real-browser scope accepted as KL-22 |
| DOD-09 | Dossier exists in JSON and printable HTML | Core 09 `:248-251` | four case/mode combinations over live HTTP |
| DOD-10 | Documentation and source are included in one zip | Core 09 `:249-251` | harness creates and validates a tracked/intended-file acceptance archive |

### 2.3 Core 01 §9 success criteria

These IDs are added to traceability in S05.

| ID | Observable success criterion | Governing lines | S05 proof |
|---|---|---|---|
| SC-01 | Refuses unsupported factory recommendation | Core 01 `:240-248` | PP public/simulated `REJECT`, route 0, support 0 |
| SC-02 | Public evidence yields meaningful decisions and exact evidence requests | Core 01 `:243-249` | steel public `INVESTIGATE`, `data_unlocks`, dossier |
| SC-03 | Ministry-shaped inputs change named gates rather than a generic score | Core 01 `:245-250` | dual state, capacity/D*/economics/rules and scenario ledger |
| SC-04 | Synthetic evidence is honest and controlled | Core 01 `:246-251` | Class D/source/flag/label/disclosure/zero leakage |
| SC-05 | Calculations, thresholds, and evidence are inspectable | Core 01 `:247-252` | threshold endpoint, rule ledger, authority/evidence objects |
| SC-06 | Output is a route and Decision Dossier, not a generic ranking | Core 01 `:248-253` | decision routes and both dossier formats |

### 2.4 Named S05 requirements

| ID | Requirement | Source | Proof |
|---|---|---|---|
| NFR-005 | Warm local analysis responses normally complete below 250 ms | Core 01 `:213-223` | `tests/test_performance.py` and live median-of-five timing record |
| TL-07 | Minimum frontend contract checks | Core 09 `:124-134` | prior/static suite plus exact Gate-G scope statement and KL-22 |
| BC-08 | Different-model final acceptance and final documents | `docs/REQUIREMENTS_TRACEABILITY.md:121`; owner mandate §27–28 | reviewer record, acceptance results, seven final documents |

## 3. Start-of-slice ritual, skills, branch, and data boundary

- Read: `AGENTS.md`; all three `.cursor/rules/*.mdc`; all of `docs/authority/00_AUTHORITY_MANIFEST.md`; methodology mirror headings plus §0–§1, §10–§12, §15–§16, Appendix B–C; Core 01, Core 03 §10, Core 06 §10, Core 09; build controls; README/release/build files; every implementation note; every script; all 13 package Python modules; all 16 current tests; all packaged static assets; state/S00–S04 completion and reviewer residual sections; S05 persona/context.
- `docs/project/` is absent. No project fact is imported from another engagement; facts come from this repository’s authority/core/config/control records and the owner mandate.
- Skills read and used:
  - `superpowers:writing-plans`: exact paths/interfaces/drafts, bite-sized TDD tasks, commands, expected outcomes, and self-audit;
  - `superpowers:test-driven-development`: RED→GREEN for behavior changes and honest characterization labels;
  - `superpowers:verification-before-completion`: fresh full command output before every success/completion claim.
- Observed branch: `slice/S05-final-acceptance`.
- Observed HEAD and merge-base with `main`: `98c1a40225a94090238867bc9861e8c6544b5839`.
- Observed pre-existing Supervisor-owned worktree entries:
  - modified `.workflow/state.json`;
  - modified `docs/BUILD_PROGRESS.md`;
  - modified `docs/KNOWN_LIMITATIONS.md`;
  - modified `docs/REQUIREMENTS_TRACEABILITY.md`;
  - untracked `.workflow/runs/s04_promote.py`;
  - untracked `.workflow/slices/S04-simulation-fidelity/completion.md`.
- The Implementer must preserve and inventory those entries. `.workflow/runs/s04_promote.py` is a transient Supervisor script and must not be staged. The S04 completion/control changes are inputs to S05 and remain Supervisor-owned until explicitly included.
- No planner command ran tests, mutated Git, contacted a live source, printed environment values, or regenerated an artifact.

## 4. Normative methodology-to-acceptance map

| Methodology statement | Mirror evidence | Requirement/traceability target | Acceptance evidence |
|---|---|---|---|
| Missing product-line facts remain unresolved; public cases demonstrate investigation and disciplined rejection | §0 `methodology_extracted.md:42-87` | FR-010, FR-053, INV-04/05/11, SC-01/02 | public goldens and live states/data unlocks |
| Investable object is Product × Specification × Application × Capability × Demand × Route; unresolved critical terms block advance | §1 `:94-117` | FR-010, FR-030–034, FR-053, NFR-003 | detail/dossier schema; public D* withheld |
| Brownfield before greenfield, no action first, uncertainty changes state | §1.1 `:120-143` | FR-054, INV-09, SC-01/03 | route 0/5 goldens; no route 7 |
| Decision states/routes retain their meanings | §1.2 `:145-165` | FR-052/054 | exact state/route/headline contracts |
| AI extracts/synthesizes only under schema; deterministic services calculate | §10 `:1002-1043` | FR-020–045, FR-060–063, INV-12 | unit formula/rule tests; approved UI manifest |
| Public support/overrides require accountable authority | §10.3 `:1044-1071` | INV-12, NFR-010 | read-only GET API; governance copy; no approval endpoint |
| Every conclusion has evidence passport, transformation, confidence, contradiction, approval boundary | §11 `:1075-1088` | FR-001, FR-011–015, NFR-001/002 | live evidence keys and retained contradiction |
| G0–G6 quality gates and time-based back-testing | §11.1–11.2 `:1090-1120` | Gates A–F, TL-01–09 | full suite/Gate B/goldens |
| Snapshot bundle stores raw/query/retrieval/hash/transformation/decision identity | §11.3 `:1122-1132` | FR-005, NFR-001, GATE-A/B/H | integrity, hashes, authority response |
| Approval/rejection is not a success label | §11.4 `:1134-1140` | explicit non-goal | no calibration/causal claim |
| End-to-end sequence freezes inputs, resolves identity, evaluates rules/capability/economics/EVSI, selects route, emits dossier | §12 `:1142-1247` | FR-001–072; journeys A–E | full live HTTP traversal and dossier |
| Dossier contains identity, demand/supply/gap/capability/economics/policy/evidence/kill condition | §15 `:1609-1625` | FR-052/064, DOD-09, SC-06 | JSON required-key assertions and printable HTML |
| Supporting pack remains reproducible | §15.1 `:1626-1649` | NFR-001/002, DOD-10 | acceptance archive and final report |
| Kill conditions are observable route-stop facts | §15.2 `:1650-1660` | FR-052 | non-empty/exact conditions and kill conditions |
| Outcome learning requires realized evidence and must not tune on approvals | §16 `:1662-1709` | accepted MVP exclusion | no calibration/outcome-learning implementation claimed |
| Thresholds are versioned, scoped defaults with sensitivity/back-test; no case-by-case mutation | Appendix B `:1737-1781` | FR-002/003, INV-07, TL-08 | config endpoint, boundaries, recursive source scan |
| Minimum dictionary requires evidence/status/version/decision fields | Appendix C `:1783-1806` | FR-010–015, FR-021/052 | HTTP contract assertions |

## 5. Owner-mandate §27 audit map

| Step | Audit action | Exact evidence owner/location |
|---:|---|---|
| 1 | Audit every normative requirement | §4 map; complete traceability diff; Supervisor/final reviewer |
| 2 | Audit requirements traceability | `docs/REQUIREMENTS_TRACEABILITY.md`; row/status audit in `acceptance_results.md` |
| 3 | Run complete test suite | `make ci` and clean-venv `pytest -q` step logs |
| 4 | Run complete local CI equivalent | fresh `make ci`, plus Docker runtime and clean pip path |
| 5 | Verify default-branch CI green | Supervisor `gh run view` before S05 and post-merge current SHA; `pr_record.md`/`completion.md` |
| 6 | Test clean install | isolated `python3 -m venv --clear` and `pip install -e ".[dev]"` |
| 7 | Test startup/restart | uvicorn port 8010 start→health→stop→new PID→health |
| 8 | Test migrations | `NOT_APPLICABLE`: architecture is local JSON/YAML, no DB/migration (`docs/core/03_SYSTEM_ARCHITECTURE.md:262-271`) |
| 9 | Test major user journeys | live HTTP Journey A–E matrix for both cases/modes |
| 10 | Test failure paths | 404, invalid-mode 422, SPA 200, TestClient evidence-integrity 422 |
| 11 | Test rollback/reversal | isolated-worktree `git revert --no-commit 98c1a40` then `git revert --abort`; S05 release rollback documented |
| 12 | Test privacy/security boundaries | prohibited/secret scan, no live/key paths, synthetic assertions, localhost binding |
| 13 | Test generated artifacts | dossier JSON/HTML, manifests/integrity, validated source zip |
| 14 | Test integrations | FastAPI/static/uvicorn/Docker local adapters pass; external integrations `NOT_APPLICABLE` by Core 01 exclusions |
| 15 | Test backup/restore | `NOT_APPLICABLE`: no mutable database/user state; immutable source/snapshots recover through Git/tag |
| 16 | Review documentation | harness required-file/content audit plus Supervisor/final reviewer |
| 17 | Search all named repository keywords | tracked/intended text scan, every hit and disposition table, unresolved hit fails |
| 18 | Different-model holistic final reviewer | S05 `reviewer_findings.md` using §17 briefing |
| 19 | Remediate valid findings | Implementer RED→GREEN cycle, implementation log/evidence |
| 20 | Run final acceptance again | second complete detached harness after fixes/review; no partial rerun |

## 6. Existing-state assessment: carried residuals

| Residual | Existing evidence | Planned disposition and proof |
|---|---|---|
| Threshold scanner exempts configured `1.0` and `3` | exempt set `scripts/check_threshold_literals.py:14`; S02 reviewer `reviewer_findings.md:199-203` | accept/document as state/formula convention; do not alter exemption semantics |
| Scanner uses top-level `glob("*.py")` | `scripts/check_threshold_literals.py:85-106`, specifically `:91`; S02 reviewer `:201` | RED nested-package test; change only `glob` → `rglob`; full scan |
| No PP GenUI `{}` concentration test | R3 default `{}` `src/ior_mvp/genui.py:7-15`, projected at `:43-50`; only steel assertion `tests/test_threshold_boundaries.py:320-329`; S02 reviewer `:202` | add passing PP characterization asserting disabled R3 and exact `{}` |
| R1-D `confidence_cap` is a literal string | config says `C` `config/thresholds.v1.yaml:17-24`; emitted literal `src/ior_mvp/rules.py:470-479`; S02 reviewer `:203` | retain behavior and record as KL-30; no domain/result edit |
| List route catches only evidence-integrity errors | `src/ior_mvp/app.py:77-84`; detail helper already catches `RepositoryError/ValueError` at `:42-54`; S03 reviewer `reviewer_findings.md:87` | RED missing-scenario list test → catch `(RepositoryError, ValueError)` and return 404 |
| Successful repository/config loads are process-sticky | `@lru_cache` `data_repository.py:29,46,58`; config `config.py:61-81`; S03 reviewer `:73` | record KL-29; operator restart instruction; no cache layer change |
| Public dossier HTML zero assertion is substring-weak | `tests/test_dossier_contract.py:15-38`, weak line `:37`; S03 reviewer `:93` | replace with exact rendered paragraph derived from JSON public count |
| Cached scenario is passed directly to `_simulate` | `decision_engine.py:727-744`, load at `:729`, simulation at `:740`; S04 reviewer `reviewer_findings.md:98` | add both-case characterization proving cached object identity/content unchanged after `analyze_simulated`; no extra production change |
| `_capacity_projection` has dead `has_equivalence` local | `decision_engine.py:369-372`; S04 reviewer `:99` | remove local; use direct `equivalence is not None` check; full simulation tests |
| FR-044 pointer names removed `_simulate_steel` | `docs/REQUIREMENTS_TRACEABILITY.md:39`; current implementation is `_simulation_economics` `decision_engine.py:460-626` and `_simulate` `:629-724`; S04 reviewer `:103` | correct pointer and cite S04/S05 evidence |
| GATE-H text still describes S01-only generator state | `docs/REQUIREMENTS_TRACEABILITY.md:108`; S04 reviewer `:104` | state authorized generator history S02–S04 and no-generator S05 policy |
| Browser proof remains static | Core 09 `:124-134`; `tests/test_static_frontend.py:8-199`; KL-22 current `docs/KNOWN_LIMITATIONS.md:28-34` | restate exact API/static scope; do not claim browser execution |
| No runtime data-root override exists | `config.py:11-16`; `data_repository.py:8,32,49,60`; repository search has no `IOR_DATA_DIR` | do not add one; live planted 422 is explicitly TestClient-level using `tests/test_api.py:72-113` |

Additional observations in prior reviewer files are either already closed by S02–S04, governed by KL-25–KL-28, private-helper-only paths prevalidated by Gate B, historical pre-merge status comments, or browser/UI consequences covered by KL-22. `docs/FINAL_BUILD_REPORT.md` must carry a residual-disposition subsection so none is silently forgotten.

## 7. Existing-state assessment: every current `IMPLEMENTED`/partial row

Each row below must move no higher than `TESTED` during the S05 branch. “Existing proof” names actual code/test lines; it is not a completion claim.

| ID (trace row) | Existing implementation/test evidence |
|---|---|
| FR-003 (`traceability:15`) | `capability.evaluate_capability` loads sector weights/hard gates (`capability.py:48-120`); simulated route test `test_capability_economics.py:47-54` |
| FR-005 (`:17`) | manifest verifier `verify_integrity.py:22-48`; manifest inventory test `test_integrity_contract.py:9-25` |
| FR-010 (`:18`) | repository keys by `opportunity.id` independent of HS (`data_repository.py:29-43`); list/detail goldens `test_golden_cases.py:7-100` |
| FR-011 (`:19`) | public records carry source/status/class/flag (`SAU-H0-721049.json:151-198`); validation test `test_synthetic_isolation.py:42-52` |
| FR-012 (`:20`) | fail-closed explicit-false check `evidence.py:21-28`; same test `test_synthetic_isolation.py:42-52` |
| FR-013 (`:21`) | policy validation/row projection `evidence.py:51-108,717-734`; label test `test_synthetic_isolation.py:64-79` |
| FR-014 (`:22`) | evidence row class/rendering `static/app.js:329-344`; `.synthetic-row` style `styles.css:251-257`; static warning test `test_static_frontend.py:24-28` |
| FR-015 (`:23`) | retained contradiction in steel snapshot `SAU-H0-721049.json:180-189`; analysis returns source evidence unchanged `decision_engine.py:93-99` |
| FR-022 (`:26`) | decomposition helpers/evaluation `rules.py:359-370,480-526`; exact worked-case test `test_rules.py:14-26` |
| FR-023 (`:27`) | degraded UV effect `rules.py:598-605`; anti-grade test `test_rules.py:101-107` |
| FR-024 (`:28`) | R9-S screen says do not publish D* `rules.py:679-687`; public golden `test_golden_cases.py:7-23` |
| FR-030 (`:30`) | effective-capacity formula `capability.py:7-18`; exact test `test_capability_economics.py:10-13` |
| FR-031 (`:31`) | profile YAML `sector_profiles.v1.yaml:1-41`; config-loaded route test `test_capability_economics.py:47-54` |
| FR-032 (`:32`) | K/U/λ calculation `capability.py:60-84`; unknown test `test_capability_economics.py:28-43` |
| FR-040 (`:35`) | unsupported values computed before support search `economics.py:43-84`; exact S* test `test_capability_economics.py:57-65` |
| FR-041 (`:36`) | deterministic NPV/IRR `economics.py:7-40`; exact S* test `test_capability_economics.py:57-65` |
| FR-042 (`:37`) | configured minimum-step search `economics.py:43-84`; exact S* test `test_capability_economics.py:57-65` |
| FR-043 (`:38`) | no-action-relative components `economics.py:87-107`; test `test_capability_economics.py:67-76` |
| FR-045 (`:40`) | EVSI formula `economics.py:111-125`; test `test_capability_economics.py:67-76` |
| FR-050 (`:41`) | public-only load/validation `decision_engine.py:65-107`; leakage test `test_synthetic_isolation.py:42-62` |
| FR-051 (`:42`) | fingerprint guard `evidence.py:699-714`; simulation applies it `decision_engine.py:737-767`; test `test_synthetic_isolation.py:55-62` |
| FR-053 (`:44`) | public selector emits only REJECT/INVESTIGATE `decision_engine.py:37-63`; public goldens `test_golden_cases.py:7-23,66-77` |
| FR-060 (`:46`) | manifest builder `genui.py:6-120`; approved-component test `test_api.py:29-46` |
| FR-061 (`:47`) | fixed component registry/guardrails `genui.py:17-119`; same approved-set test `test_api.py:29-46` |
| FR-062 (`:48`) | economics conditional `genui.py:68-79`; omission test `test_api.py:48-52` |
| FR-063 (`:49`) | no-executable-code guard and renderer map `genui.py:114-119`, `static/app.js:138-151`; API test `test_api.py:29-46` |
| FR-064 (`:50`) | dossier build/render/routes `dossier.py:7-160`, `app.py:102-117`; dossier tests `test_dossier_contract.py:13-72` |
| FR-070 (`:51`) | exact AR/EN spans `ai_extraction.py:17-49,54-81`; golden test `test_extraction.py:6-9` |
| FR-071 (`:52`) | standard/coating/dimension/environment fields `ai_extraction.py:26-47`; same golden test |
| FR-072 (`:53`) | 4/4 runner `ai_extraction.py:54-81`; exact test `test_extraction.py:6-9` |
| NFR-001 (`:60`) | hashed frozen repository plus integrity/goldens (`verify_integrity.py:6-48`; `test_golden_cases.py`) |
| NFR-002 (`:61`) | deterministic rules/capability/economics modules and formula tests (`test_rules.py`, `test_capability_economics.py`) |
| NFR-003 (`:62`) | publication conjunction `capability.py:20-33,92-120`; hard-gate test `test_capability_economics.py:15-25` |
| NFR-005 (`:64`, `IMPLEMENTED (unmeasured)`) | in-memory local API exists, but row explicitly has **no test**; S05 adds measured test/live evidence |
| NFR-006 (`:65`) | semantic native controls/select/labels `static/index.html:22-49,90-100`; static contract exists, browser keyboard run does not (KL-22) |
| NFR-007 (`:66`) | RTL markup `static/index.html:11,94,120-126`; test `test_static_frontend.py:8-15` |
| NFR-009 (`:68`) | domain `analyze` callable without web `decision_engine.py:770-776`; unit/golden callers |
| NFR-010 (`:69`) | package contains public/synthetic only; no write/upload route (`app.py:56-133`); leakage/prohibited tests |
| INV-01 (`:75`) | same public-only branch and leakage proof as FR-050 |
| INV-05 (`:79`) | public selector and D* hard gate `decision_engine.py:37-63`, `capability.py:20-33`; goldens |
| INV-06 (`:80`) | λ unknown penalty `capability.py:55-84`; unknown test `test_capability_economics.py:28-43` |
| INV-08 (`:82`) | R4-D no-grade effect/test `rules.py:598-605`; `test_rules.py:101-107` |
| INV-09 (`:83`) | route 0 before route 5 and no route 7 `decision_engine.py:662-685`; simulation fidelity controls `test_simulation_fidelity.py:311-416` |
| INV-11 (`:85`) | exact public goldens `test_golden_cases.py:7-23,66-77` |
| INV-12 (`:86`) | read-only GET adapter/no approval endpoint `app.py:56-133`; governance statement `static/index.html:129-154` |
| GATE-A (`:101`) | authority manifest/integrity implementation and tests named above |
| GATE-B (`:102`) | policy/reconciliation/back-test validator `validate_scenarios.py:99-219`; tests `test_scenario_validation.py:340-462` |
| GATE-D (`:104`) | capability implementation/tests named FR-030–034 |
| GATE-E (`:105`) | economics implementation/tests named FR-040–045 plus PP golden |
| GATE-F (`:106`) | evidence guard and exact isolation/dossier/API suites |
| GATE-G (`:107`, manual/partial scope) | static frontend and live adapters exist; no browser E2E; S05 records API/static proof and KL-22 |
| BC-03 (`:116`) | durable state/control/slice records exist (`.workflow/state.json`, `.workflow/slices/`) |
| BC-07 (`:120`) | traceability file exists and is maintained, but final row/status audit remains S05 |

Stale control statuses that are not `IMPLEMENTED` but must also be corrected: BC-01 is `PLANNED` despite S00 merge (`traceability:114`; `S00.../completion.md:1-6`), BC-05/BC-06 remain `PLANNED` despite four completed separated-model PR cycles (`traceability:118-119`), and BC-08 is `NOT_STARTED` (`:121`). FR-073 remains `NOT_APPLICABLE (production)` and must not be promoted.

## 8. Files and responsibilities

### Create

- `scripts/final_acceptance.sh` — sole acceptance runner; no companion helper file.
- `tests/test_performance.py` — median-of-five warm live-adapter/TestClient NFR-005 regression.
- `tests/test_final_acceptance_contract.py` — static contract for runner order, required commands/ports/results path, and no manifest generator.
- `docs/FINAL_BUILD_REPORT.md` — complete build/acceptance/release account.
- `docs/OPERATOR_RUNBOOK.md` — local/Docker operation, health/log/failure procedures.
- `docs/DEPLOYMENT_GUIDE.md` — WSL/Linux/Docker deployment and security boundary.
- `.workflow/slices/S05-final-acceptance/acceptance_results.md` — generated sanitized evidence; committed only after full log/results review.
- `.workflow/slices/S05-final-acceptance/implementation_log.md`, `test_evidence.md` — Implementer evidence, without approval claims.
- `.workflow/slices/S05-final-acceptance/implementation_review.md`, `plan_review.md`, `pr_record.md`, `completion.md` — Supervisor-owned records.
- `.workflow/slices/S05-final-acceptance/reviewer_findings.md` — different-model Reviewer-owned record.

### Modify test-first

- `tests/test_api.py` — list-route missing-scenario 404 and release health version.
- `tests/test_threshold_literals.py` — recursive nested-package scanner test.
- `tests/test_threshold_boundaries.py` — PP GenUI empty concentration characterization.
- `tests/test_dossier_contract.py` — exact public zero-synthetic HTML paragraph.
- `tests/test_synthetic_isolation.py` — cached scenario unchanged after simulation for both cases.
- `src/ior_mvp/app.py` — list-route `RepositoryError`/`ValueError` → 404.
- `scripts/check_threshold_literals.py` — `rglob("*.py")`.
- `src/ior_mvp/decision_engine.py` — remove unused `has_equivalence` local only.

### Modify release/documentation

- `pyproject.toml`, `src/ior_mvp/__init__.py`, `config/project.yaml`, `CHANGELOG.md` — package/API/project release identity `0.2.0`; in project config change only `project.version`.
- `uv.lock` — generated once by ordinary `uv lock`; root editable-package version only.
- `docs/DEVELOPMENT_GUIDE.md` — eight-gate order, Gate B, final harness, slice workflow, authority-change process, lock rule.
- `docs/KNOWN_LIMITATIONS.md` — final accepted KL-20–KL-30 table, including KL-22 scope, KL-29, KL-30.
- `docs/ARCHITECTURE_DECISIONS.md` — finalize ADR-004 status and append ADR-009.
- `docs/REQUIREMENTS_TRACEABILITY.md` — correct pointers, add DoD/success rows, promote only to observed status, exact Gate-G scope.
- `docs/BUILD_PROGRESS.md`, `.workflow/state.json` — Supervisor lifecycle facts only.

### Read/verify but do not modify

- `README.md`, `NOTICE.md`, `docs/implementation/**`, `.github/workflows/ci.yml`, `Makefile`, `Dockerfile`, `docker-compose.yml`, `START_DEMO_WSL.sh`, `scripts/package_project.sh`, all other `src/**` and tests.
- All `config/**` other than the single `config/project.yaml` `project.version` edit; all `data/**`, `docs/core/**`, `docs/authority/**`, including manifests, methodology DOCX/mirror, hashes, and every golden input/expectation.
- Prior slice records except already-present Supervisor bookkeeping.

### Never stage

- `.workflow/logs/**`, acceptance virtualenvs, generated acceptance zip, transient `.workflow/runs/**`, `.env`, `.venv`, caches, credentials, Windows sidecars, or any file not in the reviewed path list.

## 9. Exact residual-fix and release drafts

### 9.1 List-route 404 test — RED

Append after `test_missing_scenario_in_simulated_mode_returns_404` in `tests/test_api.py`:

```python
def test_missing_scenario_in_simulated_list_returns_404(
    monkeypatch: pytest.MonkeyPatch,
) -> None:
    monkeypatch.setattr(
        decision_engine,
        "get_synthetic_scenario",
        lambda opportunity_id: None,
    )

    response = TestClient(
        app,
        raise_server_exceptions=False,
    ).get("/api/opportunities?mode=simulated")

    assert response.status_code == 404
    assert response.json() == {
        "detail": (
            "No synthetic scenario is available for "
            "SAU-H0-721049"
        )
    }
```

Before the production change, this must fail with actual status 500, proving the missing list mapping. Do not change the expected status/message to fit the implementation.

### 9.2 List-route 404 implementation — GREEN

Replace only the exception block in `src/ior_mvp/app.py:81-84`:

```python
    except EvidenceIntegrityError as exc:
        raise _evidence_integrity_http_exception(exc) from exc
    except (RepositoryError, ValueError) as exc:
        raise HTTPException(
            status_code=404,
            detail=str(exc),
        ) from exc
```

This mirrors `_safe_analysis` without changing evidence-integrity 422 semantics. A configured list containing a public case with no simulation now returns 404 instead of an unhandled 500.

### 9.3 Recursive threshold scanner test — RED

Append before the repository-clean test in `tests/test_threshold_literals.py`:

```python
def test_scan_repository_recurses_into_python_subpackages(
    tmp_path: Path,
) -> None:
    source_dir = tmp_path / "src"
    nested = source_dir / "engine"
    nested.mkdir(parents=True)
    threshold_path = tmp_path / "thresholds.yaml"
    _write_thresholds(threshold_path)
    (nested / "probe.py").write_text(
        "result = value <= 0.40\n",
        encoding="utf-8",
    )

    findings, source_count, threshold_count = scan_repository(
        source_dir,
        threshold_path,
        root=tmp_path,
    )

    assert source_count == 1
    assert threshold_count == 1
    assert findings == [
        Finding(
            path="src/engine/probe.py",
            line=1,
            value=0.4,
        )
    ]
```

Current top-level `glob` must fail with `source_count == 0`; that is the intended RED.

### 9.4 Recursive scanner implementation — GREEN

Replace `scripts/check_threshold_literals.py:91` only:

```python
    source_paths = sorted(source_dir.rglob("*.py"))
```

All AST semantics, configured-number loading, `{0.0, 1.0, 2.0, 3.0}` exemptions, exits, and reporting remain unchanged.

### 9.5 PP GenUI characterization

Append after `test_metric_grid_receives_r3_threshold_metrics` in `tests/test_threshold_boundaries.py`:

```python
def test_pp_metric_grid_receives_empty_supplier_concentration() -> None:
    analysis = analyze("SAU-H0-390210", "public")
    r3 = next(
        row
        for row in analysis["rules"]
        if row["rule_id"] == "R3"
    )
    manifest = build_ui_manifest(analysis)
    metric_grid = next(
        component
        for component in manifest["components"]
        if component["type"] == "metric_grid"
    )

    assert r3["execution"] == "DISABLED"
    assert r3["metrics"] == {}
    assert metric_grid["props"]["supplier_concentration"] == {}
```

This should pass immediately against the existing safe default. It is characterization coverage, not a production fix.

### 9.6 Exact public zero-synthetic HTML assertion

In `tests/test_dossier_contract.py`, replace only the weak substring line:

```python
    expected_evidence_paragraph = (
        "<p>"
        f"{dossier['evidence_summary']['public_records']} "
        "public records; 0 synthetic records."
        "</p>"
    )
    assert expected_evidence_paragraph in html_response.text
```

Keep the JSON `synthetic_records == 0`, absent disclosure, and absent simulated-ledger assertions.

### 9.7 Cached-scenario immutability characterization

Append to `tests/test_synthetic_isolation.py`:

```python
@pytest.mark.parametrize(
    "opportunity_id",
    ["SAU-H0-721049", "SAU-H0-390210"],
)
def test_cached_scenario_is_unchanged_after_analyze_simulated(
    opportunity_id: str,
) -> None:
    cached = get_synthetic_scenario(opportunity_id)
    assert cached is not None
    before = deepcopy(cached)

    decision_engine.analyze_simulated(opportunity_id)

    assert get_synthetic_scenario(opportunity_id) is cached
    assert cached == before
```

The current code should pass. If it fails, stop and treat mutation as a correctness defect; do not normalize the expected object.

### 9.8 Remove the identified dead local

Replace `src/ior_mvp/decision_engine.py:369-372`:

```python
    equivalence = inputs.get("equivalence")
    if equivalence is not None and not isinstance(
        equivalence,
        dict,
    ):
        raise EvidenceIntegrityError(
            "scenario.synthetic_inputs.equivalence must be a mapping"
        )
```

The removed local is `has_equivalence`. Do not alter the subsequent `isinstance(equivalence, dict)` branch or selection semantics.

### 9.9 NFR-005 measured test

Create `tests/test_performance.py` exactly:

```python
from __future__ import annotations

from statistics import median
from time import perf_counter

import pytest
from fastapi.testclient import TestClient

from ior_mvp.app import app


client = TestClient(app)
WARM_MEASUREMENTS = 5
MAX_MEDIAN_MS = 250.0
OPPORTUNITY_IDS = (
    "SAU-H0-721049",
    "SAU-H0-390210",
)
MODES = ("public", "simulated")
ANALYSIS_PATHS = (
    *(
        f"/api/opportunities?mode={mode}"
        for mode in MODES
    ),
    *(
        f"/api/opportunities/{opportunity_id}{suffix}"
        f"?mode={mode}"
        for mode in MODES
        for opportunity_id in OPPORTUNITY_IDS
        for suffix in (
            "",
            "/ui-manifest",
            "/dossier",
            "/dossier.html",
        )
    ),
)


@pytest.mark.parametrize("path", ANALYSIS_PATHS)
def test_warm_analysis_endpoint_median_is_below_250_ms(
    path: str,
) -> None:
    warmup = client.get(path)
    assert warmup.status_code == 200

    elapsed_ms: list[float] = []
    for _ in range(WARM_MEASUREMENTS):
        started = perf_counter()
        response = client.get(path)
        elapsed_ms.append(
            (perf_counter() - started) * 1000
        )
        assert response.status_code == 200

    measured_median = median(elapsed_ms)
    assert measured_median < MAX_MEDIAN_MS, (
        f"{path} median {measured_median:.3f} ms "
        f"from samples {elapsed_ms!r} "
        f"must be below {MAX_MEDIAN_MS:.1f} ms"
    )
```

The one warm-up call is excluded. Five measured calls are recorded by pytest failure output and repeated over real localhost HTTP by the acceptance runner. Do not tune production merely to satisfy one noisy run; first reproduce a failure three times and identify the endpoint.

### 9.10 Release version RED/GREEN

Strengthen `tests/test_api.py::test_health` and add the cross-surface release identity test:

```python
def test_health() -> None:
    response = client.get("/api/health")
    assert response.status_code == 200
    payload = response.json()
    assert payload["status"] == "ok"
    assert payload["version"] == "0.2.0"


def test_release_version_matches_analysis_authority() -> None:
    health_response = client.get("/api/health")
    analysis_response = client.get(
        "/api/opportunities/SAU-H0-721049?mode=public"
    )

    assert health_response.status_code == 200
    assert analysis_response.status_code == 200
    assert (
        health_response.json()["version"]
        == analysis_response.json()["authority"]["project_version"]
        == "0.2.0"
    )
```

Observe RED (`0.1.0`) before editing release metadata. Then change:

```toml
[project]
name = "industrial-opportunity-resolution-mvp"
version = "0.2.0"
```

and:

```python
"""Industrial Opportunity Resolution Engine MVP."""

__version__ = "0.2.0"
```

and change only this field in `config/project.yaml`:

```yaml
project:
  version: "0.2.0"
```

Keep `as_of_date` and all other project fields unchanged. `config/project.yaml` is not listed in `authority_hashes.json`; after this unhashed edit, integrity must remain PASS and no hashed config may change.

Run ordinary lock regeneration:

```bash
"$HOME/.local/bin/uv" lock
```

The only allowed `uv.lock` hunk is:

```diff
 [[package]]
 name = "industrial-opportunity-resolution-mvp"
-version = "0.1.0"
+version = "0.2.0"
 source = { editable = "." }
```

If any other lock entry changes, stop and preserve the pre-run lock; do not hand-edit transitive entries or use `--upgrade`.

Prepend this exact release entry to `CHANGELOG.md` beneath `# Changelog`:

```markdown
## 0.2.0 — 2026-09-02

- Added multi-version GitHub CI and reproducible `uv`/pip/Docker quality gates.
- Moved decision thresholds to complete versioned governance with boundary and embedded-literal checks.
- Hardened synthetic evidence policy, public-marginal reconciliation, authority disclosure, and fail-closed HTTP behavior.
- Replaced opportunity-specific simulation dispatch with scenario-driven selection, planted-ground-truth back-testing, and labelled synthetic R6–R8 rows.
- Added final acceptance automation, warm-response performance proof, residual regression coverage, and release/operator/deployment documentation.
- Preserved the public steel `INVESTIGATE` and polypropylene generic-capacity `REJECT` golden outcomes.

```

## 10. Acceptance harness contract

### 10.1 One runner, artifacts, and exit policy

Create only `scripts/final_acceptance.sh`; do not create a Python helper beside it. Inline Python blocks perform JSON contracts, timings, keyword disposition, documentation/archive inspection, and Markdown rendering.

The runner declares and exports one release value:

```bash
readonly RELEASE_VERSION="0.2.0"
export RELEASE_VERSION
```

The `docker_health`, `uvicorn_health`, `restart_health`, Journey authority check, and results renderer all consume that value; none defines a second project/release version.

The runner writes:

```text
.workflow/logs/s05-final-acceptance/${RUN_ID}/steps.tsv
.workflow/logs/s05-final-acceptance/${RUN_ID}/${STEP}.log
.workflow/logs/s05-final-acceptance/${RUN_ID}/journeys.json
.workflow/logs/s05-final-acceptance/${RUN_ID}/timings.json
.workflow/logs/s05-final-acceptance/${RUN_ID}/keyword_dispositions.md
.workflow/logs/s05-final-acceptance/${RUN_ID}/source-acceptance.zip
.workflow/slices/S05-final-acceptance/acceptance_results.md
```

Every executable action has a row with stable ordinal, name, exact exit code, duration, and relative log path. A dependent action that cannot run is recorded `125`, never silently omitted. The script continues safe independent audits, performs cleanup in traps/finally paths, generates results even after failure, prints either `__FINAL_ACCEPTANCE__ status=0` or `__FINAL_ACCEPTANCE__ status=1`, and exits nonzero if any required row is nonzero or any keyword is unresolved.

The runner must never:

- call `scripts/build_manifests.py`;
- read/print secret environment values;
- mutate config/data/core/authority bytes;
- leave Docker containers, uvicorn processes, temp worktrees, or venvs outside ignored logs;
- use `|| true` to turn a required result green;
- claim browser, migration, external-integration, or backup execution.

### 10.2 Required step order and commands

| Order | Stable step | Exact action |
|---:|---|---|
| 01 | `preflight` | branch, HEAD, status, protected hash snapshot; accept S05 branch (and `main` only for Supervisor post-merge rerun) |
| 02 | `make_ci` | `make ci` |
| 03 | `clean_venv_create` | `python3 -m venv --clear "$LOG_DIR/pip-venv"` |
| 04 | `clean_pip_install` | `"$PIP_PYTHON" -m pip install -e ".[dev]"` |
| 05 | `clean_prohibited` | clean Python `scripts/check_prohibited_files.py` |
| 06 | `clean_thresholds` | clean Python `scripts/check_threshold_literals.py` |
| 07 | `clean_compile` | clean Python `-m compileall -q src scripts tests` |
| 08 | `clean_node` | `node --check src/ior_mvp/static/app.js` |
| 09 | `clean_integrity` | clean Python + `PYTHONPATH=src` `scripts/verify_integrity.py` |
| 10 | `clean_gate_b` | clean Python + `PYTHONPATH=src` `scripts/validate_scenarios.py` |
| 11 | `clean_pytest` | clean Python + `PYTHONPATH=src` `-m pytest -q` |
| 12 | `clean_smoke` | clean Python + `PYTHONPATH=src` `scripts/demo_smoke.py` |
| 13 | `docker_build` | `docker build --file Dockerfile --tag industrial-opportunity-resolution-mvp:s05-acceptance .` |
| 14 | `docker_run` | unique name, `docker run -d -p 8001:8000 ...` |
| 15 | `docker_health` | retrying `curl --fail --silent --show-error http://127.0.0.1:8001/api/health`; JSON `status=ok`, `version=0.2.0` using the same release value required from analysis authority |
| 16 | `docker_stop` | `docker stop "$CONTAINER_NAME"` |
| 17 | `docker_remove` | `docker rm "$CONTAINER_NAME"` |
| 18 | `uvicorn_start` | clean-venv Python `-m uvicorn ior_mvp.app:app --host 127.0.0.1 --port 8010` |
| 19 | `uvicorn_health` | retrying health; require `version=0.2.0`, matching analysis `authority.project_version`, plus the evidence boundary |
| 20 | `http_journeys` | §10.3 contracts |
| 21 | `http_nfr_005` | §10.4 one warm-up + median five for each analysis path |
| 22 | `http_failures` | unknown detail 404, invalid mode 422, unknown SPA path 200 |
| 23 | `integrity_422_testclient` | focused existing planted-scenario TestClient test; no live override invented |
| 24 | `uvicorn_stop` | terminate and reap first PID |
| 25 | `uvicorn_restart` | start a distinct second PID on 127.0.0.1:8010 |
| 26 | `restart_health` | health 200/version 0.2.0, matching analysis `authority.project_version` |
| 27 | `restart_stop` | terminate/reap second PID |
| 28 | `revert_worktree_add` | isolated temp worktree at current HEAD |
| 29 | `revert_apply` | in temp worktree: `git revert --no-commit 98c1a40225a94090238867bc9861e8c6544b5839` |
| 30 | `revert_diff_check` | `git diff --check` and no unmerged entries |
| 31 | `revert_abort` | `git revert --abort` |
| 32 | `revert_clean` | temp worktree `git status --porcelain` empty |
| 33 | `revert_worktree_remove` | remove/prune only the temp worktree |
| 34 | `final_prohibited` | fresh prohibited/secret scan |
| 35 | `final_thresholds` | fresh recursive threshold scan |
| 36 | `final_gate_b` | fresh scenario validation/back-tests |
| 37 | `keyword_scan` | §10.5 tracked + intended text and per-hit dispositions |
| 38 | `docs_audit` | all §28 docs/headings/content; no unsupported completion words |
| 39 | `source_archive` | build zip from tracked + intended, nonignored files |
| 40 | `archive_audit` | zip CRC/required files/no prohibited local artifacts |
| 41 | `protected_diff` | config/data/core/DOCX/manifests empty diff; goldens unchanged |
| 42 | `render_results` | generate sanitized Markdown and final status |

“Six gates + Gate B” on the pip path means the original prohibited/compile/Node/integrity/pytest/smoke gates, plus the threshold guard introduced in S02 and Gate B introduced in S03/S04. All eight are explicit above.

### 10.3 Exact live HTTP journey assertions

The inline Python block uses only `json`, `urllib.request`, `urllib.error`, `pathlib`, and `time`. It must implement these assertions:

```python
from __future__ import annotations

import json
import os
from pathlib import Path
from typing import Any
from urllib.request import urlopen


BASE_URL = "http://127.0.0.1:8010"
OUT = Path(os.environ["JOURNEY_SUMMARY"])
DISCLOSURE = "SIMULATED — NOT MINISTRY EVIDENCE"
RELEASE_VERSION = os.environ["RELEASE_VERSION"]
CASES = {
    "SAU-H0-721049": {
        "public": ("INVESTIGATE", "INVESTIGATE"),
        "simulated": ("INVESTIGATE", "ADVANCE"),
    },
    "SAU-H0-390210": {
        "public": ("REJECT", "REJECT"),
        "simulated": ("REJECT", "REJECT"),
    },
}
APPROVED_COMPONENTS = {
    "integrity_banner",
    "decision_hero",
    "metric_grid",
    "trade_chart",
    "rule_ledger",
    "capability_matrix",
    "economics_panel",
    "evidence_ledger",
    "data_unlocks",
    "decision_actions",
}
DOSSIER_KEYS = {
    "decision_headline",
    "decision_state",
    "route",
    "product_identity",
    "demand_conclusion",
    "supply_conclusion",
    "gap_diagnosis",
    "capability_route",
    "economics",
    "competition_policy",
    "evidence_summary",
    "conditions",
    "kill_conditions",
    "next_evidence_actions",
    "synthetic_disclosure",
}


def get(path: str) -> tuple[int, bytes, str]:
    with urlopen(BASE_URL + path, timeout=10) as response:
        return (
            response.status,
            response.read(),
            response.headers.get_content_type(),
        )


def get_json(path: str) -> dict[str, Any] | list[Any]:
    status, body, _ = get(path)
    assert status == 200, (path, status)
    return json.loads(body)


health = get_json("/api/health")
assert isinstance(health, dict)
assert health["status"] == "ok"
assert health["version"] == RELEASE_VERSION
assert health["evidence_boundary"] == (
    "public real decision / isolated synthetic simulation"
)

project = get_json("/api/project")
assert isinstance(project, dict)
assert project["project"]["id"] == "ior-mvp"
assert project["project"]["version"] == RELEASE_VERSION
assert project["project"]["modes"] == ["public", "simulated"]
assert {
    row["opportunity_id"]
    for row in project["golden_cases"]
} == set(CASES)

thresholds = get_json("/api/thresholds")
assert isinstance(thresholds, dict)
assert thresholds["metadata"]["version"] == "1.1.0"
assert thresholds["metadata"]["status"] == "frozen_for_demo_cycle"
assert "R11" in thresholds["rules"]
assert "route_bands" in thresholds["capability"]

extraction = get_json("/api/extraction-demo")
assert isinstance(extraction, dict)
assert extraction["passed"] == extraction["total"] == 4
assert extraction["accuracy"] == 1.0
assert all(row["passed"] is True for row in extraction["records"])
assert all(
    set(row["source_spans"]) == {"ar", "en"}
    for row in extraction["records"]
)

summary: dict[str, Any] = {
    "health": "PASS",
    "project": "PASS",
    "thresholds": "PASS",
    "extraction": "4/4",
    "cases": {},
}

for mode in ("public", "simulated"):
    listing = get_json(f"/api/opportunities?mode={mode}")
    assert isinstance(listing, list)
    assert {row["id"] for row in listing} == set(CASES)
    assert all(row["mode"] == mode for row in listing)
    for row in listing:
        expected_real, expected_active = CASES[row["id"]][mode]
        assert row["real_state"] == expected_real
        assert row["active_state"] == expected_active

    for opportunity_id, expectations in CASES.items():
        expected_real, expected_active = expectations[mode]
        prefix = f"/api/opportunities/{opportunity_id}"
        detail = get_json(f"{prefix}?mode={mode}")
        assert isinstance(detail, dict)
        assert detail["opportunity"]["id"] == opportunity_id
        assert detail["mode"] == mode
        assert detail["real_decision"]["state"] == expected_real
        assert detail["active_decision"]["state"] == expected_active
        assert detail["authority"]["methodology"]["sha256"]
        assert len(
            detail["authority"]["methodology"]["sha256"]
        ) == 64
        assert (
            health["version"]
            == detail["authority"]["project_version"]
            == project["project"]["version"]
            == RELEASE_VERSION
        )
        assert detail["snapshot_id"]
        assert detail["as_of_date"]
        assert detail["rules"]
        assert all(
            {
                "rule_id",
                "execution",
                "fired",
                "result",
                "metrics",
                "decision_effect",
            } <= set(row)
            for row in detail["rules"]
        )
        public_rows = [
            row
            for row in detail["evidence"]
            if row["synthetic_flag"] is False
        ]
        synthetic_rows = [
            row
            for row in detail["evidence"]
            if row["synthetic_flag"] is True
        ]
        assert public_rows
        assert all(
            {
                "source",
                "status",
                "evidence_class",
                "synthetic_flag",
            } <= set(row)
            for row in public_rows
        )
        if opportunity_id == "SAU-H0-721049":
            assert any(
                row.get("contradiction")
                for row in public_rows
            )

        if mode == "public":
            assert detail["simulation_decision"] is None
            assert synthetic_rows == []
            assert detail["synthetic_inputs_used"] == []
            assert detail["integrity"][
                "real_decision_uses_public_only"
            ] is True
        else:
            assert detail["simulation_decision"] is not None
            assert synthetic_rows
            assert all(
                row["source"] == "DEMO_GENERATOR"
                and row["evidence_class"] == "D"
                and row["display_label"] == DISCLOSURE
                for row in synthetic_rows
            )
            assert detail["simulation_scenario"][
                "display_label"
            ] == DISCLOSURE
            assert detail["integrity"][
                "real_decision_unchanged_after_simulation"
            ] is True
            assert detail["integrity"][
                "ground_truth_backtest"
            ]["match"] is True

        if opportunity_id == "SAU-H0-721049":
            assert detail["real_decision"]["state"] == "INVESTIGATE"
            assert detail["real_decision"]["route_code"] is None
            if mode == "public":
                assert detail["capability"]["d_star"] is None
                assert detail["economics"] is None
                assert detail["data_unlocks"]
            else:
                assert detail["active_decision"]["route_code"] == 5
                assert detail["capacity"][
                    "effective_qualified_capacity_kt"
                ] == 57.509
                assert detail["capacity"][
                    "specification_adjusted_gap_kt"
                ] == 46.491
                assert detail["capability"]["d_star"] == 0.2667
                assert detail["economics"][
                    "minimum_effective_support_m"
                ] == 18.0
                assert detail["economics"]["national_value"][
                    "incremental_national_value_m_sar"
                ] == 198.0
                assert detail["competition"][
                    "warning_fires"
                ] is False
        else:
            assert detail["real_decision"]["state"] == "REJECT"
            assert detail["real_decision"]["route_code"] == 0
            assert next(
                row
                for row in detail["rules"]
                if row["rule_id"] == "R11"
                and row.get("synthetic_flag") is not True
            )["fired"] is True
            if mode == "simulated":
                assert detail["active_decision"]["route_code"] == 0
                assert detail["capacity"][
                    "qualified_available_kt"
                ] == 80.0
                assert detail["capacity"][
                    "specification_adjusted_gap_kt"
                ] == -24.0
                assert detail["economics"][
                    "minimum_effective_support_m"
                ] == 0.0

        manifest = get_json(
            f"{prefix}/ui-manifest?mode={mode}"
        )
        assert isinstance(manifest, dict)
        assert manifest["context"] == {
            "opportunity_id": opportunity_id,
            "mode": mode,
            "decision_state": expected_active,
        }
        component_types = {
            component["type"]
            for component in manifest["components"]
        }
        assert component_types <= APPROVED_COMPONENTS
        assert {
            "integrity_banner",
            "decision_hero",
            "rule_ledger",
            "decision_actions",
        } <= component_types
        if mode == "public":
            assert "economics_panel" not in component_types
        metric_grid = next(
            component
            for component in manifest["components"]
            if component["type"] == "metric_grid"
        )
        if opportunity_id == "SAU-H0-390210":
            assert metric_grid["props"][
                "supplier_concentration"
            ] == {}

        dossier = get_json(f"{prefix}/dossier?mode={mode}")
        assert isinstance(dossier, dict)
        assert DOSSIER_KEYS <= set(dossier)
        assert dossier["opportunity_id"] == opportunity_id
        assert dossier["mode"] == mode
        assert dossier["decision_state"] == expected_active
        assert dossier["conditions"] is not None
        assert dossier["kill_conditions"] is not None
        assert dossier["evidence_summary"]["authority"] == (
            detail["authority"]
        )
        if mode == "public":
            assert dossier["synthetic_disclosure"] is None
            assert dossier["evidence_summary"][
                "synthetic_records"
            ] == 0
        else:
            assert dossier["synthetic_disclosure"][
                "display_label"
            ] == DISCLOSURE
            assert dossier["evidence_summary"][
                "synthetic_records"
            ] > 0

        html_status, html_body, content_type = get(
            f"{prefix}/dossier.html?mode={mode}"
        )
        html = html_body.decode("utf-8")
        assert html_status == 200
        assert content_type == "text/html"
        assert html.startswith("<!doctype html>")
        assert dossier["decision_headline"] in html
        assert 'dir="rtl"' in html
        if mode == "public":
            assert DISCLOSURE not in html
            exact = (
                f"{dossier['evidence_summary']['public_records']} "
                "public records; 0 synthetic records."
            )
            assert exact in html
        else:
            assert DISCLOSURE in html
            assert "Simulated R6–R8 ledger" in html

        summary["cases"][f"{opportunity_id}:{mode}"] = {
            "real_state": expected_real,
            "active_state": expected_active,
            "detail": "PASS",
            "ui_manifest": "PASS",
            "dossier_json": "PASS",
            "dossier_html": "PASS",
        }

OUT.write_text(
    json.dumps(summary, indent=2) + "\n",
    encoding="utf-8",
)
print(json.dumps(summary, indent=2))
```

This is the concrete Journey A–E proof:

- A: both list modes, snapshot/mode/states/import fields, zero public leakage;
- B: steel public rules/capability/data unlocks/INVESTIGATE;
- C: steel simulated dual states, capacity/gap/D*/S*/national value/competition/disclosure/conditions;
- D: PP public and simulated REJECT/no support/equivalent availability;
- E: manifest plus dossier JSON/HTML for all four case/mode combinations.

### 10.4 Exact live timing block

Use the same 18 `ANALYSIS_PATHS` as `tests/test_performance.py`. For each path:

1. call once and require 200, excluding its elapsed time;
2. call five more times with `time.perf_counter()`;
3. record all five samples and `statistics.median`;
4. require median `< 250.0`;
5. write a sorted JSON object to `timings.json`.

Required Python core:

```python
results: dict[str, dict[str, object]] = {}
for path in ANALYSIS_PATHS:
    assert fetch(path)[0] == 200
    samples: list[float] = []
    for _ in range(5):
        started = perf_counter()
        status, _ = fetch(path)
        elapsed_ms = (perf_counter() - started) * 1000
        assert status == 200
        samples.append(round(elapsed_ms, 3))
    measured_median = round(median(samples), 3)
    results[path] = {
        "samples_ms": samples,
        "median_ms": measured_median,
        "bound_ms": 250.0,
        "passes": measured_median < 250.0,
    }
assert all(row["passes"] is True for row in results.values())
```

### 10.5 Failure-path block

The live server assertions are:

```python
status, payload, _ = get_allow_error(
    "/api/opportunities/DOES-NOT-EXIST"
)
assert status == 404
assert json.loads(payload)["detail"] == (
    "Unknown opportunity: DOES-NOT-EXIST"
)

status, payload, _ = get_allow_error(
    "/api/opportunities?mode=invalid"
)
assert status == 422
assert json.loads(payload)["detail"]

status, payload, content_type = get_allow_error(
    "/nonexistent-static"
)
assert status == 200
assert content_type == "text/html"
assert payload.decode("utf-8").startswith("<!doctype html>")
```

There is no `IOR_DATA_DIR`-style production override (`config.py:11-16`; `data_repository.py:8-60`). Therefore do **not** add or simulate one in the live process. Record the integrity-failure path as TestClient-level and run:

```bash
PYTHONPATH=src "$PIP_VENV/bin/python" -m pytest \
  tests/test_api.py::test_evidence_integrity_failure_returns_422_json \
  -q
```

The full suite also covers the list-derived 422 and ground-truth mismatch. A live temp-data mutation would require new runtime behavior and is out of S05 authority.

### 10.6 Keyword scan and disposition policy

Enumerate NUL-delimited tracked paths from:

```bash
git ls-files -z
```

Before initial staging, add only the explicit S05 new deliverables from §8 that exist on disk (`scripts/final_acceptance.sh`, the two new tests, three new final documents, S05 evidence/review records, and generated `acceptance_results.md`). Do not enumerate arbitrary untracked files; in particular exclude `.workflow/runs/**`. After staging/commit, the same files are naturally returned by `git ls-files`. Exclude only `.workflow/**/plan.md` from keyword matching. Decode UTF-8 text; binary/invalid UTF-8 files are recorded as skipped-binary in the scan summary. Search case-insensitively for:

```text
TODO | FIXME | HACK | TEMP | temporary | placeholder | mock |
disabled | skip | xfail | hardcoded
```

Write one Markdown row per hit: path, line, token, escaped source excerpt, disposition. The conservative classifier may emit only:

- `LEGITIMATE — lexical substring` for `TEMP` inside words such as `attempt` or `template`;
- `LEGITIMATE — governed execution state` for exact `DISABLED` rule states in methodology/core/config-free source/tests/UI;
- `LEGITIMATE — isolated test fixture` for temp-path/helper terminology inside pytest tests;
- `LEGITIMATE — historical governance record` for a hit in non-plan `.workflow/**`;
- `LEGITIMATE — documented limitation/control` for an explicitly explanatory line in `KNOWN_LIMITATIONS`, traceability, ADRs, final reports, or the scan implementation itself;
- `FIXED` only when the original hit no longer exists and the evidence record identifies the correcting diff;
- `UNRESOLVED` for everything else.

The scan exits 1 if any row is `UNRESOLVED`. Broadly accepting all documentation or all tests is prohibited. Every generated legitimate row is manually reviewed by the Implementer, Supervisor, and final Reviewer; the table itself remains complete evidence.

### 10.7 Source archive

The harness creates `source-acceptance.zip` directly from the same tracked + explicit-S05-intended path list. It excludes arbitrary untracked files, ignored logs/venvs, `.workflow/runs/**`, and `.git`. `zipfile.ZipFile.testzip()` must return `None`; required members are:

```text
AGENTS.md
README.md
pyproject.toml
uv.lock
src/ior_mvp/app.py
src/ior_mvp/static/index.html
scripts/final_acceptance.sh
tests/test_golden_cases.py
docs/FINAL_BUILD_REPORT.md
docs/OPERATOR_RUNBOOK.md
docs/DEPLOYMENT_GUIDE.md
docs/DEVELOPMENT_GUIDE.md
docs/REQUIREMENTS_TRACEABILITY.md
docs/authority/Industrial_Opportunity_Resolution_Methodology_Final_KSA.docx
data/manifests/snapshot_manifest.json
```

Fail if any member contains `/.git/`, `/.venv/`, `/__pycache__/`, `/.workflow/logs/`, `Zone.Identifier`, or ends with `.pyc`, `.pem`, `.key`, or `.p12`.

### 10.8 Results file structure

`acceptance_results.md` contains real observed values only:

1. run UTC, branch, HEAD, dirty-path inventory, Python/uv/Docker versions;
2. overall `LOCAL HARNESS PASS` or `LOCAL HARNESS FAIL` (never project `COMPLETE`);
3. release identity `0.2.0`, with `docker_health`, `uvicorn_health`, project contract, and analysis `authority.project_version` results tied to that same value;
4. all step names/codes/durations/log paths;
5. Journey A–E case/mode summary;
6. every NFR-005 path with five samples and median;
7. failure-path statuses and TestClient scope statement;
8. reversal outcome;
9. protected-path/config/data/hash result;
10. package/docs artifact result;
11. complete keyword disposition table;
12. owner §27 steps 1–20, including explicit `EXTERNAL SUPERVISOR GATE` for hosted CI/reviewer/merge facts not available to the runner;
13. assumptions and accepted limitations (KL-20–KL-30);
14. links to `test_evidence.md`, `reviewer_findings.md`, and `pr_record.md`.

The renderer must never state CI/review/merge/tag success before those events. Supervisor records those events in their owned records and in the final post-merge report.

## 11. Static acceptance-runner contract test

Create `tests/test_final_acceptance_contract.py`:

```python
from __future__ import annotations

from pathlib import Path

from ior_mvp.config import PROJECT_ROOT


RUNNER = PROJECT_ROOT / "scripts" / "final_acceptance.sh"


def test_final_acceptance_runner_has_required_order() -> None:
    source = RUNNER.read_text(encoding="utf-8")
    ordered = (
        'run_step "make_ci"',
        'run_step "clean_venv_create"',
        'run_step "clean_pip_install"',
        'run_step "clean_prohibited"',
        'run_step "clean_thresholds"',
        'run_step "clean_compile"',
        'run_step "clean_node"',
        'run_step "clean_integrity"',
        'run_step "clean_gate_b"',
        'run_step "clean_pytest"',
        'run_step "clean_smoke"',
        'run_step "docker_build"',
        'record_server_step "uvicorn_start"',
        'run_step "http_journeys"',
        'run_step "http_nfr_005"',
        'run_step "http_failures"',
        'run_step "integrity_422_testclient"',
        'record_server_step "uvicorn_restart"',
        'run_step "revert_apply"',
        'run_step "revert_abort"',
        'run_step "final_prohibited"',
        'run_step "final_thresholds"',
        'run_step "final_gate_b"',
        'run_step "keyword_scan"',
        'run_step "docs_audit"',
        'run_step "source_archive"',
        'run_step "protected_diff"',
        'render_results',
    )
    indexes = [source.index(fragment) for fragment in ordered]
    assert indexes == sorted(indexes)


def test_final_acceptance_runner_locks_required_contracts() -> None:
    source = RUNNER.read_text(encoding="utf-8")
    for fragment in (
        "python3 -m venv",
        "--clear",
        'pip install -e ".[dev]"',
        "docker run -d",
        "-p 8001:8000",
        "--host 127.0.0.1",
        "--port 8010",
        'RELEASE_VERSION="0.2.0"',
        "git revert --no-commit "
        "98c1a40225a94090238867bc9861e8c6544b5839",
        "git revert --abort",
        "acceptance_results.md",
        "__FINAL_ACCEPTANCE__",
    ):
        assert fragment in source
    assert "scripts/build_manifests.py" not in source
    assert "|| true" not in source


def test_runner_is_executable() -> None:
    assert RUNNER.stat().st_mode & 0o111
```

The first run is RED because the runner does not exist. Implement the harness contract, run `chmod +x scripts/final_acceptance.sh`, then require these tests and `bash -n scripts/final_acceptance.sh` GREEN before executing the full runner.

## 12. `docs/FINAL_BUILD_REPORT.md` full-text skeleton

Create the file with this evidence-honest content. The Implementer writes the pre-review state exactly; the Supervisor replaces only the clearly dated evidence-state paragraphs with observed PR/default-branch facts after they exist.

```markdown
# Final Build Report — Industrial Opportunity Resolution MVP 0.2.0

## Report status and authority

This report describes the completion build based on the Industrial Opportunity Resolution Methodology, the frozen Core 01–09 contracts, versioned configuration, and hashed public/synthetic fixtures. It is an engineering acceptance record, not an authorization of public support and not a claim that synthetic records are Ministry evidence.

Application release: `0.2.0`.
Demonstration project contract: `config/project.yaml` version `0.2.0`, matching the package/API release.
Methodology authority: `docs/authority/Industrial_Opportunity_Resolution_Methodology_Final_KSA.docx`.
Data classification: `confidential_demo` — frozen public evidence and Class-D demo scenarios only.

While the S05 branch is under review, this report does not claim that the S05 PR is merged, that default-branch CI is green for its merge commit, that project state is `COMPLETE`, or that tag `v0.2.0` exists. Those facts are added only by the Supervisor after direct observation.

## What was built

The MVP is one offline FastAPI application with a static adaptive decision workspace. It:

- loads two frozen, hashed public opportunity snapshots;
- evaluates the deterministic R0–R12 ledger;
- applies evidence states, capability K/U/D*, hard-gate controls, economics, minimum effective support, national value, competition, and EVSI;
- preserves an immutable public `real_decision`;
- evaluates two explicit Class-D scenarios in an isolated `simulation_decision`;
- back-tests scenario-selected state and route against planted ground truth;
- emits an approved-component GenUI manifest;
- exports a structured Decision Dossier and printable HTML;
- runs the four-record Arabic/English extraction golden gate with source spans;
- provides WSL/native-Linux/Docker run paths;
- runs local and hosted CI gates for prohibited files, threshold literals, compilation, JavaScript syntax, integrity, scenario reconciliation/back-test, pytest, smoke, and Docker build;
- includes a final-acceptance runner with clean install, live HTTP journeys, timings, failure paths, reversal, repository scans, document checks, and source-archive validation.

## What was deliberately not built

The completion build does not add live Ministry/customs connectors, production authentication or approval workflow, a database, migrations, backup service, paid-data acquisition, the 1,300-product universe, generic AI chat, CRM, document management, arbitrary executable GenUI, a production LLM adapter, causal incentive-effect claims, or real outcome-learning/calibration. These exclusions are the approved Core 01 §8 and backlog boundary, not hidden incomplete behavior.

No config field other than unhashed `config/project.yaml` `project.version`, and no data, frozen core, methodology, golden expectation, or authority hash changes in S05. `scripts/build_manifests.py` was not run.

## Governing requirements completed

`docs/REQUIREMENTS_TRACEABILITY.md` maps FR-001–FR-073, NFR-001–NFR-010, INV-01–INV-12, TL-01–TL-09, Gates A–H, BC-01–BC-08, Core 09 §7 DOD-01–DOD-10, and Core 01 §9 SC-01–SC-06. FR-073 remains `NOT_APPLICABLE (production)`. Gate G/TL-07 is accepted at static frontend-contract plus live HTTP/API level; no real-browser run is claimed (KL-22).

Branch execution promotes only observed rows to `TESTED`. `COMPLETE` promotion occurs in the narrow release-state PR only after the S05 implementation PR merges and default-branch CI for that merge commit is green.

## Architecture implemented

```text
hashed public snapshots + versioned configuration
  -> repository and evidence guards
  -> public deterministic analysis
  -> immutable real_decision
  -> optional isolated Class-D scenario
  -> reconciliation + capability/economics/rule evaluation
  -> simulation_decision + ground-truth back-test
  -> constrained UI manifest and Decision Dossier
  -> static offline frontend / JSON / printable HTML
```

The runtime is stateless except for process-local read caches of immutable files (KL-29). There is no database or write endpoint. Public intervention remains an accountable human act outside the application.

## Completion slices and merged evidence before S05

| Slice | Outcome | Merge | Hosted CI |
|---|---|---|---|
| S00 | baseline v0.1.0 import and control records | `0731ae546f79c9ac3bfd92612a01f07da67937ed` | pre-CI bootstrap |
| S01 | CI, scanners, uv/pip/Docker toolchain | `432af8af1fa88a2258a0fd8d825a6855e408270f` | `33569855956`, `33570112914` |
| S02 | threshold governance and boundary tests | `c43837054c5581e68cfe7ed87d914a89cd4f63a3` | `33573669072` |
| S03 | evidence isolation, reconciliation, authority disclosure, Gate B | `ddf905d5051fe3b4468bdcd793a8a640f5040048` | `33579923763` |
| S04 | data-driven simulation, ground truth, synthetic R6–R8 | `98c1a40225a94090238867bc9861e8c6544b5839` | `33584437086` |

All listed hosted runs completed the uv/Python 3.12, uv/Python 3.14, pip/Python 3.12, and Docker-image jobs successfully, as recorded in the corresponding slice completion/PR records.

## S05 acceptance evidence

Fresh local command output, every internal exit code, Journey A–E results, NFR-005 samples, failure statuses, reversal, scan dispositions, archive checks, and protected-path audit are recorded in:

- `.workflow/slices/S05-final-acceptance/acceptance_results.md`;
- `.workflow/slices/S05-final-acceptance/test_evidence.md`.

Supervisor review is recorded in `implementation_review.md`. Different-model holistic review is recorded in `reviewer_findings.md`. Hosted PR/default-branch checks and merge identity are recorded in `pr_record.md` and `completion.md`.

The pre-merge report intentionally does not invent an S05 PR number, CI run, merge SHA, test count, or reviewer verdict.

## Acceptance demonstrations

### Journey A — Executive overview

Both portfolio modes return exactly the steel and polypropylene cases, frozen identity, public/active states, latest public import measures, and zero synthetic rows in public mode.

### Journey B — Public steel

HS 721049 remains `INVESTIGATE`; R2/R3/R4-D/R9-S evidence is visible; D* is withheld; missing effective-capacity/specification/qualification facts are named; brownfield is the route to test, not a greenfield recommendation.

### Journey C — Steel simulation

The real state remains `INVESTIGATE`. The active Class-D scenario is visibly labelled. The expected values remain: effective qualified capacity 57.509 kt, gap 46.491 kt, D* 0.2667 incremental upgrade, unsupported NPV -SAR 18m, unsupported IRR 9.5%, S* SAR 18m, incremental national value SAR 198m, competition ratio 1.0751, and `SIMULATED ADVANCE` route 5 with conditions and kill conditions.

### Journey D — Polypropylene

HS 390210 remains `REJECT` for generic capacity support in public and simulated modes. R11 fires on the configured export/import evidence. The simulation retains equivalent qualified availability 80 kt against demand 56 kt, gap -24 kt, route 0, and support 0.

### Journey E — Decision Dossier

All case/mode combinations return the machine-readable dossier and printable HTML. Public dossiers contain exactly zero synthetic records and no simulation disclosure. Simulated dossiers carry the warning label and labelled R6–R8 rows.

## Tests and validators

The required fresh evidence sequence is:

```bash
make ci
RUN_DIR=".workflow/logs/s05-final-acceptance/manual-report-proof"
python3 -m venv --clear "$RUN_DIR/pip-venv"
"$RUN_DIR/pip-venv/bin/python" -m pip install -e ".[dev]"
PYTHONPATH=src "$RUN_DIR/pip-venv/bin/python" -m pytest -q
docker build --file Dockerfile --tag industrial-opportunity-resolution-mvp:s05-acceptance .
bash scripts/final_acceptance.sh
```

The actual run directory and all exit codes are in `acceptance_results.md`; this command listing is not itself evidence.

## Security and privacy validation

- The tracked/intended-file scanner finds no prohibited path or configured secret pattern.
- The keyword scan lists and disposes every named occurrence; an unresolved occurrence fails acceptance.
- No API key, credential, uploaded Ministry record, private export, or external source is required.
- Public and simulation evidence remain separate; synthetic rows remain Class D, generator-sourced, scenario-linked, and visibly disclosed.
- Runtime proof binds uvicorn to `127.0.0.1`; Docker port publication is for local acceptance only.
- The application has no authentication and is not approved for public-network or production policy deployment.

## Deployment and operation

Use `docs/OPERATOR_RUNBOOK.md` for start/stop/restart/health/log procedures and `docs/DEPLOYMENT_GUIDE.md` for WSL, Linux, Docker, environment, security, and rollback guidance. The first controlled demonstration should bind to localhost.

## Known limitations

The authoritative final list is `docs/KNOWN_LIMITATIONS.md`. Accepted items KL-20–KL-30 include limited sector profiles, pre-existing frontend structure/tokens, no browser E2E, unproduced MONITOR/public-complete-route branches, absent scenario R8/allocation/expansion schemas, missing public retained-import denominator, process-local cache stickiness, and the R1-D confidence-cap string.

## Deferred work

Deferred work remains the P1–P6 backlog: public-universe expansion, automated public acquisition, real bilingual corpus, plant capability graph, secure Ministry pilot, and realized-outcome calibration. Each future change must preserve historical golden snapshots and pass the authority change gate when applicable.

## Residual-review disposition

- Recursive source discovery is fixed and tested.
- PP empty concentration behavior is characterized.
- List-route missing-scenario behavior maps to 404 and is tested.
- Public zero-synthetic HTML is asserted exactly.
- Cached scenarios are proved unchanged by both packaged simulations.
- The dead `has_equivalence` local is removed.
- FR-044/GATE-H traceability pointers are corrected.
- KL-29 records cache stickiness; KL-30 records the unchanged R1-D confidence-cap string; KL-22 states the browser boundary.
- Other prior observations remain governed by KL-25–KL-28 or are private-helper paths prevalidated by Gate B; no public/golden behavior is changed.

## Rollback and recovery

Before merge, remove the S05-only new files and apply reviewed inverse patches to the listed S05 modifications; do not use hard reset or broad checkout restoration. After release, create a rollback branch from `origin/main`, run `git revert v0.2.0`, execute the complete acceptance sequence, obtain review and green CI, and merge the rollback PR. Git/history and immutable source snapshots are the recovery mechanism because there is no mutable database.

## Resolved release-process rulings

The tag name `v0.2.0`, package/API/project version bump, and two-PR release-state route are approved. OQ-01 is resolved: after the S05 implementation PR merges and its default-branch CI is green, the Supervisor opens a narrow docs/state-only release-state PR, requires its four checks green, squash-merges it, verifies default-branch CI for that final merge commit, and only then tags that commit.

No domain threshold, methodology interpretation, data value, golden outcome, or release version decision is open.

## Final commit, CI, and tag

Before merge, the final commit/tag fields are deliberately unclaimed. In the release-state PR, the Supervisor records the observed S05 implementation merge and CI facts in the final report/state/progress/completion/PR records. After that PR's four checks are green, it is squash-merged; the Supervisor verifies default-branch CI for the release-state merge commit, verifies local `main` equals it, then creates and pushes annotated tag `v0.2.0` on that exact commit. The tag is never created by the Implementer or Reviewer.
```

## 13. `docs/OPERATOR_RUNBOOK.md` full-text skeleton

```markdown
# Operator Runbook — Industrial Opportunity Resolution MVP 0.2.0

## Operating boundary

This is an offline controlled demonstration using frozen public evidence and explicit Class-D synthetic scenarios. It contains no Ministry data, user accounts, write endpoint, or public-support authorization. Bind to localhost unless an approved reverse proxy/network control is in place.

## Prerequisites

- WSL or native Linux;
- Python 3.11 or newer;
- `uv` for the primary path;
- Node for JavaScript syntax validation;
- Git and Make;
- Docker only for the container path.

## Primary uv start

```bash
cd /home/barami/projects/industrial-opportunity-resolution-mvp
make uv-sync
PYTHONPATH=src "$HOME/.local/bin/uv" run --locked --extra dev \
  uvicorn ior_mvp.app:app --host 127.0.0.1 --port 8000
```

Open `http://127.0.0.1:8000`. OpenAPI is at `http://127.0.0.1:8000/docs`.

## Preserved clean pip path

```bash
cd /home/barami/projects/industrial-opportunity-resolution-mvp
python3 -m venv --clear .venv
source .venv/bin/activate
python -m pip install -e ".[dev]"
PYTHONPATH=src python scripts/verify_integrity.py
PYTHONPATH=src python scripts/validate_scenarios.py
PYTHONPATH=src pytest -q
PYTHONPATH=src python scripts/demo_smoke.py
PYTHONPATH=src uvicorn ior_mvp.app:app --host 127.0.0.1 --port 8000
```

`START_DEMO_WSL.sh` remains the interactive convenience path. The clean commands above are the noninteractive acceptance equivalent.

## Health check

```bash
curl --fail --silent --show-error http://127.0.0.1:8000/api/health
```

A healthy release reports JSON `status: "ok"`, application `version: "0.2.0"`, and the public/isolated-simulation evidence boundary.

## Stop and restart

Foreground process: press Ctrl+C once and wait for uvicorn shutdown.

Managed process:

```bash
kill -TERM "$IOR_PID"
wait "$IOR_PID"
```

Restart with the same start command, then repeat the health check. A restart is required after any approved on-disk config/data change because successful loads are process-local cached (KL-29).

## Docker operation

Build and start:

```bash
docker build --file Dockerfile \
  --tag industrial-opportunity-resolution-mvp:0.2.0 .
docker run -d --name ior-mvp \
  -p 127.0.0.1:8000:8000 \
  industrial-opportunity-resolution-mvp:0.2.0
curl --fail --silent --show-error http://127.0.0.1:8000/api/health
```

Logs, restart, and stop/remove:

```bash
docker logs ior-mvp
docker restart ior-mvp
curl --fail --silent --show-error http://127.0.0.1:8000/api/health
docker stop ior-mvp
docker rm ior-mvp
```

The repository `docker compose up --build` path also remains supported on port 8000.

## Evidence modes

- `public`: only frozen attributable public rows contribute to `real_decision`.
- `simulated`: the public result stays visible and unchanged; Class-D scenario rows affect only `simulation_decision`/`active_decision`.

Never describe a simulated value as observed, official, Ministry-provided, or Class A/B/C.

## Demonstration sequence

Follow `docs/implementation/MINISTRY_DEMO_SCRIPT.md`:

1. executive overview;
2. public steel `INVESTIGATE`;
3. steel `SIMULATED ADVANCE` with unchanged real state;
4. PP public/simulated `REJECT`;
5. Decision Dossier.

## Required gates

```bash
make ci
bash scripts/final_acceptance.sh
```

`make ci` runs locked sync, prohibited/secret scan, threshold-literal scan, Python compile, JavaScript syntax, integrity, Gate B scenario validation/back-test, pytest, and smoke. `final_acceptance.sh` additionally proves the clean pip path, container runtime, live journeys, performance, failures, restart, reversal, documents, archive, and repository keyword disposition.

Do not run `scripts/build_manifests.py` unless an approved authority change identifies the exact governed bytes and passes the Manifest §7 gate.

## Failure interpretation

- HTTP 404: unknown opportunity or unavailable scenario for the requested simulated list/detail.
- HTTP 422 invalid mode: FastAPI request validation rejected the query.
- HTTP 422 `EVIDENCE_INTEGRITY_ERROR`: scenario policy, reconciliation, contract, or ground-truth validation failed closed; no partial analysis is valid.
- Integrity failure: a governed file is missing/changed; investigate authorization before any regeneration.
- Gate B failure: a packaged scenario is invalid, unreconciled, or disagrees with its planted state/route.
- Threshold scan failure: a configured decision threshold appears as a comparison literal in source.
- Prohibited scan failure: a tracked path or configured secret pattern violates repository policy.
- NFR-005 failure: preserve samples and endpoint; reproduce before optimization.
- Docker/health failure: inspect container logs, port occupancy, image build, and process exit.

Do not weaken a test, change a golden result, or regenerate hashes to clear a failure.

## Logs and evidence

The application writes uvicorn output to its launching terminal unless redirected by the operator. Acceptance logs are under ignored `.workflow/logs/s05-final-acceptance/`; sanitized results are in `.workflow/slices/S05-final-acceptance/acceptance_results.md`.

Never paste environment values, tokens, credentials, private datasets, or unrestricted logs into an issue or model.

## Escalation

Stop and request authority if a config/data/core/methodology change is needed, a public golden outcome changes, synthetic evidence appears official, an unresolved hard gate would permit real `ADVANCE`, or a required production control is missing. The application can calculate/recommend; only accountable officials authorize public action.
```

## 14. `docs/DEPLOYMENT_GUIDE.md` full-text skeleton

```markdown
# Deployment Guide — Industrial Opportunity Resolution MVP 0.2.0

## Supported deployment scope

The supported MVP targets are WSL, native Linux, and Docker. The package is an offline controlled demonstration, not a production authorization system. It uses local immutable JSON/YAML/DOCX/Markdown assets and has no database, migration, queue, cloud service, external API dependency, authentication, or write endpoint.

## Release identity

- Python package/API release: `0.2.0`;
- annotated source tag after acceptance: `v0.2.0`;
- demo project contract: `config/project.yaml` `0.2.0`;
- threshold configuration: `1.1.0`;
- evidence policy: `1.1.0`;
- sector profiles: `1.0.0`;
- packaged synthetic scenario contract: `1.1.0`.

These are separate versioned artifacts. S05 moves the package/API and unhashed project-contract release identity together and regenerates only the root lock-package version; no hashed governed configuration changes.

## WSL

```bash
wsl -d Ubuntu
cd /home/barami/projects/industrial-opportunity-resolution-mvp
make uv-sync
make ci
PYTHONPATH=src "$HOME/.local/bin/uv" run --locked --extra dev \
  uvicorn ior_mvp.app:app --host 127.0.0.1 --port 8000
```

If `uv` or Node is installed elsewhere:

```bash
make UV=uv NODE=node ci
```

## Native Linux

From a clone of tag `v0.2.0`:

```bash
python3 -m venv --clear .venv
source .venv/bin/activate
python -m pip install -e ".[dev]"
PYTHONPATH=src python scripts/verify_integrity.py
PYTHONPATH=src python scripts/validate_scenarios.py
PYTHONPATH=src pytest -q
PYTHONPATH=src python scripts/demo_smoke.py
PYTHONPATH=src uvicorn ior_mvp.app:app --host 127.0.0.1 --port 8000
```

## Docker

```bash
docker build --file Dockerfile \
  --tag industrial-opportunity-resolution-mvp:0.2.0 .
docker run -d --name ior-mvp \
  -p 127.0.0.1:8000:8000 \
  industrial-opportunity-resolution-mvp:0.2.0
curl --fail --silent --show-error http://127.0.0.1:8000/api/health
```

Stop with:

```bash
docker stop ior-mvp
docker rm ior-mvp
```

Docker Compose:

```bash
docker compose up --build
docker compose down
```

The compose file publishes port 8000 on the host. Use it only on an approved local/controlled network.

## Ports and runtime environment

- default local/API port: 8000;
- final-acceptance container port: host 8001 → container 8000;
- final-acceptance uvicorn port: 8010;
- `IOR_HOST`: CLI/run-script host, default `127.0.0.1`;
- `IOR_PORT`: CLI/run-script port, default `8000`;
- `PYTHONUNBUFFERED`: optional container log behavior.

There is no `IOR_DATA_DIR` runtime override. Do not invent one to alter hashed inputs.

## Security posture

For the first demonstration, bind to `127.0.0.1`. If a shared host is unavoidable, put the container behind an approved reverse proxy, restrict source networks, and terminate TLS. Do not expose the application directly to the public Internet.

No confidential Ministry data belongs in this package. Before internal data or production use, implement enterprise SSO/RBAC, least privilege, Saudi-approved hosting/residency, encryption, audit snapshots, evidence access logs, secure headers/CSRF/rate limits, secrets vault, dependency/vulnerability review, environment separation, backup/disaster recovery, and named release/decision authorities as specified in `docs/implementation/SECURITY_AND_DEPLOYMENT_NOTES.md`.

Synthetic mode must be disabled or unmistakably segregated in any official production environment.

## Pre-deployment verification

```bash
make ci
bash scripts/final_acceptance.sh
```

Require fresh exit 0 evidence, zero Supervisor findings, zero different-model reviewer findings, and green current-head hosted checks. A green test run is evidence, not authorization.

## Health and smoke

```bash
curl --fail --silent --show-error http://127.0.0.1:8000/api/health
PYTHONPATH=src python scripts/demo_smoke.py
```

Health must report `status=ok` and `version=0.2.0`. Smoke must retain steel public INVESTIGATE, steel simulated ADVANCE with unchanged real state, PP public REJECT, and 4/4 extraction.

## Persistence, migration, backup, and restore

The MVP has no database or mutable user state, so schema migration and operational backup/restore are not applicable. Recovery uses the Git repository/tag plus hashed source/data artifacts. Process-local caches are cleared by restart.

## Rollback

Create a reviewed rollback branch; never rewrite `main`:

```bash
git fetch origin main --tags
git switch -c rollback/v0.2.0 origin/main
git revert v0.2.0
make ci
bash scripts/final_acceptance.sh
git push -u origin rollback/v0.2.0
```

Open a rollback PR, require independent review and all hosted checks, then merge according to ADR-007. Do not alter manifests or golden expectations during rollback.

## Release tag

Only the Supervisor creates `v0.2.0`, after the final merged commit is on `main`, default-branch CI for that commit is green, durable state is accurate, and the tag name is confirmed absent. The Implementer and Reviewer do not tag or push.
```

## 15. `docs/DEVELOPMENT_GUIDE.md` complete replacement

Replace the file with:

```markdown
# Development Guide

## Authority and data boundary

Development uses repository code, frozen public evidence, and explicitly synthetic Class-D fixtures. No API key or live industrial source is required. Never run `scripts/build_manifests.py` outside an approved Authority Manifest §7 change. The active slice ADR and PR must identify the exact governed bytes, approval basis, sensitivity/golden proof, and permitted generated diff before one controlled run.

## Prerequisites

Use WSL or native Linux with Python 3.11 or newer, Git, Make, Node, and `uv`. Docker is required for image/runtime compatibility checks.

The Makefile defaults to `NODE ?= node`. If Node is user-scoped:

```bash
make NODE="$HOME/.local/node/bin/node" ci
```

The Makefile invokes `$(HOME)/.local/bin/uv` unless overridden:

```bash
make UV=uv ci
make UV=/path/to/uv ci
```

Do not edit shell startup files merely to run the build.

## Install uv without changing shell profiles

```bash
curl -LsSf https://astral.sh/uv/install.sh \
  | env UV_NO_MODIFY_PATH=1 sh
"$HOME/.local/bin/uv" --version
make uv-sync
```

## Local CI-equivalent gates

```bash
make ci
```

The target runs, in order:

1. locked development-environment sync;
2. prohibited tracked-file and deterministic secret-pattern scan;
3. configured-threshold comparison-literal scan;
4. Python compilation;
5. JavaScript syntax;
6. governed-file integrity;
7. Gate B scenario policy/reconciliation/ground-truth back-test;
8. full pytest;
9. domain/extraction smoke.

Any nonzero exit blocks review. `scripts/build_manifests.py` is not a routine gate.

## Final acceptance

```bash
bash scripts/final_acceptance.sh
```

Run it detached under the active slice protocol and read the complete outer log plus generated `.workflow/slices/S05-final-acceptance/acceptance_results.md`. It adds a clean pip environment, Docker run/health, live HTTP journeys, median-of-five NFR-005 measurements, failure paths, restart, isolated revert/abort, repeated scans, documentation/source-archive checks, and a per-hit keyword disposition table.

The script reports local evidence only. It does not approve review, CI, merge, completion, or tagging.

## Preserved clean pip path

```bash
python3 -m venv --clear .venv
source .venv/bin/activate
python -m pip install -e ".[dev]"
python scripts/check_prohibited_files.py
python scripts/check_threshold_literals.py
python -m compileall -q src scripts tests
node --check src/ior_mvp/static/app.js
PYTHONPATH=src python scripts/verify_integrity.py
PYTHONPATH=src python scripts/validate_scenarios.py
PYTHONPATH=src pytest -q
PYTHONPATH=src python scripts/demo_smoke.py
```

## Lock maintenance

`pyproject.toml` is the dependency/package source. Regenerate `uv.lock` only after an intentional dependency or package-metadata change:

```bash
"$HOME/.local/bin/uv" lock
"$HOME/.local/bin/uv" sync --locked --extra dev
```

Do not use opportunistic `--upgrade`. Review the complete lock diff. For S05 release metadata, only the editable root package version changes.

## Docker compatibility

```bash
docker build --file Dockerfile \
  --tag industrial-opportunity-resolution-mvp:local .
docker run -d --name ior-local \
  -p 127.0.0.1:8001:8000 \
  industrial-opportunity-resolution-mvp:local
curl --fail --silent --show-error \
  http://127.0.0.1:8001/api/health
docker stop ior-local
docker rm ior-local
```

## Slice workflow

Each bounded slice follows:

1. create the recorded `slice/SXX-name` branch from `main`;
2. read authority/core/control/current code/tests and adopt the slice persona;
3. different-model Planner writes `plan.md`;
4. Supervisor reviews until zero plan findings and records `PLAN_APPROVED`;
5. Implementer executes the approved plan test-first and records evidence without self-approval;
6. Supervisor reviews the complete diff;
7. a model different from the Implementer performs read-only independent review;
8. valid findings return to the Implementer, followed by focused/full tests and review of fixed work;
9. fresh local gates and explicit-path staging;
10. Supervisor commit/push/PR, current-head CI, ADR-007 check gate, squash merge, and durable-state update.

No subagent approves or merges its own work. A green pipeline is evidence, not approval.

## Authority-change procedure

Edits to `config/*.yaml`, `data/**`, `docs/core/**`, the methodology, a golden expectation, or another hashed artifact are not ordinary refactors.

1. Classify the change under Authority Manifest §7.
2. Record rationale, scope, authority/owner approval, and affected golden cases in the active ADR/plan/PR.
3. Add deterministic boundary/sensitivity and unchanged-golden tests before changing governed bytes.
4. Audit the exact hand diff and protected paths.
5. Run every allowed pre-generation gate.
6. Run `scripts/build_manifests.py` only under the approved one-run gate.
7. Audit every generated hash/byte/path change; stop on any extra diff.
8. Run integrity, Gate B, full tests, smoke, reviews, and hosted CI.

Never regenerate hashes merely to accept unexplained bytes. Never weaken a golden expectation to obtain green.

## CI topology

GitHub Actions runs:

- locked `uv` gates on Python 3.12 and 3.14;
- the documented pip path on Python 3.12;
- an independent Docker image build.

Workflow permissions are read-only for repository contents; checkout credentials are not persisted. Cancelled/skipped jobs are not green.

## Failure interpretation

- prohibited scan 1: prohibited tracked path or secret-pattern finding; output is path/rule only;
- prohibited scan 2: Git/tracked-file read error;
- threshold scan 1: embedded configured comparison literal;
- threshold scan 2: YAML/Python/read error;
- lock/sync failure: lock absent, stale, or incompatible;
- compile/Node failure: syntax or required tool failure;
- integrity failure: governed byte mismatch; do not regenerate without authority;
- Gate B 1: policy/reconciliation/back-test failure;
- Gate B 2: execution/input error;
- pytest/smoke failure: implementation or frozen behavior is nonconformant;
- Docker/health failure: image/runtime compatibility issue;
- final acceptance nonzero: inspect every step code and its full log.

## Evidence and detached execution

Every command runs through a uniquely named ignored script/log under `.workflow/logs/`:

```bash
#!/usr/bin/env bash
set +e
(
  set -euo pipefail
  cd /home/barami/projects/industrial-opportunity-resolution-mvp
  bash scripts/final_acceptance.sh
)
status=$?
printf '\n__DONE__ status=%s\n' "$status"
exit "$status"
```

Read the complete log and require the sentinel. Sanitized command/results evidence belongs in the active slice `test_evidence.md`; raw logs/venvs/archives remain ignored.

## Test-first and review discipline

Write the smallest failing test, observe the expected RED, implement the minimum change, observe GREEN, and run affected/full regression. Characterization tests that prove existing behavior are labelled honestly rather than forced to fail. Do not mock production behavior when real local code can be exercised.

Before handoff, audit acceptance criteria, changed paths, protected paths, assumptions, limitations, rollback, and actual output. Do not write “should pass.”
```

## 16. Final limitation and ADR drafts

### 16.1 `docs/KNOWN_LIMITATIONS.md`

Keep the Closed table unchanged. Replace the complete “Accepted for this MVP” section with:

```markdown
## Accepted for this MVP (recorded, not scheduled)

| ID | Limitation | Rationale |
|---|---|---|
| KL-20 | `sector_profiles.v1.yaml` implements 2 of the 5 methodology §6.3 profiles. | Only two golden cases exist; adding profiles without cases would create untested configuration. |
| KL-21 | `styles.css` contains pre-existing hard-coded hex colours outside `:root` tokens and `app.js` remains a single module of about 450 lines. | S05 does not edit the frontend. Full tokenisation/module decomposition is post-acceptance polish and cannot justify domain/release drift. |
| KL-22 | No Playwright or real-browser interaction/visual test is part of final acceptance. Gate G/TL-07 proof is live HTTP/API payload plus static HTML/CSS/JavaScript contract: case/mode controls, dual states, approved manifest, dossier action/HTML, responsive media rules, semantic native controls, RTL markup, disclosure, and offline assets. | Core 09 §2.7 defines static checks as the MVP minimum and browser tests as a production addition. S05 does not claim painted layout, actual keyboard traversal, print rendering, or browser console behavior. |
| KL-23 | `MONITOR` is supported by the contract but is not produced by either packaged golden case. | Core 02 §5 records this explicitly; inventing a third case or route trigger is outside the frozen evidence. |
| KL-24 | The public branch does not reach the complete-route-sequence else branch of Core 07 §7.1 because both fixtures stop at REJECT/INVESTIGATE. | Implementing untested public route selection without evidence would invent behavior. |
| KL-25 | R8 simulation is explicitly `DISABLED`/`NOT_CALCULABLE`: packaged scenarios disclose committed and announced layers but contain no governed base-demand, commitment-probability, or minimum-efficient-scale field. | Values are absent from authority; raw layers remain separate and cannot fire R8. |
| KL-26 | R5 retained-import share cannot be calculated because frozen public snapshots contain neither domestic-production quantity nor retained-import flow. | The engine exposes `NOT_CALCULABLE`, reason, and configured threshold. Gross imports are not coerced into apparent consumption. |
| KL-27 | No expansion-assumption bypass exists; the disclosed-public-nameplate ceiling applies to packaged scenarios. | Core 06 §5.1 names an exception but supplies no governed schema. A future scenario requiring it needs an authority decision. |
| KL-28 | Synthetic scenarios contain no tariff-line or buyer-allocation blocks; reconciliation reports `NOT_APPLICABLE`. | Core 06 §5.1/Core 05 §8 require reconciliation when compatible blocks exist but do not define a current JSON schema. |
| KL-29 | Successful config/public/synthetic/golden loads are process-local `lru_cache` values. An approved on-disk correction is not observed until process restart or an explicit internal cache clear. | The MVP uses immutable hashed local files and no write endpoint. The operator runbook requires restart after an approved artifact change; startup/restart is acceptance-tested. |
| KL-30 | R1-D metrics emit `confidence_cap: "C"` as a stable result string rather than reading `rules.R1_D.confidence_cap` into that output field. | The emitted value equals the frozen 1.1.0 configuration and no decision threshold is hidden. S02 explicitly preserved result text; changing the projection is not required for either golden and is recorded rather than disguised. |
```

Do not close or delete an accepted limitation merely because other tests pass.

### 16.2 ADR status cleanup and ADR-009

Change ADR-004 status from `Proposed for S01` to:

```markdown
**Status:** Accepted 2026-09-02 (implemented and merged in S01).
```

Append:

```markdown
## ADR-009 — Final acceptance hardening is implementation-preserving and uses evidence-separated release gates

**Status:** Accepted 2026-09-02.
**Context:** S05 must close the explicitly carried S02–S04 review observations, measure NFR-005, prove all five user journeys and failure/reversal paths, publish final documents, and version the application without changing governed industrial logic. Current gaps are an unmapped list-route `RepositoryError`/`ValueError`, non-recursive threshold-source discovery, one dead local, and missing characterization/performance proof. There is no runtime data-directory override, and adding one solely to plant a live failure would broaden behavior.
**Decision:** Map list-route `RepositoryError` and `ValueError` to the same HTTP 404 contract as detail routes; retain `EvidenceIntegrityError` as 422. Change only `Path.glob("*.py")` to `Path.rglob("*.py")` in the threshold scanner. Remove the unused `has_equivalence` local without changing the equivalence branch. Add exact tests for PP empty GenUI concentration, public zero-synthetic HTML, unchanged cached scenarios, list-route 404, recursive scan, and median-of-five NFR-005. Add one detached final-acceptance runner that records per-step evidence but does not grant approval. Keep the planted evidence-integrity HTTP 422 proof at TestClient level because no approved runtime data-root override exists. Version the application distribution/lock and unhashed `config/project.yaml` `project.version` together at 0.2.0; all other config/data/core/methodology/golden/hash bytes remain unchanged and no manifest generator runs.
**Consequences:** Packaged public/simulated decisions, formulas, thresholds, scenarios, and evidence hashes are unchanged. The list endpoint fails consistently, future Python subpackages enter the threshold scan, and residual safe behavior is regression-locked. Gate G is stated at API/static level under KL-22; cache stickiness and the R1-D confidence-cap string remain accepted KL-29/KL-30. Local acceptance evidence, independent review, hosted CI, merge, durable `COMPLETE` promotion, and tag remain separate Supervisor gates.
```

## 17. Traceability edit and promotion rules

### 17.1 Evidence registry

Add a concise evidence registry near the status legend:

```markdown
Recorded hosted evidence:

- CI-S01-A: run `33569855956`, PR #1, head `b0b2ab4`, four jobs green.
- CI-S01-B: run `33570112914`, PR #1 promotion head `030dbfe`, four jobs green.
- CI-S02: run `33573669072`, PR #2 head `1da6a0e`, four jobs green.
- CI-S03: run `33579923763`, PR #3 head `f6ef33b`, four jobs green.
- CI-S04: run `33584437086`, PR #4 head `bd72207`, four jobs green.
- S05-LOCAL: `.workflow/slices/S05-final-acceptance/acceptance_results.md` and `test_evidence.md`; values are cited only after observed.
- S05-REVIEW/CI/MERGE: `reviewer_findings.md`, `pr_record.md`, and `completion.md`; values are cited only after observed.
```

### 17.2 Branch-time status rules

1. Replace each §7 current `IMPLEMENTED` row with `TESTED` only after:
   - its named existing test is observed in fresh S05 full pytest; and
   - the row cites the applicable historical run(s) plus S05 local evidence.
2. S01-origin implementation rows cite `CI-S01-A`, `CI-S01-B`, and the later full-suite runs `CI-S02`, `CI-S03`, `CI-S04`, demonstrating repeated execution.
3. S02-origin threshold rows retain `CI-S02` and later runs.
4. S03-origin evidence rows retain `CI-S03` and later runs.
5. S04-origin simulation rows retain `CI-S04`.
6. FR-015 gains the live S05 retained-contradiction assertion; NFR-005 gains both pytest and localhost samples; Gate G gains the exact API/static scope.
7. Current `TESTED` rows remain `TESTED` on the S05 branch. Do not write `COMPLETE`, final CI, merge SHA, tag, or final reviewer approval before observation.
8. FR-073 remains `NOT_APPLICABLE (production)`.

Correct these exact stale rows:

- FR-044 implementation → ``decision_engine.competition_warning`, `_simulation_economics`, `_simulate`; configured threshold; CI-S02/CI-S04/S05 evidence``.
- GATE-H → ``authorized generators ran only in S02–S04; S05 has no governed change and does not run the generator; make/clean-pip/integrity/Gate B/pytest/smoke/Docker evidence``.
- BC-01 → `TESTED` pre-merge with S00 commit/remote and fresh Git/GitHub read.
- BC-03/BC-07 → `TESTED` after S05 durable-file/row audits.
- BC-05/BC-06 → `TESTED` after validating S01–S04 records and the S05 model assignments; not complete until S05 lifecycle ends.
- BC-08 → `TESTED` only after the different-model final reviewer returns zero findings and all §28 docs exist.

GATE-G/TL-07 implementation/test cell must state exactly:

```text
MVP proof scope: live HTTP/API contracts plus static HTML/CSS/JavaScript checks for case selection and mode controls, dual states, adaptive approved-component manifest, dossier action and printable HTML, RTL markup, semantic native controls, responsive media rules, disclosure, and offline assets. No Playwright/real-browser interaction, paint, keyboard traversal, or print-render run is claimed; see KL-22.
```

### 17.3 Add Core §7 and success rows

Append two sections containing DOD-01–DOD-10 and SC-01–SC-06 from §2, each with implementation, exact test/harness evidence, status, and S05 slice. Their branch-time status is `TESTED` only after the associated harness step passes. DOD-08 cites KL-22 and uses the scoped proof; it is not `NOT_APPLICABLE`.

### 17.4 Supervisor-only completion promotion

The release-state PR is opened only after:

- S05 final reviewer zero findings on the fixed current diff;
- second full local harness exits 0;
- S05 PR current-head four jobs green;
- S05 squash merge observed;
- default-branch CI for the S05 implementation merge commit green;

In that narrow docs/state-only PR, the Supervisor may promote all `TESTED` rows to `COMPLETE`, preserving the evidence registry and scope text; finalize `FINAL_BUILD_REPORT.md`; set `.workflow/state.json` to `COMPLETE`; update `BUILD_PROGRESS.md`; and record S05 completion/PR facts. FR-073 remains N/A. Its four checks must be green before squash merge. The Supervisor then verifies default-branch CI for the release-state merge commit and tags `v0.2.0` on that exact commit. OQ-01 is resolved in §23.

## 18. Different-model final-reviewer briefing

The Supervisor dispatches `cursor-grok-4.6-xhigh` (or another recorded model different from both GPT Implementer and Claude Supervisor) with this exact scope:

```text
You are the DIFFERENT-MODEL FINAL REVIEWER for S05 — Final Acceptance.
You are read-only, not the Supervisor or Implementer, and cannot fix,
approve, commit, merge, or tag your own findings.

Data classification: confidential_demo. Read no secret values and use no
live industrial/private source.

Read in full: AGENTS.md; all repository rules; Authority Manifest; the
entire methodology mirror from beginning to end (the DOCX remains
authoritative); Core 01–09; build roadmap/progress/traceability/ADRs/
limitations; README/CHANGELOG/NOTICE; all implementation documents;
all S00–S05 slice records; the complete current diff; every source,
script, test, static asset, config schema, manifest, and acceptance result.

Verify the current branch/base/head and model separation. Inspect the
full S05 per-step logs through their sanitized evidence records. Do not
trust an Implementer completion statement.

Answer these owner-mandate §27.18 questions with file:line evidence:

1. Does this system genuinely satisfy the governing specification?
2. Is anything claimed but not actually implemented?
3. Did autonomous slice development introduce cross-slice inconsistency?
4. Are there architectural contradictions?
5. Are tests giving false confidence?
6. Are there missing real-world failure paths?
7. Is anything still mocked or superficial?
8. Is any feature only partly implemented?
9. Are documentation and actual behavior aligned?

Also review: public/synthetic isolation; golden invariants; route ordering;
unknown handling; threshold/config provenance; NFR-005 sample method;
list 404/422 distinctions; Docker/live journey/restart/revert evidence;
keyword dispositions; package contents; release-version/lock consistency;
config/data/core/hash protected diff; rollback; security/privacy; Gate-G
scope and KL-22; traceability status timing; and every prior residual note.

For each finding write ID, severity, requirement, evidence, problem, and
required remediation to:
.workflow/slices/S05-final-acceptance/reviewer_findings.md

Return zero findings only if no material defect remains. A green suite is
not approval. State all cannot-verify items and assumptions. End with one
verdict: REJECT with counts, or APPROVE — zero unresolved findings.
```

If the Reviewer rejects, the Implementer fixes valid findings test-first, the Supervisor reviews the fixed diff, the complete acceptance runner is rerun, and the Reviewer reviews the fixed work. Do not resubmit unchanged rejected work.

## 19. Acceptance criteria

### Residuals and behavior

1. Missing scenario on `/api/opportunities?mode=simulated` returns exact 404 JSON; policy/reconciliation/back-test errors remain 422.
2. The threshold scanner discovers nested Python files and retains its existing AST/exemption/exit contract.
3. PP public GenUI metric grid receives exact `{}` concentration metrics without rendering assumptions.
4. Public dossier HTML contains the exact `<p>N public records; 0 synthetic records.</p>` evidence sentence and no disclosure/synthetic ledger.
5. Both cached scenario objects are byte/value unchanged and retain identity after `analyze_simulated`.
6. `has_equivalence` is absent from `decision_engine.py`; selection and goldens are unchanged.
7. FR-044 and GATE-H pointers describe current code/history.

### NFR/release

8. Every one of the 18 analysis paths has one excluded warm-up and five measured calls; pytest/TestClient and live HTTP medians are each `<250.0 ms`.
9. `pyproject.toml`, `ior_mvp.__version__`, health API, and root `uv.lock` package are `0.2.0`.
10. `config/project.yaml` `project.version`, `/api/health` `version`, and analysis `authority.project_version` are all exactly `0.2.0`; `as_of_date` and all other project fields are unchanged, integrity passes, and no hashed config changes.
11. `uv lock` was run without upgrade and changes no dependency entry.
12. CHANGELOG 0.2.0 accurately summarizes S01–S05.

### Local acceptance

13. Fresh `make ci` exits 0.
14. A newly cleared pip venv installs and all eight gates exit 0.
15. Docker image builds, container starts on host port 8001, health returns 200/0.2.0, and container is stopped/removed.
16. Uvicorn starts on 127.0.0.1:8010, serves health/journeys/failures, stops, restarts under a distinct PID, serves health, and stops cleanly.
17. Journey A list/state/import/leakage contracts pass in both modes.
18. Journey B steel-public INVESTIGATE/D*-gated/data-unlock contracts pass.
19. Journey C steel-simulated dual-state/numeric/disclosure/dossier contracts pass.
20. Journey D PP public/simulated REJECT/route0/no-support contracts pass.
21. Journey E manifest/dossier JSON/HTML contracts pass for both cases/modes.
22. Unknown detail is 404; invalid mode is 422; unknown SPA path is index 200; planted evidence-integrity is exact TestClient 422 with no live override invented.
23. Isolated `git revert --no-commit 98c1a40` applies without unmerged paths and `git revert --abort` returns the temp worktree clean.
24. Final prohibited, recursive threshold, and Gate B scans exit 0.
25. Protected config/data/core/DOCX/manifest/golden diff is empty.
26. Source archive has valid CRC, every required member, and no ignored/prohibited local artifact.
27. Every keyword hit is listed with a defensible disposition; none is unresolved.
28. Migrations and backup/restore are explicitly N/A based on no database/write state; external integration is N/A while local adapters are tested.

### Documentation/traceability/review/release

29. FINAL_BUILD_REPORT, OPERATOR_RUNBOOK, DEPLOYMENT_GUIDE, updated DEVELOPMENT_GUIDE, final KNOWN_LIMITATIONS, ADRs, and REQUIREMENTS_TRACEABILITY exist and match behavior.
30. Known limitations contain KL-20–KL-30 with exact KL-22/KL-29/KL-30 scope.
31. ADR-004 is final and ADR-009 records only implementation-preserving S05 decisions.
32. Every current `IMPLEMENTED`/partial row has observed evidence and is no higher than TESTED on branch; FR-073 remains N/A.
33. DOD-01–DOD-10 and SC-01–SC-06 are traced.
34. Final reviewer uses a different model, reads holistically, and has zero unresolved findings on fixed work.
35. After any remediation, the complete runner—not a subset—runs again.
36. S05 PR current-head four checks and post-merge default-branch CI are green; skipped/cancelled is not green.
37. Only the Supervisor promotes COMPLETE, merges, and creates annotated `v0.2.0`.
38. No planner/implementer/reviewer completion or public-support authorization claim appears.

## 20. Detached execution and exact command protocol

For each focused RED/GREEN command and the final runner, write an ignored wrapper:

```bash
#!/usr/bin/env bash
set +e
(
  set -euo pipefail
  cd /home/barami/projects/industrial-opportunity-resolution-mvp
  # one exact command block from Task 0–10
)
status=$?
printf '\n__DONE__ status=%s\n' "$status"
exit "$status"
```

The comment is replaced by the task’s literal commands before execution; it is not committed. Use these concrete wrapper names: `s05-t0-preflight`, `s05-t1-red`, `s05-t1-green`, `s05-t2-characterization`, `s05-t3-version`, `s05-t4-runner`, `s05-t5-docs`, `s05-t6-first-acceptance`, `s05-t9-final-acceptance`, and `s05-t10-release`. Example launch for the Task 1 RED:

```powershell
wsl -d Ubuntu --cd /home/barami/projects/industrial-opportunity-resolution-mvp -- bash -lc "bash .workflow/logs/s05-t1-red.sh > .workflow/logs/s05-t1-red.log 2>&1"
```

Read the complete `.log` with the file tool. Require `__DONE__ status=0`. If output is empty, retry at most twice, then record the shell blocker and continue only independent work. Never inspect logs with `cat`, `head`, `tail`, or a truncating pipeline.

The complete acceptance launch is:

```powershell
wsl -d Ubuntu --cd /home/barami/projects/industrial-opportunity-resolution-mvp -- bash -lc "bash scripts/final_acceptance.sh > .workflow/logs/s05-final-acceptance.log 2>&1"
```

The process is launched in the shell tool’s managed background mode. Read both the complete outer log and generated results file; require outer `__FINAL_ACCEPTANCE__ status=0` and every internal code 0.

## 21. TDD-ordered implementation tasks

### Task 0 — Approval and immutable preflight

**Files:** read-only.

- [ ] Read `plan_review.md`; stop unless the Supervisor has approved this exact revision with zero unresolved findings.
- [ ] Repeat the complete start ritual and skills.
- [ ] Through §20 run:

```bash
git branch --show-current
git rev-parse HEAD
git merge-base HEAD main
git status --short
PYTHONPATH=src "$HOME/.local/bin/uv" run --locked --extra dev \
  python scripts/check_prohibited_files.py
```

- [ ] Require S05 branch and base `98c1a40225a94090238867bc9861e8c6544b5839`.
- [ ] Inventory/preserve every pre-existing Supervisor-owned path from §3.
- [ ] Record classification and confirm no protected/private/prohibited artifact enters work.

**Confirm:** approved plan, exact branch/base, observed dirty-path ownership.

**Validate:** changed-path intent equals §8; config/data/core/hash/goldens excluded.

**Test:** prohibited scanner exits 0.

### Task 1 — Residual RED tests and minimal fixes

**Files:** tests and three implementation files in §8.

- [ ] Add only §9.1 list test and §9.3 recursive test.
- [ ] Run:

```bash
PYTHONPATH=src "$HOME/.local/bin/uv" run --locked --extra dev \
  pytest \
  tests/test_api.py::test_missing_scenario_in_simulated_list_returns_404 \
  tests/test_threshold_literals.py::test_scan_repository_recurses_into_python_subpackages \
  -q
```

- [ ] Require two expected failures: list actual 500 vs expected 404; nested source count 0 vs 1.
- [ ] Apply §9.2, §9.4, and §9.8 exactly.
- [ ] Rerun the two nodes and require GREEN.
- [ ] Run:

```bash
PYTHONPATH=src "$HOME/.local/bin/uv" run --locked --extra dev \
  pytest tests/test_api.py tests/test_threshold_literals.py \
  tests/test_simulation_fidelity.py tests/test_golden_cases.py -q
python scripts/check_threshold_literals.py
```

**Confirm:** only 404/recursive/dead-local implementation hunks.

**Validate:** 422 and four golden outputs unchanged.

**Test:** focused files/scanner green.

### Task 2 — Characterization and NFR proof

**Files:** four characterization files plus new performance file.

- [ ] Add §9.5, §9.6, §9.7, and §9.9.
- [ ] Run each new node/file before any related production change; expect characterization/NFR tests to pass against current behavior. If any fails, investigate; do not edit expected values first.
- [ ] Run:

```bash
PYTHONPATH=src "$HOME/.local/bin/uv" run --locked --extra dev \
  pytest \
  tests/test_threshold_boundaries.py \
  tests/test_dossier_contract.py \
  tests/test_synthetic_isolation.py \
  tests/test_performance.py \
  -q
```

**Confirm:** PP `{}`, exact HTML, cache immutability, all endpoint timing are measured.

**Validate:** no new domain implementation or threshold.

**Test:** all four files green with 18 timing parameter cases.

### Task 3 — Release version test, metadata, lock, and changelog

**Files:** `tests/test_api.py`, `pyproject.toml`, `src/ior_mvp/__init__.py`, `config/project.yaml`, `uv.lock`, `CHANGELOG.md`.

- [ ] Apply the §9.10 health and health/analysis-authority equality expectations first.
- [ ] Run that node and require RED with actual `0.1.0`.
- [ ] Change pyproject/init, only `config/project.yaml` `project.version`, and CHANGELOG exactly; preserve `as_of_date` and every other project field.
- [ ] Run ordinary `uv lock` once.
- [ ] Inspect complete `git diff -- uv.lock`; require only root version hunk.
- [ ] Run:

```bash
"$HOME/.local/bin/uv" sync --locked --extra dev
PYTHONPATH=src "$HOME/.local/bin/uv" run --locked --extra dev \
  pytest tests/test_api.py::test_health \
  tests/test_api.py::test_release_version_matches_analysis_authority -q
PYTHONPATH=src "$HOME/.local/bin/uv" run --locked --extra dev \
  python -c 'import ior_mvp; assert ior_mvp.__version__ == "0.2.0"'
PYTHONPATH=src "$HOME/.local/bin/uv" run --locked --extra dev \
  python scripts/verify_integrity.py
```

**Confirm:** distribution, health, analysis authority, project config, and lock all report 0.2.0.

**Validate:** no dependency change, no project field other than `project.version`, no hashed config change, and integrity PASS.

**Test:** locked sync and health node green.

### Task 4 — Acceptance runner test-first

**Files:** new runner contract test, then shell runner.

- [ ] Create §11 test exactly.
- [ ] Run it and require RED because runner is absent.
- [ ] Implement §§10.1–10.8, including complete §10.3 assertions and stable step names.
- [ ] `chmod +x scripts/final_acceptance.sh`.
- [ ] Run:

```bash
bash -n scripts/final_acceptance.sh
PYTHONPATH=src "$HOME/.local/bin/uv" run --locked --extra dev \
  pytest tests/test_final_acceptance_contract.py -q
```

- [ ] Inspect runner source for every command/order, cleanup trap, result generation on failure, no generator, and no optional-failure escape.

**Confirm:** one executable tracked runner, no helper.

**Validate:** per-step code and safe cleanup paths.

**Test:** bash syntax/static contract green.

### Task 5 — Final documents, ADR, limitations, traceability

**Files:** docs listed in §8.

- [ ] Create §§12–15 full documents.
- [ ] Apply §16 limitation/ADR text.
- [ ] Apply §17 branch-time traceability rules and add DOD/SC rows.
- [ ] Preserve known historical run/merge evidence exactly.
- [ ] Do not invent S05 PR/CI/reviewer/merge/tag facts.
- [ ] Run:

```bash
git diff --check
PYTHONPATH=src "$HOME/.local/bin/uv" run --locked --extra dev \
  pytest tests/test_final_acceptance_contract.py \
  tests/test_ci_contract.py tests/test_static_frontend.py -q
```

**Confirm:** seven §28 documents present/updated.

**Validate:** behavior/doc/version/status alignment and exact Gate-G scope.

**Test:** doc/static contracts and diff check green.

### Task 6 — First complete detached acceptance

**Files:** runner outputs only.

- [ ] Launch the complete runner through §20.
- [ ] Read the full outer log, every failing/internal step log when applicable, `acceptance_results.md`, journey JSON, timing JSON, and keyword table.
- [ ] Require every step code 0, both process cleanups, protected diff empty, keyword unresolved count 0, and final sentinel 0.
- [ ] If a step fails, write the smallest reproducing test, fix within approved scope, and rerun the complete runner. A required config/data/core/golden/hash fix is a hard stop for owner authority.
- [ ] Record actual commands/codes/counts/timings in `implementation_log.md` and `test_evidence.md`.

**Confirm:** first full local acceptance is fresh and complete.

**Validate:** owner §27 local steps mapped; N/A items evidenced.

**Test:** runner exit 0; no subset extrapolation.

### Task 7 — Builder self-audit and Supervisor implementation review

- [ ] Re-read requirements, §19 criteria, current diff, staged-intent inventory, all generated evidence, and assumptions.
- [ ] Compare protected files byte/diff against base.
- [ ] Hand off without APPROVE/COMPLETE language.
- [ ] Supervisor reviews every changed file, every runner step/contract, test fidelity, docs, traceability, release/lock, security, rollback, and evidence.
- [ ] Valid findings return to the Implementer test-first; rerun affected tests and complete acceptance.

**Confirm:** implementation matches approved plan.

**Validate:** no scope/authority/status drift.

**Test:** all findings have reproduction and fresh proof.

### Task 8 — Different-model holistic final review

- [ ] Supervisor dispatches §18 reviewer only after Supervisor findings are zero.
- [ ] Reviewer reads full system/current diff/evidence and writes its own findings record.
- [ ] On REJECT, fix valid findings, Supervisor reviews, run complete acceptance again, then review fixed work.
- [ ] Do not reuse the Implementer model or let the Reviewer edit code.

**Confirm:** model separation and holistic scope recorded.

**Validate:** zero unresolved findings refers to fixed current work.

**Test:** reviewer evidence cross-checked, not trusted blindly.

### Task 9 — Mandatory final rerun after review/remediation

- [ ] Run the complete acceptance runner again even if the first reviewer had no findings.
- [ ] Read all output and replace `acceptance_results.md` only with this newest successful run.
- [ ] Run raw required proof commands separately in the clean venv or locked uv environment:

```bash
source .venv/bin/activate
PYTHONPATH=src python3 scripts/verify_integrity.py
PYTHONPATH=src pytest -q
PYTHONPATH=src python3 scripts/demo_smoke.py
deactivate
```

- [ ] Record actual output; do not claim success from the first run.

**Confirm:** final evidence matches reviewed bytes.

**Validate:** no post-review code/doc drift.

**Test:** complete runner plus required proof trio fresh.

### Task 10 — Explicit staging, PR, CI, merge, durable promotion, and tag

- [ ] Treat this as two sequential PRs. Do not create the release-state PR before the S05 implementation PR is merged and its default-branch CI is green.
- [ ] Supervisor runs final status/diff and stages only reviewed paths; never `git add .`.
- [ ] Exclude logs, venv, zip, `.workflow/runs/**`, and all unintended paths.
- [ ] Run staged diff check, prohibited scan, integrity, and runner static test.
- [ ] Supervisor commits, pushes, and creates S05 PR with requirements, impacts, actual evidence, review, rollback, and non-goals.
- [ ] Require current-head green:
  - `uv / Python 3.12`;
  - `uv / Python 3.14`;
  - `pip / Python 3.12`;
  - `Docker image build`.
- [ ] Verify with `gh pr checks`; cancelled/skipped is not green.
- [ ] Squash-merge the S05 implementation PR only after zero findings/current-head green.
- [ ] Verify default-branch CI for the actual S05 implementation merge commit:

```bash
MERGE_SHA="$(gh pr view --json mergeCommit --jq .mergeCommit.oid)"
RUN_ID="$(
  gh run list --branch main --commit "$MERGE_SHA" \
    --workflow CI --limit 1 --json databaseId \
    --jq '.[0].databaseId'
)"
gh run watch "$RUN_ID" --exit-status
gh run view "$RUN_ID" \
  --json conclusion,headSha,event,jobs
```

- [ ] After that default-branch CI is green, open one narrow docs/state-only release-state PR containing only:
  - `docs/REQUIREMENTS_TRACEABILITY.md` `TESTED`→`COMPLETE` promotions;
  - `docs/FINAL_BUILD_REPORT.md` final observed commit/CI facts;
  - `.workflow/state.json` `COMPLETE`;
  - `docs/BUILD_PROGRESS.md`;
  - `.workflow/slices/S05-final-acceptance/completion.md`;
  - `.workflow/slices/S05-final-acceptance/pr_record.md`.
- [ ] Require the release-state PR's four current-head checks green: `uv / Python 3.12`, `uv / Python 3.14`, `pip / Python 3.12`, and `Docker image build`; cancelled/skipped is not green.
- [ ] Squash-merge the release-state PR, obtain its merge SHA, and verify default-branch CI for that exact release-state merge commit with the same `gh run watch`/`gh run view` procedure above.
- [ ] Only after the release-state merge's default-branch CI is green, confirm tag absent, local `main` equals that release-state merge commit, and no worktree change:

```bash
git fetch origin main --tags
git switch main
git pull --ff-only origin main
if git rev-parse --verify refs/tags/v0.2.0; then
  printf 'Tag v0.2.0 already exists; stop.\n' >&2
  exit 1
fi
```

The conditional must observe that the tag is absent, and `HEAD` must equal the verified release-state merge SHA. Then Supervisor only:

```bash
git tag -a v0.2.0 \
  -m "Industrial Opportunity Resolution MVP v0.2.0"
git push origin refs/tags/v0.2.0
git show --no-patch --decorate v0.2.0
```

**Confirm:** both actual PR merges, both default-branch CI results, final durable state, and tag evidence.

**Validate:** tag points to the approved final commit; no post-tag drift.

**Test:** hosted CI and post-merge integrity/Gate B/smoke evidence.

## 22. Rollback and recovery

### Before commit/merge

Use reviewed inverse patches only:

- restore `app.py`, `decision_engine.py`, and threshold scanner hunks;
- restore modified tests/docs/release files;
- restore the root `uv.lock` and `config/project.yaml` `project.version` to 0.1.0 together with pyproject/init;
- delete only S05-created files;
- keep pre-existing Supervisor S04 bookkeeping intact.

Do not use `git reset --hard`, broad `git checkout --`, `git clean`, history rewrite, or a manifest generator. Run protected diff, integrity, Gate B, full tests, and smoke after rollback.

### After release

The atomic release rollback is a reviewed branch and `git revert v0.2.0`, as written in the Deployment Guide. Because the tag resolves to the release commit, no unknown SHA token is needed. The rollback PR must run complete local acceptance and hosted CI and preserve immutable history.

### Failure recovery

- If Docker/uvicorn cleanup fails, stop/remove only the named PID/container and record the nonzero cleanup step; never mark acceptance pass.
- If the temp revert worktree conflicts, preserve its log, remove only that worktree, and treat reversal as failed.
- If `uv lock` changes dependencies, restore the pre-run lock with a reviewed inverse one-line patch and stop for investigation.
- If a protected byte changes, stop; do not regenerate hashes.
- If a reviewer finds a domain/config requirement, return to planning/owner authority before editing.

## 23. Explicit non-goals and resolved rulings

### Non-goals

- No config change except the unhashed `config/project.yaml` `project.version` move to `0.2.0`; no data, core, methodology, mirror, authority hash, manifest, golden input/expectation, scenario, or threshold change.
- No `build_manifests.py`, hash regeneration, source refresh, live connector, Ministry upload, database, migration, backup service, authentication, authorization, or deployment.
- No new runtime data-root override solely for a planted HTTP failure.
- No decision-state/route/formula/economics/capability/rule/narrative/UI behavior change beyond list error mapping.
- No frontend redesign, CSS/token refactor, app.js split, Playwright/browser automation, screenshot claim, localization expansion, or new interaction.
- No new dependency, transitive upgrade, cloud service, microservice, queue, graph/vector database, AI chat/model, CRM, document management, calibration, outcome learning, or portfolio expansion.
- No `MONITOR`, public `ADVANCE`, greenfield, R8, allocation, or expansion-assumption behavior invented from absent evidence.
- No weakening/skipping/xfailing tests, optional CI, broad staging, force push, history rewrite, self-approval, predicted evidence, or anonymous public-support authorization.

### OQ-01 — post-merge durable `COMPLETE` persistence (resolved by PR-01)

The owner-approved route is mandatory: S05 implementation PR → squash merge → default-branch CI green → narrow docs/state-only release-state PR containing the `COMPLETE` promotions, `FINAL_BUILD_REPORT.md` final facts, `.workflow/state.json` `COMPLETE`, `BUILD_PROGRESS.md`, and S05 `completion.md`/`pr_record.md` → that PR's four checks green → squash merge → default-branch CI verified for the release-state merge commit → annotated tag `v0.2.0` on that exact final merge commit.

No direct bookkeeping commit to `main` is authorized. Implementation Tasks 0–9 are unaffected. The tag block in Task 10 runs only after the release-state merge and its default-branch CI pass.

### Conditional stop — project contract/release version semantics (resolved by PR-02)

`config/project.yaml` `project.version` moves to `"0.2.0"` with package/API release identity. The file is unhashed; `as_of_date` and all other fields remain unchanged, integrity must remain PASS, and no hashed config is touched. The test and acceptance harness require `/api/health` `version`, project contract version, and analysis `authority.project_version` to equal the same `"0.2.0"` value. No owner decision remains.

## 24. Planner self-review

- Specification coverage: owner §27 steps 1–20, §28 documents, Gates A–H, Core 09 DoD, Core 01 success criteria, NFR-005, TL-07/Gate-G scope, BC-08, residuals, release, rollback, and tag are each mapped to a task/evidence owner.
- Protected scope: only the unhashed `config/project.yaml` `project.version` field changes; no other config/data/core/DOCX/golden/hash edit or generator run is planned.
- Residual precision: dead local identified as `has_equivalence`; list 404 and recursive scan have real REDs; the three safe observations are characterization tests.
- Type/contract consistency: both performance layers use the same 18 paths, five measured warm calls, and strict `<250.0 ms`; journeys use current response keys and exact golden values.
- Evidence honesty: local runner never claims review/CI/merge/tag/COMPLETE; branch promotion stops at TESTED.
- Dynamic-fact audit: future external facts are explicitly unclaimed and dynamically recorded by Supervisor-owned files; no fake PR, SHA, run, count, or verdict is supplied.
- Assumptions: Docker/curl/Node/Git are available as required prerequisites; ports 8001/8010 must be free or acceptance fails; current packaged scenarios are the only production scenario set; `git revert --abort` is required by S05 context and must itself pass in the isolated worktree.
- Open decisions: none. PR-01 fixes the two-PR release-state route; PR-02 fixes the shared package/API/project version at `0.2.0`; the approved tag name remains `v0.2.0`.
