# Reviewer Findings — S13b Bilingual Screening Surface

Reviewer seat: `reviewer-grok` (cursor-grok-4.6-xhigh), sole approving independent reviewer; one fresh instance for the whole slice (plan, two amendments, implementation, confirmation round). Verdict files (local-only): `.autonomous-workflow/evidence/s13-public-universe-screening/plan-1-s13b-review.json`, `plan-1-s13b-amendment-1-review.json`, `plan-1-s13b-amendment-2-review.json`, `implementation-review-s13b-slot-1.json`.

## Plan reviews

- plan-1 (`560695023c…`): **REJECT** — one B3 finding F-1, owner-adjudicated VALID (OD-11): the planned passport click-journey was unsatisfiable on the frozen snapshot because none of the 5,443 screening records carries `evidence_ids`, so `/api/screening/records/{hs6}` always returns `evidence_passports: []`; the eight universe passports live on `summary.json`. Advisory: the DD-9 parity predicate could pass while English prose remained inside `dir="ltr"` islands (owner-elevated into the amendment).
- AM-1 (`341d813f…`): **REJECT** — F-AM1-1 (B3), VALID (OD-13): the amendment surfaced that `GET /api/screening` exposes no passport data at all; the owner selected the additive-route alternative (OD-12), but AM-1's oracles were written for the withdrawn default (contradictory passport-card assertions; UNAVAILABLE fixtures not mocking the new route; text still calling the route a later slice). The strengthened 11-class parity grammar and the OD-4/OD-10 protocol were found sound.
- AM-2 (`fae4ec02…`): **APPROVE**, no findings — one journey oracle, four-route explicit-state mocks with a leak check, T2 owning the additive route with named tests and a key-set pin, `API_ADDITIVE_ONLY_PASS` dry-run, KL-86/KL-89 wording, `label_leaks` mitigation. Five advisories carried to the implementer (IAC-G1…G5).

## Implementation review — product identity `8fb01e4d338380be52b01dfcc7fe26370958aefcbf1667d7c216bce29ddfb79c` (85 files on WIP HEAD `f9fec2d`, `wip_parent ab4add6`) — APPROVE, zero product findings

Recomputed and inspected by the reviewer itself: the identity (MATCH); the WIP commits `ab4add6..f9fec2d` limited to the four T8 path groups; the baselines tree OID `c2b3b66b…` from a temporary index equal to `HEAD:browser_tests/baselines`, the pin and `VISUAL_BASELINE_ENTRIES = 56`; the visual manifest (56 entries, no absolute paths, `source_tree` current); ADR-020's six canonical executions against the implementation log; `data/**`, the seven frozen configs and `src/ior_mvp/screening/*.py` other than `cli.py`/`api.py` byte-identical to `ab4add6`; `api.py` additive-only (N=157, 0 deletions); catalogues 1.2.0 (434 / 213 keys); the manifest receipt (2026-09-12T20:53:47Z; 19 authority rows, six changed; 535 snapshot rows unchanged). Commands run: `pytest -q` 2275 passed; `verify_integrity.py` PASS; `demo_smoke.py` PASS (steel INVESTIGATE / PP REJECT); `reconstruct_snapshot.py --all` PASS; functional e2e 152 passed. Not re-run by the reviewer: `make e2e-visual` (owner `make ci` and hosted CI cover it).

Visual judgement (WebP → PNG plus the drift crops, both locales): the Screening surface is coherent with the shell; Arabic is RTL-correct with mirrored chrome and Western digits; labels are catalogue-sourced; explicit states are truthful (universe AVAILABLE with eight units, partner detail not acquired, empty resilience queue, eight persistent passport cards); no `None`/`null`/TODO/raw code shown as a user label; drift on the 40 pre-existing entries limited to the new sidebar item and the version string.

Arabic parity (live AR DOM on a local server, then killed): screening summary/record and the workspace ledger — `label_leaks = []`, no Latin prose; grammar check proven real (an injected `NOT_ACQUIRED` leaks; `NO_CANDIDATE` / `un_comtrade` / `R4-F` do not). Journeys in both locales: portfolio → 06 Screening → queue → record → passport focus (URL hash unchanged) → back; empty queue truthful; UNAVAILABLE/PARTIAL mocks cover all four routes with the mock header and no real passport id leaks. KL-34: null state, `NO_CANDIDATE`, `NO_TRIGGER_FIRED`, HTTP 200; other residuals still raise; Core 07 §7.6 matches code. A-04: CLI latest snapshot by `(as_of_date, snapshot_id)`. The eight removed assertions are predicted 1.1.0→1.2.0 / 40→56 pin updates, not weakenings. The implementer's first identity figure (`611de6bf…`, non-canonical serialization) and its correction in `test_evidence.md` were judged truthful.

Advisory (not a finding): the `.workflow/state.json` s13b entry was stale — corrected by the owner lead before commit.

## Confirmation round — identity `85b34dfac9f3f9457c2722ef332ad53fbed7099024af77b757a8a8d59faedbca` — APPROVE

Identity MATCH (85 files); delta versus `8fb01e4d…` exactly `.workflow/state.json`; the file parses; the refreshed values (base `f9fec2d`, WIP `d1d1462`/`f9fec2d`, tree `c2b3b66b…`, one manifest run at 2026-09-12T20:53:47Z, state REVIEW_APPROVED) are truthful.
