# S13b plan — bilingual screening surface (planner-fable, attempt 1)

Full plan: `.autonomous-workflow/plans/s13-public-universe-screening/cycle-1/plan-1-s13b.json` (schema as `plan-1-s13a.json`; 16 DDs, 24 BFs, tasks T0–T12, 23 verification commands, 12 ACs, 9 stop conditions, 9 ODs). Decomposition: `…/cycle-1/decomposition-1.json` children[1] (owner-accepted). Base `ab4add6` = main after PR #22; branch `slice/s13b-bilingual-screening-surface`. Brief: `.autonomous-workflow/drafts/s13-public-universe-screening/planner-brief-s13b.md` (binding; owner directives 1–6 applied). Docker daemon was UNAVAILABLE at planning time (recorded; blocks only T8).

**Objective.** Mount the s13a `/api/screening` router in `app.py` ahead of the SPA fallback; deliver the analyst Screening surface (nav item 06, `#screening`) in EN and AR — summary (universe status + reason codes, coverage accounting, disposition/indication counts, unqueued persistence-only count, §8.2 mapping incl. §8.2(c) NOT_CALCULABLE), five queues with `pareto_rank`/`ordering_basis`/counts/pagination and no ordinal list, record drill-down (ledger codes through the catalogue, exclusions, warnings, needs, adjacency from the R9-S row, passport cards) with explicit UNAVAILABLE / PARTIAL / empty states; close KL-34 (Core 07 §7.6 step 8: no fired candidate signal → `NO_CANDIDATE`, null state/route, registered reason code — default `NO_TRIGGER_FIRED`, OD-1 — other residuals still fail closed) with a VERIFIED fixture recipe; satisfy UX-01 by typed `result_code`/`decision_effect_code` on every deep rule row, server-rendered bilingual `localized` ledger entries (decision_narratives 1.2.0, EN text byte-equal to engine prose) and ui_strings 1.2.0 labels for every screening code, proven by contract tests and Playwright parity tests on the workspace ledger, the methodology ledger and the screening surface; extend the visual matrix 40 → 56 entries regenerated once in the canonical container with predicted-drift analysis and the frozen-pin update; carry A-04-residual, TL-09 assertions 8–9, the pre-push portability gate, Core 01/03/07/09, ADR-020, KL-34 closed + KL-84…88, the five control documents and exactly one `build_manifests.py` run; `data/**` byte-identical; both goldens exact.

**Design in brief.** DD-1 mount before the catch-all; `mode` ignored. DD-2 CLI `_latest_snapshot` by `(as_of_date, snapshot_id)`. DD-3 codes at every `_rule`/`_synthetic_rule` call site (43 result, 21 effect codes; prose unchanged). DD-4 `narratives.localize_rule_rows` from `rule.<rid>.{name,result.<code>,effect.<code>}` templates (+87 keys → 213). DD-5 KL-34 amendment, `decision.public.no_candidate.*`, `screening_disposition` in list/banner props, `decisionChip` null-state rendering, dossier null-safe label, fixture `no-candidate-no-fired-signal.json` (advance-route-3 with two flat trade years and `technical_feasibility_confirmed=false` — verified to raise `Admitted deep case has no legal branch` today). DD-6 ui_strings 1.2.0 (+≈200 keys: chrome + dispositions, 19 reason codes, 7 queue-reason codes, 5 queue ids, 5 need slugs, 6 exclusion codes, statuses, continuity, 8 metrics, 10 screening rule names, 29 reachable result codes, 10 effect codes). DD-7 seven new ES modules under `modules/screening/` (labels, ledger-labels, summary, queue, record, record-evidence, index), literal code→key maps so `check_ui_contracts.py` stays exact, `screening.css` layer, six-column nav token at ≤900px, EXPECTED_MODULES 19 → 26. DD-8 explicit states, no fabrication, no ordinal list, passports as in-record cards with the endpoint as a technical token. DD-9 exact UX-01 predicate: zero `.source-language-island`/`.source-language-caption` in the container and no `[A-Za-z]{2,}` outside `[dir="ltr"]` subtrees, with Arabic present. DD-10 TL-09 assertions 8 (API) and 9 (DOM in simulated mode; no `mode` on screening requests). DD-11 four screening screens → 56 entries; predicted drift on the 24 non-dossier entries confined to `aside.sidebar` ∪ `.integrity-authority` (nav item 06 + ui_strings version string); dossier entries expected to pass. DD-12 pin update via a temporary-index `git write-tree`; the uncommitted candidate shows exactly two predicted pin failures, proven green in a scratch clone (OD-4). DD-13 Docker-unavailable STOP: finish T9/T10/T11-portability, interim identity, escalate with exact commands. DD-14 portability gate from an rsync copy at another absolute path + scratch-clone pin proof. DD-15 predicted test-expectation changes (tab order, nav sections, locale-aware ledger assertion, module set, catalogue counts/versions, migration allow-list, pins). DD-16 authority text and records.

**Tasks.** T0 preflight (branch/base, snapshot id, clean status, Docker state recorded, baseline gates) → T1 A-04 CLI (TDD) → T2 mount + TL-09 API + perf (TDD) → T3 ledger codes + localized rows + decision_narratives 1.2.0 (TDD) → T4 KL-34 engine/fixture/API/dossier (TDD) → T5 ui_strings 1.2.0 + config pin → T6 frontend surface, ledger localisation, null-state chip, CSS, static contracts → T7 `make e2e-functional` green in both locales (journeys, empty queue, pagination, TL-09 DOM, route-mocked UNAVAILABLE/PARTIAL, keyboard, axe, RTL, UX-01 parity, KL-34 rendering) → T8 visual matrix extension, canonical regeneration, drift table, pins (Docker; STOP per DD-13) → T9 Core 01/03/07/09, UX spec, ADR-020, KLs, control docs (before any manifest run) → T10 pre-generation regression, exactly ONE `build_manifests.py`, §11 mirror, immediate oracle → T11 portability gate + scratch-clone pin proof → T12 IAC-6 identity, test evidence, stop uncommitted.

**Gates / oracles.** `INTEGRITY PASS`; `RECONSTRUCTION PASS (2 snapshots, 20 artifacts)`; `DOCUMENT RECONSTRUCTION PASS (12 records, 12 artifacts)`; `ENTITY RECONSTRUCTION PASS (1 artifacts, 38 links)`; `SCREENING RECONSTRUCTION PASS (1 snapshots)`; `SMOKE PASS`; `FROZEN_OUTCOMES_OK`; byte-identical set prints 0 ([8]); `SCREENING_MOUNT_OK`; `RUNTIME_IMPORT_BOUNDARY_OK`; `catalogue usage exact`; `visible copy catalogue-sourced`; `ES MODULE CHECK PASS (26 files)`; `make e2e-functional` all passed; `CANONICAL CHROMIUM ASSERTION PASS revision=chromium-1234` + 4 passed (update) + 4 passed (host compare); `VISUAL_MANIFEST_OK 56`; drift table within predicted regions; `PORTABILITY_GATE_PASS`; `SCRATCH_PIN_PROOF_PASS`; one manifest run followed immediately by `INTEGRITY PASS` and 20 integrity-contract tests.

**Authority files changed (Manifest §7).** §7.3: `config/ui_strings.v1.yaml` 1.1.0 → 1.2.0; `config/decision_narratives.v1.yaml` 1.1.0 → 1.2.0 (`config/screening.v1.yaml` unchanged — the KL-34 code is registered in the deep registry and reuses `NO_TRIGGER_FIRED`). §7.4: Core 01 (FR-084…FR-087), Core 03 (§4.10, §7, §12), Core 07 (§7.6 step 8 + registered list), Core 09 (§2.7, §4 assertions 8–9, §9). Frozen oracle: `browser_tests/baselines/v0.3.0/**` regenerated once; `FROZEN_TREE_OIDS['browser_tests/baselines']`, `VISUAL_BASELINE_ENTRIES = 56`. Not hashed but amended: UX spec §1/§3/§7/§8, ADR-020, KL table, control documents, `.workflow/state.json`.

**Owner rulings requested.** OD-1 KL-34 reason-code name (default `NO_TRIGGER_FIRED`); OD-2 Core 01 placement (default new §12); OD-3 UX-02 out of scope (default; KL-85); OD-4 uncommitted candidate with two predicted pin failures + scratch-clone proof vs one WIP commit before review; OD-5 predicted drift acceptance; OD-6 Docker-unavailable path (only if triggered); OD-7 56 vs 52 entries (only if the budget is exceeded); OD-8 dossier null-state label (default include); OD-9 passport link semantics (default in-record anchor, endpoint as text).

**Seats.** implementer-composer first (file-level TDD tasks T0–T12; exact test names and strings in the JSON); reviewer-grok is the sole approving reviewer; stop uncommitted for review; no commit before APPROVE.

## Amendment AM-1 (2026-09-12; planner-fable)

Full text: `.autonomous-workflow/plans/s13-public-universe-screening/cycle-1/plan-1-s13b-amendment-1.json` (SHA-256 `341d813f6988e9ac4621b047c9762220e0f4831f290a7a66814ce12b108105d9`). Amends `plan-1-s13b.json` (SHA-256 `560695023cd626e4e6e936d7fc7d9b67465e0062d168a16eaec55597cad9f14e`, not rewritten). Triggered by reviewer-grok REJECT F-1 (B3), the owner-elevated DD-9 advisory (OD-11), and owner rulings OD-4 (owner-lead WIP commit) and OD-10 (seats). Earlier sections of this file are historical and stand as written; where AM-1 differs, AM-1 controls.

**Verified facts behind the amendment.** `api.record()` (api.py 131–157) attaches passports only through record-level `evidence_ids`; 0 of 5,443 records carry any, so `GET /api/screening/records/{hs6}` returns `evidence_passports: []` for every HS6. The eight UNIVERSE passports exist only in `summary.json.evidence_passports`, and `GET /api/screening` (api.py 71–96) does not return them (it returns `universe_status` with eight `units[]` — unit key, status, completeness basis, 64-hex `query_hash`, `selected_run_id`, `superseded_run_ids`). The passport record itself has no run-id, sha256, access-classification or terms field. The loader restores `common_record_fields` into every record (snapshot.py 560–561), so the API record payload does carry `rules_not_evaluated_at_screening` / `rules_not_evaluated_reason`; the shard bytes do not. Frozen scope forbids any new route or change to `screening/api.py`.

**New owner decision OD-12 (ruling before T6; default applied if none).** Default — in scope: the record view and the summary view render the eight universe acquisition units from `universe_status.units` as in-page cards `#universe-unit-<flow>-<year>` (source token, stage label, flow label + year, coverage-status label, completeness-basis token, query-hash token, selected/superseded run tokens) with one explicit-state line `screening.evidence.passport_detail_not_exposed`; passport ids are never reconstructed client-side. Alternative — scope change by the owner: one additive read-only route `GET /api/screening/evidence` returning the eight passports verbatim plus the units, with passport cards `#passport-<id>` and the corresponding scope/pin/oracle edits and three API tests. Planner recommends the default.

**Changed sections (C-1…C-15).** BF-25/BF-26 added. DD-6 keeps the ten `screening.record.passport_*` keys and adds ~20 `screening.evidence.*` / `screening.flow.*` / `screening.record.evidence_basis_*` / `no_record_passports` keys; `reason.no_screening_snapshot` required (IAC-3); passport `status`/`stage`/`evidence_class` and unit `completeness_basis` are technical tokens (IAC-9). DD-7: eighth module `modules/screening/evidence.js` (`renderUniverseUnitCards`, `renderCoverageAccounting`, `renderEvidenceBasis`); summary view gains a `.screening-evidence` section; `renderScreeningRecord(record, queueId, summary)` gains an evidence-basis block — coverage accounting (partner detail `0 of 0` + `PARTNER_DETAIL_NOT_ACQUIRED`, tariff tree / production aggregates `NOT_CALCULABLE`, entity artifact), the not-evaluated rules from the loader-restored record fields (test asserts equality with `summary.json.common_record_fields`), the record's own `evidence_passports` rendered only when non-empty (else the explicit `no_record_passports` state, never blank), and the unit cards rendered inside the record view with `data-universe-unit-ref` in-page anchors (OD-9); need codes only through labels; `ES MODULE CHECK PASS (27 files)`; DD-15(4) +8. DD-8: anchors resolve within the same view; nothing about a passport is derived client-side. DD-9 replaced by the two-part predicate: (a) no ≥3-word Latin prose outside `dir="ltr"` islands (the stricter `[A-Za-z]{2,}` check is retained as a0); (b) every island classified by the ordered technical-value grammar in `browser_tests/parity_grammar.py` — hs_code, hex_digest, run_id, iso_datetime, url, version, governed_id (digit required), code_token, catalogue_key, number, number_range — or a captioned `lang="en"` source span; anything else fails; per-class counts recorded to `.artifacts/e2e/…/parity-*-ar.json` and test_evidence.md; `assert_arabic_parity(report, expected_source_spans=0)`. DD-12/T8/T11/T12/identity/delivery per OD-4: implementer never touches git; STOP after pin computation with `T8_HANDOFF_WIP_PENDING`; owner lead commits exactly the four T8 paths as `wip(s13b): regenerated visual baselines (56 entries) and frozen-tree pin`; resume with HEAD = WIP; pins test fully green; `pytest -q` and `make ci` fully green on the reviewed tree; scratch-clone proof optional; IAC-6 identity base = WIP sha (`wip_parent` ab4add6) with `WIP_HEAD`, `WIP_FILE_SET_PASS`, `FULL_DELTA_FILE_COUNT`; reviewer inspects the WIP content and the full delta versus ab4add6. Seats per OD-10: `implementer-sol` first (fresh), `implementer-fable` fallback after two failed Sol attempts, `reviewer-grok` sole approving reviewer. AC-2/AC-4/AC-11/AC-12 and KL-86 reworded accordingly.

**Tests.** Renamed `test_screening_journey_queue_record_evidence_basis_back[en|ar]`; amended summary/keyboard/UNAVAILABLE-PARTIAL/parity tests; new fixture `tests/fixtures/parity/parity-negative-cases.json` driving `test_arabic_parity_predicate_fails_on_injected_english[ar-…]` (five cases: prose island, hyphenated English island, empty island, bare prose, two-word bare) and `tests/test_parity_grammar.py` (four deterministic boundary tests under `pytest -q`); `test_screening_need_codes_are_rendered_only_through_labels`. Verification: [3] fully green on WIP HEAD + delta; [22] optional; new [23] grammar/static pre-check and [24] four parity reports.

**Owner rulings requested.** OD-12 (default in-scope unit cards vs additive evidence route). Status: pending reviewer-grok re-review of AM-1 and the OD-12 ruling before dispatch to `implementer-sol`.

## Amendment AM-2 (2026-09-12; planner-fable)

Full text: `.autonomous-workflow/plans/s13-public-universe-screening/cycle-1/plan-1-s13b-amendment-2.json` (SHA-256 `fae4ec0276077609bbb049472470a682b36a55ada43b5633bda2b6ceca46a58a`). Amends the base plan (`560695023c…`) and AM-1 (`341d813f…`), neither rewritten; precedence AM-2 > AM-1 > base. Triggered by owner ruling OD-12 = ALTERNATIVE (one additive read-only route `GET /api/screening/evidence`), reviewer-grok REJECT F-AM1-1 (AM-1 oracles written for the withdrawn default are not jointly satisfiable under the alternative) and OD-13. The AM-1 default (universe-unit cards, `#universe-unit-*`, `renderUniverseUnitCards`, `no_units`) is withdrawn in full; no "default applied if none" clause remains.

**Reconciled clauses (A-1…A-10).** A-1 `scope.out[2]` permits exactly the one route; decomposition OUT wording amended by the owner. A-2 T2 owns the route — appended after api.py line 157, verbatim pass-through of `evidence_passports` and `universe_status.units`, `source_boundary: public`, `synthetic_flag: false`, no `mode`, explicit no-snapshot payload with `reason_codes: ['NO_SCREENING_SNAPSHOT']` — with four appended tests in `tests/test_screening_repository_and_api.py` (`test_evidence_route_returns_eight_universe_passports_verbatim`, `test_evidence_route_without_snapshot_is_explicit`, `test_evidence_route_ignores_mode_and_carries_no_synthetic_flag_true`, and the reviewer-IAC-2 key-set pin `test_existing_three_route_key_sets_are_unchanged`); DD-10 assertion 8 and the NFR-005 perf test cover the route. A-3 `api.py` moves from `files.byte_identical_to_head` to `files.modify (additive-only)`; verification [8] drops api.py; new [25] additive-only oracle (`API_ADDITIVE_ONLY_PASS`: first 157 lines byte-identical to ab4add6; zero deleted lines in api.py and in the existing route-test file; dry-run on the current tree passes); SC-3, O-2, AC-1, AC-6, AC-10 and the §7.2 mapping reworded. A-4 one persistent `#screening-evidence` region inside `<section id="screening">` (sibling of `#screening-view`, rendered once from the evidence route, present in every view) holds exactly eight `#passport-<passport_id>` cards (`tabindex="-1"`; passport fields as tokens/labels, endpoint as a technical token, `retrieved_at` as an ISO token, matching `universe_units` coverage fields joined by `unit_key`; unmatched units render an explicit card) and one `passport_detail_not_exposed` line (now: access classification and terms observation only); the record view's evidence-basis block carries coverage accounting, the loader-restored not-evaluated list, the explicit `no_record_passports` state and eight `data-passport-ref` anchors `href="#passport-<id>"` — no duplicate cards; anchor click focuses and reveals the card without mutating `location.hash`; `screening.record.passport_*` keys renamed `screening.evidence.passport_*`; tab-order tuple unchanged. A-5 T6 static strings updated. A-6 one journey oracle `test_screening_journey_queue_record_passport_anchor_back[en|ar]`: document-wide passport-card count == 8 in every view, `#screening-view` never contains a card, record view has 8 universe anchors, first anchor → `document.activeElement.id == 'passport-<first id>'` and card within viewport → back. A-7 UNAVAILABLE/PARTIAL mock all four routes (summary, `queues/*`, `records/*`, `evidence`) from pinned fixtures; UNAVAILABLE asserts `[data-evidence-state="none"]` with `screening.evidence.no_snapshot`, the `reason.no_screening_snapshot` label and the `NO_SCREENING_SNAPSHOT` token, zero cards, no real passport id or snapshot id in `#screening` text, every screening response carries the mock header. A-8 KL-86, AC-2, Core 01/03 §4.10/§7/§12, Core 09 §2.7/§4, UX spec, ADR-020: route delivered in s13b; residual = access classification and terms observation not on the passport (registry not imported at runtime). A-9 DD-9 residuals recorded as KL-89 (ALL-CAPS English via `code_token`; snake_case/dotted English via `catalogue_key`; Title-Case-plus-digit via `governed_id`) with a required mitigation — `label_leaks`: no admitted `code_token`/`catalogue_key`/`governed_id` island may equal a `ui_strings`/`decision_narratives` EN value after normalisation; `assert_arabic_parity` requires `label_leaks == []`; two new grammar unit tests and a conditional sixth browser negative case. A-10 T0 records the route's 404 RED baseline; tests lists and PR body updated.

**Status.** Pending reviewer-grok re-review of AM-2; no dispatch to `implementer-sol` before APPROVE.
