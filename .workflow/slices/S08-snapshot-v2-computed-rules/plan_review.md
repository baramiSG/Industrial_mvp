# Supervisor Plan Review — S08 Public Snapshot Schema v2 and Computed Rule Ledger

Reviewer: Supervisor (Claude, `claude-fable-5-1-thinking-max`). Planner: agent 334c755b (GPT-5.6 Sol, `gpt-5.6-sol-max`). Plan read: `plan.md` §1–§28 (1,991 lines) with full attention to §5 architecture, §7 golden JSON (every number checked against methodology §13/§14 and the v1 files), §8 algorithms and decision tables, §9–§10 API/rendered-text changes, §18 Core wording and generator controls, §23 task sequence, §26 open questions.

## Verification performed (2026-09-02)

- Golden v2 JSON: every numeric value in §7.1/§7.2 traces to methodology §13.1–13.3 / §14.1–14.2 or the byte-identical v1 files; every fact the methodology does not supply is the exact string `UNAVAILABLE`. `snapshot_id`/`as_of_date` unchanged, `schema_version: "2.0.0"`, `supersedes` to the historical path (SD-1). Nothing invented (SD-2).
- Rule derivations (§8): R1-D cap from config; R2 pair = two latest observed rows (2023→2024), CAGR `(Q_latest/Q_prev)^(1/years) − 1`, frozen 4-dp decomposition preserved (0.2419/0.5047/−0.2625/0.6579); R3 on both bases with rows-over-disclosure precedence and disclosed value HHI 0.36 as FULL (SD-2); R4-D row path and disclosed path, descriptive only; R5 coexistence proxy plus §3.3 formulas with per-input `UNAVAILABLE` reasons; R9-S from typed signals and known-failure gates; R10 FULL on designation, DEGRADED resilience review when R3 fires; R11 from ratio AND established nameplate capability (PP 450,000 + 720,000 t/y observed). Execution vocabulary unchanged. Sound.
- Historical retention and loader non-recursion (§5.3), forbidden authored-outcome keys (§5.2), KL-32 `--user` fix with ownership sweep (§5.7), dossier contradiction register with public/synthetic separation (§5.6), 12 enumerated rendered-text changes (§10) driving one baseline regeneration: consistent with `context.md` SD-1..3 and SLICE_GRAPH S08.
- Sanad ledger recounted mechanically: 43/120/19/72/5 — matches §28.

## Round 1 findings and rulings

| ID | Severity | Requirement | Evidence | Problem | Required correction |
|---|---|---|---|---|---|
| PR-01 | MEDIUM (RULING) | Methodology §14.1 (ratio computed from gross flows), §4 R11; Core 07 v2 R11 derivation; the slice's own purpose (no authored artifacts) | §8.9 and §9 keep `metrics.export_import_value_ratio` as JSON `null` for steel although the frozen steel rows carry both `imports_usd_m` and `exports_usd_m` for every year (2024: 27.1 / 236.9 = 0.1144). The v1 author supplied the ratio field only for PP; §7.1 v2 steel rows omit it and §8.9 treats "unavailable" as the steel outcome. | A computable measure is presented as unknown — the same kind of author-shaped input S08 exists to remove — and steel R11 stays `DEGRADED` for a non-data reason. | Compute `export_import_value_ratio = exports_usd_m / imports_usd_m` from the trade row whenever both values are present and positive; when a disclosed ratio is also present, validate consistency with the computed value (tolerance for the one-decimal disclosed figure, e.g. PP 4670.5 / 92.3 = 50.60 vs 50.6) and report both. Steel R11 becomes `FULL`/`false` with ratio 0.1144 (< 50). Re-base `test_r11_missing_ratio_is_degraded_and_does_not_fire` onto a fixture whose exports are genuinely `UNAVAILABLE` so the `DEGRADED` path stays tested. The migration-equivalence test then asserts equality on all fields except an explicit, documented allow-list of intended differences (steel R11 execution/ratio; new metric keys), so it remains an honest anti-drift proof. Record the change in ADR-012 and the Core 07 v2 R11 text. Update §7.1 (add computed-ratio semantics or keep rows and compute), §8.9 table, §9, §10 if the R11 result text differs, §17, §21. |
| PR-02 | LOW (RULING — resolves OQ-02) | Manifest §6.7 / AGENTS #7 (thresholds versioned per rule, not reused implicitly); methodology §5.2.2 "disable the diagnostic when coverage is inadequate" (no figure given) | §8.5 step 3 reuses `thresholds.rules.R4_F.minimum_valid_value_coverage` as the R4-D coverage gate. | A rule-level threshold borrowed from another rule is an implicit coupling that hides R4-D's own calibration. | **Approve** quantity-weighted quartiles (named fractions), the coverage gate and the non-confirmed `outlier_candidate`, but declare the gate explicitly: add `rules.R4_D.minimum_valid_value_coverage: 0.70` to `config/thresholds.v1.yaml` with `rationale: "Methodology §5.2.2 disables the degraded diagnostic when comparable coverage is inadequate but states no figure; the R4-F comparable-quantity coverage floor is adopted as the initial R4-D gate."`, `sector_scope: all`, `revision_date: "2026-09-02"`, `metadata.version` 1.1.0 → 1.2.0 (§7.3, ADR-012); boundary tests 0.6999 / 0.7000 / 0.7001; the threshold scanner must see the new key. |
| PR-03 | LOW (RULING — resolves OQ-01) | Core 06 §9 / catalogue governance | §10 RT-07..12 Arabic strings. | Governed Arabic text needs an approval record. | **Approved** as Supervisor-approved, owner-amendable defaults (not "owner-approved"); record in ADR-012 and the PR; implementation may not vary them. Catalogue 1.0.0 → 1.1.0. |
| PR-04 | LOW | Manifest §8 single-generator discipline; ADR-005/008/011 precedent | §18.5/§23 schedule two generator runs by design (Core/data first, catalogue later). | Two planned runs double the governed-diff audit for no reason; the catalogue edit is known up front. | Sequence all governed edits — Core 02/04/07/09, `thresholds` 1.2.0, catalogue 1.1.0, data moves and v2 files — before **one** `scripts/build_manifests.py` run after full regression; a second run is permitted only for a later justified governed fix and must be recorded (as in S07). Update §5/§6.5/§18/§23/§24 and the `persona.md` note; enumerate the single expected diff (two moved v1 rows, two new v2 rows, thresholds row, catalogue row, Core 02/04/07/09 rows, `generated_on`). |

No BLOCKER/HIGH findings. Schema, derivations, historical policy, KL-32 fix, dossier register and TDD sequence are approved as planned.

## Round 2

Planner (same agent) revised `plan.md`. Supervisor verified in the text: PR-01 — the ratio is computed from every row with positive imports and exports, the disclosed ratio is validated within 0.05 and reported alongside (§7 rule 6, §8.9 lines 1138–1140: steel `FULL`/false at 0.1144; PP 50.6013 vs 50.6 consistent; RT-06 result text); the DEGRADED branch retains a fixture with `UNAVAILABLE` exports; the migration-equivalence proof carries an explicit allow-list. PR-02 — `rules.R4_D.minimum_valid_value_coverage: 0.70`, thresholds 1.1.0 → 1.2.0 with the stated rationale, boundary tests; OQ-02 resolved. PR-03 — six Arabic strings recorded as Supervisor-approved, owner-amendable defaults; catalogue 1.1.0; OQ-01 resolved. PR-04 — one generator run after all governed edits and full regression, second run exceptional and recorded (§5 line 297, §18.6 diff enumeration).

Record-integrity correction RI-01: the planner's §28 ledger stated SPECIFIED = 100; the Supervisor's mechanical count is 135 (VERIFIED 43, DERIVED 18, PROPOSED 63, OPEN 0 all match). The Supervisor corrected the §28 line and annotated it. Planner seats continue to misreport the mechanical count; the Implementer prompt will require the recount command output to be pasted verbatim into the records.

Unresolved findings: **0**.

## Decision

**PLAN_APPROVED** — 2026-09-02, Supervisor. Implementation may begin on `slice/S08-snapshot-v2-computed-rules` from base `9f045a4ecf929b82a0c4ad9013d255e148bc837d`, executing the plan's TDD sequence in order: schema v2 validator and pure metric layer RED→GREEN; computed rules with boundaries; historical move and v2 goldens; migration-equivalence proof before removing `rule_context`; computed R11 into `_public_decision`; dossier contradiction register (catalogue 1.1.0); KL-32 `--user` fix and ownership test; functional browser suite green; baseline regeneration through the fixed canonical container; all governed edits (Core 02/04/07/09 v2 markers, thresholds 1.2.0, catalogue 1.1.0, data moves) then ONE `scripts/build_manifests.py` run with the enumerated diff; ADR-012; docs; full verification. Authority changes permitted under ADR-010 and this approval: exactly those listed. No `sector_profiles`, `evidence_policy`, `data/synthetic`, `data/golden` or DOCX change; goldens exact.
