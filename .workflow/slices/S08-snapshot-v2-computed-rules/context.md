# Context — S08 Public Snapshot Schema v2 and Computed Rule Ledger

## Base and branch
- Base: `main` at `9f045a4ecf929b82a0c4ad9013d255e148bc837d` (S07 squash merge; default-branch CI run 33623528164 success, five jobs). Branch: `slice/S08-snapshot-v2-computed-rules`.
- Working tree carries Supervisor-owned post-merge S07 records (`.workflow/slices/S07-bilingual-interface-foundation/pr_record.md`, `completion.md`; modified `.workflow/state.json`, `docs/BUILD_PROGRESS.md`, `docs/KNOWN_LIMITATIONS.md` (KL-21/31 closure facts, new OPEN KL-32), `docs/REQUIREMENTS_TRACEABILITY.md` (S07-CI registry, V3-A8/A10/G7 COMPLETE), `.gitignore` (`.artifacts/`)). They are committed with this slice; the Implementer must not modify them except to add S08 rows.
- State: 377 default tests (3.12/3.14); browser suite 18 tests / 122 nodes (118 functional, 4 visual) with 40 governed lossless WebP baselines; `make ci` = prohibited → threshold → UI contracts → compile → ES modules → integrity → Gate B → pytest → smoke → preflight → functional e2e → visual e2e; five hosted CI jobs.

## Governing decisions for this slice
- `SLICE_GRAPH.md` S08 (approved): schema v2 with partner-level rows and typed evidence; rules computed from fields (R4-D dispersion/coverage, R5 coexistence plus §3.3 formulas, R9-S from producer process-family/signals, R10 from a responsible-authority designation field, R11 from export/import ratio plus established capability); R3 on value and quantity bases (I2, fires on either basis, `NOT_CALCULABLE` on a basis without rows); R2 CAGR over the observed span (I3); KL-30 (R1-D cap from config); `rule_context` removed; contradiction register in dossier JSON/HTML (D4). Decision selection still consumes `public_decision_contract` until S09.
- **Supervisor decision SD-1 — snapshot-ID policy for schema migration (DERIVED from Manifest §7.5 and Core 05 §5 step 12; to be recorded in ADR-012):** re-expressing identical frozen facts (same as-of date, same source rows, same methodology §13/§14 numbers) in schema v2 is a *representation* change, not an evidence refresh. The `snapshot_id` and `as_of_date` stay unchanged; the record gains `schema_version: "2.0.0"` and `supersedes: "<historical v1 path>"`. The v1 files are moved byte-identical to `data/snapshots/public/historical/v1/` (their manifest rows change path, not hash); the repository loader stays non-recursive over `data/snapshots/public/*.json` and must reject a historical file if it appears there. A test proves v1 and v2 bytes-in → identical rule ledger and public state.
- **Supervisor decision SD-2 — the two goldens may carry only the methodology's frozen public numbers.** Partner-level *value* facts available: steel 2024 China + Korea 76.3 % of import value, partner-value HHI 0.36, Austria ≈1.0 kt at ≈USD 4,179/t, bulk band USD 776–864/t; PP export/import value ratios 74.9×/52.8×/50.6×, 2024 import UV ≈1,649 vs export UV ≈1,097. No full partner row set and no partner quantities exist in the methodology. Therefore schema v2 supports both `partner_observations` (rows; preferred; enables HHI on both bases) and `disclosed_concentration` / `disclosed_dispersion` aggregates (status `calculated` by the frozen source, with the citation). R3 value basis stays `FULL` (computed by the worked case from complete partner values) with HHI 0.36; quantity basis is `NOT_CALCULABLE` for both goldens. Nothing is invented.
- **Supervisor decision SD-3 — KL-32 is in scope as a prerequisite:** rule-result wording and the dossier contradiction block change rendered text, so the 40 visual baselines must be regenerated; `scripts/run_visual_baseline_container.py` must first run the canonical container as the host user (`--user uid:gid` or equivalent) with a test asserting host ownership of written files; regeneration follows the governed procedure with `IOR_BASELINE_CHANGE_REF="S08-…"`. Rendered-text changes must be minimal and enumerated.
- Authority changes (ADR-010 basis; slice ADR-012): Core 04 v2 (§2.3 partner observations, §2.8 passport fields incl. `period`, `retrieved_at`, `transformation`, `reviewer_status`, `contradiction`; §6 invariants: no authored rule outcomes), Core 07 v2 (§3 R2 CAGR, R3 dual basis, R4-D derivation, R5 formulas, R9-S/R10/R11 derivations; §10), Core 02 §3 R-rule map updates, Core 09 v2 (§2.3 rule tests, §3 boundaries, §5 snapshot policy incl. schema migration); `data/snapshots/public/**` (v2 files + historical v1); `data/manifests/snapshot_manifest.json`; one `build_manifests.py` run (a second only if a later governed edit in this slice is justified and recorded). No `thresholds`, `sector_profiles`, `data/synthetic`, evidence-policy, catalogue (unless a new UI string is needed for the contradiction block — then catalogue 1.1.0 and a justified second run) or DOCX change.
- Traceability maintenance (Core 02 §9): every new domain function mapped before review closes.

## Observed facts (Supervisor read, 2026-09-02)
- `rules.py`: R4-D fires from `rule_context.r4_degraded_dispersion_signal` (l.596); R5 from `domestic_production_exists`/`material_imports_exist` (l.608–645, share `NOT_CALCULABLE` always); R9-S from `same_process_family_plus_signal` (l.678); R10 from `strategic_resilience_review` (l.690); R11 from `generic_capacity_reject` + ratio (l.704–731); R3 uses `partner_value_hhi`/`largest_supplier_share`/`top_two_value_share` from `supplier_metrics_2024` (l.539–584; PP has no block → DISABLED); R2 growth = `latest/previous − 1` (l.497); R1-D cap literal `"C"` (l.478).
- Snapshots: steel has `supplier_metrics_2024` {top_two_suppliers, top_two_value_share 0.763, partner_value_hhi 0.36, bulk_uv_range_usd_t [776, 864], small_high_uv_observation {Austria, 1.0 kt, 4179}}; `domestic_capability` {verified_present, same_process_family, coarse_adjacency_signals[4], producer_evidence[2], public_dimension_states, unresolved_hard_gates}; `rule_context` {6 flags}; `public_decision_contract`; `evidence[4]` (one `contradiction`). PP: no supplier block; `rule_context.generic_capacity_reject: true`; trade rows carry `export_import_value_ratio`.
- `decision_engine._public_decision` (l.37–65) reads `rule_context.generic_capacity_reject` and R11 — S08 must give it an evidence-derived replacement input (e.g., the computed R11 row) so that removing `rule_context` does not break selection; selection logic itself is S09.
- Golden assertions: steel R1-D/R2/R3/R4-D/R9-S fire, INVESTIGATE, D* None; PP REJECT route 0, R2 not fired, R11 fired; `test_rules.py` asserts R2 FULL, R11 DEGRADED when ratio absent, R3 firing on HHI or largest share, R4-F DISABLED, R5 threshold from config; boundary tests in `test_threshold_boundaries.py`.
- Dossier (`dossier.py`) projects no contradictions; `evidence` rows carry `contradiction` on `S-UNICOIL-SPEC`. UI evidence ledger renders class/source/title/status/boundary only (S07 `renderers/evidence.js`).
- Baseline tooling: `scripts/run_visual_baseline_container.py` + `browser_tests/visual_container.py` run Docker without `--user`; written files are root-owned (KL-32). Docker 29.7.2 available; canonical image already pulled locally; host browser needs `LD_LIBRARY_PATH=/tmp/ior-s06-browser-libs`.

## Constraints
- Goldens exact: steel INVESTIGATE (R1-D, R2, R3, R4-D, R9-S fire; ΔlnV ≈ 0.242, ΔlnQ ≈ 0.505, ΔlnUV ≈ −0.262, share ≈ 0.658; HHI 0.36); PP REJECT route 0 (R2 not fired; R11 fires at 50.6× > 50); both simulated outcomes unchanged.
- No engine threshold literals; scanner must stay green; new keys only through `thresholds.v1.yaml` §7.3 if truly needed (none expected).
- Public dossier: zero synthetic rows; contradiction register lists only public contradictions in public mode and marks synthetic ones in simulated mode.
- Rendered-text changes enumerated in the plan; baselines regenerated once through the fixed container after the functional suite is green.
- `PYTHONPATH=src pytest -q` browser-independent; `make ci` and hosted CI within budget.

## Records to produce
`plan.md`, `plan_review.md`, `implementation_log.md`, `implementation_review.md`, `reviewer_findings.md`, `test_evidence.md`, `pr_record.md`, `completion.md`; ADR-012.
