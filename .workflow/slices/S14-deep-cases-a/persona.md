# Persona — S14 Deep Demonstration Cases A

**Discipline stance:** data engineering and deterministic decision modelling on a governed evidence pipeline (public acquisition → passported artifacts → case briefs → engine proof), with the analyst portfolio and simulation work in the second child.

**Seats (fresh pair for S14):** planner `planner-fable` (decomposition-1, plan-1-s14a, amendments) → supervisor plan review → `reviewer-grok` plan review → implementer ladder starting at `implementer-sol` (owner decision OD-10, recorded deviation; `implementer-fable` fallback) → supervisor implementation review → `reviewer-grok` implementation review (sole approver) → owner acceptance → delivery by the owner lead agent. No seat approves its own work.

**What S14 is:** at least five deep cases across `coated_steel`, `fabricated_aluminium` and `technical_plastics`, each with a v2 public snapshot built only from acquired, passported evidence and a reconciled Class-D scenario with planted ground truth, exercising the outcomes and route logic the evidence honestly supports, with bilingual narratives, golden tests, Playwright coverage and route-coverage matrix rows — the two original cases untouched. Split (OD-1): **s14a** (deterministic case selection from the frozen screening snapshot; `product_families` 1.1.0 for fabricated aluminium and the technical-plastics conversion headings; producer-disclosure and WITS partner windows; entity mentions v2; CaseBrief artifacts and a deterministic public-snapshot builder proven by the public engine; no portfolio change) → **s14b** (five public snapshots and scenarios into the portfolio, goldens, `project.yaml`, Playwright, §9 route rows, one visual regeneration under the owner-lead WIP protocol).

**What it is not:** no fabricated public field, nameplate, capability state or product fact without a verified verbatim span (capability stays `U`/UNAVAILABLE otherwise); no forcing of any outcome — a brief that yields a state other than the expected one stops the work for honest reassessment; no route 8 (S16); no pharma/API or fertilizers (S15); no edit to the frozen roots in s14a; no synthetic marker anywhere in public artifacts.

**Non-negotiables held:** synthetic evidence never changes `real_decision`; unknown capability never improves adjacency; thresholds only from versioned YAML; both public goldens exact; one `build_manifests.py` run per child; robots/terms consulted and recorded before any request, no bypass of any wall or TLS failure; write-once raw store; portability copy check before push; deterministic tests; no absolute paths in governed artifacts.

**Data classification:** public evidence only (official producer disclosures, UN Comtrade/WITS observations, WCO nomenclature); no personal data; the Comtrade key is never read by agents and is not needed for the WITS window.
