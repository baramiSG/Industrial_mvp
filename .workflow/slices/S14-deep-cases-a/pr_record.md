# PR Record — S14a Case Selection, Evidence and Families

- PR: https://github.com/baramiSG/Industrial_mvp/pull/25
- Base: `ab6211f86307ad95a0e61f0597664023f09b7177` (main after the S13b records, PR #24); `origin/main` equalled the base at delivery (no integration rebase needed).
- Commits on the slice branch (no force push):
  1. `13eeea1e9ea1300cfc67c431112062719ee7e227` — candidate identity `78c9b0c6c0085cdc89e303d159a56041b5923fd720b64637ff6829c5009fc6cb` (153 files) plus seven slice-record files; `reviewer-grok` implementation APPROVE zero findings; owner acceptance OD-20.
  2. `274e7df` — CI-F-01 test-mechanism fix, candidate identity `89ad2d5cd9a81f1a8d28e0a1ac6f19af35106598db571135d3ee00354cf659de` (two test files) plus two updated slice records; bounded APPROVE; OD-22.
- Hosted CI:
  - `13eeea1`, run `34732904164`: Docker build green; `uv / Python 3.12` failed two tests (`2 failed, 2414 passed`) — `test_make_acquire_partners_quotes_variant_ampersand_for_cli_contract_and_url` invoked the guarded live `acquire-partners` path (refused by the Makefile's `CI may not acquire` guard, by design) and `test_1_3_0_history_copy_is_byte_identical_to_superseded_config` compared against `git show HEAD:config/acquisition_sources.v1.yaml` (the 1.4.0 candidate on CI). Both passed locally only because local HEAD was the base commit. Remaining jobs were cancelled by fail-fast.
  - `274e7df`, run `34733383228`: all five jobs succeeded on the exact head — uv Python 3.12 (4m12s), uv Python 3.14 (3m23s), pip Python 3.12 (4m24s), Docker image build (22s), browser Chromium Python 3.12 (7m51s).
- CI-F-01 (owner decision OD-21): test-only correction — quoting asserted via `make -n` dry run plus the Python-level contract capture (no CI-guard bypass); the superseded 1.3.0 config pinned by SHA-256 `fbe061496bc6c9b5ddd039b25536d78fb24c22ba3d84650fcd2c9374977673cc` (byte-identical to `git show ab6211f:config/acquisition_sources.v1.yaml`) with `metadata.version == 1.3.0`; verified in a CI-equivalent committed scratch clone with `CI=1` (2416 passed) by implementer, owner lead and reviewer. New standing rule: pre-push CI-equivalent scratch-clone gate.
- Merge: squash with owner merge approval OD-23 at head `274e7df`; merge commit `ec859f72a1b438b7e5946334f60a4d72cbbf1fb1`; remote branch deleted; `git diff 274e7df ec859f7` empty.
- Default-branch CI: run `34733717382` on `ec859f7` — all five jobs succeeded.
- Post-merge verification on local `main` (`ec859f7`, equal to `origin/main`): `INTEGRITY PASS`; `2416 passed, 1 warning`; `SMOKE PASS` (steel public `INVESTIGATE`; polypropylene public `REJECT generic capacity`); `CASE RECONSTRUCTION PASS (0 snapshots, 5 briefs)`, `RECONSTRUCTION PASS (4 snapshots, 34 artifacts)`, `DOCUMENT RECONSTRUCTION PASS (20 records, 20 artifacts)`, `ENTITY RECONSTRUCTION PASS (2 artifacts, 44 links)`, `SCREENING RECONSTRUCTION PASS (1 snapshots)`.
- Manifest §7: `config/product_families.v1.yaml` 1.0.0 → 1.1.0; `config/acquisition_sources.v1.yaml` 1.3.0 → 1.4.0; `config/history/**` retained superseded bytes; Core 02/04/05/09 additive text; three owner-authorized `build_manifests.py` runs receipted in ADR-021 (2026-09-13T00:25:27Z T9; 01:15:40Z AM-2 Case B; 02:28:00Z AM-3 Case C); 535 base snapshot rows unchanged (final 628); authority set 19 with six authorized rows changed. Frozen public/synthetic/golden roots, browser tests, existing partner snapshots and top-level modules byte-identical.
- Approved plan and amendments: `plan-1-s14a.json` (`8fbfa4e6…`), AM-1 (`8c61a0a2…`), AM-2 (`5bccc8a5…`), AM-3 (`6e596932…`); decomposition `decomposition-1.json` (`48741467…`).
- Owner decisions: OD-1…OD-23 in `.autonomous-workflow/owner-decisions/20260913-s14-plan-1-rulings.md` (local-only); pre-rulings PR-1…PR-7 (2026-09-12).
- Seats: planner `planner-fable`; implementer `implementer-sol` slot 1 (OD-10); reviewer `reviewer-grok`; delivery owner lead agent (Cursor, `claude-fable-5.1`).
- Secret handling: the UN Comtrade key was loaded in-shell from the git-ignored `.env` only inside the W-C acquisition commands and never printed, stored or committed (fail-closed scan of every staged path: zero hits; stored contracts carry no `ocp-apim` header value).
