# DRAFT — S18a PLAN v1 Amendment 3, Revision 1: geometry-gate correction

**Status:** narrow planner correction draft for Claude Code's independent re-review and owner re-acceptance. It is **not approved**, does not authorize a probe rerun or resumed implementation, and is kept outside the retained candidate. The approved AM-3 SHA-256 `396492a0a8d949f94b575785637a35633aa0546fe6b4040281a2933ca8ba7067` and its evidence bundle SHA-256 `b3cc5547a6b8203da5e9e18d8b951fbbc7694d02bbabd0f98b9d54333a9e2451` remain unchanged. The owner accepted the original AM-3, but its §5.3a gate stopped on text-driven flex widths; this revision supersedes that plan **only after** Claude approves these new bytes and the owner re-accepts the new hash. Proposed precedence then: AM-3 R1 > approved AM-3 > corrected AM-2 (`4022bceed625101b4de66c4fcda06de4f6084d09f256d9e60677f187e6eee662`) > AM-1 (`b362205aa72d7b6a90892d97e185a1d2c66f57a30de90f6bda31bd1459b137ab`) > PLAN v1 (`1b300b0f05fcb7b658f6df95d1c8022ad179554106a1a77d70fbd72704cf017f`). All unrelated obligations and historical approvals remain.

## 1. Trigger, evidence and classification

The single authorized S18a canonical refresh stopped correctly under AM-2 step 10: a prediction of 112 byte-identical WebPs was falsified for two desktop public-steel `evidence_to_change` captures. The design error was treating a graph input change as pixel-neutral even though the Relationships list renders the graph's run-qualified Decision ID. `graph/projection.py:_engine_run_id` changes that ID when governed inputs change; `graph/model.py:relationship_key` then changes derived edge keys. The inspected fixture calls `evidence_linkage` on the current projection. The accepted, unchanged `investigation-evidence.json`/`.md` establish B1(a)–(e) and the captured-pixel portion of B1(f), including exact localization, 22 unchanged graph decisions and current public/simulated real-decision equality across all eleven cases. The third Relationships row extends below the 900-pixel viewport; its off-screen full height is not proven by these images and requires the later DOM geometry/browser gate. No source correction or generator retry was made. `affected-test-pin-inventory.md` supplements the accepted evidence for A3-F1; it is not a test run.

**Recommended Manifest §7 class:** §7.2 implementation-preserving with a specifically governed visual-oracle change. Node/edge semantics, decisions, routes, frozen public/scenario/golden data and operating config remain unchanged; only run/projection identities and their displayed provenance change. This is not §7.1 editorial because pixels changed, nor §7.3 configuration or §7.5 evidence refresh. The owner must accept this classification and exact oracle exception; the required unit/golden/integrity proof remains in Task 8 below. If the owner or Claude finds a semantic or layout defect, this exception is rejected and AM-2's stop remains in force.

## 2. Exact visual exception, conditional on review and owner acceptance

No new screenshot path, viewport, capture, UI/static/browser source or visual tolerance is allowed. Of 112 WebPs, 110 remain byte-identical to HEAD. Permit only these two already-produced images, with their current bytes; do not regenerate them under this amendment:

| Path under `browser_tests/baselines/v0.3.0/` | Prior SHA-256 (bytes) | Candidate SHA-256 (bytes) |
|---|---|---|
| `ar/desktop-1440x900/journey-i-graph-evidence-to-change.webp` | `0c6577782b4c9aac2618ddd944532f7ddde0c838498880bb7992fb5ba45fe230` (150,666) | `5036d3830cdf61a8d5125c97789864f9a697c0a79ca445b30321edbaa7faac4a` (149,804) |
| `en/desktop-1440x900/journey-i-graph-evidence-to-change.webp` | `95656ee425e4bf3521498ab2de77fe61ed5662c10bbbc11b8b7f14272ba0300b` (163,320) | `36c00ff77c934879dcf4d4d0ee5ecf65974784075f2139fa7fb222e71a8eebf1` (162,080) |

Bind the existing visual manifest SHA-256 `216aa8e0a85f35151a065f53e057f314bccadc14a6bdc802be9540ffb880dfb2`, change ref `S18A-EXECUTIVE-API-PROVENANCE-1`, and candidate `browser_tests/baselines` tree OID `dfb2612b9e637367ae4441db489dda4f5d17bf36` obtained from the separate checkpoint candidate tree. Reconfirm all three immediately before changing the pin. Do not change budgets, comparison thresholds, paths, dimensions or other baseline bytes. The current refreshed manifest passed read-only structural/provenance/hash/budget validation, but a fresh canonical browser comparison is still required later.

## 3. Core 09 decision — recommend the minimal historical-expectation route

**Recommendation: option (b) from Claude's review.** Keep the existing Core 09 bytes and `tests/test_executive_isolation.py:38–40` unchanged in S18a. Core 09 lines 712–718 explicitly call 112-image equality a *pre-generation expectation*, not an observed result. AM-3 supersedes that failed prediction for S18a acceptance; do not cite the old assertion as proof of pixel equality. Correct the actual outcome in non-graph-input ADR-028, BUILD_ROADMAP, BUILD_PROGRESS, and the `V3-S18A-SCOPE` traceability row. Add **KL-132** in `docs/KNOWN_LIMITATIONS.md`, narrowly naming the two approved run-ID captures, the historical Core 09 sentence, and its correction at the next separately authorized Core/graph generation (target S18b). Do not edit Core 09 or graph-discovered source after the bound S18a generation, and do not silently allocate another graph, manifest or visual run.

This route preserves the bound 228-input graph and one-shot generation receipts. The owner accepted option (b) under the original AM-3; **re-acceptance of this corrected R1 hash remains pending**, without reopening that substantive choice. If the owner instead requires Core 09 correction now (option a), this R1 route does not authorize it: revise the plan with ID-free Core 09 wording, its test, a new read-only graph identity binding, and separate second graph/manifest/visual allocations before any execution. The regenerated two-image hashes and tree OID would then have to be rebound; the values above cannot be assumed final.

## 4. Roles and review authority

The owner's 2026-09-23 chat instruction assigns **Codex as planner, Windsurf Cascade as implementer, Claude Code as approving independent reviewer** for continuation. That already-authorized assignment is now recorded at `/home/barami/projects/industrial-opportunity-resolution-mvp/.autonomous-workflow/owner-decisions/s18-visual-release-1.json`: Codex maps to the S18 planner seat, Cascade to the implementer seat, and Claude Code to the `reviewer-grok` approving-reviewer seat named by `AGENTS.md:69` for this continuation. No further role authorization is requested. This is an owner-directed seat reassignment, not an execution-mode change; preserve `MODE_CHANGED=NO`. The historical Grok PLAN v1/AM-2 approvals and Claude's original AM-3 verdict are already in `plan_review.md`; preserve them verbatim and do not retrospectively change their authors. After R1 review, append Claude's R1 verdict with the **full R1 plan SHA-256 and the new revision `SHA256SUMS` SHA-256**, without replacing the original AM-3 binding. Update `persona.md` or an additive role handoff entry to name the current roles. Do not rewrite historical `.workflow/state.json` S05 records as if they described S18a; any current-state reconciliation is separate and evidence-bound.

The live owner-control file at the absolute path above confirms the seats and accepts the bounded visual exception, §7.2 classification and Core 09 option (b) **under the original AM-3 hash**. It does not accept this R1 gate correction; a separate R1 owner re-acceptance draft is outside the candidate and remains pending Claude review. The checkpoint's Cursor Other Models percentage is still unverified after the stopped operation; do not start a Cursor F1 semantic unit without its external quota check. This plan does not change fallback mode or invoke prior Cursor agents.

## 5. Superseding Task 8 verification for the exact final candidate

AM-1/AM-2 supersede PLAN §10's stale “no graph input” and Task 8's “no canonical update”; AM-3 supersedes only their now-falsified all-WebP-equal expectation. This R1 corrects its geometry pass criterion and permits truthful status reconciliation. The stopped candidate tree `effb73f21b21183ad059c2011bd088bf0a8c27c4` must remain unchanged during planning; after R1 is approved and copied into the candidate, a **new exact final tree** must be built and verified rather than treating `effb73f2…` or checkpoint tree `0eecea9d…` as final proof:

1. The stopped `effb73f2…` candidate already contains the **only** permitted visual-tree literal at `tests/test_frozen_public_evidence_pins.py:48` (`dfb2612b9e637367ae4441db489dda4f5d17bf36`) and live-manifest expectation at `tests/test_s17_status_docs.py:35` (`S18A-EXECUTIVE-API-PROVENANCE-1`). Cascade retained pre-edit proof; on continuation, reverify the two image SHA-256s/sizes, all 110 unchanged WebPs, manifest digest, baseline tree OID, candidate status, no new graph-hashed input change, those two exact test literals, and every historical S17 assertion at lines 36–49. No further test edit is permitted; under Core 09 option (b), `tests/test_executive_isolation.py` remains unchanged. After Claude approves R1 and the owner re-accepts it, carry **exact R1 bytes** into the slice record and make only the already-permitted additive review/role and non-graph-input truth/status corrections named above and in §5.1a, including KL-132. The owner-control ruling lives at the absolute main-worktree path in §4 and is local/ignored, not part of the S18 Git candidate tree. Leave the ordinary index unstaged.
2. Use a **new** temporary alternate index, leaving both existing indexes intact, to collect the exact final candidate and compute its Git tree OID. These commands may write **unreferenced Git objects only** to the source repository object store; they must not create/update refs, `HEAD`, the ordinary index or the existing checkpoint/candidate index. From a copy of that new index, materialize its tracked bytes into an empty scratch directory with `git checkout-index --all --prefix=<scratch>/`. Create a local-only Git repository there, commit those exact bytes with local test identity and no push, then require scratch `HEAD^{tree}` equals the final candidate tree OID; require clean status, no extra frozen-root paths, and `HEAD:browser_tests/baselines` equals the new pin. The scratch commit is verification infrastructure, not S18 delivery or a source-worktree commit.
3. In that clean committed copy, run the actual frozen-pin tests, visual manifest validation, full `PYTHONPATH=src pytest -q`, integrity, scenario validation, reconstruction, smoke, graph validate/build-check and the approved prohibited-file/secret/compile/threshold/UI scans. Re-run all eleven public/simulated outcomes, real-decision equality, steel `INVESTIGATE`, polypropylene `REJECT`, and synthetic isolation. Before accepting the visual exception as fully verified, execute the separate-copy read-only DOM geometry probe in §5.3a; its third row is clipped in the stored screenshot. Run `make e2e` in compare-only mode; no baseline update. Require all commands exit 0, not merely a smaller expected failure set. A failure outside the two specifically permitted test literals stops work: retain exact command, exit code, diagnostics and hashes, make no opportunistic code/test/document change, and return to Codex planning plus Claude review of a revised amendment before resuming.
4. Clone that exact local scratch commit to a second different-path clean root; require both roots' `HEAD^{tree}` equal the final candidate OID. Run complete `make ci` in **each** clean root with separate fresh isolated graph containers/credentials, as AM-1 requires. Do not treat previous previsual or postmanifest receipts as current-candidate passes. Record exact commands, exit codes, hashes, CI/browser counts and environment identities without credentials.
5. Submit the exact final tree and proof to Claude for independent implementation review with zero unresolved findings. The owner separately accepts the reviewed candidate before ordinary staging, commit, PR/CI or delivery. No S18b, Aura, graph/manifest/visual generator retry, pin shortcut or test weakening is implicit. Any changed graph input, new pixel drift, hash mismatch, missing review authority or failing clean-root gate stops the path and returns to a reviewed amendment.

### 5.1a Truthful status and stop-report reconciliation

After Claude approves R1 and the owner re-accepts its hash, Cascade may correct the **already-permitted** `docs/BUILD_PROGRESS.md` S18a cell and `docs/REQUIREMENTS_TRACEABILITY.md` `V3-S18A-SCOPE` row before implementation review. State separately that the owner accepted the bounded exception, that verification is **stopped at §5.3a** until the corrected probe and downstream gates pass, and that implementation acceptance remains pending. Remove the premature “AWAITING IMPLEMENTATION REVIEW” status and the contradictory statement that no owner acceptance is claimed. Update these descriptions again only to reflect gates actually completed at submission time. Preserve S17 historical text, Core 09 and all graph-hashed inputs.

Keep the original external `AM3-STOP-REPORT.md` and raw measurements as historical evidence; add an external erratum before implementation review stating that **both widths and their shared horizontal edge** moved. The read-only tree diff `0eecea9d9dafe4d49f3e16848b56c14fc7da836a` → `effb73f21b21183ad059c2011bd088bf0a8c27c4` contains **10 changed files**: nine implementation paths plus the additive `plan_review.md` entry, not “7 edits.” Do not relabel the stopped probe as a pass or edit the raw probe files. These reporting corrections are within the already permitted control-document scope; no new product or test edit is authorized.

### 5.3a Read-only old/new Relationships geometry probe (implementation after approval)

Use **two separate, isolated scratch copies**, each with its own application server, graph fixture imports, temporary browser artifacts and no writes back to `/home/barami/projects/ior-worktrees/s18`: (A) an archive of old HEAD `564b1b7a3924ba14d219af8ed027c54311911910`, committed in its **own scratch-only Git repository** with a local test identity; (B) the exact final candidate tree materialized through the new alternate index in step 2 and locally committed only in its **different** scratch repository. Verify A's `HEAD^{tree}` equals old HEAD's tree OID and B's `HEAD^{tree}` equals the recorded final candidate OID; require both scratch roots clean. Do not swap `data/graph/current.json`, any manifest, or any source file in the retained worktree. Do not make either scratch copy read data or assets from the other; each server and `browser_tests.graph_fixtures` process must resolve its own root. All probe scripts, logs and measurements stay in scratch.

Launch the repository's canonical `ior-visual-baselines:playwright-1.62.0-noble` producer in **both** roots, verifying its **full actual image ID** `sha256:938b534c8bac0f80012299fc0bba33c00d29f2ade02416bbc1df9d9c028f8112` and `browser_tests/visual/Dockerfile` base `mcr.microsoft.com/playwright/python:v1.62.0-noble@sha256:aa81288e738725378becba5b3e06cb0f3a7f012a610e87e8d767a090ea3f740d`. Use `chromium-1234`, canonical fonts and launch flags, reduced motion, device scale factor 1, and the same `en-US`/`ar-SA` locale settings as `browser_tests/harness.py`. Use a 1440×900 viewport for **English and Arabic**. In each of the four sessions, follow the exact `browser_tests/test_visual_baselines.py` Journey I route: `install_graph_routes`, `goto_portfolio(public, locale)`, `select_case(STEEL, public, locale)`, `prepare_graph_capture(page, "evidence_to_change")` for `SAU-H0-721049`; wait for graph request completion, `document.fonts.ready` and stable layout. Assert the graph view, case, mode, locale, viewport and Relationships section are correct before measuring. **Every newly produced measurement JSON** (old, new, new-after-rewrite, repeat, per-locale and aggregate comparison) must record both full image ID and full Dockerfile base digest alongside the environment and source-tree identity; the retained first-run files lack image identity and are not retroactively edited. Do not update or regenerate governed baselines.

Query `.graph-native-controls > section:nth-child(2) .graph-button-list > button[data-graph-select="edge"]`; measure **every** Relationships edge button. Require exactly 12 rows in each root/locale, of which exactly five contain the run ID. Before measuring, extract each row's visible label from its direct `span` and its source and target from the two `small > bdi` technical tokens (as rendered by `render.js` and `dom.js`). Replace only the old/new `ENGINE-` run suffix with a common placeholder, then require the complete **ordered list** of `(label, source, target)` tuples to be equal between old and new for each locale. Pair rows by list index after that equality check; duplicate normalized tuples are allowed. For each paired button, capture its full `getBoundingClientRect()` width, height and top/bottom coordinates and the same geometry for its direct `span` label and `small` technical text. Count rendered lines separately in `span` and `small`: traverse all descendant non-whitespace text nodes, select each node's full text with a `Range`, collect every nonzero `Range.getClientRects()` fragment, and merge fragments into a line band when their vertical intervals overlap by more than half the shorter interval. This counts wrapped fragments and merges same-line nested `technicalToken`/Arabic bidi fragments instead of treating a container's single bounding box as one line. Require at least one band per nonempty container; record band count and top/bottom offsets from the button top. A row below the viewport is still measured from its live DOM rectangles: explicitly record full geometry and line bands for one-based rows **3–5** (including the clipped third row and rows wholly below 900 pixels); do not infer off-screen geometry from the WebP crop.

**Unmodified-DOM comparison.** Compare recorded geometry after the existing 0.01-CSS-pixel rounding, with exact equality and **no additional tolerance**. Require identical environment and the already specified 12/5 row counts, ordered normalized tuples and index pairing. For every row, the following fields must equal the old-root values exactly: Relationships-section `{left,right,top,bottom,width,height}`; button `{left,right,top,bottom,width,height}` and `button_relative_{top,bottom}`; direct `span` and `small` `{top,bottom,height}`; `span_bands`, `small_bands`, both complete band-offset lists, `token_count`, `below_fold` and `clipped_at_fold`. Require rows 3–5 clipping unchanged. For **English**, `span.left` and `small.right` must also be identical; only `span.right`, `small.left`, `span.width` and `small.width` may differ, and only in run-ID-bearing rows. For **Arabic**, `span.right` and `small.left` must also be identical; only `span.left`, `small.right`, `span.width` and `small.width` may differ, and only in run-ID-bearing rows. The shared flex gap (`small.left − span.right` in English; `span.left − small.right` in Arabic) must remain identical. Rows **6–12** must have exact equality of **all** section/button/span/small box fields, band fields and clipping fields. Any difference outside this explicit field list stops the exception.

**Controlled rewrite.** After storing each unmodified new-root measurement, change **only the live DOM in that new scratch root**: in the five run-ID-bearing rows, replace exactly one occurrence per row of the approved `ENGINE-7ae34188bdec` suffix with the old `ENGINE-a30f1c62564e` suffix in its `small > bdi` technical token. Require exactly five replacements per locale and no other text-node change; do not touch labels, old-root DOM, source files, projection files, manifests or baselines. Re-measure **all 12 rows in both languages** using the same measurement function and record separate new-after-rewrite JSON files. After rewrite, require the ordered **unnormalized** `(label, source, target)` tuples and **every** recorded geometry field to equal the old-root measurement exactly after the same 0.01-CSS-pixel rounding: section and button full boxes, `span`/`small` full `{left,right,top,bottom,width,height}` boxes, relative positions, line counts, complete band offsets and clipping flags, including off-screen rows 3–5. No numeric tolerance or excluded field is allowed. Discard the modified page afterward; the change is an in-memory control, not a screenshot update.

Record the old/new/rewrite per-locale measurements, aggregate comparison, full source-tree OIDs, full producer image ID and Dockerfile base digest, Chromium revision, font hashes, viewport/device scale and exact commands. Any missing row, unexpected text edit, changed ordered tuple, changed environment, unpermitted unmodified-DOM difference or failed rewrite-control equality stops the visual exception and returns to reviewed planning; no source-worktree repair is implicit. After this corrected probe passes, continue the unchanged Task 8 order: exact-candidate `make ci` in **both** clean roots, Claude's independent exact-tree implementation review, then separate owner implementation acceptance before staging or delivery.

## 6. Pending decision and approval gates

The substantive owner decisions and role assignment were accepted under the original AM-3 and remain historical. The **only pending owner action** is to re-accept those unchanged §7.2, two-image and Core 09 option (b) decisions **under this R1 hash** after Claude approves the corrected gate. Until both occur, the stopped `effb73f2…` candidate, both existing indexes, screenshots, pins and approved AM-3/evidence bytes remain untouched. R1 does not itself accept implementation or authorize staging, commit, PR/CI or delivery.

The accepted investigation evidence remains byte-for-byte bound to SHA-256 `55b35c07549d5a154988909e85bbec3b0f9ea5b4b2f6d83f449516b3b12bff1e` (`.md`) and `3c395706b83ef583d9d0a0ff1a189e2cd62d4ba859ab861bc957a26f7cad88d1` (`.json`). The A3-F1 inventory remains `dca7f459e20df12eb8c2f854941d4b835651aafc61c2d1c470dcb01d787f64cf`. The original approved `SHA256SUMS` remains `b3cc5547a6b8203da5e9e18d8b951fbbc7694d02bbabd0f98b9d54333a9e2451`; it pins the pre-acceptance role record `af40c35a…`, preserved in `/home/barami/.cache/industrial-opportunity-resolution-mvp/s18-am3-acceptance/owner-record-reviewed-preimage.json`. The live owner record SHA-256 `658200e16ead84483bca88ae1a58d81a6dde5ad04bef7b547d419056abba0215` binds the original AM-3 and must not be changed during R1 drafting. The new R1 plan/diff/evidence checksum list and separate owner re-acceptance draft live outside the candidate; Claude's R1 verdict and their final hashes belong in an **additive** `plan_review.md` entry only after review, followed by a new owner binding.
