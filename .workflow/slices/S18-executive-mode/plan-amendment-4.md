# DRAFT — S18a PLAN v1 Amendment 4: rejected executive-candidate correction

**Status:** planner proposal for Claude Code's independent plan-and-handoff review, then owner acceptance. The initial AM-4 packet was rejected for AM4-F1; this corrected packet awaits Claude Code re-review and owner acceptance. No candidate edit, staging, regeneration, or implementation is authorized by this draft. It supersedes only the repair and verification instructions stated here; the approved PLAN v1, AM-1, corrected AM-2, AM-3 and AM-3 R1, including the exact two-image exception and corrected geometry gate, remain binding. Current rejected tree: `2ab8d401c2312725540aa6270d449da16a96e01f` on `slice/s18-executive-mode`, HEAD `564b1b7a3924ba14d219af8ed027c54311911910`. Codex plans, Windsurf Cascade implements after approval and owner acceptance, and Claude Code alone approves the plan and exact implementation tree. Historical approvals and the owner-accepted AM-3/R1 decisions retain their original attribution.

## 1. Trigger, evidence, authority and decision requested

Claude's exact-tree review of `2ab8d401…` returned **REJECT** on B1–B3 and D1. Cascade independently reproduced all four in the retained scratch tree without editing it; the report is `/home/barami/.cache/industrial-opportunity-resolution-mvp/s18-am3-r1/REJECT-2ab8d401-reproduction.md` (SHA-256 `a2f98285a32b5857e768383e9a538d1863b40175f90c7af9b18dae5b10e65475`). Claude's review and later boundary correction are the assistant messages at lines 1080 and 1099 of `/home/barami/.claude/projects/-home-barami-projects-industrial-opportunity-resolution-mvp/c22afe51-efb5-4451-a04c-bada16c2695a.jsonl`; the packet's evidence index distinguishes these reviews from this planner's limited probes. Earlier green receipts establish only that the rejected tree passed the then-executed gates, not implementation acceptance or the correctness of the repaired tree.

These are corrections to the already approved S18a executive projection contract: all loaded cases must be explainable, absent values typed, linked contradictions retained, and cross-scenario evidence fail-closed. They must not change real/simulated engine decisions, methodology terms, evidence boundary, route ordering, thresholds, frozen data or the two bound WebPs. Manifest §7.4 is inapplicable because no governing methodology meaning or Core text changes. Ordinary §7.2 describes unchanged observable behavior, while B1/B2 necessarily correct observable executive API output. **Owner ruling requested:** accept this bounded contract-conformance correction under the existing S18a implementation authority with §7.2's unit/golden/integrity proof and the additional exact-tree gates below; record explicitly that the executive API's previously nonconforming observable responses change. This is a proposed classification, not an assertion that literal §7.2 already covers it. If the owner or Claude finds a methodology interpretation or new authority boundary, stop under Manifest §10 and amend the plan before implementation.

## 2. Exact repair contracts and focused proof

### B1 — legitimate absent preferred route

`case_projection.py:119` currently sends `preferred_hypothesis=None` into `_mapping`, yielding a false integrity 422 for eight cases. In `build_steps`, distinguish a **present** `preferred_hypothesis` with value `None` from an absent or malformed field. Accept a present `None` **whatever the route-status mix**, and project `PUBLIC_CONCLUSION.preferred_route_code` as the existing typed `ExecutiveValue(availability=NOT_CALCULABLE, value=None)`. Accept a mapping when its `route_code` is an integer 0–8 and belongs to the validated `route_hypotheses` route codes. Consume the engine's typed result; do not re-implement `select_preferred_hypothesis` (`route_hypotheses.py:1079–1155`), synthesize a route, or impose any condition on `real_decision.route_code` or route statuses.

For this preferred-hypothesis validation, raise `ExecutiveIntegrityError` and the governed typed API 422 **only** when: (1) the key is missing; (2) its value is neither `None` nor a mapping; or (3) it is a mapping whose `route_code` is missing, is not an integer 0–8, or is absent from the validated `route_hypotheses` route codes. Do not coerce strings, floats or booleans into an integer route code. Add no other preferred-hypothesis precondition; independent validation of the route list remains in force and is not a second selection rule.

Add parameterized service and TestClient API coverage over the exact 11 loaded case IDs: all `build_executive_case` calls and `GET /api/executive/opportunities/{id}` return typed 200 models with eight steps and four vectors; the eight currently false-422 cases expose `preferred_route_code` as `NOT_CALCULABLE`/null. Keep exact public and simulated outcome equality, steel `INVESTIGATE` and polypropylene `REJECT`. Add a valid fixture with `preferred_hypothesis=None` and mixed `fails`/`NOT_CALCULABLE` route statuses: service build succeeds, API returns 200 and the preferred route value is `NOT_CALCULABLE`/null. Do not require its real-decision route code to be null. Negative fixtures cover a missing key, wrong-type value, missing/non-integer/out-of-range mapping code, and an **inconsistent mapping with an integer 0–8 code absent from the validated route list**; require the governed typed 422, not a 500 or silent null. A `None` value with mixed route statuses is never the malformed "inconsistent" fixture.

### B2 — simulated claim contradiction

`provenance.py:201–203` currently assigns every simulated claim `SUPPORTED`. Preserve the existing all-stored-ID and current-scenario synthetic-evidence checks. Index the combined validated public/current-scenario evidence rows and **reuse the public claim path's contradiction predicate** (`claims.py:175–179`) through one shared helper within the already permitted `claims.py`/`provenance.py` paths, called by both public and simulated claim construction. Do not re-implement the predicate separately: any linked row with a truthy `contradiction` makes that claim `CONTRADICTED`; otherwise a fully linked claim remains `SUPPORTED`. Do not drop the contradicting ID, relabel public rows as synthetic, infer contradiction from unrelated rows, or change decision values. Tests must show steel `decision.simulated` and `step.CONDITIONS_AND_KILL.simulated` remain linked to `S-UNICOIL-SPEC` and become `CONTRADICTED`, while public claims and branch metadata remain intact. Add an injected contradiction on a linked current-scenario synthetic row and a non-linked contradiction control, verifying exact status in each case.

### B3 — verified cross-scenario dependency only

Delete the `endswith("::shared_enabler")` exemption in `provenance.py:235–242`. Traverse `simulation_decision` evidence references with their source paths and reject malformed/non-string/empty IDs rather than silently skipping them. Every public or current-scenario reference must resolve to the validated combined evidence index. A foreign reference is permitted **only** in route 8's `evidence_ids` and its nested `shared_enabler.evidence_ids`, not in another route or arbitrary decision field. Validate the entire allowance, not a suffix:

1. Require one well-formed route-8 `shared_enabler` payload with current opportunity and scenario membership. Independently obtain the validated, pinned graph artifact's `shared_enabler_inputs(graph_projection(), opportunity_id, branch=("simulated", current_scenario_id))`; require the payload's enabler ID, graph projection ID, ordered dependent opportunity IDs and evidence-ID set to match that feed. A missing/unavailable/mismatched artifact becomes `ExecutiveIntegrityError`, not an untyped 500 or an invented allowance.
2. For **every** dependent opportunity in the feed, use the loaded, validated synthetic-scenario repository. Require a unique scenario ID, the same declared `synthetic_inputs.shared_enabler.enabler_id`, and membership of the exact `scenario_id::shared_enabler` ID in that scenario's validated `synthetic_evidence_rows`. The current opportunity must be one member. Require the route-8 and nested payload evidence-ID sets to equal exactly this verified dependency set, with no duplicate, missing or extra ID. Do not hard-code aluminium IDs as a general rule; the governed aluminium pair is the regression fixture.
3. Only after that validation, omit the **verified foreign** dependency IDs from the scenario-local `ClaimReference.evidence_ids`; retain them in the underlying route-8/graph payload and record in ADR-028 that a single-scenario executive claim cannot present another scenario's Class-D row as its own. Keep current-scenario synthetic evidence and any linked public evidence in the claim. No generic suffix filtering remains.

Tests must reject `ANY-UNRELATED::shared_enabler`, `ANY-UNRELATED::other_suffix`, other absent IDs, a real foreign ID injected in route 0 or an unrelated field, a foreign ID with a forged or missing dependency declaration, and missing stored foreign evidence. Both governed aluminium opportunities must build and return API 200 with their exact two declared route-8 dependency IDs validated, while every emitted simulated claim remains limited to public plus its own scenario's stored evidence. Document this narrow rule in ADR-028 only; leave Core 07 and all graph inputs unchanged. If the selected implementation needs a different dependency source or cannot maintain the claimed membership invariant, stop for plan review instead of broadening the exemption.

### Recommended corrections admitted into this same narrow pass

`INTERVENTION` contains only simulated values. In `case_projection.py`, prefix its five value keys `simulated_`, associate the available step only with `step.INTERVENTION.simulated`, and remove the public step/route claim IDs from that simulated-only step. If simulation is unavailable, keep the step typed `UNAVAILABLE` with no simulated claim or fabricated value. Its Class-D evidence IDs, labels and numeric values remain unchanged. Focused tests check key/claim branch identity for steel, polypropylene and unavailable simulation; the public claim registry itself remains available separately.

Remove `claims.py:164–165`'s fabricated `("identity/tariff-line",)` default. If a claim has neither stored support nor an actual computed missing-need code, raise `ExecutiveIntegrityError`; keep `ClaimReference`'s existing invariant rather than inventing a need or weakening the model. Test the no-evidence/no-needs failure and a real declared-need unresolved claim. Cascade's exhaustive current-case check found the default does not fire on the 11 frozen cases, so this is a latent correction, not a claim that a current output was wrong.

### D1 — stable, truthful control records

Before computing the repaired candidate tree, update only the S18a portions of ADR-028 in `docs/ARCHITECTURE_DECISIONS.md`, `docs/BUILD_ROADMAP.md`, `docs/BUILD_PROGRESS.md`, and the `V3-S18A-*` rows/adjacent S18a note in `docs/REQUIREMENTS_TRACEABILITY.md`. Preserve S17 and earlier history. Use this substance: **the R1 exact tree completed Task 8, compare-only e2e, the bilingual R1 geometry probe and two clean-root `make ci`; Claude rejected its implementation for B1–B3 and D1; AM-4 repairs require those same gates on a new exact tree, independent review and separate owner implementation acceptance.** Record the existing AM-3/R1 approvals and bounded visual exception as historical approvals, not implementation approval. ADR-028 also states B3's validated dependency rule. Do not say the new candidate passed before its receipts exist.

Use a stable status such as `R1 EXACT TREE REJECTED; AM-4 EXACT-TREE VERIFICATION GATED`. The new tree OID and gate outcomes belong in an external, hash-bound implementation receipt, not in the four documents whose bytes define that tree. Wording must state the requirement and where the result is recorded, without claiming it remains pending after a pass. Finish all control-document text **before** creating the new tree. After gates, submit the external receipt; any required later repository text change creates a different tree and forces all exact-tree gates to restart. Do not embed the final tree ID in its own contents.

## 3. Strict file boundary and no-regeneration proof

**Only these implementation paths may change:**

- `src/ior_mvp/executive/case_projection.py` (B1, INTERVENTION);
- `src/ior_mvp/executive/provenance.py` (B2, B3);
- `src/ior_mvp/executive/claims.py` (shared B2 contradiction predicate, missing-need fallback);
- `tests/test_executive_service.py`, `tests/test_executive_api.py`, `tests/test_executive_claims.py` (focused and all-11 regressions);
- `docs/ARCHITECTURE_DECISIONS.md`, `docs/BUILD_ROADMAP.md`, `docs/BUILD_PROGRESS.md`, `docs/REQUIREMENTS_TRACEABILITY.md` (D1/ADR-028 only).

After plan approval and owner acceptance, the **only** additional candidate record paths are a new `.workflow/slices/S18-executive-mode/plan-amendment-4.md` holding these exact approved bytes and an additive `.workflow/slices/S18-executive-mode/plan_review.md` entry quoting Claude's verdict and the full plan/packet hashes. The owner-control update is local/ignored at `/home/barami/projects/industrial-opportunity-resolution-mvp/.autonomous-workflow/owner-decisions/s18-visual-release-1.json`, preserving earlier decisions. External receipts/errata remain outside the candidate. No other file, test, pin, baseline, artifact, hook, index or Git ref may change. In particular, `screening/*.py` is absent from the hashes but **not needed or permitted**; `src/ior_mvp/app.py`, top-level `src/ior_mvp/*.py`, `cases/*.py`, `graph/*.py`, `docs/core/**` (including Core 07), `config/**`, `data/**`, browser/static sources and baselines are excluded. A newly needed path requires another reviewed amendment.

Read-only inspection of the actual bound manifests found 228 graph projection inputs, 83 visual `source_tree` entries, 724 snapshot-manifest files and 20 authority-hash files, **zero existing hash drift**. None of the ten implementation paths above, the two slice records, or any `screening/*.py` path appears in those four lists. `src/ior_mvp/app.py` does appear in graph and visual lists; Core 07 appears in graph and authority lists. Thus the scoped AM-4 edits require **no graph, manifest or canonical screenshot regeneration**. Recheck this set intersection and every existing digest before and after implementation; any new hashed-input/visual-source delta stops and returns to a reviewed plan. Do not invoke any generator, change a visual pin, or repeat the consumed visual allocation.

## 4. Execution and exact-candidate acceptance order

1. **Approval gate:** Claude reviews the complete external AM-4 packet (plan **and handoff**) and returns an exact-byte verdict with full plan and packet SHA-256s. The owner then accepts this AM-4 scope/classification under those hashes. Only afterward append the exact verdict and hashes to `plan_review.md`, add the accepted decision to the existing owner record without rewriting AM-3/R1 history, and hand Cascade the verified packet. Do not request implementation from Cascade before those records are complete.
2. **Preservation gate:** Cascade reconfirms branch/HEAD, rejected tree `2ab8d401…`, unstaged ordinary index and the three retained alternate indexes, approved AM-3/R1 hashes, owner decision, 228-input graph/visual/snapshot/authority manifests, two WebP SHA-256s/sizes, visual manifest `216aa8e0a85f35151a065f53e057f314bccadc14a6bdc802be9540ffb880dfb2` and baseline tree `dfb2612b9e637367ae4441db489dda4f5d17bf36`. Stop on drift. Copy approved AM-4 bytes into the **new** slice amendment file; preserve AM-3 and R1 files byte-identically.
3. **Focused red-to-green:** Add the B1–B3, INTERVENTION and missing-need regressions first; capture failures on the rejected logic, then implement only the permitted source changes. Run focused tests and all-11 API/case checks. Correct the four control documents per D1 before the final tree. Preserve every governed public/simulated decision and real-decision equality. Any unexpected test or contract failure stops rather than prompting an unplanned edit.
4. **Exact tree:** use a **new** temporary alternate index, without changing refs, HEAD, the ordinary index or any pre-existing alternate index. Unreferenced Git objects may be written. Materialize the candidate in a clean local-only committed scratch root; require scratch `HEAD^{tree}` equals the newly recorded candidate OID, clean status, unchanged frozen-root/baseline tree OIDs and the bound pin. The scratch commit is verification infrastructure, not delivery.
5. **Required gates on that exact tree:** run all AM-3 R1 Task 8 checks—frozen pins and status tests, full `PYTHONPATH=src pytest -q`, integrity, scenario validation, all reconstruction stages, smoke, graph validate/build-check, prohibited/secret/threshold/UI/ES/compile scans, all eleven outcomes and synthetic isolation—then `make e2e` **compare-only**. Run the approved R1 bilingual old-HEAD/new-tree geometry probe, including the full producer/base identities, 12/5 rows, unchanged unmodified-DOM fields and controlled rewrite in both locales. After it passes, run complete `make ci` from **two different-path clean roots of that same exact tree** with fresh isolated graph containers/credentials. Record commands, exit codes, hashes, counts, environment and tree identities without credentials. Prior `2ab8d401…` passes cannot substitute for any new-tree result. All gates must exit zero; a failing probe, pin, browser, test or clean-root gate stops the path.
6. **Review and delivery gate:** preserve a hash-bound external exact-tree receipt. Claude independently reviews the **new exact tree** and evidence with zero unresolved findings; the owner separately accepts that implementation before ordinary staging, commit, PR/CI or delivery. AM-4 plan/owner acceptance alone is not implementation acceptance. Do not start S18b, Aura, generation or any adjacent repair through this amendment.

## 5. Nonblocking observation triage

- **Included, source-confirmed:** INTERVENTION's unlabeled simulated keys/public claim association (`case_projection.py:226–245`) and the unused fabricated need fallback (`claims.py:164–165`). The latter is latent: Cascade found zero current uses after correcting its first mistaken scan.
- **Deferred, Cascade-reproduced but not an AM-4 blocker:** case-level EVSI has no explicit `availability` marker while summary reports seven AVAILABLE/four UNAVAILABLE. Current absent numeric values remain non-AVAILABLE; adding a case-level schema field would expand the approved API contract and likely touch models/tests. Record for a separately reviewed S18b/API follow-up; do not silently label it fixed. If a required AM-4 gate demonstrates a false assertion or leakage, stop and amend.
- **Deferred, agent-reported and not reproduced as endpoint failures:** untyped malformed-input paths, weak model-level `EvidenceReference` partition validation, trivial `rule.R4` test, missing broader validator tests, overbroad R12 needs on unresolved R6/R7/R8/R10 claims, and single-missing-suffix behavior. The B1/B3 focused negative cases do require typed fail-closed errors and may incidentally expose a specific instance; fix only within this plan's permitted paths, otherwise stop. The partition observation is visible in `provenance_models.py` but upstream analysis validation currently rejects the bad row; no new model edit is authorized. A missing required simulated suffix may properly fail closed, so do not relax it without a separate contract ruling. Track the rest for independent follow-up, not as silently passed checks.

## 6. Stop and self-audit boundary

Stop on missing approval/owner ruling; new required file; altered graph/visual/snapshot/authority input; changed engine/public/simulated outcome; fabricated need, unsupported claim status or foreign evidence; changed WebP or pin; a test/geometry/CI failure; or a conflict between this amendment and higher authority. Return the exact failing input, command, exit code and tree identity to Codex and Claude for a reviewed amendment. Do not weaken a test or broaden the foreign-ID exception to make the fixture pass.

This draft relies on the reproduced B1–B3/D1 findings and the read-only aluminium payload/manifest inspections cited in `EVIDENCE-INDEX.md`; it does **not** claim to have run AM-4 tests or repaired the candidate. The only new owner decision is the bounded contract-conformance classification and scope acceptance under the final review hashes. No implementation or delivery decision is presumed.
