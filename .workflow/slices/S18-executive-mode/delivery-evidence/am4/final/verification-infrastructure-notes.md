# Verification infrastructure notes

The frozen candidate is `1364789a964f96f6ebabf8eae1d239a5bad0b15e`. These diagnostics do not change the candidate or a prescribed acceptance gate.

- The first alternate-index assembly's optional `git diff --cached --check` exited 2 solely because the authorized pre-existing `plan_review.md` ends with a blank line (line 79). Its exact approved SHA-256 remains `176d54bdc511dde78e56e2e9202c7721629797bd54f42a36d49e77892b48deba`. The approved bytes were preserved and the whitespace diagnostic recorded, not silently repaired.
- The next assembly helper initially compared the expected `browser_tests/baselines` tree OID to the narrower `browser_tests/baselines/v0.3.0` subtree. This diagnostic assertion failed because those are different trees. The correct bound parent subtree is `dfb2612b9e637367ae4441db489dda4f5d17bf36`; the version subtree is `d2b8a1987101dc3e4f404ea0f62bdae6d01b256f`. Both match the rejected candidate exactly. The external helper was corrected; no source, manifest or image changed. All temporary indexes were retained.
- The exact candidate was then recorded by the fresh `am4-final-candidate-r3.index`. Only unreferenced source Git objects were written. Local proof commits exist only in the approved isolated scratch repositories.
- `focused-red.log` is the initial sandbox-interrupted attempt (130), not completed red proof. `focused-red-host.log` is completed pre-repair evidence (53 failed, 53 passed, exit 1). `focused-green.log` retains the unexpected new polypropylene fixture KeyError (105 passed, 1 failed, exit 1), which caused the required stop. The owner explicitly authorized the prepared optional-field test correction; `focused-green-resumed.log` then reports 113 passed, exit 0. The earlier stop report remains historical and is superseded only by the resumption and final exact-tree receipts.
- The external outcome-capture helper was checked against the actual decision-projection schema before its first execution. It checks synthetic metadata on the typed public projection and real-decision equality on the underlying engine objects; its first execution passed.
- CI uses external Compose overrides solely to give each approved proof root its own fresh graph container, network and volume. The pinned image, service, ports, commands and candidate Compose bytes are unchanged. The pre-existing `industrial-mvp-neo4j-data` volume is preserved.
