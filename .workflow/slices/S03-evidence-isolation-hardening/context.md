# Context — S03 Evidence-Isolation and Scenario-Validation Hardening

## Base and branch
- Base: `main` after S02 squash merge (record SHA in plan Task 0). Branch: `slice/S03-evidence-isolation-hardening`.
- State after S02: 136 tests; `make ci` = prohibited scan → threshold-literal scan → compile → node → integrity → pytest → smoke; thresholds 1.1.0; generic scripts `.workflow/runs/slice_commit_push_pr.sh` and `slice_ci_merge.sh` exist (Supervisor use only).

## Exact defects to close (Supervisor findings from the full read, 2026-09-02)
1. `config/evidence_policy.v1.yaml` `synthetic_isolation.required_fields` = [synthetic_flag, scenario_id, seed_basis, evidence_class, source]. Core 06 §4 mandates also `opportunity_id`, `display_label`, `synthetic_inputs`, and fixes `evidence_class: D` and `display_label: SIMULATED — NOT MINISTRY EVIDENCE`. `evidence.validate_synthetic_scenario` does not check `evidence_class == "D"` or the label value; `synthetic_evidence_rows` then indexes `scenario["display_label"]` → a scenario missing it raises `KeyError`, not `EvidenceIntegrityError` (KL-04).
2. No reconciliation of synthetic scenarios to public marginals exists (Core 06 §5.1, §10.2; Core 05 §8; Core 09 Gate B) (KL-05). Current fixtures reconcile: steel target demand 104 kt ≤ 2024 public imports 287.9 kt; nameplate 250 kt = UNICOIL 250,000 t/y; PP target demand 56 kt ≤ 56.0 kt imports (equality must PASS); PP nameplate 1,170 kt = Advanced 450,000 + Tasnee 720,000 (SABIC null → excluded from the disclosed total). Downside ≤ target (100 ≤ 104; 51 ≤ 56). PP `qualified_available_kt` 80 ≤ physical nameplate × availability × yield. No tariff-line/buyer allocation blocks exist → that rule is NOT_APPLICABLE and must be reported as such, not skipped silently. Do not invent rules beyond Core 06 §5.1 / Core 05 §8.
3. Core 09 §4 leakage assertion 6 ("real dossier does not include a synthetic disclosure") has no test (TL-09 partial).
4. FR-001: analysis response exposes `snapshot_id` and `as_of_date` but not the methodology identity or the configuration versions in force (KL-06). Sources that exist and are hash-locked: `docs/authority/authority_hashes.json` (DOCX path + sha256), `config/thresholds.v1.yaml` `metadata.version` (1.1.0), `config/sector_profiles.v1.yaml` `metadata.version` (1.0.0), `config/evidence_policy.v1.yaml` `metadata.version` (→ 1.1.0 after this slice), `config/project.yaml` `project.version` (0.1.0). No version string may be typed into code; read them.
5. R5 in `rules.py` is a boolean proxy (`domestic_production_exists and material_imports_exist`) while `thresholds.v1.yaml` `R5.retained_import_share_of_apparent_consumption: 0.20` cannot be computed (no domestic production series in public data). Metrics must state `retained_import_share_of_apparent_consumption: "NOT_CALCULABLE"` with the reason and the configured threshold (owner mandate §24; KL-26).

## Constraints
- Governed change: ONLY `config/evidence_policy.v1.yaml` by hand (required_fields extended to the Core 06 §4 set; add `required_evidence_class: D` and keep `display_label`; `metadata.version` 1.0.0 → 1.1.0; `effective_date` 2026-09-02). Hashes regenerated exactly once via `scripts/build_manifests.py` after full regression (plan must state the same conditions and audits as S02 §17). Manifest §11 row for `evidence_policy.v1.yaml` copied from the generated JSON. `data/**`, `thresholds.v1.yaml`, `sector_profiles.v1.yaml`, `docs/core/**`, DOCX unchanged.
- Golden outcomes unchanged (steel INVESTIGATE/ADVANCE route 5; PP REJECT/REJECT). Existing synthetic scenarios must PASS reconciliation without any data edit; if a rule would fail on current data, the rule is wrong or out of scope — report, do not edit data.
- Typed errors: `EvidenceIntegrityError` for every policy/reconciliation failure; the simulated API path must return 404/409-style JSON, not a 500 (check `app._safe_analysis` catches it — currently catches `RepositoryError` and `ValueError`; `EvidenceIntegrityError` is a `RuntimeError` → decide and test; a fail-closed 4xx with the integrity message is the expected behaviour, never a 200 with partial data).
- New CI gate: `scripts/validate_scenarios.py` (Gate B) after `verify_integrity` in both Python jobs and `make ci`; CI contract test updated; a planted violation must be shown to exit non-zero (test via a temp scenario file and a temp public case, not by editing `data/**`).
- Threshold-literal guard: any new comparison in engine code must use config keys or scenario/public values, never numeric literals in the config set.
- SG-TR-007 type hints; SG-TR-008 tokens for any CSS (banner may need a small style; use existing tokens only). UI: banner shows methodology hash prefix (first 12 hex) and the three config versions; dossier `evidence_summary` gains an `authority` block; HTML dossier prints it.
- Execution conventions as S01/S02 (short shell; detached long runs; logs under `.workflow/logs/`). Commit/push/PR/merge Supervisor-controlled.
