# Context — S09 Generalized Public Decision Engine and Five Sector Profiles

## Base and branch
- Base: `main` at `8b6d55cc8f1f10b828f3af7a6a1e045364cb5537` (S08 squash merge; default-branch CI run 33637594756 success, five jobs). Branch: `slice/S09-public-decision-and-profiles`.
- Working tree carries Supervisor-owned post-merge S08 records (`.workflow/slices/S08-snapshot-v2-computed-rules/pr_record.md`, `completion.md`; modified `.workflow/state.json`, `docs/BUILD_PROGRESS.md`, `docs/KNOWN_LIMITATIONS.md` (KL-26/30/32 closure facts; KL-33 open), `docs/REQUIREMENTS_TRACEABILITY.md` (S08-CI registry; V3-D2/D4/D7/D12 COMPLETE)). Committed with this slice; the Implementer adds S09 rows only.
- State: 506 default tests (3.12/3.14); browser suite 18 tests / 122 nodes; 40 governed baselines (host-owned regeneration works); schema v2 snapshots with computed rules; `public_decision_contract` still present in both live snapshots and consumed by `decision_engine._public_decision` (REJECT if computed R11 fires, else INVESTIGATE — an authored-contract shortcut that S09 replaces).

## Governing decisions for this slice
- `SLICE_GRAPH.md` S09 (approved with amendments) and the owner's amended interpretations I1 and I5 and rulings R-1/R-2 (`GAP_ANALYSIS.md` §7/§7A; ADR-010). These are governing text: the planner writes them into Core 07 v2 before code.
- **SD-1 — Decision-critical fields and their evidence class (Core 07 v2; evidence_policy `advance_gate`):** the four fields are product identity (target specification / decision object), target-specification demand, domestic capability (dimension states and hard gates), and hard process/regulatory gates. A field's evidence class is derived from the passports whose `supports` entries cover that field (the planner defines the field→support vocabulary precisely; absent coverage → Class E; contradicted (`contradiction` non-null on a covering passport with `reviewer_status` unconfirmed) → treated as unresolved for the gate). The public goldens: steel identity partially resolved (C), target-spec demand absent (E), capability C with three unresolved hard gates → gate fails → not ADVANCE; PP likewise not ADVANCE.
- **SD-2 — ADVANCE is evidence-gated, not source-type-gated (R-1):** the same gate function serves public and simulated branches; public Class A/B/C evidence passing identity, demand-at-specification, capability (Kmin, resolved hard gates, D\* band), unsupported-then-supported economics, national value, competition and additionality reaches `ADVANCE`. A test-only fixture snapshot (under `tests/fixtures/`, never `data/`) proves the positive path and each single-field D/E downgrade proves the negative path. No public code path may branch on `source`, `synthetic_flag` or source name for the gate.
- **SD-3 — Amended I1 exactly:** formal states remain `ADVANCE / INVESTIGATE / MONITOR / REJECT`; a separate `screening_disposition` field (`CANDIDATE`, `NO_CANDIDATE`, `SCREENED_OUT`) is emitted; formal `REJECT` requires an evidenced rejection condition (satisfied §4.2 exclusion; false/measurement gap; equivalence of idle qualified supply with demand; uneconomic at efficient scale; structural overcapacity — the planner types each); R3-only resilience → `MONITOR` with the named trigger; a decision-critical unresolved or contradictory fact with positive value of information → `INVESTIGATE`; `MONITOR` always carries a named observable trigger (demand/regulation/technology/supplier concentration/capacity state). Deep cases (the two goldens) are `CANDIDATE`.
- **SD-4 — Amended I5 exactly, public branch as hypotheses:** the §7.1.1 sequence is evaluated in order; each route is recorded `passes / fails / NOT_CALCULABLE` with reasons; a lower-cost route that fully resolves the binding constraint blocks escalation; among remaining feasible, additional and policy-permissible routes the one with the highest defensible incremental national value is selected when the economics inputs exist, otherwise the selection is a `preferred_hypothesis` with `NOT_CALCULABLE` economics; financial support only after unsupported and applicable non-financial routes fail; route 8 is `NOT_CALCULABLE` / `GRAPH_REQUIRED` (R-2) — no in-memory substitute. Steel: `INVESTIGATE` with `preferred_hypothesis` = route 5 (brownfield) and `route_code` `null` (unchanged public contract); PP: `REJECT` route 0.
- **SD-5 — Hard exclusions (I7):** six typed checks from §4.2 with explicit inputs (planner names the schema fields; where the goldens carry no input the field is `UNAVAILABLE` and the check is `NOT_CALCULABLE`, which never passes and never rejects). Snapshot schema advances to `2.1.0` for the added exclusion-input block and the removal of `public_decision_contract`; IDs and as-of dates unchanged (SD-1 of S08 applies); v1 history untouched; the v2.0.0 files are not retained separately (the v2.1.0 files supersede them in place — the S08 migration-equivalence suite adapts its live-vs-converted comparison to the new schema, keeping the intended-difference allow-list explicit).
- **SD-6 — Gap taxonomy (I6):** ordered deterministic tests yielding exactly one primary class (`false_or_measurement`, `specification_or_grade`, `capacity_or_availability`, `cost_or_competitiveness`, `capability_or_technology`, `qualification_or_certification`, `commercial_or_relationship`, `evidence` — planner aligns names to §5.3 wording) plus secondary classes; `evidence` when decision-critical inputs are unresolved.
- **SD-7 — Governed bilingual narrative catalogue:** `config/decision_narratives.v1.yaml` (hashed; planner extends `build_manifests.py`) with en/ar templates and placeholders for headlines, rationales, conditions, kill conditions, missing-fact templates, exclusion texts, MONITOR triggers and route-hypothesis reasons; the English text for the two goldens' headline and rationale must reproduce the current strings exactly ("INVESTIGATE — binding constraint unresolved", "REJECT — generic capacity support", "Test brownfield first; greenfield is not justified from public evidence.", "No intervention for generic capacity", "Brownfield priority to test"); Arabic strings are Supervisor-approved, owner-amendable defaults (never "owner-approved"). Missing facts are computed from unresolved fields/gates through templates; the goldens' current missing-fact substance (line-level production by grade; availability/yield/qualification; importer specifications; re-export decomposition; delivered cost/import parity) must remain represented by computed items — the planner shows the mapping.
- **SD-8 — Five sector profiles:** `sector_profiles.v1.yaml` 1.0.0 → 1.1.0 adding pharma/API (10/20/10/10/15/15/5/10/5), fertilizers (15/20/15/10/5/5/10/15/5), fabricated aluminium (10/15/15/15/10/15/10/5/5) with hard gates from the §6.3 text; weights as decimals summing to 1.0; existing two profiles unchanged; per-profile boundary tests for Kmin/D\* publication; no case yet uses the new profiles (S14/S15).
- **SD-9 — Baselines and text:** decision headline/rationale/conditions/kill conditions/missing facts become bilingual governed text (Arabic UI shows Arabic narratives, no longer English islands for those fields); enumerate every rendered-text change; regenerate the 40 baselines once through the canonical container after the functional suite is green.
- Authority changes (ADR-010; slice ADR-013): Core 07 v2 §4–§8/§10–§11, Core 01 v2 §4.1–§4.2/§6.4 mapping, Core 02 v2 §4–§8, Core 09 v2 §2.4/§3/§6; `sector_profiles` 1.1.0; new `decision_narratives.v1.yaml`; `evidence_policy` field-class contract if needed (1.3.0); snapshots 2.1.0; one generator run after all governed edits. No `thresholds` value change (new keys only if genuinely required, with rationale); no `data/synthetic` change (S10 owns the simulation branch — the simulated selector must keep producing ADVANCE route 5 / REJECT route 0 with identical numbers; if the shared gate function is used there, prove identity).

## Observed facts (Supervisor read, 2026-09-02)
- `decision_engine._public_decision` (l.40–67) returns REJECT/INVESTIGATE from computed R11 and the authored contract; `_simulate` (S04) implements Core 07 §7.4 → §7.3 → INVESTIGATE for the simulation branch with routes 0/5 only; `analyze_public` builds capability/adjacency, missing facts from the contract, `data_unlocks`.
- `evidence_policy.v1.yaml` 1.2.0 has `advance_gate` (decision-critical fields list and blocked classes) unused in code; `class_definitions` A–E.
- `sector_profiles.v1.yaml` 1.0.0: two profiles with weights and hard gates; `capability.py` reads profile by `opportunity.sector_profile`, computes K, U, D\* with Kmin and hard-gate rules.
- Snapshot v2 fields available for exclusions/gap/identity: `opportunity.decision_object_status` (`partially_resolved` / `generic_hs6_only`), `application_boundary`, `domestic_capability.*`, `criticality_designation`, `domestic_flows`, `disclosed_*`, evidence passports with `supports`/`contradiction`/`reviewer_status`.
- UI: decision hero renders headline/rationale/route_label/conditions/kill_conditions as source-language islands in `ar` (S07); dossier renders the same; catalogue keys exist for chrome only.
- Browser suite asserts state labels and some headline text; baselines will change.

## Constraints
- Goldens exact: steel `INVESTIGATE` (fired R1-D/R2/R3/R4-D/R9-S; D\* None; brownfield hypothesis), PP `REJECT` route 0 (R11 fired); simulated outcomes and all numeric exacts unchanged (57.509 / 46.491 / 0.2667 / −18 / 9.5 % / 18 / 198 / 1.0751).
- Route 8 `GRAPH_REQUIRED` only; no graph code.
- No case may reach public `ADVANCE` from the goldens (they cannot); the positive path is proven on a test fixture.
- `PYTHONPATH=src pytest -q` browser-independent; `make ci` and hosted CI within budget.

## Records to produce
`plan.md`, `plan_review.md`, `implementation_log.md`, `implementation_review.md`, `reviewer_findings.md`, `test_evidence.md`, `pr_record.md`, `completion.md`; ADR-013.
