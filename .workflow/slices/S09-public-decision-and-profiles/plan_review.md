# Supervisor Plan Review — S09 Generalized Public Decision Engine and Five Sector Profiles

Reviewer: Supervisor (Claude, `claude-fable-5-1-thinking-max`). Planner: agent bd14302c (GPT-5.6 Sol, `gpt-5.6-sol-max`). Plan read: `plan.md` (2,847 lines) — Core 07 v2 text (§3–§11 of the proposed core), module architecture, PublicSnapshot 2.1.0, field assessment and gate, exclusions, taxonomy, state table, fixtures, hypotheses and amended-I5 selection, evidence needs, narrative catalogue (all keys), profiles, rendered-text inventory, simulation preservation, tests, generator diff, ADR-013, TDD sequence, Sanad ledger.

## Verification performed (2026-09-02)

- Core 07 v2 text reproduces the amended interpretations faithfully: amended I1 (formal `REJECT` only on a satisfied exclusion or typed evidenced rejection condition; `INVESTIGATE` on unresolved/contradictory decision-critical fact with positive value; `MONITOR` only with a named observable trigger; separate `screening_disposition`; unmatched branch fails closed); R-1 (gate consumes field, class and resolution status only — never `source`, `synthetic_flag`, producer or IDs; public A/B/C may reach `ADVANCE`); amended I5 (precedence gates, then greatest unrounded incremental national value among feasible/additional/permissible routes; tie → lower code; financial only after non-financial fail; `preferred_hypothesis` when economics unavailable); R-2 (route 8 `GRAPH_REQUIRED`); I6 (§5.3 vocabulary with one primary class, `evidence` when decision-critical fields unresolved; a separate optional `constraint_class`); I7 (six typed exclusions, `NOT_CALCULABLE` never passes or rejects). Simulation branch preserved (§7.9).
- Decision-critical field assessment through a controlled `supports` vocabulary with per-field coverage, best class among non-contradictory covering passports, contradiction → `CONTRADICTORY` blocks the gate. Matches SD-1.
- Sector profiles 1.1.0: weights are exactly the §6.3 percentages as decimals (10/20/10/10/15/15/5/10/5; 15/20/15/10/5/5/10/15/5; 10/15/15/15/10/15/10/5/5) with snake_case hard gates from the §6.3 text; per-profile boundary tests. Matches SD-8.
- Golden outcomes: steel `INVESTIGATE`, `route_code` null, route 5 preferred hypothesis; PP `REJECT` route 0 via the computed generic-capacity rejection condition; simulated outputs untouched.
- Sanad ledger recounted mechanically: 19/58/16/44/1 — matches §31 (the planner pasted the actual command output).

## Round 1 findings and rulings

| ID | Severity | Requirement | Evidence | Problem | Required correction |
|---|---|---|---|---|---|
| PR-01 | MEDIUM (RULING) | AGENTS non-negotiable "no per-case special-casing"; Core 07 v2 §8 (needs derived from controlled reason codes); SD-7 ("substance", not exact strings) | §14 catalogue keys `missing.steel.line_production … missing.steel.economics` and `missing.generic.tariff_invoice … missing.generic.niche_economics`; §13.2/§13.3 map the goldens' legacy strings to these keys. | Templates named after a case ("steel") or the legacy PP wording ("generic") are per-case dispatch in disguise: a new coated-steel case in S14 would inherit steel-authored prose; the exact legacy strings were never a requirement — their *substance* was. | Key every evidence-need template by **need code and evidence-state variant only** (e.g., `need.identity.tariff_line` when `decision_object_status == generic_hs6_only`; `need.specification.line_production` when the decision object is partially resolved and line-level production is unknown; `need.capacity.availability_allocation`; `need.demand.importer_specification`; `need.flows.reexport_origin_decomposition`; `need.economics.route_delivered_cost` with a `{route_label}` placeholder). Document, for every variant, the field predicate that selects it (never an ID, name or profile). The goldens must still produce five substantive needs each covering the same substance; the exact legacy English sentences are **not** required — update the dossier/browser assertions accordingly (they change anyway with regeneration). Amend §13, §14, §17, §19, §24. |
| PR-02 | MEDIUM (RULING) | Core 06 §4 (scenario carries its own narrative and parameters); SLICE_GRAPH S10 (scenario contract 2.0.0 owns simulation narratives); no case-specific facts in governed generic catalogues | §14 keys `decision.simulated.route5_advance.*` and `decision.simulated.route0_reject.*` embed the steel/PP scenario facts ("50 kt … within 18 months", "falls below 72 kt", "brownfield specification upgrade", "internal grade matrix"). | Scenario-specific narrative and numbers belong to the scenario record, not to a governed generic catalogue; duplicating them creates two sources of truth for the same text and bakes case facts into configuration. | Remove every scenario-specific simulated key from `decision_narratives.v1.yaml`. In S09 the simulated branch keeps rendering the scenario's existing `decision_narrative` (English, as source-language islands in `ar`) exactly as today; S10 makes scenario narratives bilingual inside the scenario contract 2.0.0. The catalogue may keep only generic simulated wrappers that carry no case fact (e.g., the "SIMULATED" prefix template and the generic `decision.simulated.investigate.*` texts if `_simulate`'s fallback text is truly generic — verify against `data/synthetic/*` `decision_narrative`). Amend §7.9, §14, §16, §17, §18, §19. |
| PR-03 | LOW | Amended I5 generality | §14 `decision.public.investigate.route: "Brownfield priority to test"` is a fixed INVESTIGATE string although the preferred hypothesis may be any route. | An INVESTIGATE whose preferred hypothesis is, e.g., route 3 would still be labelled "Brownfield". | Derive the label from the preferred hypothesis: a generic template `route.hypothesis.priority: "{route_short_label} priority to test"` with a governed short-label table (`route.5.short: "Brownfield"`, `route.3.short: "Certification support"`, …) so steel still renders exactly "Brownfield priority to test" while remaining general. Same pattern for the `ar` locale. |
| PR-04 | LOW (RULING — resolves OQ-01) | Core 06 §9; catalogue governance | §14 Arabic strings. | Governed Arabic text needs an approval record. | **Approved** as Supervisor-approved, owner-amendable defaults for every key that survives PR-01..PR-03; the restructured keys' Arabic will be re-read by the Supervisor at implementation review. Never described as owner-approved. Record in ADR-013. |

No BLOCKER/HIGH findings. Decision algorithm, gate, exclusions, taxonomy, hypotheses, profiles, schema 2.1.0 (including removal of `public_decision_contract`), fixtures, simulation preservation, single generator run and TDD sequence are approved as planned.

Note (no change required): `MONITOR` carries route code 0 ("no intervention" while the trigger is watched) — acceptable under §1.2; the state distinguishes it from `REJECT` route 0, and `route.0.reason.monitor` explains it.

## Round 2

Planner (same agent) revised `plan.md`. Supervisor verified in the text: PR-01 — need templates keyed by need code and evidence-state variant (`need.identity.tariff_line`, `need.specification.line_production`, `need.specification.producer_grade_matrix`, `need.capacity.availability_allocation`, `need.demand.importer_specification`, `need.demand.importer_application_qualification`, `need.flows.reexport_origin_decomposition`, `need.economics.route_delivered_cost` with `{route_label}`, `need.economics.named_exception_delivered_cost`) with field predicates documented; no `missing.steel.*`/`missing.generic.*` keys remain (the three residual mentions are the removal note). PR-02 — scenario-specific simulated keys removed; only the generic `decision.simulated.investigate.*` wrapper remains, with the scenario's own `decision_narrative` taking precedence (to be verified at implementation review). PR-03 — `route.hypothesis.priority: "{route_short_label} priority to test"` and `route.N.short` tables (en/ar); steel renders "Brownfield priority to test". PR-04 — OQ-01 resolved; Arabic strings are Supervisor-approved, owner-amendable defaults. Sanad ledger recounted mechanically: 20/61/16/43/0 — matches §31 (pasted output).

Unresolved findings: **0**.

## Decision

**PLAN_APPROVED** — 2026-09-02, Supervisor. Implementation may begin on `slice/S09-public-decision-and-profiles` from base `8b6d55cc8f1f10b828f3af7a6a1e045364cb5537`, executing the plan's TDD sequence in order (Core 07/01/02/09 v2 text first; narrative catalogue; field-class assessment; gate; exclusions; taxonomy; state selection; hypotheses; profiles 1.1.0; schema 2.1.0 with `public_decision_contract` removed and the migration suite adapted; UI/dossier; browser text; baseline regeneration through the canonical container; the single generator run; ADR-013; docs; verification). Authority changes permitted under ADR-010 and this approval: Core 01/02/07/09 v2 edits as specified, `sector_profiles` 1.1.0 (three §6.3 profiles), new `config/decision_narratives.v1.yaml` 1.0.0 (hashed), `evidence_policy` field-class/gate keys if the plan requires (1.3.0), snapshots 2.1.0 in place (IDs unchanged; v1 history untouched), one `build_manifests.py` run. No threshold value change; no `data/synthetic` change; goldens and every simulated exact unchanged; route 8 `GRAPH_REQUIRED` only.
