# S19 assembly correction preservation incident

**Status:** paused for root and independent review. The protected ordinary worktree index no longer matches the initial byte hash. I made no candidate file edits, staging, commit, ref change, freeze, or index restoration.

The first Git read in this correction was `pwd && git status --short --branch && git rev-parse HEAD` in the S19 worktree without `GIT_OPTIONAL_LOCKS=0`. The command execution timestamp was not retained. The observed index mtime is 2026-09-25 13:54:41.359228766 UTC; it is not a command timestamp. This command is a plausible cause of a stat-cache refresh; the precise cause is an inference. Subsequent Git reads used `GIT_OPTIONAL_LOCKS=0`.

`INITIAL-INDEX-BINDING.json` expects SHA-256 `799d94a7b865644e0cd658e52687a9805224beb40c846f6403d0b87b3dd49722` for `.git/worktrees/s19/index`; the current 272,314-byte index is `6451da67da6dbcf8344a897b806a4cbce20d75751185593c0659ec906cbb57e3`. An independently located preserved preimage, `overnight-2026-09-23/reviews/s19-canonical-other56-r1/W19-INDEX-OBSERVED-799d94.bin`, hashes to the exact expected value and has the same byte length. It was **not restored**. The changed index is copied to `observed-index-6451da67.bin` with mode 0600 and the same SHA-256. All other 44 protected indexes match their expected hashes.

Read-only `git ls-files --stage -z` output hashes identically for current and preserved preimage (`0dcfc2970e2bc5e589027ebccbabd04d71c7005a43630b7f18bc0d4deea362c4`). The ordinary staged diff has zero paths; branch remains `slice/s19-bilingual-dossier`, HEAD `ce407db9832b61c5a8a85dfda2e3b7623da2fbc9`. All 3,227 paths in the original `POST-ASSEMBLY-SOURCE-BINDING.json` still match its byte hashes. These observations do not waive the index-byte invariant.

The assembly gap is real: the approved physical delivery-evidence tree has 1,520 files and accepted packet 26 has 20; 74 approved copied `.log` paths are omitted by `.gitignore:9` from the original 3,227 Git-visible paths. The explicit approved union would contain 3,301 paths. The one-off audit ran the unchanged `scan_tracked_files` function and found no findings before it stopped on the protected-index mismatch, so **no completed corrected scan/ledger receipt is asserted**. The audit script is preserved at `audit_once.py`; it has not modified candidate files. No current candidate README, state, or implementation log correction was written.

Root requested a pause and independent disposition before any restoration or freeze. Exact source checks, assumptions, and limits are in `INCIDENT.json`; the original partial receipts remain untouched. Sanad: `INITIAL-INDEX-BINDING.json`, SHA-verified preimage/current index, equal stage listings, original 3,227-path ledger rehash, `.gitignore:9`, physical approved trees. Muhasabah: **FAIL for the index-byte preservation requirement; paused**, with no claim of completed assembly correction or candidate acceptance.
