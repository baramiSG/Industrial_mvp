# Observed canonical chart regions

The single corrected read-only inspection completed56existing scene paths with the native table closed, using unchanged stock navigation/anchors and exact canonical viewports. It did not call VisualBaselineSession or alter baselines. The first invocation failed at import before browser startup; that failure remains in sibling r1.

Four Executive Signal paths (EN/AR ×1440x900/1024x768) visibly contain the new exact scale disclosure and native summary. All prior header/chart/legend boxes and both new text/control boxes pass the existing stock full-viewport assertion. Eight representative PNGs were directly viewed: four Signal pages confirm readable labels, chart and closed summary; four steel Analyst pages confirm the unchanged top-of-workspace anchor with chart below the viewport.

All52Analyst note/summary pairs are below the viewport at the existing canonical anchor. Fifty chart cards are fully below; Arabic desktop SOP and streptomycin have only a0.453125px card edge intersecting the bottom, while chart/note/summary remain below. Those observations do not claim52chart-content image changes. The four Signal paths declare the visible note/control and necessary same-column flow below the card; no unrelated chrome/content/geometry change is admitted. Final actual byte/RGB changed paths still require the single canonical operation and native-resolution before/after review.

DECLARATION.json binds exact paths/rectangles/visible intersections, every viewed PNG hash and source identity. All109visual sources,1759source files and44indexes remain unchanged.

Sanad: actual closed-state records, exact governed text assertions, stock Signal bounds, eight direct views and source hashes. Muhasabah PASS for this finite declaration only: no eligibility-equals-change claim, no hidden failed setup, no capture or source write, and finalfunctional/PDF/canonical results remain pending.
