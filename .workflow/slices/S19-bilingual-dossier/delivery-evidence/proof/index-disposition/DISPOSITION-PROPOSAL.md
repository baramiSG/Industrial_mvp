# S19 active-index metadata discrepancy — finite disposition

Proposed by the coordinator under existing blocker-resolution authority; independent review required. No product, test, canonical image, checkpoint index or alternate index change is proposed.

The coordinator ran `git status --short` without `GIT_OPTIONAL_LOCKS=0`; the reviewer also used read-only Git commands without that setting. The live S19 ordinary index changed from recorded SHA-256 `fb883938560671c74a8fdb90e24d61353f8bf11384dc9bf61a81de9b4f0dd696` to `799d94a7b865644e0cd658e52687a9805224beb40c846f6403d0b87b3dd49722`. A Git stat-cache refresh is the supported explanation, not a proven byte-by-byte comparison against the unavailable original. No staging command was issued. The changed bytes are preserved as `observed.index`; historical hashes and all prior records remain intact.

Direct `CONTENT-COMPARISON.json` shows all 1,697 current index paths, modes, blob identities and stage values exactly equal unchanged HEAD ce407db/tree789577; all flags are ordinary H; staged diff is empty. The other 43 protected indexes retain exact historical hashes. Existing source and canonical receipt checks remain exact. A search hashed 18 plausible index preimages and found no exact fb883938 copy; writer is checking its retained records. Do not synthesize original bytes or call exact old-index preservation passed.

If no exact preimage exists, the smallest correction is to retain the current semantically unchanged ordinary index and bind its observed hash prospectively in one additive index-preservation record. Preserve the historical failed byte-preservation observation, do not rewrite old bindings, and explicitly distinguish this recovery from uninterrupted byte preservation. Require `GIT_OPTIONAL_LOCKS=0` for further read-only Git operations and preserve the current byte preimage until authorized final staging. Checkpoint and alternate indexes retain their existing exact hashes. All product and delivery gates stay unchanged; no product rerun is warranted by a cache-only index discrepancy with unchanged source and entries.

The independent reviewer must verify current index/HEAD equality, flags, empty staging, all other indexes and current source preservation, then APPROVE or REJECT this exact prospective binding. Root records delegated acceptance only after that verdict. No owner quote or Claude approval is invented. If actual entries differ, this disposition cannot be used.

Sanad: observed index bytes, direct Git entry/flag/HEAD comparison, complete protected-index rehash, preceding tool calls and retained source receipts. Muhasabah: the old byte preimage is presently unavailable, so exact restoration is not claimed. This is a disclosed coordinator operational deviation; no product correctness requirement or failed result is relabelled. Scope is the single active index's prospective preservation binding.
