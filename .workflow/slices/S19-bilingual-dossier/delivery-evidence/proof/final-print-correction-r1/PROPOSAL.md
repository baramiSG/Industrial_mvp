# Final PDF correction proposal — two observed presentation findings

Status: PROPOSED; production source remains frozen. This is a finite implementation correction within accepted Task3/4, requiring independent concurrence and coordinator acceptance before edits. It introduces no helper, test function, schema, graph operation, font, margin, tolerance or content change.

Preparation: refreshed the accepted Task5/6 and print/source boundaries, current source/canonical bindings and independent reader findings; reused the original authority/Core/control and owner-delegation preparation. Persona: senior bilingual paged-media and browser-regression engineer. Skills applied: project-orientation, executing-plans, verification-before-completion, task-standards, al-Muhasibi, Sanad and Muhasabah. Existing reviews remain independent.

## Observed defects and exact two source paths

`PREIMAGES.json` binds current source and retained page rasters. In `src/ior_mvp/dossier.py:717/719`, the final gap-control cell concatenates narrative or technical reason with inline evidence anchors. On actual foil simulated AR11, profiles simulated AR15 and independently observed streptomycin simulated AR11, separate LTR tokens wrap into the same RTL line: reason suffix and reference prefix interleave. Existing `bdi` isolates preserve token bytes but do not create separate reading lines. The earlier strict44/1948 PASS remains a geometry/content result; this visual failure prevents overall PDF approval.

The actual steel simulated EN24 ends with the nested definition term “Brownfield versus greenfield comparison”, while its first “Incremental capacity” fact starts EN25. This is a direct `dt`→`dd` nested-record break, not the previously repaired top-level introduction. Preserve both current pages as RED.

Edit only:
1. `src/ior_mvp/static/css/dossier.css`.
2. The existing `test_dossier_print_media_and_pdf_are_valid` body in `browser_tests/test_dossier.py`; retain its name, matrix, fixtures and every inherited assertion. No other test body or inventory literal changes.

## Exact minimal CSS candidate

Add a screen/print rule adjacent to the existing dossier-table rules:

```css
.dossier-section table[data-dossier-table="exclusions"] td:last-child > a,
.dossier-section table[data-dossier-table="rejection-conditions"] td:last-child > a,
.dossier-section table[data-dossier-table="rejection-conditions"] td:last-child > .technical-token {
  display: block;
}
```

Each reason and each distinct reference gets its own wrapping block. The exclusions narrative remains as its original text node before the first block anchor. Existing source links, technical LTR isolation, locale fonts, responsive wrapping and all text remain exact. Do not use nowrap, clipping, hidden overflow, smaller type, global link rules or wider print margins.

In the existing print block add:

```css
.dossier-record .record-group > dt { break-after: avoid; }
```

This is the smallest missing break constraint on the observed nested definition term. It must be proven on the actual steel output: do not infer that the constraint traverses the nested `dd` successfully. Long record bodies remain breakable; do not mark all record groups unbreakable. If actual targeted output still orphans the term, stop and report that concrete failed candidate for reviewed correction rather than escalating markup or silently retrying.

## Meaningful RED → GREEN and targeted proof

Before production CSS repair, strengthen the existing print test body with actual element/Range geometry for both gap tables after fonts load and print-media emulation. For every cell containing evidence anchors, collect the text range before its first anchor and each anchor’s actual rendered rectangles. Require the first reference to begin below the preceding narrative/reason, and every subsequent reference below the preceding complete reference (tight0.01px tolerance). Require each expected original reason/reference text and href to remain present/equal to its source when inspecting the affected rows. This detects logical interleaving even without glyph overlap. Preserve actual old-source assertion failures and diagnostics; do not substitute a computed CSS-string assertion.

After PDF production, in the same existing test require the English simulated counterfactual term and its first actual field label/value to occur on the same emitted page. Use the current English catalogue label and source q3 value; do not reverse Arabic extracted text or claim extraction as visual proof. Existing complete JSON/public-decision, IDs/fonts/page and policy assertions remain. The retained steel EN24/25 must fail this same predicate before repair.

First produce only the four actual failing reports using the existing stock pinned browser recipe: foil simulated AR; profiles simulated AR; streptomycin simulated AR; steel simulated EN. Inspect both gap tables and counterfactual first-content page/adjacency, and perform the unchanged strict checker and144dpi renderer on these four complete PDFs. Preserve original and corrected exact files/logs/source bindings. No full cohort is started until these four actual cases and finite source delta are independently accepted. If another affected page shifts, inspect it; do not infer visual closure from pass counts.

Then freeze the corrected source and run the existing required full functional command once, producing the final44PDFs. Run the unchanged strict checker/every-page renderer. Independent visual coverage must include all44first pages, longest EN/AR, steel contradiction/PPzero/public/simulation and both gap-control tables plus nested counterfactual context; identical previously reviewed raster bytes may be reused only with exact hash binding, changed pages receive actual review. Preserve the failed prior1948-page cohort and its source identity. Update only the external durable staging after the corrected final44 is accepted; no premature whole-PDF APPROVE.

## Exact generation/provenance impact

The CSS file is absent from the actual230graph inputs and present in the109visual capture sources. `browser_tests/test_dossier.py` is also absent from those230graph inputs. All22dossier JSON,22analysis JSON, complete public decisions and44HTML bodies must remain byte-identical through actual API comparison; CSS is linked as a separate static resource. Verify all230input hashes, the six generated postimages, source pathset and protected indexes unchanged except the two expressly permitted source hashes. No graph/manifest rebuild, new ID, token/count adoption or source-schema modification is required or authorized if those checks hold. `dossier.py` and authority/config files remain untouched.

The full source binding changes, and the CSS hash inside visual source_tree changes. Rebind the existing canonical operation preparation to these corrected source bytes, current completed functional/final44 approvals and unchanged128old image preimages before root allocates the still-unconsumed single capture. The existing image, fonts,109source membership,128paths/dimensions, budgets, chart declaration and16actual dossier-bounds callsite requirements remain. Do not silently reuse the old whole-source receipt.

Sanad: exact source717/719 and `DossierHTML.refs/technical`, current scoped CSS and actual230/109 input inventories; author directly viewed foil AR11 and steel EN24; the other named pages are independently attributed reader findings and bound retained rasters. Assumptions: the proposed dt break constraint may suffice but has not run; actual targeted PDF proof decides. Risks: vertical spacing/pagination changes and nested break propagation, covered by targeted then complete44proof without relaxed bounds/fonts/content. Muhasabah PASS for this bounded proposal only: no source edit or browser/generator/canonical rerun has occurred; visual closure, corrected full functional/final44 and later candidate/delivery gates remain unverified. S19-only stop boundary remains.
