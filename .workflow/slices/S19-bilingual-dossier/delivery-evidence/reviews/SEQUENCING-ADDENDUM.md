# Task5/6 sequencing clarification

**APPROVE the existing sequence: complete the current full functional prerequisite, then the separately approved graph/manifest operation and immediate gates; perform the broad final44 strict/raster/manual cohort after actual graph/token closure and before canonical visual binding.** Source readiness, the retained targeted print corrections and44-case inherited Arabic-font closure, together with the exact output comparison below, satisfy the source/render preparation needed for that graph/manifest step once the current full functional run actually passes. They do not constitute final44 acceptance.

This clarifies the phrase “applicable write” in REVIEW.md and OPERATION-REVIEW.md. Parent Task5§1–4 and delivered-base handoff lines52–57 place final functional/PDF/visual closure after graph/manifest closure and before canonical binding. Final44 raster/manual completion is therefore not an additional prerequisite to the graph/manifest operation. All existing source/input/preimage/44-index checks and separate coordinator acceptance still apply. No new plan, gate or framework is introduced.

## Direct output result

Using the exact approved1,757-file frozen source (`5ddecc186da54df7111a5cb8361a50a96dc7f2dcdf01608fe017f230643569e5`), I executed the actual in-process API routes for all11 cases×2modes and both HTML locales. The current side loaded the real retained stored graph `GRAPH-SAU-2026-09-12-49b458a614f3`. The successor side overrode only the graph repository return with the validated retained canonical successor `GRAPH-SAU-2026-09-12-ccd1a2abd05c`, SHA256 `066b366033cc70e00480668355ceccf19f71813cae8969067aff1743e85d7a88`. No builder or generated write occurred.

All176 actual responses returned200. Complete response-body comparison found:

- **44/44 HTML bodies byte-identical.** There are zero changed rendered HTML reports, including all four simulated aluminium locale combinations.
- **20/22 dossier JSON bodies byte-identical.** Only simulated `SAU-H6-760429` (profiles) and `SAU-H6-760711` (foil) change. Each has exactly these three old→new graph-ID leaves: `/route_hypotheses/8/shared_enabler/graph_projection_id`; `/blocks/decision/records/active_decision/route_hypotheses/8/shared_enabler/graph_projection_id`; `/blocks/capability/records/routes/8/shared_enabler/graph_projection_id`. Those are the complete six export-JSON differences; nothing was normalized or excluded from the comparison.
- **20/22 analysis JSON bodies byte-identical.** The same two simulated cases each change the graph ID in top-level, active-decision and simulation-decision route8. Complete real/public decisions remain equal. Authority metadata does not introduce further differences in this current-source comparison.

The renderer does not emit the shared-enabler graph-projection field; its visible route data and authority rows are unchanged. Exact HTML comparison establishes that fact without assuming the two JSON changes imply four changed PDFs. Retained complete outputs and hashes are in SEQUENCING-COMPARISON.json (`8a0e4410c427ba80fd47ac54ff40e1784e2c64cb73027e3168d40c01c1998846`), SEQUENCING-OUTPUTS.json and SEQUENCING-EVIDENCE.json. All1,757 frozen/live source hashes still match after the probe.

## Proof reuse and remaining execution

1. The current r14 browser/PDF run is pre-generation source-quality evidence. Preserve its actual result and original graph/source binding. A passing run, source-only approval and the existing targeted print/font closures permit the already reviewed graph/manifest step; current success has not been assumed here.
2. After the actual operation and its immediate gates, verify the persisted canonical graph and actual unpatched API responses. Compare all22 dossier JSON,22 accompanying analysis JSON and44 HTML bodies to these retained expectations: the20 unchanged exports must remain exact, the two successor exports must contain only the six bound leaf changes, analysis changes must be exactly those recorded above, and all44 HTML must remain exact. Bind the actual final engine-token fixture under its existing permission. This is required actual post-generation verification; the in-process successor probe is not a substitute.
3. Perform the prescribed post-generation full functional/PDF proof and the final44 every-page strict/raster/manual cohort before canonical visual binding. **Defer the broad r14 raster/manual cohort until then**, so this work is performed once on the durable final44. No extra four-report graph-only print cycle is needed: the direct comparison found no changed HTML. The ordinary whole-run PDF producer and final44 checks remain mandatory.
4. Reuse unchanged source, tool, font, negative-control and bounded-review evidence by their exact hashes. The44 HTML-byte equivalence can support the explicit input-provenance link after the actual API check; it does not by itself prove PDF bytes, pagination or raster equality. Reuse any already completed PDF/raster/manual observation only for its genuinely identical bound artifact (or directly established final raster equality), retaining the old receipt and adding the new provenance link. Do not silently relabel a pre-generation PDF bundle or an earlier browser run as a final-tree gate. If actual outputs differ beyond the stated leaves, stop and review the actual delta.

Canonical/R1 review and two complete exact-candidate CI roots, their required PDF/browser gates, hosted checks and exact-head/delivery approval remain unchanged. No expensive mandatory gate is replaced by this probe, and no post-generation outcome is claimed.

Sanad: direct actual-route output capture/comparison, source reads and after-probe hashes; prior targeted print/font and source/operation approvals are the bound retained records. The successor repository override is explicitly distinguished from a future persisted graph/API observation. Assumptions: unchanged approved source, stock producer/assets and exact accepted graph output; each must be rebound at execution. Risk/unverified: current full functional completion, actual post-generation responses and all final44/exact-tree gates. Only external probe outputs/review evidence were written; no source/index/service/generated changes.

**Muhasabah: PASS.** The initially suspected four changed HTML reports are disproved by full byte comparison; all six export-JSON leaves are explicit. Generation sequencing follows the accepted handoff, and every final rendered and exact-tree requirement remains intact.
