# Embedded-CSS comparison correction

Verdict: **APPROVE** the finite expectation correction only.

Refreshed the accepted two-path proposal/owner boundary and actual renderer/CSS preimages; persona: independent rendering and evidence reviewer. Applied strict-reviewer, Sanad, al-Muhasibi, verification-before-completion and Muhasabah. The previous proposal and my concurrence incorrectly accepted the assertion that CSS was externally linked. Actual dossier.py loads the CSS through `_dossier_styles()` and embeds it in the sole `<style>` block. Preserve that erroneous statement and failed API comparison as history; this additive record corrects it.

Directly rederived all four supplied target comparisons from their actual old/new HTML bytes: each contains the complete bound CSS exactly once inside its sole style block, and replacing exactly that preimage with the postimage reproduces the complete new HTML byte-for-byte. Nothing else changes in those four responses. The retained API attempt stopped on its first HTML mismatch and is not a completed88-response proof.

For the complete comparison require22 dossier JSON plus22 analysis JSON byte-equal and complete public decisions equal. For each of44HTML responses, require exactly one style block, exactly one complete bound CSS preimage/postimage occurrence inside it, and full response equality after that one explicit substitution. No normalization, removal of arbitrary style text, unbounded replacement, or allowance for other HTML differences. Preserve all other bytes, including content, IDs, links and policy text. This is a correction to the expected output of the already-approved CSS change, not a weakened product assertion or permission for new behavior, source paths or graph replay.

Sanad: HTML-CSS-EXPECTATION-EVIDENCE.json binds diagnosis785e20c4, actual pre/post CSS, four directly checked HTML pairs and the original failed stderr. Assumption: the remaining40HTML obey the same exact relation; they must actually be measured. Risk: an overbroad comparison could hide unrelated changes, prevented by the single-occurrence/in-style/full-byte requirements. Unverified: complete88 outcome, source/regression acceptance, targeted visual closure and later full gates. Muhasabah: **PASS** for this expectation correction; prior mistaken reviewer premise acknowledged, no mutation of source or historical evidence.
