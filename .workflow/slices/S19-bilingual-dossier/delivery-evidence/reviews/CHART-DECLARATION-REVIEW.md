# Observed chart-region declaration review

**APPROVE the pre-capture chart-region declaration.** This closes the existing observed-region prerequisite in the canonical preparation review. It does not approve a canonical result, changed-pixel count or the pending functional/final44 PDF gates.

Reviewed `s19/canonical-chart-regions-r2/DECLARATION.json` SHA256 `6c3361e04822763ba9526cab3770056d3e98ac3a02b908ffec933c09d8c29f93` and RECEIPT.json `0493a07bc4b5884a9a492b68ead2f26b5c13df613287d49a962bc94eb65726c3`; all18 entries rehash exactly. Refreshed the owner's20260925 S19-delivery-then-stop record, accepted trade amendment Task3, original canonical callsites and the earlier operation review. Independent visual QA/provenance persona; strict-reviewer, verification, Sanad, al-Muhasibi and Muhasabah applied. No later-slice or redesign scope is advanced.

I directly read the inspection body and compared its navigation/anchors with stock capture: Analyst uses the same case/mode selection and #workspace anchor; Signal uses the same preparation and complete-scene visibility assertions. Exact1440×900 and1024×768 viewports, pinned producer flags, closed details state, governed note/summary strings and clean browser channels are retained. No VisualBaselineSession/canonical collector is invoked and no baseline is writable in the source mount. The first attempt's import error occurred before browser startup and remains exit1; the only r2 body correction imports install_graph_routes from its actual graph_pages module. The completed r2 command exits0.

All56 raw records exactly match the already approved chart eligibility set. Independently recomputing rectangle/viewport intersections confirms:

- Four Signal paths—EN/AR at both canonical dimensions—show the scale note and native closed summary. All five required boxes remain fully within the viewport: the three inherited header/chart/legend boxes plus the two new note/summary boxes.
- All52 Analyst note/summary pairs and chart bodies are below the existing viewport anchor. Fifty whole chart cards are below it. Only Arabic desktop SOP and streptomycin have a0.453125px card-edge intersection; their chart/note/summary remain below. No52-image visible chart-content change is claimed.

I directly viewed all eight bound PNGs at native resolution: four Signal and four steel-public Analyst images. Signal headers, chart curves/year labels, unit legends, scale disclosure and closed summaries are readable and contained; the existing upper Analyst anchor shows identity/public conclusion with no chart in view. This is eight-image manual coverage plus56-record geometry verification, not manual inspection of56 screenshots. No concrete layout finding arises within this assigned declaration.

The four Signal paths declare only the scale note/summary and necessary same-column flow below that card. Other chrome/content is outside this permission. The two subpixel Analyst card edges do not establish any actual changed pixel. Full source1759, visual-source109, graph-input230, protected-index44 and predecessor-baseline130 hashes remain exact. No source, baseline or index was changed by this review.

The single later canonical operation must still produce the actual old/new byte/RGB ledger and every actual changed-pair review, including any unexpected region. The declaration neither predicts four changed images nor transfers approval to unobserved output. All16 real dossier helper callsite observations, source preflight, completed functional/final44 proof and root allocation remain required under the already approved operation.

Sanad: direct packet/probe/stock-callsite reads; all18 hashes; independent56-path/intersection/count checks; eight actual native image views; fresh source/input/index/baseline hashes. Browser execution is attributed to its sealed command and raw output; I did not replay it. Exact viewed files/hashes are in CHART-DECLARATION-EVIDENCE.json. Unverified: future canonical pixels and all remaining gates. **Muhasabah: PASS** — prior failure remains distinct, visual coverage is explicit, eligibility/visibility is not misrepresented as pixel change, and this finishes an existing prerequisite only.
