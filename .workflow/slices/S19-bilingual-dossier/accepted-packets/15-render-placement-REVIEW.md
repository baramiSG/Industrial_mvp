# S19 renderer-placement and keyboard-expectation amendment review

**APPROVE.** Zero blocking findings in this exact bounded plan correction. This is independent same-model Codex review, not implementation/source-readiness approval, delegated acceptance, Claude approval or permission for generation.

Reviewer: `/root/s19_source_reviewer`; author: `/root/s19_delivered_base_handoff`. Root coordinates acceptance and the separate writer executes.

| Exact subject | SHA-256 |
|---|---|
| `planning/s19-render-placement-r1/AMENDMENT.md` | `921655ade190052e9e0e71b47c1795a1b7918667073c511fc62784c61e3f57e3` |
| `BINDING.json` | `c61c5b9afe479fc0e17fc6491033872b173c2b937fd3e50659ffb88f5addf6f5` |
| `SHA256SUMS` | `76c50ec0e47f7face9a53ca2fa80120edd4841cf1ac21f780f2610fc7e514e83` |
| Independent `EVIDENCE.json` | `d7565fe4d956d3d6c27d335ae5f7cbdba82a0c22e4b94560500f8cba4d35c2cd` |

Reused [completed preparation](../s19-source-preparation-r1/PREPARATION.md), then read the complete sealed amendment/binding, all three renderer preimages, complete existing keyboard function, actual checker/lookup functions and retained failure summary. For this delta, applied Python integration and regression-contract reviewer roles plus the already-read strict-reviewer, verification, GitHub-flow, design, Sanad, Al-Muhasibi and Muhasabah disciplines. Review stayed within the demonstrated placement/control integration gap.

## Findings and rationale

- **Cause verified.** Actual checker and retained S18b checker are byte-identical at `95dc7279…`. `referenced_ui_keys` and `copy_findings` include `dossier.py` as their sole Python renderer path. Preserved new helper files contain real rendering code and dynamic dossier-key construction outside those inventories. Full literal mappings in the actually called renderer address both placement and dynamic discovery; inert strings would not satisfy the required runtime-consumption tests. `config.ui_text` independently rejects missing keys. The plan correctly avoids claiming its known-key-filtered static scanner proves runtime unknown-key refusal.
- **Finite correction.** Consolidate actual HTML helpers in already permitted `dossier.py`, retain projection/validation separation and both public interfaces, preserve preimages and omit only the two newly introduced modules. Read-only accepted-base tree inspection confirms neither omitted module is a historical base file. No checker, dependency, schema, authority, domain, fixture, service or generator expansion is authorized. Existing graph/visual top-level Python discovery sees the final placement; source closure still precedes generation.
- **Substantive proof retained.** The writer log ends `6 failed, 174 passed`; its D* source-path failure and unfinished trade usages remain distinct. Required44 HTML combinations and explicit optional/empty fixtures must exercise each introduced mapping through the public renderer with independently named catalogue/source expectations and observed translation calls. Exact pre/post JSON/analysis/public-decision equality and public/render no-retrieval constraints remain. Unused-key, reached-unknown-key and literal-copy mutations, stock usage/copy/default checkers and parent tests preserve sensitivity. Static references alone cannot pass this obligation.
- **Single keyboard hunk is precise.** Preserved and actual S18b/S19 `test_accessibility.py` hash to `32f3e725…`; its function at lines47–133 hashes exactly to `dd7c670e…`. The literal tuple has the specified metric-to-rule insertion point. Only `"id:trade-data-summary"` may be inserted after observed unique-control/old-tuple RED confirms that exact order. Existing DOM/focused equality, wrap identity, every member/order, focus visibility/pixel signatures, artifact retention and every other file byte remain. The entire unchanged parameter matrix must subsequently pass. Actual order is an execution obligation, not assumed here; a different order returns this hunk to review.
- **Parent acceptance preserved.** Full real-decision/public isolation, §15/supporting content, bilingual semantics,22 JSON/44 HTML/PDF, per-page print proof, AR-V01/trade requirements, inventory admission, source/UI readiness, the bound single generation sequence, strict R1, two complete isolated CIs, exact candidate/head and protected delivery remain. No partial pass closes unrelated product findings.

Independent checks matched all9 sealed manifest entries, both checker/keyboard copies and the exact keyboard function body. All7 bound references matched at initial read. During evidence recording, the separately writable `dossier_projection.py` reference changed from `4a727523…` to `6fe9167e…`; the other6 references and sealed packet still match. This is an observation of continued unrelated implementation, expressly permitted by the binding, not a frozen-candidate mismatch. Changed projection content is neither inspected nor approved here. Execution must preserve/rebind the actual newer inputs as amendment step1 already requires.

The source inspections and a small in-memory literal-pattern probe confirm why dynamic prefixes do not constitute complete key literals. No product test or browser ran in this plan review. The actual future checker, mapping and keyboard results remain required.

## Sanad / Muhasabah

Direct evidence in `EVIDENCE.json`: packet/reference hashes, source/AST/regex inspection and accepted-base tree lookup. Prior authority/approval/delivery facts remain attributed to the preparation's sources; the six-failure log is the writer's retained result, independently read rather than rerun. Assumptions are execution after root acceptance and rebinding newer preimages. Real mapped-label behavior, consolidation equality, native focus position/RED→GREEN, PDF quality and candidate gates remain unverified.

Risks are dead mappings, unknown-key fallback, accidental projection changes and extra focus stops; the plan retains falsifiable checks and bounded stop conditions. The first reviewer evidence-capture attempt stopped before writing files when its continued-reference-equality assertion detected the projection edit; the subsequent record preserves that observation without freezing the writer. Only this review and minimal evidence record were written. No mutable renderer source was treated as an approved candidate; no source/index/service/GitHub/generated write or subagent occurred.

**Muhasabah: PASS for the bounded plan review.** APPROVE applies to this correction's content; unfinished S19 product work remains subject to its existing independent gates.
