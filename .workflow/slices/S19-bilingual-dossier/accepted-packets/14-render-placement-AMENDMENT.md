# S19 bounded renderer placement and keyboard expectation correction

**Proposed for separate independent review and root delegated acceptance; not implementation approval.** Preserve the parent plan `7749006ae850adf66fea862934c8362d9ea5c9254aaf74ded702504e69ce44d1`, all approved amendments and their historical wording. This changes source placement and one exact existing keyboard expectation, before any S19 generation allocation.

Preparation: reused completed AGENTS/external authority, actual DOCX§15, Core03/04/09, roadmap/control and S19-plan reading; refreshed current base/writer acceptance, parent exact files, actual checker, three renderer files, catalogue tests and the real failing log. After reading, selected a Python integration architect and regression-contract engineer persona. Applied the installed writing-plans and systematic-debugging skills, strict-reviewer criteria for self-check only (no author approval), github-flow boundaries, Sanad, al-Muhasibi and Muhasabah. No new framework or repeated feasibility work.

## Cause and exact scope

`referenced_ui_keys()` and `copy_findings()` in unchanged `scripts/check_ui_contracts.py` explicitly scan static HTML/JS and `src/ior_mvp/dossier.py`; neither scans the newly introduced `dossier_rendering.py` or `dossier_sections.py`. Usage discovery matches full quoted key literals. The writer's `render-css-first.log` retains actual unused dossier-key failures: **6 failed,174 passed** overall. Its separate D* source-path assertion and unfinished trade-key usages are not resolved by this amendment. Checker SHA `95dc727943c30f80530f6e60bdc031314242935368ee4d79a9279f174a5626ad`; failed-log SHA `ef7e57d650d283d351fb93531aa4b404266846829268551bfc7ca8526c3ca871`. Full observations and preimage hashes: [BINDING.json](BINDING.json).

Amend the parent’s production placement: keep the **actual HTML renderer and its small formatting/section helpers in already permitted `dossier.py`**, beside the existing public façade. Keep pure structured projection in `dossier_projection.py` and validation in `dossier_validation.py`. Omit the two newly authored, uncommitted `dossier_rendering.py` and `dossier_sections.py` from the final candidate after preserving their bytes externally and consolidating their implementation. Current three files contain191+178+97 lines; this is an observation, not a new line-budget waiver or final-size claim.

`build_dossier(analysis)` and `render_dossier_html(dossier, locale)` remain the public interfaces. No new product file, checker/Makefile/CI edit, dependency, schema meaning, authority rule, evidence, threshold, scenario, golden, service or generation permission. The sole additional test-path permission is the one existing keyboard expectation literal below. Existing tests may change only within the parent's named dossier/catalogue/UI test paths for this correction's regression; no browser inventory expansion follows from module placement. Relevant S19 implementation/architecture records may truthfully state the final placement within their existing allowance.

## Concrete implementation and proof

1. After independent approval/root acceptance, the assigned writer pauses these three files, verifies current bytes and preserves exact current preimages plus hashes in its existing external evidence directory; the observed copies here already preserve this proposal's three inputs. If unrelated work changed them meanwhile, retain the actual newer inputs too—do not overwrite history. Consolidate real functions/classes/imports in `dossier.py`; remove circular/obsolete renderer imports. Remove only those two new source files once their code is preserved and migrated; no retained or tracked historical file deletion.
2. Merely moving dynamic prefixes is insufficient. Replace renderer-only dynamic key assembly with **finite literal mappings actually consumed by the rendering paths** for section headings, supported field labels, availability/reasons and other dynamic dossier chrome. For example, section dispatch consumes `{"identity": "dossier.section.identity", ...}` and the real table label lookup consumes `{"d_star": "dossier.field.d_star", ...}`. Populate only actual approved meanings, use those mappings in the existing renderer, and keep fallback behavior limited to genuinely unknown technical/source identifiers under the parent contract. No inert list, unreachable call, comment-only reference, duplicate hidden text or catalogue expansion solely to satisfy discovery. Every emitted label still resolves through `ui_text`; no hard-coded EN/AR catalogue copy.
3. Preserve the failure log as RED. In already allowed dossier/catalogue tests, exercise complete44 real HTML combinations and necessary explicit unavailable/empty/optional fixtures. Assert actual section/field/status text against independently named catalogue keys and source values, and observe real translation calls through the public renderer to prove the introduced mappings are consumed. Each introduced dossier mapping/key must have a genuine rendering case; a static-reference assertion or testing the mapping by itself is insufficient. Compare pre/post consolidation JSON/analysis/public decisions for exact equality; this placement change must not change projection results or introduce scenario retrieval into rendering/public mode. Existing parent behavior corrections remain independently testable, not silently attributed to placement.
4. Retain unknown/unused/copy sensitivity with focused disposable-copy or monkeypatch controls in existing allowed tests: an extra EN/AR catalogue key with no production consumer must make the stock usage check report `unused-key`; a genuinely reached renderer key changed to a nonexistent key must raise existing `UIStringConfigurationError` (not silently display the key or return blank success); placing literal catalogue copy in `dossier.py` must still be detected by unchanged `copy_findings`. Restore mutations/caches in `finally`. The static checker filters references to known keys, so **runtime lookup**, not that checker, proves unknown-key rejection. No claim of broader static detection than the code provides.
5. Run stock `python scripts/check_ui_contracts.py --check-catalogue-usage`, `--check-copy`, the default checker, and focused complete dossier/catalogue/UI-token tests in the locked environment. Record each actual command/status. Other unfinished product assertions remain failures until their separate authorized fixes pass; do not label the entire earlier six-failure run repaired by placement alone. Run all parent browser/PDF/isolation/RTL and exact-candidate gates when product readiness reaches their scheduled phase.

## Sole additional test hunk: the approved chart summary

The approved trade amendment introduces a real native summary control. Current `browser_tests/test_accessibility.py::test_keyboard_tab_order_reaches_every_interactive_control_with_visible_focus` compares complete literal DOM and focused orders, and its expected tuple lacks that new control. The old file/function preimages are bound in BINDING.json and preserved externally. This is a concrete required expectation extension, not permission to change the accessibility harness.

- In already approved `src/ior_mvp/static/modules/renderers/trade.js`, give the actual native summary the unique ID `trade-data-summary`. Keep native semantics and all approved chart source/value/keyboard behavior.
- Preserve original new-control/old-tuple RED and actual focus-report evidence. Verify exactly one summary in this actual Analyst page and its focus position immediately after the final metric claim and before the rule claims. Setup failure, duplicate IDs or another unexpected stop/order change does not qualify.
- Then change **only** the `expected` tuple in `browser_tests/test_accessibility.py` by inserting this single literal at that verified position:

```python
        "claim:step.INTERVENTION.simulated" if mode == "simulated" else "claim:metric.generic_capacity",
        "id:trade-data-summary",
        *(f"claim:rule.{rule}" for rule in ("R0", "R1-F", "R1-D", "R2", "R3", "R4-F", "R4-D", "R5", "R6", "R7", "R8", "R9-S", "R10", "R11", "R12")),
```

- Preserve every inherited member/order, `report.dom_order == expected`, `report.focused_order == expected`, wrap identity, every visible-focus/pixel-signature check, artifact retention and every other function/byte in that file. No discovery/checker/skip change, no expected-from-actual and no new test name. This supersedes prior S19 exclusion of this file **only for this literal**; all other accessibility changes remain outside the permitted set.
- Run the entire existing keyboard matrix for that exact test and retain GREEN alongside RED. Continue the already required native Enter/Space/Tab chart tests and complete functional gate. If actual DOM order differs, stop this hunk for concrete review rather than alter additional expectations. Finalize the product summary ID before source/generation binding; this grants no extra generator allocation.

The public/synthetic boundary, complete methodology§15/supporting pack,22JSON/44HTML/PDF, per-page disclosure/bounds and visual inspection, AR-V01/trade repairs, full real_decision equality and frozen source preservation remain unchanged. Source placement is finalized before read-only graph construction/source binding: current top-level discovery will naturally see `dossier.py`, projection and validation, without the omitted two modules. No extra graph, manifest or canonical run is authorized. The existing single reviewed generation sequence, strictR1, two isolated complete CIs, exact-tree/head review and GitHub delivery still bind.

Stop this correction for independent review if preserving interfaces/output requires a new path, checker change, weakened lookup/parity guard, or a further generated allocation. Ordinary source or translation findings still must be fixed within their approved product scope; unused keys cannot be hidden to obtain PASS.

**Sanad:** direct checker/source/log/test reads and SHA-bound preimages support the placement cause and smallest correction; the inherited focus tuple directly supports the additional-control expectation, whose actual DOM/RED evidence remains required before editing. Prior plan acceptance and delivered-base facts come from their retained records. No fresh test or consolidation was run by this author; future GREEN and final rendering quality are unverified. **Muhasabah: PASS for this bounded proposed amendment.** Only this new external packet/preimages changed; no self-approval, gate waiver, invented result, Claude attribution or claim that unrelated failures are fixed.
