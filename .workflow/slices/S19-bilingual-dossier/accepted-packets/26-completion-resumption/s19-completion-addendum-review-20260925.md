# Independent review of the S19 completion addendum (Codex) — read-only

Reviewer: Claude Code (independent reviewer seat). Date: 2026-09-25. **Review only.** No candidate, index, generator, service, Aura, Git or worktree state was changed; the S19 worktree was read with `sed`/`grep`/`ls` and one `GIT_OPTIONAL_LOCKS`-safe path (no Git command was run inside it for this review); retained rendered pages were viewed as images. Scratch files live only in the session scratchpad and this review file.

**Review subject (bound by hash):** packet `/home/barami/.cache/industrial-opportunity-resolution-mvp/s19-completion-plan-20260925/` — `S19-COMPLETION-ADDENDUM.md` `51cd644ce3ab189db108bd3998e6b18e8feb91e7927efc0c12ab6babc979b8ad`; `SHA256SUMS` `59677f10a496238a9e0e497292bfd596982906e4f916eef6328984c583019c36` (`sha256sum -c`: 8 OK, 0 failed — README, PACKET-CHECKS.json, SOURCE-BINDINGS.json, notes/{authority-aura.md, dossier-output.md, state-evidence.md, state-evidence.json}). All 30 SOURCE-BINDINGS entries are stated as matching; I re-derived the load-bearing ones below rather than trusting the packet.

**Controlling authority:** `.autonomous-workflow/overnight-2026-09-23/S19-OWNER-STOP-HANDOFF-20260925.md` (sha256 `1ce621b5…f310c`, recorded 09:14:50.574 UTC) — "STOPPED: owner withdrew autonomous authorization … supersedes earlier continuation instructions/checkpoints." It supersedes the 09:09:08 candidate-freeze release I cited in my PLAN v3 review; that release is now history, not authority. This review authorises nothing.

---

## Verdict: APPROVE the addendum as the completion plan, with the open product question adjudicated as follows — **D-S19-1 is a required correction, not an optional presentation improvement.** Completion is released only by the owner decisions in §6; the shortest sound path is §4.

Plainly:

1. **State is as the addendum says.** Branch `slice/s19-bilingual-dossier` at `ce407db`, nothing staged, no candidate tree, no S19 commit/PR/CI; 44 protected indexes plus the new baseline index match; two pin literals edited and untested; graph `ccd1a2abd05c` 925/1045; 128 canonical images; durable staging of 1,000 files still external (§2).
2. **The steel summary does not misrepresent, but it omits a required distinction.** Page 1 prints `Evidence passport ledger: S-WITS-721049 S-UNICOIL-EPD S-UNICOIL-SPEC` in EN/AR, public/simulated. Nothing marks S-UNICOIL-SPEC as the contradicted reference "retained for confirmation", which the accepted experience design and DOCX §15 both require on the summary. The correction is paragraph-sized and needs no catalogue change (§3).
3. **Because `dossier.py` is both a graph input and a visual-manifest source, the fix reopens generation.** One replacement graph→manifest→functional/PDF→canonical→R1→pin sequence is required, under a new owner allocation; `ccd1…` and its receipts stay as the prior accepted operation (§4, §6c).
4. **The remaining gate sequence in the addendum matches the approved plan** (Task 6) and the delivered-base handoff: durable assembly → freeze → full gates on the exact candidate → actual-predecessor R1 → compare-only e2e → two clean-root `make ci` each with its own 44-PDF proof → exact-tree review → owner acceptance → staging/PR/hosted checks/merge/main verification (§4). Retained receipts remain evidence for unchanged subjects only.
5. **Aura stays a separately scoped obligation.** S19 delivery is GitHub delivery; the addendum's post-delivery live checkpoint is a separate owner decision with the corrected target binding; S21 remains the plan-of-record for final live certification. Nothing here claims S21 (§5).
6. **Page count is not a defect.** 27–73 pages per report is the consequence of "one summary page plus the complete evidence pack"; no cap exists in the accepted plan; navigation/page numbers are CARRY (§3.3).

---

## 1. Preparation

- **Read:** AGENTS.md (S19 copy, sha256 `10506046…f81` = the ce407db copy); Manifest §7–§10; governing DOCX §15 directly (P1082–1118: block table incl. P1103–1104 "Evidence — Top supporting and contradictory evidence, confidence, missingness and reviewer status"; §15.1 P1108–1118); the accepted S19 packets in the worktree slice record (`00-…IMPLEMENTATION-PLAN.md` incl. Evidence row `:78`, Task 5 `:104-122`, Task 6 `:124-137`, stop conditions `:141`; `01-…EXPERIENCE-DESIGN.md:15,17,35`; `23-delivered-base-EXECUTION-HANDOFF.md:40-70`; `05-…SUCCESSOR-PIN-AMENDMENT.md`); the 09:14:50 owner-stop handoff in full; today's owner-decision records (canonical-result/candidate-freeze release 09:09:08, active-index disposition 09:06:47, final44/canonical release 08:36:10) and the S18a Aura result acceptance; the addendum, README, SOURCE-BINDINGS, PACKET-CHECKS and all four notes; `ops-final-r1/PIN-RECEIPT.json`; retained steel dossier JSON (public and simulated) from `s19/nonrule-caption-correction-r1/actual-api-outputs/`; rendered page text and images from `s19/final-pdf-review-r4/rendered/`.
- **Personas:** (1) decision-dossier contract auditor (does the rendered summary meet the accepted design and DOCX §15?); (2) release-provenance and change-control auditor (what does a hashed-source correction invalidate, and which allocations/records are needed?); (3) Arabic/English print-output reviewer (are the summary and appendix fit for their intended uses?); (4) graph/Aura operations reviewer (is the live obligation correctly bounded?).
- **Skills invoked (Skill tool, this session):** strict-reviewer, sanad-provenance, al-muhasibi, muhasabah-gate (re-invoked for this review), plus project-orientation and task-standards loaded earlier in the session. No subagents were used for this review; every finding below is my own direct observation or an attributed retained record.

---

## 2. State verified against the addendum §1

| Claim | Verdict | Evidence |
|---|---|---|
| Branch/HEAD/empty staging/no candidate | VERIFIED | Stop handoff "Exact retained Git state"; `notes/state-evidence.json` `/git` (1,697 index entries all equal HEAD, staged list empty, all commands with `GIT_OPTIONAL_LOCKS=0`); my PLAN v3 review's own `--no-optional-locks` reads earlier today agree (HEAD `ce407db`, 0/0 vs `origin/main`, no remote S19 ref) |
| 45 protected indexes match; original `fb883938…` bytes lost, not restored | VERIFIED (attributed) | `state-evidence.json` `/indexes` before/after rows; stop handoff "Most recent operational defect"; owner record `…active-index-metadata-disposition-1.json` (sha256 `ac775c9e…c178`, bound in SOURCE-BINDINGS) |
| 1,759-path inventory matches except two pin literals | VERIFIED (attributed) | `state-evidence.md` "Direct retained-state findings"; `ops-final-r1/PIN-RECEIPT.json` (status `MECHANICAL_TWO_LITERAL_ADOPTION_COMPLETE_NOT_FINAL_CANDIDATE`, changed files `tests/test_frozen_public_evidence_pins.py`, `tests/test_s17_status_docs.py`, baseline subtree `0bc664f2…`, intermediate tree `dea1e781…`) |
| Graph `ccd1a2abd05c` 925/1045; 128 images, 92 changed/36 identical; final44 = 44 PDFs/1,958 pages | VERIFIED (attributed) | stop handoff; `state-evidence.md`; `final-pdf-review-r4/INPUT-BINDING.json` page counts as tabulated in `notes/dossier-output.md` (27–35 public, 52–73 simulated; max 73) |
| Durable staging 1,000 files + README, not yet in W | VERIFIED (attributed) | `state-evidence.md` "Durable package"; `FINAL44-SELECTION.json`/`CANONICAL-SELECTION.json` hashes in stop handoff |
| Retained reviewers were Codex, not Claude | VERIFIED | stop handoff "Reviewed"; owner records' `authority` fields ("Independent Codex review; not Claude approval") |
| S19 delivered? | **NO** | No commit, push, PR, hosted CI or merge (stop handoff "Actually delivered"; my `ls-remote`/`gh pr list` earlier today) |

---

## 3. Adjudication of D-S19-1 (steel summary evidence roles)

### 3.1 What the accepted contract requires
- DOCX §15 block table, row "Evidence": **"Top supporting and contradictory evidence, confidence, missingness and reviewer status"** (P1103–1104); §15 opens "The one-page decision is backed by a reproducible analytical record" (P1083); §15.1 lists the supporting pack incl. "Contradiction register, expert overrides and decision history" (P1118). DOCX §13 worked case, P901–902: "The web/EPD range difference is retained, not harmonised away. Producer confirmation is required before using the envelope for a target-specification conclusion."
- Accepted experience design `:15`: the first printed page is the decision summary containing "… top supporting **and contradictory** evidence references, lead condition and kill condition … every omitted detail remains in the supporting pack."
- Accepted implementation plan Evidence row `:78`: "Deterministic stable ordering; **support and contradiction separate**, no ordinal confidence score."

### 3.2 What is actually rendered (direct observation)
- `final-pdf-review-r4/rendered/steel-public-en-page-01.txt:21` = `Evidence passport ledger: S-WITS-721049 S-UNICOIL-EPD S-UNICOIL-SPEC`. Viewed images: `steel-public-en-page-01.png`, `steel-simulated-en-page-01.png` (same line under the simulated footer/header labels), `steel-public-ar-page-01.png` (`سجل جوازات الأدلة: S-UNICOIL-SPEC S-UNICOIL-EPD S-WITS-721049`). The three IDs are typographically identical links under one neutral heading; nothing on the page names a contradiction. The headline is "INVESTIGATE — binding constraint unresolved" / «تحقّق — القيد الملزم غير محسوم».
- Viewed `steel-simulated-en-page-39.png`: section "Evidence passports and contradictions" → "Public evidence contradictions: S-UNICOIL-SPEC: Published coating range differs from EPD and is retained for confirmation." So the contradiction is fully present in the pack, and the passport table shows class/status/reviewer status.
- Retained dossier JSON (public and simulated, `actual-api-outputs/SAU-H0-721049-{public,simulated}-dossier.json`): passports in order WITS (supports TRADE_VALUE, TRADE_QUANTITY, SUPPLIER_CONCENTRATION), EPD (DOMESTIC_NAMEPLATE_CAPACITY, DOMESTIC_PROCESS_ROUTE, DOMESTIC_SPECIFICATION_ENVELOPE, DOMESTIC_LABORATORY_METROLOGY), **SPEC (supports BILINGUAL_SPECIFICATION_EXTRACTION, DOMESTIC_SPECIFICATION_ENVELOPE, DOMESTIC_DIMENSION_ENVELOPE and carries `contradiction: "Published coating range differs from EPD and is retained for confirmation."`)**, HADEED (DOMESTIC_PROCESS_FAMILY); `contradiction_register.public = [S-UNICOIL-SPEC]`, synthetic empty; `real_decision` INVESTIGATE in both modes.
- Source (read-only, `W/src/ior_mvp/dossier.py:666-676`, byte-equal to the retained source per `notes/dossier-output.md`): `summary_refs = [first two public passports]`, `contrary = [contradiction_register.public ids]`, rendered as `<p class="summary-evidence">{evidence.title}: {refs(summary_refs + contrary)}</p>`. One template serves both locales and modes.

### 3.3 Ruling
- **Misrepresentation?** No. The line does not call S-UNICOIL-SPEC "supporting"; it calls the three IDs a passport ledger, and SPEC genuinely is a passport with three supported claims. No decision, number or evidence is wrong; nothing is deleted from the pack.
- **Required correction?** **Yes.** The accepted design's summary content list and the DOCX Evidence block both name the *distinction* as required content, and the implementation plan says "support and contradiction separate". On page 1 the distinction is absent, so a reader of the one-page decision cannot see that the reference underlying the specification envelope is contradicted and awaiting producer confirmation — which is precisely the "binding constraint" the headline announces and the confirmation DOCX P902 demands. That is an accuracy-of-explanation gap against an accepted requirement, not taste. It must be closed before publication.
- **Bounded correction (exact):** in `dossier.py` render two labelled groups instead of one concatenation: supporting references under the existing key `evidence.title` ("Evidence passport ledger" / «سجل جوازات الأدلة», `config/ui_strings.v1.yaml:662/:1768`) and contradictory references under the existing key `dossier.public_contradictions` ("Public evidence contradictions" / «تناقضات الأدلة العامة», `:751/:1857`) — both already in the uncommitted 1.7.0 catalogue, so **no catalogue edit and no version change** are needed, which preserves the successor-pin amendment literals (1.6.0→1.7.0 for S19). Keep IDs, order and the `[:2]` selection; a reference with both roles (SPEC) appears in the contradictory group and is not suppressed; when the public register is empty print nothing for the second group (no invented "none" claim on page 1); keep the line on the single physical summary page (measure; do not shrink fonts). Tests: focused summary assertions for steel in both modes/locales plus an empty-register case (`tests/test_dossier_*.py`) and the existing print/one-page and public-isolation browser checks (`browser_tests/test_dossier.py`). No new rule, ranking, calculation or narrative. Optional (owner taste, not required): one short contradiction sentence on page 1 — only if measured to fit.
- **Separated from optional presentation work (CARRY, not required for completion):** U1 appendix length/repetition and U2 missing printed page numbers/running headers. The intended uses are met as designed: page 1 is one legible physical page with the required blocks (after D-S19-1), and the pack is the complete reproducible record §15.1 demands. The 1,958-page aggregate is 44 separate exports, not one document; the measured maximum 73 is a binding, not a cap. I viewed the sampled terminal page (steel simulated EN page 39 is a full section page); the retained r13/r4 reviews cover terminal-page completeness and are reused as attributed evidence.

---

## 4. Shortest sound completion path (confirms and refines addendum §3)

0. **Owner record releasing resumption** (§6 a–c): verdict recorded, D-S19-1 disposition, assigned implementer, bounded operations, stop after delivery; preserve every earlier acceptance and the 09:14:50 stop.
1. **D-S19-1 source correction** (files: `dossier.py`, dossier unit/browser tests; no catalogue, CSS only if the two-group line needs a break rule) → meaningful RED (current concatenation fails the new assertions) → GREEN → source-readiness review by the independent reviewer. Records: append the ADR/slice-log entry that this correction reopens the generation operation.
2. **One replacement generation/capture allocation (new owner record; see §6c)**, because `src/ior_mvp/dossier.py` is in the graph `ENGINE_IMPLEMENTATION` input group (`graph/projection.py:294-303`: every `src/ior_mvp/*.py`) and in the visual manifest source tree (`browser_tests/baselines/v0.3.0/manifest.json:1844`). Sequence, each exactly once: read-only double construction and full normalized equality incl. all 11 `real_decision` (only identities may differ) → one projection write with a **new** ID (bound at review, not guessed; `ccd1a2abd05c` retained as a historical root) → one manifests build (**expect 728 → 730**: the retained `ccd1` directory stays, so exactly two new paths; verify, never force; the two live manifest-count literals are already admitted by the base-adoption amendment) → integrity/reconstruction/graph validate/build-check → functional run and **all 44 PDFs** with the established renderer/strict wrapper and first-page manual review (page 1 changes in every report because the template is shared) → **one canonical capture** (128 paths; the 16 dossier images redesign; others only run-ID/version text; new `change_ref` bound at review) → same-layout R1 against the **delivered-base predecessor** (`ce407db` canonical, not the superseded `ccd1` capture; five identifier-only rewrites, 12/5 rows, geometry invariance) → rebind the two pin literals (baseline subtree OID, change_ref) — the current untested `0bc664f2…` edits are superseded, not tested as final.
3. **Durable/control assembly**: append current status/chronology to the five controls, `.workflow/state.json` and the slice log (implemented → subject-reviewed → stopped 09:14:50 → resumed → corrected → regenerated → pending gates); complete the reviewed durable subset (retained cohorts kept as history; the replacement cohort bound separately); run the prescribed publication/credential/prohibited scan on the **actual assembled union** (the r3 scan predates final44/canonical); mark older "finish S19 then stop" appendices as superseded.
4. **Freeze one exact candidate** via a newly bound alternate index and proof commit; no final tree ID inside the tree; no branch commit/staging yet.
5. **Gates on that candidate only** (Task 6, `:126-137`): full `pytest -q`, `verify_integrity`, reconstruct/validate/scenario/smoke/static/prohibited/threshold/package checks; compare-only `make e2e`; R1 (already bound in step 2 — re-run on the frozen tree); **two complete `make ci` runs in distinct clean roots** with reviewed AM-4 namespace controls and **each root's own 44-PDF proof**; every receipt shares the final tree identity; any failure or unexpected delta stops and creates a new candidate.
6. **Exact-tree independent review (Claude) → owner implementation acceptance → staging == candidate → commit == candidate → push → PR → hosted checks at the exact head → reviewer approval of that head → merge → main CI verification → additive delivery record.** Mark S19 delivered only with that commit/PR/main evidence. Stop; no S20.

Reuse of retained evidence is legitimate for unchanged subjects (RED→GREEN history, renderer feasibility, index disposition, rejected cohorts as history). It is **not** legitimate for any output changed by D-S19-1 (all 44 PDFs' page 1, the 16 dossier canonical images, graph/manifest identities) or for any final-candidate gate.

---

## 5. Aura — explicit and separately scoped

- The approved S19 plan says local Neo4j evidence is not Aura evidence (`:132`), and the delivered-base handoff reserves accepted-projection live Aura plus bilingual application verification to S21 (`:59`). S19 completion therefore = GitHub delivery. Nothing in this review or the addendum claims S21.
- Target binding verified from the S18a acceptance record `20260924-s18a-aura-result-acceptance-1.json`: `instance_id` `8a7338e0`, `database` `8a7338e0`, `instance_name` `Industrial_mvp`, last live projection `GRAPH-SAU-2026-09-12-461bf4840962`, tree `1364789a…`. The stop handoff's "database Industrial_mvp" shorthand is inexact; the addendum's correction is right and must be recorded additively.
- The addendum's proposal — a bounded post-delivery live checkpoint of the delivered S19 projection using the existing operator/`graph-aura-load`/`graph-aura-verify`/operator-test procedure, never the clearing graph-test fixture, with fresh exclusivity/recovery/isolation proof and independent GO — is acceptable **as a separate owner decision (§6d)**. If D-S19-1 is corrected, the projection to bind is the replacement, not `ccd1…`. Failure leaves live acceptance open; success verifies the delivered checkpoint, not S21.

---

## 6. Exact owner decisions needed to release completion

- **(a) Adopt the D-S19-1 ruling:** required bounded correction as specified in §3.3 (dossier.py + dossier tests; existing catalogue keys; no version bump), or, if the owner instead accepts the current summary treatment, record that sourced acceptance explicitly as a deviation from experience-design `:15` — I do not recommend the second option.
- **(b) Resume S19 completion by a new owner record:** named implementer (a seat; not the stopped coordinator by default), boundary = §4 steps 1–6, explicit "stop after delivery", preserving the 09:14:50 stop and all earlier acceptances; disable optional Git locks for every read.
- **(c) Release one replacement generation/capture allocation** bound to the post-correction source ledger hash: one projection write, one manifests build, one functional/44-PDF proof, one canonical capture, R1 against `ce407db`, pin rebinding — superseding, for the corrected tree only, the consumed records `20260925-s19-single-generation-execution-acceptance-1.json` and `20260925-s19-final44-canonical-operation-release-1.json`; `ccd1a2abd05c` and its receipts retained as the prior accepted operation.
- **(d) Accept or defer the separately scoped post-delivery Aura checkpoint** (§5) — not a condition of S19 delivery.
- **(e) External workflow authority:** SOURCE-BINDINGS records that the adopted `salim-autonomous-workflow` rule/skill was not located and the environment aliases are unset. Per AGENTS.md, an unavailable external authority must not be replaced by an invented protocol. Before step 0 the owner either points the executor at the installed authority or records an explicit supersession for this bounded completion.

No other decision is outstanding; the two pending PLAN v3.1 corrections are unaffected and remain with Codex.

---

## 7. Precision items on the addendum (CARRY; none blocks approval)

1. State that the catalogue is **not** touched by D-S19-1 (existing keys `evidence.title` and `dossier.public_contradictions` suffice), so 1.7.0 bytes and the successor-pin literals are preserved.
2. Name the R1 predecessor explicitly: delivered-base `ce407db` canonical, not the superseded `ccd1` capture.
3. Expect snapshot-manifest count 728 → 730 after the replacement generation (retained `ccd1` root + new root); verify, never force.
4. The two untested pin literals are superseded by the replacement capture; say so to prevent testing/freezing them as final.
5. Page 1 of all 44 reports changes text under D-S19-1 (shared template); the first-page manual review must therefore cover all 44 again, not only steel.
6. Record the Aura database/display-name correction additively next to the stop handoff, as the addendum already proposes.
7. `notes/dossier-output.md` records a historical transcription typo (PP page "93.3" vs actual "92.3"); it is correctly not treated as a defect — keep it out of the control docs.

---

## 8. Sources (Sanad)

Packet (hashes in header): `S19-COMPLETION-ADDENDUM.md` §§1–5; `README.md`; `SOURCE-BINDINGS.json` (30 entries); `PACKET-CHECKS.json`; `notes/state-evidence.md`, `notes/state-evidence.json` (`/git`, `/indexes`, `/source`, `/closure`, `/visual`, `/durable`), `notes/dossier-output.md`, `notes/authority-aura.md`.
Authority: DOCX sha256 `5717cbd4…ac9ce` paragraphs P901–902, P1082–1118 (my one-based `<w:p>` count); AGENTS.md; Manifest §7–§10 (read for PLAN v3 review, unchanged copy).
Accepted S19 packets (`/home/barami/projects/ior-worktrees/s19/.workflow/slices/S19-bilingual-dossier/accepted-packets/`): `00-…IMPLEMENTATION-PLAN.md:74-80,:104-141`; `01-…EXPERIENCE-DESIGN.md:13-19,:33-37`; `23-delivered-base-EXECUTION-HANDOFF.md:40-70`; `05-…SUCCESSOR-PIN-AMENDMENT.md:25,:42,:44` (read for PLAN v3 review).
Evidence root `.autonomous-workflow/overnight-2026-09-23/`: `S19-OWNER-STOP-HANDOFF-20260925.md` (full); `s19/ops-final-r1/PIN-RECEIPT.json`; `s19/final-pdf-review-r4/rendered/steel-public-en-page-01.{png,txt}`, `steel-simulated-en-page-01.png`, `steel-public-ar-page-01.png`, `steel-simulated-en-page-39.png` (viewed); `s19/nonrule-caption-correction-r1/actual-api-outputs/SAU-H0-721049-{public,simulated}-dossier.json`; directory listings/timestamps of the root and `s19/final-pdf-review-r4/`.
Owner records `.autonomous-workflow/owner-decisions/`: `20260925-s19-canonical-result-candidate-freeze-release-1.json` (09:09:08), `20260925-s19-active-index-metadata-disposition-1.json`, `20260925-s19-final44-canonical-operation-release-1.json`, `20260925-s19-single-generation-execution-acceptance-1.json`, `20260924-s18a-aura-result-acceptance-1.json`.
S19 worktree (read-only): `src/ior_mvp/dossier.py:73-111,:640-700` (sha256 `0e74a4f0…7acaa` per SOURCE-BINDINGS and visual manifest `:1844`); `config/ui_strings.v1.yaml:662,:744-756,:1768,:1850-1862`; `browser_tests/baselines/v0.3.0/manifest.json:1844-1846,:1862,:1869`. Graph input closure from the byte-identical isolated copy `graph/projection.py:284-308` (sha256 `2b26425a…50c86` in both).
Earlier today (my PLAN v3 review): `git ls-remote origin`, `gh pr list`, S19 worktree `--no-optional-locks` reads.

## 9. Self-audit (al-Muhasibi; Muhasabah gate)

**Khawatir.** First instinct: "the contradiction is in the appendix and the decision is right, so the summary concern is cosmetic — approve without correction." Second instinct: "1,958 pages is absurd — reject the output." Both lazy: the first ignores that the accepted design names the distinction as summary content; the second infers a defect from an aggregate the plan never capped.

**Muraqaba.** Risks watched: (i) over-reading "top supporting and contradictory evidence" into a demand for new prose or ranking — I limited the correction to labelling with existing keys; (ii) letting the addendum's own recommendation decide my ruling — I re-derived it from the DOCX, the accepted design, the source and the rendered pages; (iii) treating Codex's retained approvals as if they were mine — they are attributed and none is re-issued here; (iv) authorising anything by implication — every action is placed behind an owner decision.

**Mujahada.** Verified directly: packet hashes; stop-handoff timestamp and supersession; the DOCX §15 text; the accepted design/plan clauses; the rendered page text and four page images; the retained dossier JSON roles; the summary template in source; the catalogue keys; the graph-input group and visual-manifest pin that make regeneration unavoidable; the Aura binding record; the pin receipt. Attributed and not re-run: test passes, PDF strict/manual reviews, canonical capture, index-hash sweep.

**Muhasaba (gate).** Provenance — every claim carries a path:line, hash, record or viewed image: PASS. Assumptions — (a) the retained r4 cohort is the intended last accepted cohort; (b) the shared summary template means all 44 first pages change under the fix (from the source, not re-rendered); (c) DOCX paragraph numbering is my count; stated: PASS. Fabrication — none; no gate result, ID, count or successor was invented; the 730 expectation is derived and marked "verify, never force": PASS. Requirements — verdict bound to hashes; D-S19-1 adjudicated against actual output with required vs optional separated; page count not used as a defect; replacement authorization identified precisely; gate sequence confirmed; Aura scoped separately; owner decisions listed; review saved and hashed: PASS. Reversibility/risk — read-only; nothing to revert; the only consequence of my ruling is one additional bounded operation cycle, which I judge cheaper than publishing a summary that hides its own binding contradiction: PASS.

**Final.** APPROVE the addendum as the completion plan; D-S19-1 is a required, bounded correction; one replacement generation/capture allocation is needed; release only by owner decisions (a)–(e). What would change the verdict: a rendered summary showing the two roles (then no correction and no regeneration), or evidence that the two-group line cannot fit one physical page without font reduction (then a reviewed design amendment before the fix).
