# S19 Bilingual Decision Dossier and PDF implementation plan

Status: PROPOSED v2; resolves separate R1 plan findings P1/P2 with preserved v1 bytes in revisions/. Goal: project every methodology§15 block and supporting evidence into a professional bilingual Decision Dossier for all11 cases and both evidence modes, with faithful JSON, printable HTML and Chromium-generated PDFs. Read with S19-EXPERIENCE-DESIGN.md. Independent plan review and coordinator acceptance are prerequisites; generator allocations need their own exact-input binding. No writer may approve its own work.

## Authority, base and boundaries

Authority read: AGENTS/Manifest; direct DOCX§15/15.1/15.2 plus prior domain sections; Core04§9/§10, Core03§4.8, Core02 methodology map and Core09 browser/isolation gates; configs; SLICE_GRAPH S19, UX_GENUI_DEMO_SPEC, current five controls and KL33/KL84; current dossier/app/tests/browser capture contracts. Personas and applied skills are recorded in the experience document. Source observations are from retained S18a; coordinator binds actual merged S18b base and writer lock before work, using a new isolated `slice/s19-bilingual-dossier` worktree. Preserve owner primary/S18 worktrees, alternate indexes, previous packet bytes and all historical generated roots.

Domain decisions, real/simulated routes, thresholds, snapshots, scenarios and golden expectations remain unchanged. No new source acquisition, external graph query, live browser network source, numerical decision formula, AI prose, authorization act or product runtime dependency. The separate free proof-only renderer overlay is exactly bounded by PDF-RENDERER-ENVIRONMENT.md; it changes neither product dependencies nor the canonical image. S19 is an export projection; availability labels do not cure missing evidence. Existing dossier API routes remain; no server Chromium PDF endpoint or new product report system. Browser Save PDF is the explicit supported export workflow.

### Approved-change requests made by this plan

1. Core04§9 documents DecisionDossier2.0.0: retain old top-level keys/meaning, add explicit public decision and structured methodology blocks/evidence pack, and adopt one version for all cases. Do not repurpose historical values. Existing selection metadata remains only for the four selected S15 cases. Core02 mapping and Core03 dossier responsibilities update; Core09 adds exact projection/print acceptance.
2. UI catalogue successor version after accepted S18b (expected1.6.0→1.7.0), new dossier-only EN/AR labels and print-token names. No threshold/evidence-policy/profile changes. Authority changes are justified in ADR/PR before any manifest build.
3. One new graph projection follows finalized Core/catalogue/top-level Python inputs; one manifest build; one canonical visual refresh under a separately bound S19 change reference. Expected graph925/1045 semantics unchanged. Expected baseline128 paths unchanged, with sixteen dossier screens deliberately redesigned and unrelated screens limited to version/run-ID text.

### Exact permitted file set

Production:
- `src/ior_mvp/dossier.py` (legacy façade/projection orchestration).
- New `src/ior_mvp/dossier_projection.py`, `dossier_rendering.py`, `dossier_sections.py`, `dossier_validation.py` (small focused helpers; top-level so existing graph/visual discovery covers them).
- `src/ior_mvp/app.py` only shared dossier-export wrapper/typed error handling; existing analysis/executive/graph endpoint behavior untouched.
- `src/ior_mvp/static/css/dossier.css`, `src/ior_mvp/static/css/tokens.css` only new scoped print/dossier tokens.
- New `src/ior_mvp/static/modules/dossier-actions.js` for native print control; no inline handler/model HTML.
- `config/ui_strings.v1.yaml` only version and dossier strings. Existing values unchanged except a specifically incorrect dossier label justified in the change log.

Tests:
- `tests/test_dossier_contract.py`, new `tests/test_dossier_projection.py`, `tests/test_dossier_isolation.py`, `tests/test_dossier_availability.py`.
- `tests/test_api.py` only dossier status/route/malformed-context assertions.
- `tests/test_s15b_selection_view.py` only live dossier_version assertions1.2/1.3→2.0.0 while preserving selection meaning/exact bytes elsewhere.
- `tests/test_ui_catalogue.py`, `tests/test_ui_tokens.py`, `tests/test_static_frontend.py` only new export behavior/catalogue successor/print tokens.
- `browser_tests/test_dossier.py`, new `browser_tests/dossier_pages.py`, `browser_tests/test_dossier_accessibility.py`, `browser_tests/test_dossier_failure_states.py`. These tests may call the existing accepted Executive→Analyst control; no executive product module is added to scope.
- `browser_tests/test_visual_baselines.py`, `browser_tests/visual_baselines.py` only dossier capture-helper integration/source provenance; all path identities and capture dimensions retained.
- `tests/test_visual_baseline_contract.py`, `tests/test_frozen_public_evidence_pins.py` only authorized current visual provenance/subtree pin; frozen data/golden pins unchanged.
- `tests/test_s17_status_docs.py` only current successor manifest count/change_ref assertions; S17 historical assertions unchanged.
- `tests/test_performance.py` only extend dossier coverage without relaxing existing budgets.

Documents/records:
- `docs/core/02_METHODOLOGY_IMPLEMENTATION_MAP.md`, `03_SYSTEM_ARCHITECTURE.md`, `04_CANONICAL_DATA_MODEL.md`, `09_TEST_ACCEPTANCE_AND_GOLDEN_CASES.md` only S19 mapping/projection/print contracts and live successor counts.
- Five control documents, `README.md`, `docs/OPERATOR_RUNBOOK.md`, `docs/DEPLOYMENT_GUIDE.md`, `docs/implementation/UX_GENUI_DEMO_SPEC.md` only dossier workflow/limitations/evidence.
- `.workflow/state.json`, new `.workflow/slices/S19-bilingual-dossier/**` (plans/reviews/evidence/delivery); existing slice records not rewritten.

Generated operation paths:
- `data/graph/current.json`, exactly one new `data/graph/projections/<bound-id>/{projection.json,manifest.json}` pair using actual existing directory layout; verify exact builder-emitted paths before authorization rather than inventing paths.
- `data/manifests/snapshot_manifest.json`, `docs/authority/authority_hashes.json`, current generated table in `docs/authority/00_AUTHORITY_MANIFEST.md`.
- Existing `browser_tests/baselines/v0.3.0/**`128 paths/manifest and authorized current frozen subtree pin.

External proof-only additions: `planning/s19/renderer-proof/**` (verified wheel/site, scripts, samples, logs/JSON/renders); each candidate proof root may write its distinct `pdf-rendered/**` and receipts. Copy only reviewed recipe/scripts/hashes and selected results into `.workflow/slices/S19-bilingual-dossier/delivery-evidence/renderer/`; no wheel/site, venv or cache in Git. Both roots use the same immutable image plus exact verified overlay from PDF-RENDERER-ENVIRONMENT.md.

No other product path. A required additional path, new schema meaning, dependency, generator replay or fixed capture-policy change returns to planning/review first. No `.pdf` binary golden files added. The current prohibited-path guard permits PDF artifacts; retain all44 accepted PDFs plus text/metadata/checksums as a reviewed delivery-evidence subset under the new slice record, with no credentials or unnecessary binary bundle. Existing guard remains unchanged. Raster screenshots/contact sheets use an explicitly reviewed bounded subset; the full local render bundle stays preserved.

## Task1 — establish original defects and input map before fixes

1. Copy exact accepted plan/design into new slice record and record task persona/context. Bind base commit/tree, indexes and frozen roots. Run baseline integrity and focused dossier tests; capture source hashes and valid JSON output for all22 case/mode combinations.
2. Independently enumerate available analytical paths and all11 scenario input-key unions. Preserve typed availability distinctions; do not assume steel-shaped economics. Existing read-only baseline `S19-BASELINE-PROBES.json` is diagnostic only:22 API combinations returned200, all22 Arabic HTML rendered raw supply JSON. It does not replace regression RED.
3. Write new regression tests first: structured producer rows present instead of raw dictionary/code; Arabic rule result/name/effect equals `row.localized.ar` instead of English island; missing§15 HTML sections now required; public conclusion visible and equal real_decision in simulated export; unavailable economic null distinct from0; no hidden internal D*; S15 selection metadata unchanged.
4. Run those against untouched base implementation in an isolated proof copy. Save exact commands/tree/log/assertion failures. If a proposed regression already passes, refine it or record that requirement as preservation rather than claim an original defect.
5. Add contract2.0.0 tests that fail initially for the additive fields. Keep behavioral regression RED distinct from expected new-feature absence. New tests must assert source equality and partition, not reimplement serializer logic.

## Task2 — pure field projection and exact source references

`build_dossier(analysis)` stays the supported caller interface. It calls a narrow helper to obtain scenario context only when `analysis.mode == simulated`; public branch asserts no call. Repository retrieval is read-only. Validate supplied context against analysis opportunity, scenario_id, scenario_version, source DEMO_GENERATOR, current evidenceClassD, synthetic_flag and the existing raw display_label against policy. Raw scenario files do not carry a display_labels map; use existing evidence.synthetic_display_labels() for both policy labels and require analysis.simulation_scenario.display_labels to equal that result. Do not invent a new required raw input. A mismatch is a `DossierIntegrityError`; shared app wrapper returns422 with a stable `DOSSIER_INTEGRITY_ERROR` code for JSON and HTML routes. Do not wrap arbitrary programming errors or change the existing `_safe_analysis` contract.

Project old fields unchanged except dossier_version2.0.0 and no longer inventing missing numeric zeros in legacy demand_conclusion. Frozen valid output differences must be enumerated: new fields/version and authorized replacement of absent-value prose only, never changed decisions/data. All projected structures are detached copies; dossier/rendering must not mutate analysis, scenario repository caches or public decision.

Add `public_decision` (exact detached real_decision), `blocks` and `evidence_pack`. Each block has a stable key, branch identity, status and projected data; individual scalar records carry `availability`, `value`, `unit`, `source_path`, explicitly supplied `evidence_ids` and reason when unavailable. Use `AVAILABLE`, `UNAVAILABLE`, `NOT_CALCULABLE`, `NOT_APPLICABLE` exactly as documented. `AVAILABLE` accepts genuine numeric0/false; null is never0; unknown capability remains unknown. ClassD and both labels attach to every simulated block/component. Source paths are canonical analysis/scenario JSON paths, never claims of a nonexistent passport.

| §15 block | Actual source mapping | Missingness/invariants |
|---|---|---|
| Decision | active_decision + real_decision; state/route/localized narrative/conditions | Null route is unselected; preferred hypothesis stays a hypothesis. Public always visible in simulation. |
| Identity | analysis.opportunity full identity plus matched scenario.target_specification only in simulation | Missing national tariff/grade/application/customer qualification remains unavailable, not inferred from HS or unit value. Public boundaries remain public. |
| Demand | latest trade as public signal; public trade_quality/domestic_flows/supplier_metrics/partner_detail; branch-correct R8.metrics; simulation.demand whitelist | Imports are not target/base demand. Base/committed/announced/downside/probability use actual values/status. Buyers/concentration/timing absent are marked unavailable; no synthetic allocation in public. |
| Supply | domestic_capability.producer_evidence/coarse_adjacency_signals/profile hard gates; branch capacity; simulation.plant_line and upgrade whitelist | Named observed producers, source/period/class; public nameplate separated from simulated effective capacity/utilisation. Never infer observed expansion from simulated upgrade. |
| Gap/controls | active gap_class/hard_exclusions/rejection_conditions/R11/trade_quality | Display false-positive reasons/NOT_CALCULABLE as delivered; no score or greenfield conclusion derived. |
| Capability/routes | capability dimensions/K/U/published d_star/hard gates; active route_hypotheses plus public comparison; decision.counterfactual | Omit internal_d_star_before_gate from displayed metrics; preserve9-route order/status/precedence/economics, route8 foreign references explicitly external. |
| Economics | active economics fields/national_value; active route economics; simulation cash-flow inputs | Show unsupported NPV/IRR, minimum support, national value, downside result where engine exposes it. Missing separate downside result is NOT_CALCULABLE; never compute NPV in exporter. PP minimum support0 remains0 while unsupported NPV absent staysnull. |
| Competition/policy | competition plus active route additionality/policy/competition; instrument, conditions/kill conditions | Preserve capacity/downside ratio/displacement/non-additionality/HHI nuance. No sunset date or policy authorization invented; absent sunset marked unavailable. |
| Evidence | actual public and synthetic passports, supports, contradiction, reviewer_status, missing-fact structures | Deterministic stable ordering; support and contradiction separate, no ordinal confidence score. Passport fields remain exact. |
| Conditions/kill/next facts | active structured narrative + full public conditions; optional EVSI only simulated | All conditions retained in appendix; selected summary references full list. Missing fact is not an approval. EVSI absent remains unavailable and its prose is original-language attributed content. |

Scenario whitelist (no raw dump): target_specification `name,standard,alloy,temper,application,coating_mass_g_m2,tin_coating_g_m2,thickness_mm,thickness_um,width_mm,customer_qualification_required`; plant_line `nameplate_kt,availability,yield,qualification_share,market_allocation_share,current_utilisation`; demand `base_demand_kt,target_spec_demand_kt,downside_demand_kt,committed_demand_kt,announced_demand_kt,commitment_probability`; upgrade `description,incremental_capacity_kt,schedule_months`; economics `currency,cash_flows_without_support,hurdle_rate,minimum_efficient_scale_kt,support_instrument,support_required,reason,national_value`; selected route_evidence `route_code,downside_cash_flows_m_sar,hurdle_rate,basis`; buyer_allocation `basis` and ordered `buyers[].buyer_id,segment,quantity_kt` only, as validated by scenario_contract._validate_buyer_allocation. A buyer_id is a synthetic identifier, not an observed customer name. Unrecognized scenario fields fail projection review rather than auto-display. No class_if_confirmed promoted to evidence class.

Require exact local supporting passport resolution for scenario blocks; resolve `<scenario_id>::<block>` only if that exact ID exists in analysis.evidence, not by fabricated suffix. Route8's already evaluated cross-scenario IDs are separately marked external dependencies and never inserted into local passports or counted as local public/synthetic evidence. Any new cross-case traversal is outside S19. Evidence pack includes exact source URL/title/period/retrieval/transformation/reviewer-status/contradiction and document/page/line only when source provides it. Allow safe http/https source links with escaped attributes; unsafe schemes are plain text. Printed references remain resolvable without live graph service.

Supporting§15.1 pack sections reference available revision/concordance, trade cleaning/quantity coverage/re-export treatment, supplier diagnostics, specification spans, capability matrix, demand inputs, cash flows, competition/value, contradictions and decision history. If no source block/override/reviewer exists, record UNAVAILABLE or NONE_RECORDED according actual semantics. Do not label missing history a completed expert review.

## Task3 — bilingual HTML and summary/appendix layout

1. Implement reusable escaping, localized narrative segments, technical-token isolation, field/status, table and passport helpers; renderer is pure `render_dossier_html(dossier, locale)` and never fetches evidence. Use approved fields only.
2. Build first-page summary and full appendix per design. Preserve each§15 block even when unavailable. Use compact rows, no raw JSON as a user-facing supply/economics substitute. Summary lists lead condition/kill plus real more-item count and appendix anchor; source text is never silently truncated.
3. Use existing localized rule rows for public and synthetic ledger name/result/effect. Show execution/fired/status via catalogue, both policy labels on each simulated ledger/group, and actual field metrics with units. Unknown governed label or absent localization fails parity tests; no English-prose escape disguised as a technical token.
4. Add screen-only export toolbar, native print button via external module, downloadable JSON anchor and return link generated from validated case/mode/locale. The link performs no graph load; unknown input never becomes arbitrary URL/selector. No product prompt injection or model-generated HTML.
5. Scoped CSS/tokens support desktop1440/tablet1024/mobile390 and RTL. A4 summary page, appendix page break, print table headers, natural row wrapping, accessible contrast/minimum type sizes, no clipped IDs/tables. Repeated synthetic EN+AR labels on every printed page must not overlap content. If the browser cannot meet that safely, stop for reviewed print architecture adjustment; don't silently remove per-page disclosure.
6. Empty producers/passports/conditions show truthfully labelled empty or unavailable states. HTML export errors remain typed JSON422/404 as appropriate; caller workflow retains supported unavailable feedback. No fabricated empty successful report after request failure.

## Task4 — regression, isolation and rendered/PDF proof

Unit/API matrix: all11×2 modes, both HTML locales. JSON remains locale-neutral; old field source equality and new block equality. Mutate one scenario identity/version/marker at a time to demand422; assert no public scenario loader call. Deep-freeze/fingerprint analysis/scenario/public before and after projection/render. Inject missing trade/demand/capacity/economics/public passports, nonnumeric/bool/nonfinite values, absent optional EVSI, true0, empty producer list and malicious HTML/URL. Reject malformed required structures with typed errors; genuine optional missing data stays typed unavailable. No broad coercion or blank success.

Preserve existing tests: synthetic contradictions are separate and actual public steel contradiction remains visible; both policy labels absent from public; selection only applicable cases; NO_CANDIDATE remains valid; exact localised decisions/conditions; legacy evidence counts align arrays. Version assertions1.2/1.3 alone change to2.0.0 because Core04 explicitly approves it; no count/equality/isolation assertion removed. Add a source-to-block mapping oracle with manually enumerated paths, not assertions generated from production mapping.

Browser matrix: existing all11×2×2 popup/JSON/print journeys remain, plus keyboard toolbar/download/return, locale/mode retention along the existing Analyst→Dossier journey and the explicit Executive→Analyst→Dossier journey, responsive390/1024/1440, no console/network failures, no horizontal overflow and axe serious/critical violations. Add `test_executive_to_analyst_dossier_retains_context`, parameterized across11cases×2modes×2locales: open Executive with case/locale, click its existing Analyst link, assert case/locale retained, explicitly choose the requested analyst evidence mode through the existing toggle, open the existing dossier action, assert popup identity/mode/locale/public-state and use the dossier return link to recover the same analyst context. Do not assume Executive has a global selected mode or direct export button, and do not change executive sources/pixels to satisfy this test. Inspect actual Arabic rule ledger prose and technical islands with the current parity grammar/label-leak rules. Use unavailable/empty/contradiction/NOT_CALCULABLE fixtures without claiming they are governed case facts.

PDF gate: wait `document.fonts.ready`, emulate print, generate44 PDFs with pinned Chromium, A4 and print backgrounds. Use existing pypdf6.16.1 (already a dev dependency) for page/text/font metadata; no dependency changes. Require valid structure, nonempty selectable text on every page, every section heading and expected full technical ID somewhere in text, exact canonical numbers/units, public real-decision equality and absence of synthetic labels/IDs/scenario-only projected fields (do not reject a coincidentally equal public numeric value), both policy labels on every simulated page, no replacement boxes, summary exactlypage1 and appendix beginningpage2. Check character/element boxes against page content region with a documented diagnostic; text extract order can differ in Arabic, so use both token assertions and visual review rather than reverse text blindly. Retain PDFs/text/page metadata and raster renders outside canonical baselines.

Use the established pypdfium2==5.13.0/PDFium153.0.7999.0 proof-only overlay and exact immutable image in PDF-RENDERER-ENVIRONMENT.md. Its SHA-bound wheel and binary, actual4PDF/14page sample, repeated identical renders and recorded existing bounds failures establish feasibility before implementation. Reuse identical tool bytes in both clean candidate roots, with network disabled, PDFs/tool mounted read-only and per-root outputs only writable. No product dependency or canonical-image change. Render all44PDFs/every page at144dpi, no crop. The acceptance wrapper must fail on missing documents/pages or any tight glyph box beyond page/content/footer boundaries; source and print-region checks complement raster inspection, never substitute for it. Preserve a deliberate off-page fixture and the two real old-dossier outliers as negative controls; the sample renderer's diagnostic exit0 is not acceptance.

Inspect all44 first pages and all pages of longest EN/AR reports, steel contradiction, PP zero-support and route8-dependent aluminium cases; create contact sheets of remaining pages and inspect any clipping/wrapping anomaly at full resolution. Record exact filenames and findings; screenshots are evidence, not proof of unviewed screens. Print page count must remain reasonable with no empty/orphan-only page; bind measured maximum after first reviewed prototype rather than inventing an untested contractual page cap. Font size/content completeness cannot be traded away to meet a count.

Meaningful RED→GREEN proof includes original raw supply, Arabic English-result defect, omitted analytical sections, hidden public simulation context and absent numeric-vs-unavailable distinctions. Test artifact filenames/tree IDs/commands retained; full suite green alone does not prove original defect detection.

## Task5 — docs, limitations and generation closure

Before binding any graph identity, finish Core04 contract2.0.0 (including whitelist/status/external-dependency semantics), Core02/03/09 mappings/gates, catalogue successor, all top-level Python/static/capture helpers, UI/print tests and intended source docs. Record explicit ADR with exact approved file list, public outcome invariants, no canonical evidence changes and source/visual generation rationale. KL33/KL84 close only after structured supply and Arabic ledger proof; leave unsupported source details, no-domain-review evidence, AM4 API residuals and route8 demonstration limitations explicitly open. Do not claim PDF-UA certification, real Ministry evidence, or complete missing demand/supply data.

Generation operation, one allocation each:
1. Capture old graph/current, snapshots/authority inventory, baseline tree and all frozen hashes. Finish sources and run focused/full unit/browser functional tests; classify only expected stale graph/manifest/visual pins as gated, never hide other failures.
2. Call read-only graph projection builder to report input manifests/new ID, counts and full semantic comparison against accepted base. Separate reviewer/coordinator bind exact identity and authorize one write. Expected topology925nodes/1045edges unchanged modulo deterministic run/projection identities; mismatch stops.
3. Build exactly one new projection with actual governed CLI. Old roots preserved; current pointer equals bound ID. Build manifests once and mirror Manifest table. Expected snapshot count baseline+2 (if S18b726, then728), authority file set20 unchanged but Core02/03/04/09 andui_strings hashes differ; verify, never force counts. Run integrity/reconstruction/graph validate/build--check immediately.
4. Bind new S19 canonical visual change_ref after functional/rendered proof. Existing128 paths/count/dimensions/tolerances/budgets unchanged. New helper sources and every capture import must appear in visual source_tree before binding; no generated self-hash cycle. Single canonical update in existing pinned image. Review all before/after hashes and pixels:16 dossier paths deliberately redesign, other paths only version/run-ID text. No masks/tolerance increase/font switch/content erasure.
5. Preserve the inherited R1 same-layout identifier-only diagnostic when graph-dependent screens change: both locales12relationship/5run-ID rows, ordered normalized tuples, exactly5 one-token replacements, no unrelated text change, repeat/roundtrip equality,0.01 section/button/line/clipping invariance except inherited locale-specific span/small widths/shared edges. Violation requires reviewed operation amendment, not another silent allocation.
6. Compute final baseline subtree using a new temporary alternate index and update only approved current pin. Preserve existing indexes. Update only live successor assertions in test_s17_status_docs.py; never edit historicalS17 facts. No graph/visual-hashed source changes after binding. Needed changes reopen operation review and invalidate the prior exact candidate.

## Task6 — exact candidate review, gates and delivery

Freeze candidate including exact plan, additive reviewed amendments, implementation log and truthful control docs. Pause writer. Separate implementation reviewer receives isolated exact tree, source diff, old/new exports, RED→GREEN logs, all44PDF proof, screenshots, source/graph/visual identities and secret/prohibited scans. Review tests as product code; no same-agent approval. Any substantive fix creates a new candidate and affected/full required gates; after two unsuccessful corrections of the same finding use a fresh specialist or different approach.

Run against exact clean candidate roots using project `uv --locked` and approved graph fixtures:
- Full `pytest -q`, integrity and every reconstruct/validate/scenario/smoke/static/prohibited/threshold/package check already in `make ci`.
- Full `make e2e` compare-only; no canonical update during verification. Retain dossier44PDF evidence and AR/EN complete journeys.
- Same-layout graph-ID proof and source/visual receipt checks above.
- `make ci` in two clean proof roots with separate project-owned compose projects/fresh graph containers; record tree before/after equality and all actual counts. Local Neo4j evidence is not Aura evidence.
- Ensure every passed gate shares final tree identity; no green result borrowed from S18a/S18b. Inherited gate timings are planning estimates, not a reason to omit work.

After exact candidate APPROVE and delegated coordinator acceptance, stage only approved paths; staged tree==reviewed candidate; commit tree==candidate; push task branch/open PR. PR contains schema compatibility, resolved KL33/KL84, full receipt SHA256, exact tree/head, generation and PDF/rendered proofs, limitations and same-model review attribution. Inspect required hosted checks/logs at exact PR head; separate reviewer approves that final head after checks pass. Only coordinator may merge after protections pass. Verify actual merge and targetCI before additive delivery record claims MERGED. No release tag/final cross-model certification.

Durable evidence: publish all44 PDFs with metadata/text/checksums, reviewed representative rendered PNGs/contact sheets, source-map/regression proof, generator/probe receipts and plan/review hashes through allowed slice delivery-evidence. These reproducible pre-freeze render artifacts must not embed the final candidate tree ID. Exact-tree gate logs/results and final delivery facts remain outside the candidate during verification, then enter a separately reviewed additive post-merge record change; never create a self-referential receipt tree or rewrite original review bytes. Scan artifacts for credentials/prohibited content, including extracted PDF text. Preserve the full local raster bundle with checksum inventory; a README states the durable subset and full-bundle location without claiming all rasters are published. Tomorrow's essential proof is reachable from Git at exact commit references, not only expiring CI artifacts or local paths.

## Stop conditions and self-audit

Stop to coordinator/planning/review on: unclear canonical mapping; required data absent but hidden; public synthetic leakage; changed real/simulated decision; invented source/claim; Arabic parity failure disguised as source quote; impossible readable one-page summary; missing font/rendered PDF proof; unexpected test failure; authority/frozen path expansion; topology/count drift; extra generator allocation; unsafe artifact publication; stale exact head. Genuine source gaps remain typed and documented, not silently closed.

Sanad: source and API probes directly observed; accepted S18a outcomes and future S18b contracts are inherited and must be rebound to actual base. Planner ran22 read-only API combinations and, during P1 correction, generated4 actual old S18a ChromiumPDFs/rendered14pages to establish the isolated renderer; no S19 implementation tests,44PDF acceptance or Aura gate ran. No implementation or gate success asserted. Assumptions and export architecture are explicit. Risks/requirements above have falsifiable checks; PDF text alone cannot prove Arabic layout, UI alone cannot prove branch isolation. Muhasabah PASS for proposed plan, with implementation/unverified source details and independent review pending.
