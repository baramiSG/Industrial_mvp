# S19 TRADE-SCALE-01 presentation amendment

> **For agentic workers:** Execute only after separate plan review, coordinator delegated acceptance and the actual delivered S18b binding. The separately assigned implementer uses the accepted S19 plan with executing-plans/subagent-driven-development; this authored packet grants no current implementation operation.

**Goal:** Disclose the existing independent chart scales and expose exact public year/value/unit observations without hover, in both Analyst and Executive Signal.

**Architecture / stack:** Extend the existing vanilla-JavaScript `renderTradeChart(props)` once. Keep its curves, native observations and callers; add a visible bilingual explanation and a native `<details><summary>` containing a semantic three-column table. Reuse catalogue, formatter, CSS tokens, pytest/Node and Playwright. No dependency, framework, new chart type or numerical index.

**Spec and status:** R2 DRAFT FOR INDEPENDENT REVIEW; corrects only TC-P1 from the [independent R1 rejection](../../reviews/s19-trade-chart-r1/REVIEW.md). The sealed [R1 packet](../s19-trade-chart-r1/AMENDMENT.md) remains historical REJECTED; no former result is relabelled. The [adjudication](../../reviews/s18b-final-candidate-r2/TRADE-CHART-ADJUDICATION.md) and additive owner-control record `20260924-s18b-trade-scale-s19-disposition-1.json` require **TRADE-SCALE-01 — OPEN_REQUIRED_S19_PRODUCT_REPAIR**. Full hashes of them, the [accepted S19 plan/design](../s19/S19-IMPLEMENTATION-PLAN.md), [AR-V01 handoff](../s19-ar-v01-continuation/IMPLEMENTER-CONTINUATION.md), [base-adoption amendment/review](../s19-base-adoption-r1/AMENDMENT.md) and successor-pin amendment are in [BINDING.json](BINDING.json). Preparation and applied skills: [persona.md](persona.md).

## Dependency and fixed boundaries

The observed S18b tree `789577cbd16cec9cd6c260b5e98a5eac0ae83c20` is frozen, not a delivered S19 base. Before accepting this amendment for execution, bind actual S18b merge commit/tree, final independent approval, delegated acceptance, passing main CI and sole S19 writer in the existing control record. Recheck selected preimages; material differences return to review. Work only in the assigned isolated S19 worktree. Preserve candidate, historical failures, approved packets and indexes.

This closes an inherited presentation limitation; it changes no values, graph facts, branch selection, rule, route, scenario, formula, frozen public input or golden expectation. All existing 16 trade catalogue leaves and SVG value/quantity curves remain unchanged. The imports-are-a-signal/gross-not-retained-demand warnings remain. No claim of economic convergence, common scale, new metric, official synthetic evidence or quantified user-study result.

## Exact additional permissions

Parent S19 permissions remain. Expand their previously dossier-only restrictions **only** as follows:

| Path | Permitted change |
|---|---|
| `src/ior_mvp/static/modules/renderers/trade.js` | Chart-local semantic grouping, scale note and native details/table; no curve normalization, point sorting, input mutation or caller contract changes. |
| `src/ior_mvp/static/css/workspace.css` | Only new `.trade-chart`-scoped note/details/table/technical-value responsive and focus styles using existing tokens. No existing global/chart geometry rule changes. |
| `config/ui_strings.v1.yaml` | Add only the six paired `trade.*` keys below, within the already planned S19 catalogue successor. Retain existing values. |
| `tests/test_static_frontend.py` | Add actual Node renderer behavioral tests for scale note, data semantics and nonmutation; keep all existing assertions. |
| New `browser_tests/test_trade_chart.py` | Focused shared-renderer/consumer data, disclosure, native keyboard, parity and responsive regressions using the existing harness/callers. No new reusable test framework or capture import. |
| `tests/test_browser_harness_contract.py` | In `test_browser_suite_has_the_approved_top_level_shape`, append only the new literal `"test_trade_chart.py"` in addition to the three dossier-file literals already admitted by the successor-pin amendment. In the named-test inventory, append only the actual trade-test names separately reviewed/accepted by the existing admission process below, and change only the inventory function name's numeric fragment to the independently established complete S19 total. Preserve all inherited members, exact-set equality, discovery/checker logic and all other assertions. |
| `browser_tests/test_visual_baselines.py` | Within `_executive_scene_payload`'s SIGNAL branch only, append actual scale-note and closed data-summary text/visibility assertions to the existing required boxes. Keep all old chart/header/legend checks, anchors, dimensions and other scenes. This is additional to the parent's separate dossier capture permission. |
| `docs/core/03_SYSTEM_ARCHITECTURE.md`, `docs/core/09_TEST_ACCEPTANCE_AND_GOLDEN_CASES.md`, `docs/implementation/UX_GENUI_DEMO_SPEC.md` | Concise additive shared-chart interpretation/keyboard-data contract and corresponding tests/visual boundary; no other Core semantics. |
| Existing five controls and new S19 slice records | Track TRADE-SCALE-01, this approved delta, actual evidence and final disposition alongside AR-V01 and AM-4 residuals; retain history. |

No executive product module, graph/capture helper, `pages.py`, accessibility/parity checker, Makefile, CI or generator edit is authorized. If required, return a concrete amendment before writing that path.

## Task 1 — original-defect proof and shared repair

**Consumes:** Actual bound predecessor, unchanged trade rows, current locale/catalogue. **Produces:** One shared rendering repair and preserved RED→GREEN evidence.

- [ ] Add tests first and run the new disclosure/native-data assertions against the unmodified bound predecessor in an isolated copy. Both EN and AR Analyst/Executive Signal must fail specifically for absent scale explanation/data control, not setup, missing locale key or service failure. Preserve each command, source identity and assertion. Existing correct value/trend behavior is preservation coverage, not a falsely claimed original failure.
- [ ] Add exactly these catalogue pairs (Arabic wording preserves the same meaning):

| Key | English | Arabic |
|---|---|---|
| `trade.scale_note` | Each line uses its own scale. Compare trends within a line; heights of the value and quantity lines are not comparable. | لكل خط مقياس مستقل. قارن الاتجاه داخل كل خط؛ لا يمكن مقارنة ارتفاع خط القيمة بارتفاع خط الكمية. |
| `trade.data_summary` | View observed values | عرض القيم المرصودة |
| `trade.data_caption` | Frozen public trade observations | مشاهدات التجارة العامة المجمدة |
| `trade.year` | Year | السنة |
| `trade.data_unavailable` | Unavailable | غير متاح |
| `trade.data_empty` | No trade observations available | لا تتوفر مشاهدات تجارية |

- [ ] Add the visible note adjacent to the chart with semantic grouping, never tooltip-only. Add a closed native details control below it, using the summary above; Enter/Space toggles it without JavaScript. Its captioned table uses `scope="col"` headers Year / existing `trade.value` / existing `trade.quantity`, and year row headers. Preserve ascending stored years and all rows; use the actual `imports_usd_m` and `imports_kt`, existing numeric formatting and explicit native units. No interpolation for absent years or precision invented beyond source/established display formatting. Keep technical numbers directionally isolated in Arabic. `knownNumber` controls each cell: finite zero displays zero, null/missing/string/bool/nonfinite displays the explicit localized unavailable label, never zero or NaN. Empty rows produce the localized empty message with no invented point. Escape all rendered content; keep points' existing titles as supplementary access.
- [ ] Keep the existing chart path/scale math, point count, gaps and native units unchanged. Do not mutate `props.trade`. The same function covers analyst Public/Simulation (trade remains public) and executive public Signal; add no fetch or simulation-derived chart value.

## Task 2 — meaningful behavioral and visual checks

- [ ] Run `uv run --locked pytest -q tests/test_static_frontend.py tests/test_ui_catalogue.py`. Add Node-renderer cases with three unsorted observed years, zero, null, missing key, string, bool, NaN/Infinity and empty trade; independently assert sorted year/cell text/units, explicit unavailable/zero distinction, no input mutation and unchanged curve paths. Use the real function and governed locale bundles, not static source-presence assertions or expectations generated by the renderer.
- [ ] Run `uv run --locked pytest -q -m e2e browser_tests/test_trade_chart.py` through the existing browser environment. Cover all11 actual cases × EN/AR for Analyst public, Analyst simulated and Executive Signal; compare the entire table's year/value/quantity rows with each actual public analysis response, preserve complete `real_decision` before/after, correct units and both branch warnings. The steel counterexample explicitly shows 2024 value236.9 million USD versus287.9kt alongside the non-comparable-height explanation; equal plotted maxima cannot be interpreted as equal native values.
- [ ] For steel and polypropylene, inspect EN/AR at390/1024/1440 in both analyst modes and executive Signal, closed then keyboard-open table: Tab reaches native summary with visible focus; Enter and Space open/close, focus remains usable; full caption/header/value text stays readable, source units and Arabic parity hold, no horizontal page overflow or clipped/hidden data. Use existing axe/console/network checks without changing their thresholds or grammar. Retain actual representative screens and precise observations; no claim of assistive-technology certification from DOM checks alone.
- [ ] **TC-P1 inventory admission, using the accepted successor-pin amendment Task3 without a new mechanism:** bind the actual predecessor's `tests/test_browser_harness_contract.py` preimage and complete literal file/function sets. Current observed sets are23 files/76 unique top-level synchronous test names; the new chart file is absent. The parent dossier additions plus exactly this file imply27 files only if that23-file base is actually delivered; verify complete membership, never force the count. Retain every predecessor member and substantive assertion.
- [ ] Once the real chart tests exist, include their exact function name/path/body SHA, requirement mapping, collected parameter IDs, behavioral RED/GREEN or preservation proof, duplicate-name audit, complete old/new sets and proposed literal hunks in the parent's existing `S19-BROWSER-INVENTORY-ADDITION.json`. A separate reviewer inspects the actual tests and finite additions; coordinator records delegated acceptance **before** inventory literals change. This permission admits no other unexpected file or function. Only approved names whose actual bodies are in `browser_tests/test_trade_chart.py` are additional to the parent's independently admitted dossier names. No speculative future name/count, stub, fake collection, deleted/renamed old test or self-approval.
- [ ] Append the one chart-file literal and exactly those accepted real test-name literals; update only the numeric fragment of `test_browser_inventory_has_exactly_76_named_tests` to the independently audited complete S19 named-function total, distinct from parametrized node counts. Preserve `assert actual == expected`, the actual-discovery loops and all inherited literal members. Never derive expected from actual at runtime, import the admission JSON as an oracle, wildcard entries, loosen equality or remove the numeric binding.
- [ ] Preserve inventory RED with the genuine new file/functions against the old literal sets, then run `uv run --locked pytest -q tests/test_browser_harness_contract.py` for GREEN after the reviewed literal updates. Extend the existing member-preservation negative controls to the chart addition: independently prove old-set subset and exact admitted delta; disposable-copy removal of an inherited expected member/file and introduction of an unapproved file/test name must fail the relevant equality. Keep real failures and corrected passes separate; inventory sensitivity never substitutes for chart behavior proof. This unit inventory file is neither a graph nor visual input, so its finite update adds no generator allocation.
- [ ] Re-run the original-defect controls on corrected code and retain GREEN. Run the full unmodified functional command and parent S19 export/PDF/AR-V01 gates. A test that only finds new markup does not satisfy source-value or keyboard proof.

## Task 3 — integrate the single S19 generation and delivery sequence

Static renderer/CSS are visual inputs, not graph inputs; catalogue/Core03/Core09 are graph authority inputs and visual source context. `test_visual_baselines.py` is a named visual input. The new behavioral test is not imported by canonical capture. Direct discovery-code evidence is bound in BINDING.json. Finish all these sources, tests and authorized Core/UX/ADR changes **before** S19's existing read-only double build and operation bindings. Declare the Manifest§7 implementation-preserving catalogue/Core amendment in the ADR/PR before manifest generation; no domain/config threshold change.

- [ ] Use the accepted stock sequence once: reviewed read-only graph double construction/full normalized semantic comparison including edge multiplicity; full all11 `real_decision` equality; separately bound graph write; single manifest build; integrity/reconstruction/build-check; complete functional/PDF/visual readiness; single bound canonical capture; actual subtree pin; strict R1; final candidate gates. Authorize only actual metadata/own-derived-ID changes already bounded by the parent, with the added exact source hashes; never normalize real_decision differences. No extra generator allocation or custom prediction framework.
- [ ] **Expressly expand** the parent plan/design's “16 dossier redesigns; all non-dossier changes only versions/run IDs” rule: chart-content/layout changes are additionally permitted only in actual chart consumers—`journey-j-executive-signal-public` and the thirteen Analyst `*-workspace` scenes (steel2, polypropylene2, other nine public cases), in both locales/two existing viewports. This is an upper eligibility set of56 current images, not a claim that56 will change. Declare observed changed paths and chart-visible region/necessary same-card or below-card flow before canonical allocation; only changes caused by this note/control are eligible. Non-chart chrome/content, the other executive steps, portfolio, screening, selection, graph geometry and unrelated layout remain unchanged except parent-authorized IDs/versions. Preserve all128 paths, dimensions, anchors, pinned producer/fonts, tolerances and600KiB/16MiB budgets. Do not scroll/resize/hide old required content to make the new Signal scene fit. If it cannot fit readably, return to review.
- [ ] In canonical Signal keep the table closed; require the new note and summary plus every old required box fully visible. Review all actual changed before/after images. Open-table/narrow diagnostics remain separate evidence, with no new golden-image paths. Preserve AR-V01 dossier repair, all44 PDFs/every-page proof, 22 JSON/44 HTML and all44 executive→analyst→dossier journeys.
- [ ] Freeze all source before visual binding/pin; preserve the base-adoption test literal/count amendment and strict R1 sensitivity. Run two complete isolated clean-root `make ci` instances using the approved AM-4 prevention controls; inventory comparison is only supplemental evidence. Separate exact-tree review and delegated acceptance precede staging/commit; then actual hosted checks, independent exact-head approval, protected merge and main CI. Final S21 live Aura/final projection/bilingual application and S22 cross-model release gates remain required. No S18b pass substitutes for a S19 result.

## Provenance, closure and self-audit

Directly read: actual renderer/catalogue/callers, generation/visual discovery and accepted planning/owner records. Selected bytes and all referenced hashes are in BINDING.json. Inheritance/steel screenshot interpretation and scope disposition are attributed to the independent adjudication; I ran no browser, generator or test. The strategy is a design choice, not a claim of repaired usability.

Close TRADE-SCALE-01 only after source equality, meaningful RED→GREEN, both consumers/locales, native keyboard/rendered evidence and independent S19 implementation review pass. Until then it remains OPEN_REQUIRED_S19_PRODUCT_REPAIR. The current frozen S18b chart is not declared quality-approved. Any source/scale/data/branch drift, missing regression sensitivity, unreadable chart/capture, unlisted path, unintended generated semantics or unrelated visual change stops this repair for reviewed correction; no weakened check or failure relabeling.

**Muhasabah: PASS for this bounded authored amendment, not unfinished-product acceptance.** All requirements map to a concrete shared change and existing tests/gates; explicit uncertainties are actual delivered predecessor, screen fit and future test/generation results. Only this new external packet is written. No self-approval, Claude attribution, new framework, future passing result or service/index mutation.
