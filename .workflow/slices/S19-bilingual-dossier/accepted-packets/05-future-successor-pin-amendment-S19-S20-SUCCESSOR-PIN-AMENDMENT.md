# S19/S20 finite successor-pin implementation amendment

> **For agentic workers:** Use the separately assigned implementer and installed executing-plans workflow after a different reviewer approves this amendment and root records delegated acceptance. The author cannot approve it. Execute each slice on its own actual accepted predecessor; this packet authorizes no source edit or generator operation before those prerequisites.

**Goal:** Make the already approved S19 UI1.7 and S20 UI1.8 plans executable through the existing strict catalogue, module, browser and visual contracts without weakening those contracts.

**Architecture:** Advance only exact live-version literals and append only already-planned paths and independently reviewed real test names to existing literal inventories. Retain every historical fixture, old inventory member, assertion and validator. Coordinate these changes before each slice's separately reviewed graph→manifest→visual generation.

**Tech stack:** Existing Python/YAML validators, pytest literal contracts, synchronous Playwright browser tests, ES modules, immutable graph/visual artifacts and existing pinned producer. No dependency, service, API or data-model addition.

**Spec:** Approved S19 plan `7749006ae850adf66fea862934c8362d9ea5c9254aaf74ded702504e69ce44d1`, design `db03f9330e75f1e857dfcc0493fbad792321a2f522e4498ed610d30b6dfdead1`; approved S20 plan `312213c9980e872a9e4aa271c782504ca7f3c57d415ef5d385bf5f671e254a88`, design `16155bc553cf684f91d5c266c39103a5bf4c65f402e2717a761bf01a1126d1a9`. Existing accepted S20 acquisition/sampling/transport amendments continue to apply; this amendment grants no acquisition or source-use disposition. Preserve those packets byte-for-byte. Root's retained FUTURE-CATALOGUE-SUCCESSOR-BOUNDARY-OBSERVATION.json is the initiating finding.

**Status:** Proposed additive amendment, not independent approval, candidate identity or slice completion. S18b and S19 accepted predecessor identities are deliberately unbound here. The S18b AM4v2 proposal remains a separately reviewed packet; this amendment neither accepts nor changes it.

## Preparation and governing boundary

Read AGENTS and authority manifest, reused completed original-DOCX/full external-workflow preparation, and refreshed Core03/09, roadmap, S19/S20 plans/designs, actual S18b AM1/AM2v2/AM3 and AM4v1/v2 handoffs/findings, exact validators/inventories/adapter/preflight and graph/visual discovery. The authority environment variables remain unavailable; the existing externally adopted workflow and recorded owner/root delegation govern. No replacement orchestration is introduced.

After reading, adopted API-contract and verification-engineer personas: the issue is strict successor compatibility, historical evidence preservation and reviewable test coverage. Actually invoked writing-plans, systematic-debugging, test-driven-development, verification-before-completion, GitHub-flow, Sanad, al-Muhasibi and Muhasabah. Their generic branch/commit suggestions do not override the owner's unstaged-source/exact-candidate workflow or the prerequisite separate review.

`evidence/READ-AND-PREIMAGE-RECEIPT.json` binds sixteen selected current S18b files and eighteen governing/plan references. `CURRENT-LITERAL-INVENTORIES.json` confirms the observed literal sets equal actual files/functions:50 modules,23 browser files,76 unique top-level synchronous test functions. These are selected worktree observations, not a frozen or accepted S18b tree. Copied source preimages are diagnostic evidence only.

## Global constraints

- UI catalogue transitions are exact1.6.0→1.7.0 for S19 and exact1.7.0→1.8.0 for S20. One current version is accepted at each stage; no range or backward-compatible fallback.
- Frozen no-candidate fixture input stays UI1.3.0 in both authority objects. Its historical thresholds1.2.0 and narratives1.3.0 remain. Only the live adapter's output UI version advances; its existing threshold1.3.0/narratives1.4.0 upgrades remain untouched.
- Synthetic isolation, real decisions, all eleven public outcomes, policy labels, thresholds, snapshot/golden bytes and graph semantics925nodes/1045relationships are unchanged.
- S19 keeps128 canonical paths=112non-graph+16graph. S20's already-approved sixteen extraction images yield144=128non-graph+16graph. No path deletion/rename, dimension/font/producer/tolerance/budget increase.600KiB per-image and16MiB aggregate remain; a larger matrix is not a resource waiver.
- Tests retain literal expected inventories. No expectation is discovered from implementation at test runtime, wildcard-expanded, replaced by a lower-bound check, or updated solely to silence a failing count.
- Keep all historical plan/review/owner/generated records. New approval/acceptance and amendment references are additive. Only root coordinates actual candidate creation, staging, commit, PR, checks and merge; no tag or Claude attribution follows.

## Exact file-boundary additions

This table is the complete new support-file permission, applied only in the indicated slice. Line references refer to copied S18b observations; bind actual predecessor hashes and matching AST locations before editing. A shifted line alone is not permission to change a different assertion.

| File | S19 newly admitted change | S20 newly admitted change |
|---|---|---|
| `src/ior_mvp/config.py` | `validate_ui_strings`: exact comparison and diagnostic1.6.0→1.7.0 only | Same two literals1.7.0→1.8.0 only |
| `tests/test_authority_disclosure.py` | Existing live `authority.config_versions.ui_strings` literal1.6.0→1.7.0 | Same live literal1.7.0→1.8.0 |
| `tests/test_s17_graph_contract.py` | Existing live catalogue version/date expectation and version-bearing unit-test name; strengthen existing adapter test with exact two-field delta and historical-input negatives | Same live successor expectations/adapter proof for1.8.0; preserve graph-key/prose/count/policy assertions |
| `browser_tests/graph_fixtures.py` | One live assignment in `adapt_no_candidate_expected`,1.6.0→1.7.0 | Same assignment1.7.0→1.8.0 |
| `tests/test_es_modules.py` | Append literal `modules/dossier-actions.js` to `EXPECTED_MODULES`; all old50 members and all checker assertions unchanged | **No edit permission needed or granted**: no new JS module in S20; retain inherited51 |
| `tests/test_browser_harness_contract.py` | Append exactly the three planned files and separately reviewed actual new test names; update only inventory function's numeric name to its independently counted total | Append exactly the two planned files and separately reviewed actual new test names; same bounded numeric-name update |
| `browser_tests/test_graph.py` | **No new edit permission**: final retained count remains112 | Only `graph_visual_preflight_root`'s literal `len(retained)==112`→`==128`; every other fixture statement/assertion remains |

Thus S19 has six newly admitted support files; S20 has six, with seven distinct paths across both. No other source/test/checker file is silently admitted. The already approved files below are clarified, not broadened:

- `tests/test_ui_catalogue.py`: existing exact metadata/date/endpoint successor assertions and strict negative-version cases described below. S19 already names it; S20's generic catalogue-test allowance is made explicit as this path only for these checks.
- `config/ui_strings.v1.yaml`: only each parent's already approved successor/version/date and dossier/extraction copy. No new key or translation is proposed here. Metadata dates are literal approved values, bound once the real slice catalogue is prepared; never read the date from the same payload to manufacture the expectation.
- `browser_tests/visual_baselines.py`, `test_visual_baselines.py`, `tests/test_visual_baseline_contract.py`, `test_frozen_public_evidence_pins.py`, `test_s17_status_docs.py`: retain parent generation/provenance/count/subtree permissions exactly. No alternate pixel acceptance or S20 golden-root exception is introduced.

Existing slice-record allowance admits a byte-identical `successor-pin-amendment.md` plus additive plan_review, implementation_log, test_evidence and reviewer_findings references under `.workflow/slices/S19-bilingual-dossier/` and `.workflow/slices/S20-bilingual-extraction/`. Five controls/ADR/Core remain within parent scope. This is not permission to edit historical S18b records or approved packets.

## Task 1 — bind the actual predecessor and prove strict catalogue succession

**Consumes:** Each slice's independently accepted predecessor commit/tree, current catalogue, original parent plan/amendment approval, sole-writer assignment and protected-index records.
**Produces:** Exact finite pre/postimage ledger, literal successor expectations and original-defect RED/GREEN evidence.

- [ ] Root binds actual accepted S18b for S19, then actual accepted S19 for S20, in separate isolated task worktrees. Record commit/tree, catalogue version/date, all approved intervening changes, frozen root hashes and existing ordinary/checkpoint/alternate indexes. Do not use the dirty primary or the copied diagnostic preimages as an accepted base. A different predecessor version/module/path inventory requires explicit reconciliation and review before writing; do not infer completion from a plan.
- [ ] Hash the six slice-specific support files before edits; require the existing strict validator/adapter predicates and inventory members. Confirm original no-candidate JSON remains identical. Preserve the predecessor source/diff receipt externally.
- [ ] In already allowed `tests/test_ui_catalogue.py`, preserve the real-current-catalogue full-validator test and both mounted locale endpoint tests. Change only the exact successor metadata/version/date literals and expand the invalid-version parameter list to the independent literals below. Keep the existing missing-version test, missing-Arabic-key HTTP500/UI_CATALOGUE_INTEGRITY_ERROR test, policy-label and placeholder guards. Clear config caches in `finally` wherever tests mutate them.

S19's complete invalid-version list becomes:
```python
["1.5.0", "1.6.0", "1.8.0", "1.6", "1.7", None, True, False, 1.6, 1.7]
```
S20's complete invalid-version list becomes:
```python
["1.5.0", "1.6.0", "1.7.0", "1.9.0", "1.6", "1.7", "1.8", None, True, False, 1.6, 1.7, 1.8]
```
Every case must raise the same `UIStringConfigurationError` matching `metadata.version`. A formerly future exact version necessarily moves to the positive current case when adopted; replace that negative with the next future version and retain the former-current rejection. This is the only semantic change to version-case expectations. No boolean/string coercion or exception weakening.

- [ ] Before changing the validator, run the current-catalogue and mounted locale tests against the prepared successor catalogue in an isolated proof copy and retain actual version-rejection failures. Also retain failure of the new former-current rejection test against the old guard. Collection/import errors are not RED. Keep a predecessor catalogue control to distinguish expected old behavior from malformed test setup.
- [ ] Apply only these two literal edits to the actual predecessor validator:
```python
# S19
if metadata.get("version") != "1.7.0":
    raise UIStringConfigurationError("UI catalogue metadata.version must be 1.7.0")
# S20 applies the same existing comparison/diagnostic with exact "1.8.0".
```
Preserve the source's surrounding formatting and every remaining validator statement. No global1.6→1.7 or1.7→1.8 replacement: acquisition config, thresholds, narratives and historical fixtures have separate versions.
- [ ] Run the whole catalogue test file and both real mounted `/api/ui-strings/en` and `/api/ui-strings/ar` tests. Valid responses must contain the literal current catalogue version, all keys, and policy-sourced synthetic labels. Wrong/missing version and malformed catalogue must retain typed failure. Save real command/exit/log evidence; copied-function diagnostics in this packet do not replace candidate tests.

## Task 2 — preserve frozen history while advancing live disclosures

**Files:** The authority-disclosure test, graph-contract test and graph fixture adapter in the table. No fixture/data edit.

- [ ] Change the exact live authority test literal and graph catalogue literal to1.7.0 or1.8.0. Update the graph unit-test's version-bearing name correspondingly; its nineteen node-label/fifteen edge-label counts, exact English/Arabic graph hint, parity, policy-label nonduplication and graph isolation assertions remain. Its effective_date must equal the independently bound new catalogue date as a literal.
- [ ] Change only `versions["ui_strings"] = "1.7.0"` (S19), then `"1.8.0"` (S20), in the existing adapter. Preserve its deepcopy, opportunity ID, full component shape, original1.3.0 UI preconditions in both authority objects, other historical version preconditions/upgrades, action equality, appended graph descriptor and original-not-equal result.
- [ ] Strengthen the existing `test_no_candidate_adaptation_is_guarded_and_does_not_mutate_history` using independent literals. Compare predecessor-adapted and successor-adapted outputs in isolated copies: the only two differing paths must be `/analysis/authority/config_versions/ui_strings` and `/ui_manifest/components/0/props/authority/config_versions/ui_strings`. Assert literal current values in both; full remaining output equality and untouched original JSON. Preserve existing malformed-component rejection. Independently mutate each original authority object's UI1.3.0 input to the successor value and require AssertionError, proving this is a frozen-input adapter, not a current-fixture rewrite.
- [ ] Demonstrate RED using the old live assignment against the new literal output expectations, then GREEN after the one-line assignment. Run the complete authority and graph-contract files. Existing actual no-candidate browser journey must pass after the parent changes; inherit AM4's precise natural executive404 oracle, never fabricate200 or suppress collector errors.

## Task 3 — append only finite modules/files and reviewed real test names

**Files:** `tests/test_es_modules.py` in S19 only; `tests/test_browser_harness_contract.py` in both slices. Product/browser additions themselves are already within the original parent scopes.

S19 appends this single literal module member, preserving all50 predecessor members:
```python
"modules/dossier-actions.js",
```
The expected total is51. Its actual implementation must have named exports, no default export, local resolved imports, valid module syntax and≤199 physical lines. Do not modify `_production_modules`, equality assertions, the line cap or syntax checker. S20 modifies existing extraction/state/events modules only; its expected module set remains the same51. Additional modules require a separate boundary amendment.

S19 appends exactly these top-level browser-file literals,23→26:
```python
"dossier_pages.py",
"test_dossier_accessibility.py",
"test_dossier_failure_states.py",
```
S20 appends exactly these literals,26→28:
```python
"extraction_pages.py",
"test_extraction.py",
```
No helper subdirectory or extra file is implicitly authorized. All preexisting names, notices and harness files stay in the literal expected set. Preserve all other harness assertions: dependencies, marker collection, locale matrix, Makefile gates, offline assets, axe/collector sensitivity, environment allowlist, print/PDF proof and fixed visual contract.

### Exact real-test inventory procedure

The parents specify behavior and permitted test files, not final function names. This amendment deliberately supplies **no invented future names or final named-test count**. It authorizes a bounded later literal append only through this concrete sequence:

1. Bind the actual accepted predecessor's complete literal named-test set and its one-to-one mapping to real synchronous top-level `def test_*` bodies. Expected S18b set has76; verify, never force. For S20 use the actual accepted S19 set, not76. Preserve every old name and its substantive assertions. Parametrized node IDs are counted separately from named functions.
2. Author the parent-required meaningful new tests in the exact already permitted files. No placeholders, pass-only bodies, stubs, duplicate names, fake collection or test-label renames to inflate coverage. Extend existing bodies where appropriate, without deleting/weakening assertions. Any substantive old behavior change must already be justified in the parent plan; inventory admission alone never authorizes it.
3. Before changing the inventory expectation, freeze an external `S19-BROWSER-INVENTORY-ADDITION.json` or `S20-BROWSER-INVENTORY-ADDITION.json` containing actual predecessor commit/tree and set, exact new function name/path/body SHA, requirement mapping, collected parameter IDs and meaningful behavioral RED/GREEN or preservation evidence. Include complete old/new file/function set diff, duplicate-name audit and proposed literal hunk. AST discovery supplies **observations**, not an automatically accepted expected set. No endpoint/service run is authorized by this planning packet.
4. A different reviewer inspects each actual test body and assertion, its failure sensitivity and scope; issues a SHA-bound APPROVE/REJECT for the exact finite additions. Root records delegated acceptance. An unexpected file/module or removal requires a separate amendment, not approval via this list. Author cannot approve its own inventory.
5. Only then append those exact approved name strings to the existing literal `expected` set; preserve `assert actual == expected` and the existing actual-discovery loop. Rename only `test_browser_inventory_has_exactly_76_named_tests`'s numeric fragment to the independently verified new total; do not remove the count from its name to avoid binding it. No function body computes expected from actual, imports the admission JSON as a runtime oracle or wildcards names. No future count is fixed before real tests exist.
6. Retain inventory RED with the genuine added files/functions against old expected sets, then run the exact file/function/module contracts after the literal update. Independently check old-set subset and literal exact delta. In disposable copies, removing an old member/file or adding an unapproved name/module must fail equality; preserve those negative results without committing mutations. This is inventory sensitivity, not proof of behavioral tests by itself.

## Task 4 — exact visual count and pre-generation sequencing

- [ ] S19 leaves the graph fixture's literal `len(retained)==112` unchanged. Only the already approved dossier pixel changes and generated source identities may change under its separately bound operation. Its final matrix remains128, including16graph; no new visual scene is admitted here.
- [ ] S20 changes only this existing fixture line after binding the accepted128-image S19 base:
```python
assert len(retained) == 128
```
Keep literal16fresh graph captures,16existing graph baselines, exact graph-screen set,≤600KiB per capture, full retained+new≤16MiB and complete receipt publication. Final matrix is144=128non-graph+16graph; the additional16 are exactly the parent extraction scenes/locales/viewports, not invented graph replacements.
- [ ] Follow the existing parent F1 permissions for final144 matrix/path/count assertions in `test_visual_baseline_contract.py`, `VISUAL_BASELINE_ENTRIES` and the actual visual subtree OID in `test_frozen_public_evidence_pins.py`; do not broaden this amendment into a second frozen-data permission. S20's four-file additive golden-root change remains solely under its approved F1 controls. Update only live manifest count/change_ref in `test_s17_status_docs.py`; every historical S17 graph ID/722/112/F01 statement stays literal and unchanged. Actual generated counts/OIDs are verified, never forced.
- [ ] Apply the AM4-P1 lesson honestly in S20: before its new16 extraction baselines are generated, accepted S19 has128total/16graph/**112retained**, so the new final128-retained assertion will fail at teardown. Run and retain the full unmodified functional diagnostic; only this exact observed112-versus128 generated-inventory predicate may be classified as pending for preliminary source/UI inspection. Report the command nonzero, separate test-body/collector/axe results, and require all other behavior/collector/axe assertions to pass. No skip, xfail, fixture edit, phase-aware predicate, copied canonical baseline or general error exception. Fresh tracebacks/counts must match; another failure blocks dependent work.
- [ ] Since that count failure precedes later fixture checks, independently verify the actual16fresh graph captures' exact two-locale/two-viewport/four-screen matrix, dimensions, lossless opaqueRGB and600KiB; bind the112 inherited non-graph identities and measure **provisional128 =112+16** against16MiB, with every input hash and subtotal. A resource failure is a concrete blocker, not the permitted count classification. The16 future extraction paths remain ungenerated/size-unverified in the planned144 ledger; this diagnostic does not prove final144 resources. Do not re-encode or change baselines outside the accepted generation procedure.
- [ ] After separately approved generation, require full unmodified functional exit0, the entire unchanged graph preflight body/receipt, final144/full128retained accounting, final144 resources and all parent gates. No pre-generation pending count or manifest failure is waived for the final candidate. S19 must likewise achieve full128 final functional/visual success. Do not copy S18b's ten named stale-output failures into either future slice as a blanket allowance; bind and independently classify actual future parent-authorized generated-output failures individually.

## Task 5 — generated input closure, exact verification and additive records

- [ ] Before any generator, append a slice ADR: exact catalogue successor, omitted support-file boundary repair, preserved frozen1.3 input, finite literal path additions and reviewed test-name packet. Include the S20 final retained-count calculation and truthful pre-generation diagnostic distinction. Original plans/designs/reviews retain their approved bytes; this amendment and review references are additive.
- [ ] Actual graph discovery already includes `src/ior_mvp/config.py` via top-level ENGINE_IMPLEMENTATION. Each parent's operation packet must now list this exact two-literal source delta in addition to its existing permitted Core/catalogue/product inputs. No graph-discovery code, output-schema, counts, topology or semantics change. New input identity requires the original graph→manifests→visual order and separate exact operation review.
- [ ] Visual discovery already includes config.py, all static modules, graph_fixtures.py and test_graph.py. Consequently both catalogue/adapter changes and S20's fixture count change must be final before visual source hashes are bound. Unit contract files here are not discovered graph/visual inputs; do not fabricate that status. Keep each parent's explicit new capture helper and all transitive helper hashes. No generated manifest self-hash cycle. Missing discovery beyond already authorized helper additions requires a concrete reviewed correction, not implicit scope.
- [ ] Run `python -m pytest -q tests/test_ui_catalogue.py tests/test_authority_disclosure.py tests/test_s17_graph_contract.py tests/test_es_modules.py tests/test_browser_harness_contract.py` in the slice's governed locked environment; retain meaningful RED/GREEN and all old assertions. Run original UI/ES-module checker commands, relevant complete browser journeys and full diagnostics under the parent gates. Do not replace them with this packet's pure AST probe.
- [ ] Freeze source/UI and all evidence for separate implementation inspection before costly generation. The actual operation must bind expected graph925/1045 semantic equality and all final source hashes. Generation allocations, snapshot count arithmetic, preserved old artifacts, fonts/producer/strict same-layout R1 with12/5 rows and exactly five-token controls/repeats, all11 decisions, final compare-only matrix, full tests/reconstructions/integrity/smoke and two clean-root `make ci` remain exactly as required by each parent.
- [ ] Compute actual permitted subtree/candidate identities only with freshly allocated external proof indexes; preserve ordinary/checkpoint/alternate indexes. Verify every exact final gate before implementation approval; stage/commit only approved paths, verify tree identity, then PR/hosted exact-head checks and separate final review before coordinator merge. Keep actual post-merge evidence and remaining limitations, with no release tag or claim of Claude approval.

### Additive ADR wording

S19 and S20's approved UI catalogue increments require coordinated exact validator and live disclosure/adapter successors. This amendment admits only the missing support-file boundaries and literal inventory additions for already approved modules/tests; each new browser test name requires actual-body review before literal admission. Historical no-candidate1.3 fixtures and all old contract members remain immutable. S20's sixteen new extraction visuals require128 retained non-graph images within144 final images, with the pre-generation112-retained mismatch explicitly reported and separately measured. No validator, checker, budget, domain, graph or final gate is weakened. Actual predecessor identities and all generated inputs are bound at execution; this record is not a completion claim.

## Sanad and Muhasabah

Direct evidence: selected current source/authority/plan reads and hashes; AST literal-to-actual inventory equality50/23/76; isolated copied validator probes reject successor under the old exact guard and accept it only under the corresponding two-literal change; malformed/boolean/missing/locale/policy negatives remain rejected. The pure adapter probe shows exactly two live fields change and both historical1.3 guards reject altered input. These are diagnostic function probes, not actual future catalogue/API/browser or candidate proof. See `probe_successors.py` and evidence JSON.

Attributed evidence: S18b AM1/AM3 reviewed fixes, parent approvals and AM4-P1 independent count finding. Current source is a selected worktree observation; accepted S18b/S19 completion, later metadata dates, real new test bodies/names, generated counts/OIDs, source/UI/resource proof and exact final gates remain unverified. Do not mistake proposed51/26/28 arithmetic for implemented inventory.

Assumptions/risks: parents retain the known50/23/76 starting boundary and128/144 matrices; actual accepted predecessors must confirm or trigger reviewed reconciliation. New capture images may exceed the unchanged16MiB budget, and UI version changes necessarily change generated input identities. Separate exact operation and candidate reviews contain these risks. This plan neither revisits S20 source-policy disposition nor authorizes acquisition.

Muhasabah PASS for this bounded authored plan: all requested support boundaries, strict negatives, immutable history, inventory review and generation consequences are explicit. No source/index/network/service/browser/graph/generator mutation or self-approval occurred. Only this external packet was written; a different reviewer must approve or reject it. Same-model delegation remains accurately attributed.
