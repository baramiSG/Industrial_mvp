# Independent SRC-VAL-01 correction review

Verdict: **APPROVE** the frozen two-file correction only. SRC-VAL-01 and its synthetic-passport addendum are closed for the demonstrated export consumers. This is not S19 source readiness or PDF/candidate approval.

I reviewed the actual validator and appended tests as an independent API-contract/integrity reviewer, applying strict-reviewer, Sanad, al-Muhasibi and Muhasabah. The author is a different agent and remains paused. Governing preparation and the unchanged r8 semantic review are retained in `../s19-source-preparation-r1/`.

The shared boundary now validates the demonstrated required rule/metadata/route/exclusion/dimension consumers and adjacent explicitly consumed types before projection. Optional absence and nullable fields remain distinct from malformed values. Synthetic-origin markers and exact scenario-prefixed supporting IDs require the complete policy identity independently of the flag, closing the false-public passport defect. Public validation rejects contradictory markers without retrieving scenario inputs. No decision calculation or policy thresholds change.

Direct verification: all 2,941 sealed packet entries rehashed without mismatch; all 914 fixed r10 source/config/data inputs match the preservation basis, with only the validator overlay. The old 10,256-byte test file remains an exact prefix. Both integrated product paths currently equal the frozen postimages. I independently replayed the original seven missing-field cases, three synthetic-passport corruptions (false flag, combined marker substitution retaining the exact ID, English label), and two public-origin corruptions in the pinned image with the isolated source mounted read-only. All twelve raise DossierIntegrityError; all 36 JSON/English-HTML/Arabic-HTML requests return DOSSIER_INTEGRITY_ERROR/422. Public probes forbid scenario retrieval. Real public/simulated positive controls build and render both locales without mutating their analyses. Exact commands/results are in DIRECT-PROBE.json.

I independently compared the retained actual before/after bytes: 22 JSON exports, 44 HTML exports, 22 analyses, 11 scenarios and their capture index are equal, without normalization. That supports reusing rendering evidence for this validation-only overlay; it does not cover subsequent captions, labels or pagination changes. The retained author evidence binds original-validator 315 failures + one positive pass to corrected 316 passes, and the final additive test image to 649 focused passes. The nine supplemental original-validator failures are genuine build/API failures; the two unsuccessful replay setups are retained and excluded. I did not rerun the entire unchanged focused suite or all PDF production.

Sanad: direct findings are source/patch inspection, rehashes, byte comparisons and the twelve replayed probes. Test-suite counts are attributed to the sealed author logs, which I read and checked against bound source/test hashes. EVIDENCE.json gives exact hashes. Assumption: the accepted finite exporter-consumer scope remains the governing validation boundary; this is not an engine-wide arbitrary-malformation schema. Risk: malformed records previously exported may now correctly reject with 422. Unverified here: integrated final frozen source, remaining UI findings, final 44-PDF layout, generation, strict R1, two exact CI runs, hosted/exact-head and release gates. These remain required.

Muhasabah: **PASS** for this bounded approval. No unsourced pass claim, source change, test weakening, self-approval, or transfer of approval to mutable product bytes.
