# Independent r12 finite source-delta review

Verdict: **REJECT** the exact frozen r12 source correction for one new renderer regression, R12-SRC-01. The source binding is `source-prototype-r12.json` SHA256 26caf1ad24a83865f97cb62476b01fef682d26ea6d6874ecb0125a23f71bc866; POSTIMAGES.json is b9325be9f194bdb3be8849533e5b0f48cbed2651a9ee0de0d66178f00312217a. This is the existing finite correction review, not whole-source or PDF acceptance.

**R12-SRC-01: new caption/intro shape assumptions produce HTML500 for values previously rendered.** At dossier.py:745, the counterfactual caption lookup unconditionally calls `.get` on the outer value and nested q3 value. The new record_group helper also assumes every nonempty value has `.items()`. I directly used a detached actual steel simulated analysis with (a) a nonempty counterfactual list, and (b) a mapping whose q3_brownfield_versus_greenfield is null. Both retain successful JSON build, and the r11 renderer handles both. Frozen r12 raises AttributeError in each; actual API requests return JSON200/HTML500. Exact read-only pinned-image commands and results are in COUNTERFACTUAL-PROBE.json. These are boundary regression fixtures, not claims that current production cases contain malformed counterfactuals.

I concur with the smallest correction within the already approved renderer/test paths: inspect outer and nested mapping types before the caption lookup; use the first-pair helper only for a nonempty mapping; otherwise retain the existing h.value rendering path for prior nonmapping/null forms. Do not add a new validator/schema or silently substitute source values. Add the two meaningful shape regressions to the existing test file, retain actual r12 RED and corrected GREEN, and establish exact 22JSON/44HTML equality between r12 and the guard-only successor for all valid sources. The valid targeted PDF attempt may be reused only under that actual equality binding. This correction does not need another plan cycle.

Other completed checks: all 1,732 frozen source entries rehashed without mismatch. The four preimages exactly match r11. The only six r11→r12 path changes are the four approved presentation paths and the two separately approved validator paths; docs, checkers, admitted browser bodies, domain/projection logic and other source bytes are unchanged. The previous projection tests remain an exact byte prefix. Catalogue changes are only Description/الوصف. CSS adds only the bounded intro break-inside rule; font sizes, margins, footer and geometry predicates are untouched.

I read the complete four-path delta and additions. Ten caption consumer sites are present with the preserved language/direction rendering. Route intros contain the existing four-row feasibility table; rule intros contain the first localized result, and the synthetic-ledger title/disclosure occurs once with its first rule. Evidence retains the complete contradiction-register and exactly one section h2. Definition-list first pairs and remaining values are rendered in order. Selection provenance is moved unchanged beside authority metadata. Long metrics/records remain outside intro units. These source facts support the proposed design; they do not establish actual pagination closure.

Independent preservation probe: on all 22 retained real analyses, r11 and r12 build the same dossier JSON without mutating input. Across 44 rendered outputs, the complete text-node multiset is unchanged after removing source-caption paragraphs; every ID and link is retained, IDs remain unique, and all internal references resolve. Both renderers intentionally used the same frozen r12 catalogue, isolating the separately reviewed two label changes. The multiset check proves no text omission/duplication under that comparison; it does not prove presentation order or visual quality. Exact command/result is VALUE-PRESERVATION.json. The author logs show corrected-fixture original34RED and final212PASS; the first wrong PP route-evidence fixture attempt is preserved and excluded from the corrected regression evidence.

Sanad: direct evidence is frozen hash/delta inspection and the two new actual API/render regression probes plus 22/44 preservation checks. Test totals are attributed to the bound author logs, not a new independent full-suite run. Persona: independent API-contract and bilingual paged-media reviewer; strict-reviewer, Sanad and Muhasabah applied, retaining the earlier governing preparation and al-Muhasibi review discipline. Assumption: valid source behavior must remain exact under the guard, which requires measured equality before PDF-proof reuse. Risk: targeted/final page layouts remain unverified here; source structure alone cannot close print findings. Remaining original requirements include exact corrected delta, targeted output closure, integrated paused-source readiness, final44/every-page proof and downstream generation/strict/CI/hosted/exact-head gates.

Muhasabah: **PASS** for this bounded REJECT and concrete guard concurrence. No product edits, no expanded validator scope, no borrowed PDF approval, and no concealed partial-test limitation.
