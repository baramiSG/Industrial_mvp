# Finite authority terminal-record correction

Product remains paused pending independent concurrence and root acceptance. Scope only src/ior_mvp/dossier.py and tests/test_dossier_projection.py. No CSS, catalogue, validator, domain/projection/JSON, browser-body, footer, font, margin or strict-check changes.

Actual r12 target rasters show terminal integrity fragments: fertilizer simulated AR60 has only route code0/matchYes, PP simulated EN54 only matchYes, SOP public EN28 only latest-import USD/kt. The earlier introduction and selection relocation are separately measured; their success does not close these remaining fragments.

At the authority renderer only, reuse existing intro() and its existing print keep-together styling. Public integrity is the complete five-row short record (public-only boolean, isolation boolean, unavailable reconciliation, latest-import value and quantity). Render the Evidence boundary heading plus all five pairs in one intro. Simulated integrity keeps the current heading/first-fact intro and long reconciliation record flowing; render its four-field terminal group (latest imports USD/kt, unchanged public-decision boolean, complete ground_truth_backtest with expected/actual state+route and match) inside one intro. Keep the existing order, labels, values, classes for individual record pairs, and current Arabic original-source caption.

This is explicit rendering for the actual authority integrity consumer. It does not add generic recursive unbreakability. The long scenario reconciliation remains outside the terminal wrapper. Guard mapping/null optional behavior from the approved predecessor remains untouched. No source-value reconstruction, omission, translation or reassignment.

ACTUAL22.json binds exact complete public shapes and exact simulated suffix order/value shapes across all11cases/bothmodes. The simulation terminal unit contains8scalar facts with their existing group labels; no long prose/check arrays are enclosed. Add meaningful source RED→GREEN within the existing projection tests asserting full public short-record containment and complete simulated terminal-group containment/unchanged values for22outputs andbothlocales. Existing introduction tests remain.

Retain actual r12 target PDFs and visual failure. Produce the three affected reports first with the same pinned network-none stock browser/PDFium recipes. Require complete terminal logical context on one emitted page (public heading+all5pairs; simulated latest-import values+unchanged-public boolean+full expected/actual/match record), inspect final/preceding rasters, and retain unchanged strict geometry/footer/font/content checks. Preserve9-target prior intro proof only where output-page bytes match or repeated checks establish it. After independent target/source closure, run the required full functional browser matrix once to produce44PDFs and perform the unchanged every-page acceptance/render/manual review.

Sanad: actual target PNG/text paths under r12-targeted-pdf/rendered; root and independent visual reviewer observations; ACTUAL22.json direct analysis/projection inspection. Muhasabah PASS for this bounded proposal: actual remaining failure acknowledged; grouping feasibility is an assumption requiring targeted output, not an asserted correction. All delivery/generation gates remain pending.
