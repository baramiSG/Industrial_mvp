# R12-SRC-01 independent guard closure

Verdict: **APPROVE** the exact frozen two-path guard correction. R12-SRC-01 is closed. This approval covers the source regression and the guard-only equality needed to reuse r12 rendered output; it does not approve the nine targeted layouts, final44, or whole-source readiness.

The reviewed guard ledger is 071d98d4e0e37fab98ea3361a5cc9d2d65c3b7be7ec35e9c2e3aa85930804c97; two-path POSTIMAGES is e9ddaf117f44b63bcab0a04f9167acaac5a182779662a32521a5ca230ffa0879. All 1,761 listed entries rehash correctly. Compared against the prior 1,732 source-file ledger, only dossier.py and tests/test_dossier_projection.py change. The additional 29 entries are existing Python 3.14 bytecode caches copied into the guard ledger; they are not additional product source changes and were not used by my pinned Python 3.12 probe. All previous test bytes remain an exact prefix.

Direct source review confirms the approved minimal behavior: outer and nested mapping checks precede the caption lookup, and the first-pair helper operates only on a nonempty mapping, otherwise passing the original value to h.value. No validator, source data, configuration, CSS, typography, policy, or output value change. The appended two-shape test exercises both actual API exports and both locales, retaining JSON values and rendered original/unavailable content.

I replayed my two exact failing cases using the immutable guard snapshot in the pinned read-only, network-none image. Both now build/render successfully and return JSON200/HTML200; r11 behavior remains the comparison. GUARD-PROBE.json retains the full command and output. I then independently rebuilt all 22 real analyses with old r12 and the guarded renderer: 22 serialized JSON exports and all 44 UTF-8 HTML outputs are byte-equal, no input mutation occurs, and every resulting HTML hash matches the author's sealed EQUALITY.json. GUARD-EQUALITY.json retains this independent calculation. Therefore the guard itself does not require reprinting the nine r12 targeted reports, provided those reports retain their exact source/HTML/producer binding; their layout and strict checks remain to be reviewed.

The retained author logs show the two meaningful regressions failing before correction and the complete 214-test projection file passing afterward. I read and bound those logs; I did not repeat the entire unchanged suite. The earlier r12 content/reference preservation proof and unaffected validator/source evidence remain applicable under the exact two-file delta.

Sanad: direct evidence is the frozen diff/hash review, original-probe replay and complete 22/44 equality rederivation. Suite counts are attributed to the author logs. Persona and skills remain the independent API-contract/bilingual paged-media reviewer with strict-reviewer, Sanad and Muhasabah, using the existing governing preparation. Assumptions: valid-output reuse applies only to the proven guard delta and exact bound r12 producer; no broader source approval is inferred. Risk/unverified: the nine targeted layout corrections and required final44/source/generation/CI/hosted/exact-head gates remain open. Muhasabah: **PASS** for this bounded closure; no product edits or unverified PDF-success claim.
