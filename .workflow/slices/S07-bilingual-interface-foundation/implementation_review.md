# Supervisor Implementation Review — S07 Bilingual Interface Foundation

Reviewer: Supervisor (Claude, `claude-fable-5-1-thinking-max`). Implementer: agent ffdeb9c4 (GPT-5.6 Sol, `gpt-5.6-sol-max`). Candidate: uncommitted working tree on `slice/S07-bilingual-interface-foundation` at base `6d00e27`; 148 candidate paths (58 tracked modifications incl. one deletion, 98 new paths incl. 40 baseline WebPs, fonts, modules, CSS, tests, records).

## What I inspected (2026-09-02)

- Protected paths: `git diff --name-only -- data config/thresholds.v1.yaml config/sector_profiles.v1.yaml <DOCX> docs/core/03–08` → empty. Governed diff limited to `config/evidence_policy.v1.yaml` (1.2.0; only `display_label_ar` added), new `config/ui_strings.v1.yaml`, Core 01/02/09 (v2 markers and the exact plan wording), `authority_hashes.json` (five rows changed/added, no `generated_on` change) and the Manifest §11 table (same rows). `snapshot_manifest.json` untouched. S06 documentary reference directory unchanged.
- Python diffs read: `evidence.synthetic_display_labels()` as the single policy source; additive `display_labels` on synthetic rules, evidence rows, simulation decision and scenario disclosure; `genui` passes `synthetic_labels` and (per plan §5.5) `conditions`/`kill_conditions` to the hero; `config.py` catalogue loader with exact key/placeholder/NFC/duplication validation; `app.py` locale resource with the exact 404/500 codes and `?locale=` on dossier HTML; `dossier.py` embeds `tokens.css` + `dossier.css`, localizes chrome via `ui_text`, islands for engine text.
- ADR-011 present and correctly attributes the Arabic label and digit policy as Supervisor-approved, owner-amendable defaults. CI diff as planned (UI contracts + ES-module checker in both Python jobs; `fonts-noto-core` removed; browser job renamed). Module line counts ≤ 132 (max), 19 modules. Catalogue 23 KB, parity-validated.
- Fonts: exact Fontsource bytes/hashes/licences as planned (201,780 bytes). Baselines: 40 lossless WebPs, 5,037,854 bytes, manifest records canonical image digest `aa81288e…`, `chromium-1234`, browser 151.0.7922.34, flags, font and source hashes.

## Verification I ran myself

| Check | Result |
|---|---|
| `make ci` with `LD_LIBRARY_PATH=/tmp/ior-s06-browser-libs` | exit 0 in 2m27s: prohibited scan (244 tracked), threshold scan, `UI CONTRACT CHECK PASS`, `ES MODULE CHECK PASS (19 files)`, `INTEGRITY PASS`, Gate B PASS, `377 passed`, `SMOKE PASS`, `BROWSER PREFLIGHT PASS`, functional + visual browser suites (visual summary: collected 4, passed 4) |
| Governed diff audit | exactly the authorized rows (see above) |
| Visual inspection of captured baselines (converted to PNG and viewed): `ar` desktop steel-simulated workspace, `ar` desktop portfolio-simulated, `ar` desktop steel-simulated dossier, `en` desktop portfolio-public, `en` tablet steel-public workspace | Bilingual shell is real and convincing: RTL sidebar/topbar, Arabic chrome from the catalogue, both governed labels stacked on every synthetic surface, English analytical text captioned as source language. **Six visible defects were captured into the governed baselines — see SR-01.** |

## Findings

| ID | Severity | Requirement | Evidence | Problem | Required correction |
|---|---|---|---|---|---|
| SR-01 | HIGH | Owner ruling R-4 and plan Task 10 ("never retain a known defect"); NFR-006/NFR-007; SG-TR-008 bidirectional layout verified; UX spec §7/§9 | Viewed baselines: (a) `en`/`ar` portfolio cards and metric notes show the year with a grouping separator — "2,024 imports" / "واردات 2,024" (`opportunity.imports: "{year} imports"` receives a grouped number); (b) `en` state chips render the localized label and the raw code twice — "Investigate INVESTIGATE", "Reject REJECT" (portfolio cards, hero badge, banner); (c) label/value concatenation without a separator — route pill "RouteBrownfield priority to test" (`en`), "…line expansionالمسار" (`ar`); (d) technical tokens lose logical order in RTL — workspace title "721049 HS", card code lines "390210 HS"/"721049 HS", dossier "721049 / H0 HS" (should read "HS 721049", "HS H0 / 721049"): the token is not wrapped as a single LTR isolate; (e) `en` tablet 1024×768 workspace: the large hero state badge overlaps the "Conditions" column text; (f) `ar` portfolio: the synthetic disclosure block sits at the inline end (left) instead of the inline start. | Known visual/bidi/layout defects are frozen into the governed oracle; the Arabic UI misorders technical identifiers; the English UI shows duplicated state text and a corrupted year; a layout overlap exists at an approved width. | Fix each: (a) years and other integer identifiers render without grouping (formatter rule: `year`, HS codes, counts of controls are technical tokens; only quantities/currency use grouping) and add a browser assertion that no 4-digit year is rendered with a separator; (b) render the raw state code only when the localized label differs from it (i.e., in `ar`), otherwise once — apply the same rule to execution/fire chips; add a browser assertion that no chip text contains a word repeated twice; (c) label/value patterns come from the catalogue as `"{label}: {value}"` with `<bdi>` isolation and a visible separator — apply to route pill and any other label+value composition; assert in the browser that the route pill text contains the separator; (d) wrap every technical token (`HS <code>`, `HS <rev> / <code>`, snapshot/scenario IDs, SHA prefixes) in one `<bdi dir="ltr">` element and assert in `ar` that the element's `textContent` equals the logical string and its computed `direction` is `ltr` with `unicode-bidi: isolate`; (e) at ≤ 1180 px stack the hero badge above the content and add a functional assertion (both locales, all four viewports) that the badge and the conditions/kill-conditions boxes do not intersect; (f) align the disclosure block to inline start and include it in the mirroring assertions. Then: rerun the 118 functional nodes green → regenerate the 40 baselines only through the canonical container procedure (`IOR_UPDATE_VISUAL_BASELINES=1 IOR_BASELINE_CHANGE_REF="S07-SR-01-defect-fixes" make e2e-update-baselines`) → rerun `make e2e` compare → record before/after evidence for each defect in the log. The generator must NOT be rerun (no governed artifact changes) unless a catalogue string is added/changed — in that case the catalogue hash changes and one further generator run is required; record it explicitly if so. |
| SR-02 | LOW | Evidence integrity of `make ci` | `make ci` runs functional and visual pytest separately; `.artifacts/e2e/run-summary.json` is overwritten by the visual run (collected 4). | The functional-suite summary is lost from the artifact set. | Write mode-specific summaries (`run-summary-functional.json`, `run-summary-visual.json`) or merge; update the S06 contract test that reads the summary if any. |

No BLOCKER findings. SR-01 must be fixed before the independent review so the reviewed candidate — including its oracle images — is final.

## Round 2

Implementer (same agent) fixed SR-01 (a–f) and SR-02, added the browser assertions required for each defect class (ungrouped years, no repeated chip words, route separator, single LTR isolates for technical tokens with computed `direction`/`unicode-bidi`, badge/conditions non-intersection at all four viewports and both locales, disclosure mirroring), regenerated the 40 baselines through the canonical container (`IOR_BASELINE_CHANGE_REF="S07-SR-01-defect-fixes"`), and — because the label/value pattern added a catalogue string — ran the generator a second, justified time: the governed diff versus `main` remains exactly evidence policy 1.2.0 (hash `30018295…` unchanged since round 1), the catalogue row (now `59ccb67a…`), Core 01/02/09 rows; `snapshot_manifest.json` untouched.

Supervisor re-verification: `INTEGRITY PASS`; `377 passed` (3.12); `make e2e` → functional `118 passed, 4 deselected in 120.57 s`, visual `4 passed`; mode-specific summaries `run-summary-functional.json` (118/118) and `run-summary-visual.json` (4/4); `check_ui_contracts.py` PASS; `check_es_modules.py` PASS; `git diff --check` clean. Regenerated baselines re-inspected (converted to PNG and viewed): `ar` steel-simulated workspace shows "HS 721049 · …" in logical order and "المسار: Conditional brownfield …" with the separator; `en` tablet hero badge stacked above content with no overlap; `en`/`ar` portfolio cards show "2024 imports" / "واردات 2024", single-code chips, HS tokens in order, and the Arabic disclosure aligned to inline start. No known defect remains in the oracle images.

Candidate identity captured for the independent review: SHA-256 list `/tmp/s07_candidate_hashes_v1.txt` (excluding the Supervisor/reviewer records `plan_review.md`, `implementation_review.md`, `reviewer_findings.md`). Binary additions: baselines 5.0 MB (40 lossless WebPs), fonts 202 KB.

Unresolved Supervisor findings: **0**. Handing to the independent reviewer (Grok 4.6).

## Round 3 — after independent review

Grok returned **APPROVE — zero unresolved findings** with eight non-blocking residuals. The Supervisor acted on residual 6 (ADR-011 claimed a single generator run while two justified runs occurred) by correcting one sentence in `docs/ARCHITECTURE_DECISIONS.md`; Grok re-verified the sentence against the fix-round records and the hash diff, confirmed no other candidate change (`377 passed`), and confirmed the APPROVE stands. Residuals carried forward (recorded in `completion.md` and KNOWN_LIMITATIONS at completion): copy scanner does not HTML-parse JS template literals (1); token scanner does not read the `styles.css` façade (2); JS motion/SVG numeric literals (3); `decision_object_status` rendered without a source-language island (4 — carry to S18/S19 rather than regenerate baselines now, per reviewer); governance card receives both policy labels in public mode via a data attribute while decision surfaces do not (5); CHANGELOG Unreleased readability (7); host `fc-match` is DejaVu while product fonts are vendored IOR Noto (8).
