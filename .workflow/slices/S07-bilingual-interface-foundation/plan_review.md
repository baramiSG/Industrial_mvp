# Supervisor Plan Review — S07 Bilingual Interface Foundation

Reviewer: Supervisor (Claude, `claude-fable-5-1-thinking-max`). Planner: agent 398bdb6f (GPT-5.6 Sol, `gpt-5.6-sol-max`). Plan read: `plan.md` §1–§27 (1,198 lines) against `SLICE_GRAPH.md` S07 (approved with amendments), owner ruling R-4, Core 01 §7, Core 06 §8–§9, Core 09 §2.7, UX spec, SG-TR-008, ADR-004/007/010, the S06 harness and the current frontend.

## Verification performed (2026-09-02)

- Scope: module graph (19 named-export modules < 200 lines), token layer with a strict scanner grammar, governed catalogue `config/ui_strings.v1.yaml` served by `GET /api/ui-strings/{locale}`, locale runtime (URL → localStorage → `en`), source-language islands for engine text (matches the binding interpretation in `context.md`), vendored OFL fonts, localized dossier chrome with `?locale=`, KL-31 by removing the demo `/docs` link, Arabic browser matrix, governed visual baselines with a canonical Noble container for updates, evidence_policy 1.2.0 with `display_label_ar`, Core 01/02/09 v2 markers, ADR-011, one generator run with an enumerated diff. Consistent with S07 scope, R-4 and ADR-010. No engine, data, threshold, sector-profile or DOCX change.
- External pins independently verified by me: PyPI `Pillow==12.3.0` (current latest; MIT-CMU; `>=3.10`); `mcr.microsoft.com/playwright/python:v1.62.0-noble` manifest-list digest `sha256:aa81288e…f740d` matches §5.9 (`docker buildx imagetools inspect`); Fontsource assets exist at the exact URLs with the exact byte sizes stated (35,820 and 165,960).
- Sanad ledger recounted mechanically: 40/105/22/136/2 — matches §27.1.
- Baseline determinism reasoning is sound in principle (same Chromium revision, vendored fonts, `--font-render-hinting=none --disable-lcd-text --force-color-profile=srgb`, `--with-deps` font set on both runner and canonical image), with two gaps ruled below (uncovered symbol glyphs; matrix weight).

## Round 1 findings and rulings

| ID | Severity | Requirement | Evidence | Problem | Required correction |
|---|---|---|---|---|---|
| PR-01 | MEDIUM (RULING) | Owner A6 ("visual-regression screenshots for all principal journeys"); repository hygiene; CI time budget | Plan §5.9 proposes 80 lossless PNGs (≤ 1.5 MiB each, ≤ 32 MiB total) at 4 viewports × 2 locales × 10 screens; §17.2 eight visual nodes. | Every later UI slice (S13, S17, S18, S19) must regenerate the full set; at up to 32 MiB per regeneration the repository grows by an order of 100 MiB over the milestone for oracle images whose 1920/2560 variants add little beyond the 1440 baseline. The owner requirement is per principal journey, not per width. | Reduce the governed matrix to **40 images: 10 screens × 2 locales × 2 viewports (desktop 1440×900, tablet 1024×768)**, encoded as **lossless WebP** through Pillow (`lossless=True`, deterministic normalization to opaque RGB before hashing/comparison), per-file ≤ 600 KiB and total ≤ 12 MiB. Presentation widths (1920×1080, 2560×1440) stay covered by the functional overflow/actionability/keyboard nodes. Visual nodes become 4; total browser nodes 122. Update §5.9, §6.1 (baseline paths `*.webp`), §8.4, §16.2, §17.2, §19 AC-15/16, §20, §21 (V3-A6), Task 9/10 and the manifest tests accordingly. |
| PR-02 | LOW (RULING) | Deterministic rendering for baselines (R-4); NFR-006 text-not-colour | Chrome text uses `→`, `✓`, `✕`, `·`, `—` (`app.js`, `index.html`, `dossier.py`). The Fontsource Noto Sans "latin" subset's `unicode-range` covers General Punctuation (U+2000–206F: `—`, `·`) but not U+2192 `→`, U+2713 `✓`, U+2715 `✕`, which would fall back to host fonts and differ between machines. | Uncovered symbols become a non-deterministic pixel source and an accessibility weak point. | Render every symbol outside the vendored fonts' `unicode-range` (and outside the Arabic block for `ar`) as inline SVG (or CSS-drawn) with a catalogue-driven accessible name; add a browserless contract that scans catalogue values and renderer/dossier template literals for characters outside the vendored `unicode-range`s ∪ ASCII ∪ Arabic blocks, allow-listing nothing else. Record the exact `unicode-range` values from the Fontsource CSS in `SOURCE.json`. |
| PR-03 | LOW (RULING — resolves OQ-01) | Core 06 §9 (never describe synthetic evidence as Ministry evidence); evidence_policy 1.2.0 | §26.1 OQ-01 proposes `محاكاة — ليست بيانات أو أدلة صادرة عن الوزارة`. | The English label has no governed Arabic counterpart. | **Approved as proposed.** The wording faithfully states "simulation — not data or evidence issued by the Ministry" and is stricter than the English. Record in ADR-011 as owner-amendable governed text (the owner may replace the wording in a later §7.3 change) and in the PR description. Implementation may not vary it. |
| PR-04 | LOW (RULING) | Core 08 §5 numeral normalization; UX parity | §5.4 proposes Western (`latn`) digits in both locales for technical comparability; source spans preserved verbatim. | A digit policy is a presentation decision an Arabic-speaking owner might amend. | **Approved as proposed** (Western digits in both locales; Gregorian dates; source spans verbatim). Record in ADR-011 and UX spec §8 as owner-amendable. |
| PR-05 | LOW | Plan §5.9/§16.2 canonical update container | The runner script mounts allow-listed paths and uses `--network=none`, but does not assert that the container's bundled Chromium revision equals the host pin, nor define behaviour when Docker is absent. | A silently different Chromium in the container would produce baselines that never match CI; a missing Docker daemon would fail obscurely. | `scripts/run_visual_baseline_container.py` must (a) exit 2 with a clear message when Docker is unavailable, (b) assert inside the container that the Playwright Chromium revision equals the host's (`chromium-1234` for 1.62.0) and record browser version/flags in `manifest.json`, (c) never be required by `make e2e` compare mode (host comparison stays Docker-free). Add unit tests for (a) and the mount allow-list. |

No BLOCKER/HIGH findings. The slice is large but coherent; it is not split, on condition that Task 8 (functional Arabic matrix) is fully green before Task 10 captures baselines, and that the PR description reports the byte size of binary additions (fonts, baselines).

## Round 2

Planner (same agent) revised `plan.md`. Supervisor verified in the text: PR-01 — 40 lossless WebP baselines at 2 locales × 2 viewports × 10 screens, ≤ 600 KiB each / ≤ 12 MiB total, 4 visual nodes, 122 total nodes; no residual "80 PNG"/"32 MiB" mention. PR-02 — exact Fontsource `unicode-range` values recorded in `SOURCE.json`; `→ ✓ ✕` rendered as inline SVG/CSS with catalogue accessible names; scanner allow-list = ASCII ∪ vendored ranges ∪ Arabic blocks (lines 280–281, Task 5). PR-03 — OQ-01 resolved with the exact label (lines 450, 1157, 1211). PR-04 — digit policy recorded owner-amendable. PR-05 — runner exits 2 without Docker, asserts `chromium-1234` in the container, records browser version/flags in the manifest; `make e2e` compare is Docker-free (lines 346, 349, 705). Task 10 preconditioned on Task 8 green; PR description to report binary byte sizes. Sanad ledger recounted mechanically: 40/123/22/127/0 — matches §27.1.

Supervisor correction RI-01 (attribution): three phrases called the Arabic label "owner-approved"; the label is a Supervisor-approved default under ruling PR-03, owner-amendable and not yet seen by the owner. The Supervisor corrected the wording directly in `plan.md` (authority attribution only) and flags the wording to the owner in the end-of-turn report.

Unresolved findings: **0**.

## Decision

**PLAN_APPROVED** — 2026-09-02, Supervisor. Implementation may begin on `slice/S07-bilingual-interface-foundation` from base `6d00e27ff156e1342d488495c7b48e68eeefe100`, executing plan Tasks 0–12 in order. Authority changes permitted in this slice, under ADR-010 and the slice's ADR-011: `config/evidence_policy.v1.yaml` 1.1.0 → 1.2.0 (`display_label_ar` only), new `config/ui_strings.v1.yaml` 1.0.0 hashed through an extended `scripts/build_manifests.py`, Core 01/02/09 v2 markers with the exact minimal wording in plan §7.3, and exactly one `scripts/build_manifests.py` run after full regression with the diff limited to the enumerated rows (§18). No `thresholds`, `sector_profiles`, `data/**` or DOCX change. Goldens and both simulated outcomes unchanged. The Supervisor-owned S06 post-merge records in the working tree are not to be modified by the Implementer.
