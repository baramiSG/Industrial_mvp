# Ministry F implementation and verification

Approved scope: the combined F1/F2 correction in Ministry PLAN v3.1. Company discovery and the Ministry journey are the next delivery, not part of this candidate. No final tree ID is embedded here.

## Source corrections

- One finite classifier retains RESOLVED, KNOWN_FAILURE, NOT_APPLICABLE and UNAVAILABLE, including both admitted spellings; unknown values remain unknown. Summary identifiers are deduplicated without combining the underlying declarations. Missing required decision gates remain UNAVAILABLE.
- Graph profile gate nodes carry their own status, raw declaration and profile evidence. The existing Scenario carries the independent decision-specific status, declaration and evidence map. A known failure in one scope cannot falsely confirm a resolved declaration in another.
- Broad needs target Product; genuine specification/intervention references must exist in the same scenario and branch. No arbitrary sorted-first Capability fallback remains. No engine feed, formula, threshold, public validator, scenario or golden input was changed.
- Core02/04/07/09 document this reviewed contract correction; the methodology DOCX is unchanged under the accepted ADR precedent.

## Executed evidence

The primary source implementation was Cursor Grok4.7 High through the existing subscription. GPT-6 Sol High corrected the two actual missing-gate and scoped-attribution findings and adopted the admitted generated test literals. Independent Claude implementation approval remains pending.

Source correction: six meaningful RED regression failures, then the corrected focused cases and 251 affected tests passed (one old generated-identity assertion was explicitly pending generation). An independent comparison with the delivered base passed 198 unchanged decision/economic/public-scope checks across all11 cases; all22 branches retained need codes, fields, effects and evidence. The combined graph delta has no added/removed nodes: 42 broad-need targets move Capability→Product,12 corresponding evidence-link sources move,61 simulated gate property records gain typed/raw status, and11 Scenario records retain separately attributed decision gates. Remaining normalized records are unchanged.

The single delegated source-bound generation produced GRAPH-SAU-2026-09-12-fd70279732aa, ENGINE-bd1cbfb71689,925nodes/1045edges. Snapshot membership is the previous730 plus exactly the new immutable pair, yielding732; authority membership remains20 and only four admitted Core hashes changed. All231 graph source hashes remain exact. Generation, manifests, integrity, scenarios, full reconstruction, graph validation and deterministic build-check exited0.

The three live snapshot assertions now bind732. Existing graph browser assertions bind the emitted engine, the admitted Product endpoint and independently corroborated selected-edge source-span counts0/2. The historical CSS mutation token, warning/source-removal controls and all other passport expectations are unchanged. Steel1440 English/Arabic tests passed; remaining graph selectors are running. Four affected streptomycin public/simulated English/Arabic PDF tests passed. Threshold, UI,51-module ES syntax, compilation and smoke checks passed.

Detailed original receipts are retained in local recovery mission-20260925/ministry-foundation; the final evidence index will identify the published subset. Earlier RED/pending-generation failures remain history, not successful executions.

## Remaining delivery gates

Canonical capture and narrow baseline pins; exact-candidate full pytest/frozen roots, source and bilingual geometry proof; actual Claude substantive implementation review; separate delegated acceptance; PR with all six required hosted checks; merge and green main. The latest owner workflow removes blanket duplicate local make-ci/44-PDF cohorts. Required repository and hosted checks remain. No new Aura operation or full-MVP acceptance is claimed.

Sanad/Muhasabah: executed subjects and pending gates are separated. Earlier source report said all17 initial RED failures were product bugs; two were fixture AttributeErrors. Its synthetic sorting example chose an alloy node, while the governed defect chose capacity-time-window. The original report is preserved, and these corrections avoid overstating it.


### Canonical execution checkpoint

All42 graph cases passed; four canonical matrix tests passed in391.40s. Four evidence-to-change images change within the reviewed endpoint/identity scope;124unchanged. Baseline successor0cb0932eb283e2cd63206944372fc484e553b7fb and live change_refMINISTRY-F-GATE-PROVENANCE-1 adopted only after inspection. [Evidence](test_evidence.md) separates the final pending checks.

## Replacement after the exact-candidate public API failure

The first candidate failed one unchanged API fixture (4,017 passed / 1 failed). The sole correction returns the typed decision-specific gate mapping only for mapping declarations, restoring the public list-based payload shape without changing simulated gate meaning. The unchanged fixture then passed;95 focused capability/scenario/graph/all-case tests passed. All925 nodes and1,045 semantic edges remain equal to the first generated F projection after only generated IDs/relationship keys are normalized. The API fixture and adapter are unchanged.

The source-bound second allocation generated GRAPH-SAU-2026-09-12-4152824cfa67 (ENGINE-64e24689a59f), SHA-256 ab5890d7af3037e548827b17d498a19ebc221eca5df5941f55b18e3a978a1e60. All seven stock generation/integrity/reconstruction commands passed. Snapshot membership is734: the earlier F pair remains immutable history and the replacement adds two files. The first canonical output and failed candidate remain historical, not final acceptance. Final capture, exact-candidate suite, strict R1 and independent review follow.

The first freeze recorded an ordinary-index stat-cache change during the prohibited-file scanner (causal inference from timestamps); zero staged paths and HEAD-equivalent index content were verified. No byte restoration was attempted. GIT_OPTIONAL_LOCKS=0 is now set for every Git/check process. See the retained index disposition rather than claiming the bytes never changed. No branch commit or Aura operation has occurred.
