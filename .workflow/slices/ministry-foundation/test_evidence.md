# Ministry F verification evidence

The [evidence index](evidence-index.json) records exact copied bytes and original locations. It does not replace retained failures or claim independent approval. The source-only receipt and its later corrections are distinct.

Completed: corrected source regressions; 198 exact-base decision/economic/public-scope comparisons across11cases and22need-provenance comparisons; single graph/manifests generation and five immediate checks; three actual generated-pin tests;42 affected graph browser cases;4 affected streptomycin PDF cases; threshold/UI/ES51/compile/smoke; canonical128image capture. Exactly124 images are byte-identical and4 admitted evidence-to-change images change. The actual output review records graph/native-list reflow from the corrected endpoint; it does not assert old endpoint geometry equality. Fonts/producer/tolerances/dimensions are unchanged;11,743,220total bytes,153,438maximum, within16MiB/600KiB. Baseline subtree pin comes from an alternate index, with the ordinary index preserved.

Pending at this assembly: final exact-tree fullpytest/frozen-source mutation proof and R1 successor; actual Claude substantive implementation review, separate delegated acceptance, allsix required hosted checks, merge and green main. Existing retained Aura proof is unchanged; no F projection has been loaded into Aura. Comprehensive Ministry and final live demonstration proof remains later.

The owner's 2026-09-26 proportionate-verification instruction controls prospectively: no duplicated local make-ci runs or separate44-PDF cohorts for this focused correction. Fullpytest/integrity are retained because this is a cross-cutting domain/graph change and AGENTS requires them. All required hosted jobs remain mandatory; hosted live-Neo4j behavior cannot be certified by offline tests.

Sanad/Muhasabah: PASS for truthful executed/pending separation, not implementation acceptance.

## Replacement after the exact-candidate public API failure

The first candidate failed one unchanged API fixture (4,017 passed / 1 failed). The sole correction returns the typed decision-specific gate mapping only for mapping declarations, restoring the public list-based payload shape without changing simulated gate meaning. The unchanged fixture then passed;95 focused capability/scenario/graph/all-case tests passed. All925 nodes and1,045 semantic edges remain equal to the first generated F projection after only generated IDs/relationship keys are normalized. The API fixture and adapter are unchanged.

The source-bound second allocation generated GRAPH-SAU-2026-09-12-4152824cfa67 (ENGINE-64e24689a59f), SHA-256 ab5890d7af3037e548827b17d498a19ebc221eca5df5941f55b18e3a978a1e60. All seven stock generation/integrity/reconstruction commands passed. Snapshot membership is734: the earlier F pair remains immutable history and the replacement adds two files. The first canonical output and failed candidate remain historical, not final acceptance. Final capture, exact-candidate suite, strict R1 and independent review follow.

The first freeze recorded an ordinary-index stat-cache change during the prohibited-file scanner (causal inference from timestamps); zero staged paths and HEAD-equivalent index content were verified. No byte restoration was attempted. GIT_OPTIONAL_LOCKS=0 is now set for every Git/check process. See the retained index disposition rather than claiming the bytes never changed. No branch commit or Aura operation has occurred.

Replacement capture passed4 matrices (128 images).126 are byte-identical to the first F capture; only the EN/AR desktop evidence-to-change run-ID text changes. Source closure and resource limits pass. The replacement baseline subtree is`dccfb0e104978fbdcbbab54fa1e0f50e63cfe53f`; two bilingual selected-token checks passed. The final exact candidate suite and unchanged strict R1 procedure are the next gates; their receipts and the independent review are recorded separately.
