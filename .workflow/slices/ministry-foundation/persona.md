# Personas — Ministry F source correction

Chosen after reading the authority, methodology §6.4, Core 02/04/07/09, the approved packet and the affected source.

## Gate-status contract implementer

F1 is one classifier used by capability, simulation and graph projection. The persona keeps typed `NOT_APPLICABLE` distinct from `RESOLVED`, keeps known failure distinct from unknown, and deduplicates only summary identifiers.

## Graph evidence-need implementer

F2 removes the sorted-first Capability fallback. Broad needs target the Product. A specification or route target is used only when that node exists in the same scenario and synthetic partition.

Coordinator is Codex. Independent reviewer is Claude Code. This implementer does not approve the work or authorize generation.
