# Read-only public API/UI/dossier shape observation

Senior API-contract and visual-provenance auditor personas. Source under observation: corrected worktree `src/ior_mvp/capability.py` SHA-256 `53959e6fda4deb8ec71b70edb7007d81866762543795de185820848c27c30369`, compared with immutable delivered-base archive SHA-256 `df28e028a3a730703ef4d198a25dbaf3fe0fd041b0a7ad7b97718d00b9ee4112` for that file. The corrected current graph pointer observed during the probe was `GRAPH-SAU-2026-09-12-4152824cfa67`; delivered base was `GRAPH-SAU-2026-09-12-6f43b1a8c4aa`. No source, generated file, baseline, index or reference was edited.

## Command and measured result

Executed one `GIT_OPTIONAL_LOCKS=0 python3 - <<'PY' ... PY` host probe, spawning the existing worktree `.venv/bin/python -B -c <inline TestClient probe> <literal eleven IDs>` separately with `cwd` set to the delivered-base archive and corrected worktree. Each child set `PYTHONPATH` to its own `src`, `PYTHONDONTWRITEBYTECODE=1`, and `GIT_OPTIONAL_LOCKS=0`. The inline probe used existing `ior_mvp.app.app` routes for all 11 approved F case IDs: `GET /api/opportunities/{id}?mode=public`, `GET /api/opportunities/{id}/ui-manifest?mode=public`, and `GET /api/opportunities/{id}/dossier?mode=public`. It extracted only `analysis.capability`, the `capability_matrix` props, and `dossier.capability_route`; it did not compare entire responses, run pytest/browser/PDF suites, launch a service, or render a new PDF.

All 66 requests returned HTTP 200. For every case and both roots, `decision_specific_hard_gates` is **absent** in all three public capability objects; each root's analysis/UI/dossier capability objects are equal. The base and corrected capability **key sets are identical** for all 11 cases. The only differing capability value field across roots is `unresolved_hard_gates`: steel and polypropylene remain 8 each; the nine H6 cases have the approved F deduplication (10→5, 12→6, or 14→7 according to profile). [Machine-readable per-case evidence](PUBLIC-SHAPE-OBSERVATION.json), SHA-256 `e8aef79743285465577cb81bf8d111c333fa78b3f344bd96c617344d26702d45`.

## Receipt reuse and minimal checks after replacement generation

- The previous four streptomycin public/simulated EN/AR PDF cases (`affected-pdf/RESULT.json`, exit 0) remain evidence for the F known-failure and print-layout behavior: simulated mapped gate output is unchanged by the public-shape correction, and `dossier.py`'s HTML capability section renders selected fields and gate lists rather than the removed empty typed-status key. The current **public dossier JSON** is a different exact object from the earlier F candidate because it no longer carries that empty key; the PDF receipt does not certify new JSON bytes. No repeat 44-case PDF cohort follows from this single correction. This is a source-traced reuse inference, not a fresh PDF run.
- The prior affected graph functional/source-disclosure and CSS-negative browser receipt (`affected-graph/RESULT.json`, exit 0), two steel-1440 EN/AR anchor cases (`pin-adoption/RESULT.md`), and R1 successor remain evidence for unchanged F graph semantics and UI behavior. They are bound to the earlier engine/projection identity, so they are not exact-current-ID proof. The read-only corrected-source comparison in `public-shape-correction/RESULT.md` found equal normalized multisets of 925 nodes and 1,045 edge semantics with only generated identifiers/relationship keys changed. After replacement generation, the minimal identity-sensitive browser check is the existing `test_graph_narrow_panel_contains_controls_and_text` steel-1440 EN/AR selection with its new engine token, followed by the approved R1 successor and canonical source/pixel capture checks. The actual changed image set must be enumerated; the earlier 4-changed/124-unchanged capture is historical and must not be copied as a new result.
- `browser_tests/visual_baselines.py::_source_hashes` hashes top-level Python sources, graph current and projection members, so the manifest source-tree pin must be rebound even if unchanged scenes reproduce byte-for-byte. Repository/hosted required gates remain intact; the earlier full pytest 1-failed/4,017-passed candidate is not a final green receipt. One final exact-candidate full pytest after generated pins is justified by that failure; blanket duplicate local browser/PDF cohorts are not.

The existing exact no-candidate fixture test is green after correction. The normal-public `tests/test_api.py::test_detailed_analysis_exposes_additive_public_snapshot_v2_contract` checks only a subset of analysis fields and `test_public_manifest_omits_economics_panel` checks panel presence; neither alone would catch an extra capability key. This 11-case existing-API observation closes that specific gap without a new test or framework.

Sanad: observed API responses from the two specified roots, corrected and base capability source hashes, `PUBLIC-SHAPE-OBSERVATION.json`, approved F §2–3, `public-shape-correction/RESULT.md`, prior PDF/graph/capture/R1 receipts, `dossier.py` field selection and `visual_baselines.py` source-hash list. Assumption: the replacement graph remains semantically equal as the corrected-source in-memory comparison predicted; exact generated output and pixels require coordinator checks. Muhasabah: PASS for this read-only note—public summary changes are preserved, old receipts are scoped to behavior rather than represented as exact-current-source proof, and no new delivery approval is asserted.
