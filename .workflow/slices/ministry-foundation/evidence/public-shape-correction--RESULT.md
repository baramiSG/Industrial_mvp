# F public capability-shape correction — source only

Status: stopped after one admitted `src/ior_mvp/capability.py` edit and read-only checks. Senior backend test-engineer persona; applied project-orientation and task-standards from earlier F work, systematic-debugging (recursive root-cause trace), TDD (unchanged meaningful fixture RED/GREEN), github-flow's source hygiene, sanad-provenance and muhasabah-gate. No frozen fixture/adapter, public validator, UI, graph implementation, authority, generated artifact, baseline, Git index/ref, or proof-copy edit; no implementation approval.

## Root cause and correction

The exact frozen proof's full pytest result was 1 failed / 4,017 passed at `tests/test_api.py::test_no_candidate_expected_payloads_match_engine_output`. Read-only recursive comparison of the original no-candidate fixture found precisely two actual-only empty mappings: `/analysis/capability/decision_specific_hard_gates` and `/ui_manifest/components/5/props/decision_specific_hard_gates`. `decision_engine.analyze_public` passes a list of gate names into `evaluate_capability`; the F implementation had added the typed mapping key unconditionally, and GenUI copies that capability object to the matrix props. The frozen UI1.3 expected adapter remains its approved two-field version-delta adapter.

The sole source edit conditionally adds `decision_specific_hard_gates` to the returned capability object only when the declaration argument is a mapping. The list path retains its unresolved/known calculations and original public output shape; the mapping path retains typed statuses, including missing required simulated gates. No threshold, formula, source declaration or validator changed. Source preimage SHA-256 `8bc80f7ec7c83d7008060a2b4597c85c5bfca5687ce3d48106b809c0ce4d62ce`; corrected SHA-256 `53959e6fda4deb8ec71b70edb7007d81866762543795de185820848c27c30369`. `git diff --check -- src/ior_mvp/capability.py` exited 0.

## Executed checks

- Unchanged exact no-candidate API fixture test before edit: **1 failed** at `tests/test_api.py:417`. Same test after edit: **1 passed** (one existing Starlette/httpx deprecation warning). [Recursive diff evidence](RECURSIVE-DIFF.json), SHA-256 `3ade66ed812e9c244171ced90e54ac60d77ea39c4eaff718fb390c5a9f80cc2b`, records the original two paths and **zero after-correction differences** across the whole list-entry, analysis and UI-manifest object.
- Existing focused `tests/test_capability_economics.py`, `tests/test_scenario_contract.py`, `tests/test_graph_projection.py`, and `tests/test_s17_generation.py::{test_all_eleven_public_and_simulated_outcomes_remain_unchanged,test_ordered_22_branch_gate_lists_and_decision_equality}`: **95 passed**. These cover F-R1 omitted/partial/mixed gates, mapped statuses, graph scopes, all 11 decisions and the ordered 22 branch gate lists. No full 4,018-test suite rerun was made after the edit.
- `scripts/verify_integrity.py`: **INTEGRITY PASS** for existing snapshot and authority manifest files. `python -m ior_mvp.graph validate`: **GRAPH VALIDATION PASS** for the stored immutable `fd70279732aa` artifact. These check existing artifacts, not agreement with the corrected source.
- Expected pending source-bind failures: `test_s17_generation.py::test_in_memory_g17_is_deterministic_and_writes_nothing` fails only at the current projection ID pin (`4152824cfa67` in-memory versus stored `fd70279732aa`); `python -m ior_mvp.graph build --check` exits 1, `GRAPH BUILD FAIL: committed projection differs`. Do not label these gates green until authorized regeneration, manifest/pin replacement, and rechecks.

## Read-only graph semantic comparison

`discover_inputs` still enumerates 231 rows. The **only** changed row is `src/ior_mvp/capability.py`: byte count 8,364→8,447 and SHA-256 preimage→corrected hash above. The in-memory projection is `GRAPH-SAU-2026-09-12-4152824cfa67`, engine `ENGINE-64e24689a59f`, versus stored `GRAPH-SAU-2026-09-12-fd70279732aa`, engine `ENGINE-bd1cbfb71689`. Authority-basis SHA remains `84f3f582c963e9f5773901541bf5afb8fadef2212123454515834eeee7cb7d68`; counts remain 925 nodes / 1,045 edges with identical per-label/per-type counts.

After replacing only those two observed projection IDs and two observed engine-run IDs with comparison markers, all 925 node records form an equal multiset. For edges, excluding only the top-level and `properties.key` relationship hashes, all 1,045 normalized semantic records form an equal multiset. The normalized non-input header is equal; 308 relationship keys changed and 737 remained common. Thus the graph-semantic comparison found no other node/edge difference, while explicitly preserving the unregenerated identity/hash difference as pending.

Sanad: exact frozen proof pytest log, read-only recursive fixture reproduction, unchanged RED/GREEN test output, focused 95-test output, `discover_inputs`, canonical stored projection and new in-memory projection comparisons, integrity/graph command exits, source hashes. Muhasabah: PASS for this bounded source correction. The comparison normalizes only dynamic identities and relationship keys; it does not claim generated artifacts, current-source provenance, full-suite green, visual pins, CI, or independent review are complete. A fresh source-bound generation allocation is required before those claims.
