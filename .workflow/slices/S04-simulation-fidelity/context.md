# Context — S04 Simulation-Branch Fidelity

## Base and branch
- Base: `main` after S03 squash merge (record SHA in plan Task 0). Branch: `slice/S04-simulation-fidelity`.
- State after S03: 191 tests; gates = prohibited scan → threshold scan → compile → node → integrity → scenario validation (Gate B) → pytest → smoke; thresholds 1.1.0; evidence_policy 1.1.0; `evidence.reconcile_synthetic_scenario` + `scripts/validate_scenarios.py` exist and must be extended (not duplicated) for the ground-truth back-test.

## Exact defects to close (Supervisor findings, 2026-09-02)
1. `decision_engine.analyze_simulated` dispatches on `opportunity_id == "SAU-H0-721049"` / `"SAU-H0-390210"`; `_simulate_steel`/`_simulate_pp` embed conditions, kill conditions, headlines, route labels and rationale as string literals (KL-08). Core 06 §5.3/§10.4–10.5 require each scenario to carry an explicit intended route so the engine can be back-tested; Core 07 §7.3 (ADVANCE conjunction → route 5) and §7.4 (equivalent qualified availability ≥ target demand → REJECT route 0) are the two selection rules and are generic.
2. Simulated `rules` is the unchanged public ledger; R6/R7/R8 stay `DISABLED` although the scenario supplies the inputs Core 02 §3 says the simulation "resolves" (KL-07). Thresholds already exist: `R6.minimum_effective_utilisation 0.85`, `R6.minimum_spec_matched_shortage 0.10`, `R7.maximum_effective_utilisation 0.70`, `R7.require_specification_equivalence true`, `R8.minimum_probability_adjusted_demand_addition 0.20`, `R8.minimum_mes_fill 0.25`.
3. Core 09 §2.7 frontend contract checks "public and simulated states can display together" and "dossier action exists" have no tests (TL-07 partial).

## Fixture arithmetic the plan must reproduce (no data value changes)
- Steel: effective capacity 250×0.92×0.94×0.38×0.70 = 57.5092 kt; target 104 → gap 46.4908; D\* 0.2667 publishable; S\* 18; ΔNV 198; ratio (57.5092+50)/100 = 1.075 ≤ 1.25 → ADVANCE route 5. R6 synthetic: utilisation 0.89 ≥ 0.85 AND shortage (104−57.5092)/57.5092 = 0.8084 ≥ 0.10 → fires; "sustained period" not evaluable from one synthetic observation → execution DEGRADED, fired true, metric `sustained_period: NOT_CALCULABLE`. R7 synthetic: 0.89 > 0.70 → does not fire; equivalence block absent → `specification_equivalence: NOT_CALCULABLE`; execution DEGRADED. R8 synthetic: no base demand, no MES, no probability → NOT_CALCULABLE; execution DISABLED with reason; metrics disclose layers committed 74 / announced 22 / target 104 / downside 100 (Core 02 §3 "separate demand layers").
- PP: formula capacity 1170×0.93×0.97×0.18×0.55 = 104.4 kt (currently `formula_capacity_kt`); equivalence true, qualified available 80 ≥ target 56 → gap −24 → REJECT route 0 (§7.4); support 0. R6 synthetic: 0.78 < 0.85 → not fired (also shortage negative). R7 synthetic: 0.78 > 0.70 → not fired although equivalence true; execution FULL (both inputs present). R8: NOT_CALCULABLE (committed 0, announced 0, no base/MES).
- Decide and document the R6 shortage denominator (effective qualified capacity, per Core 07 §3 R6 "specification-matched demand exceeds effective qualified capacity by ≥10%"). Do not invent a "sustained" window.

## Governed change (the ONLY hand-edits to hashed files)
- `data/synthetic/SYN-MINISTRY-STEEL-001.json` and `SYN-MINISTRY-PP-001.json`: add `scenario_version: "1.1.0"`, a `ground_truth` block (`expected_simulation_state`, `expected_route_code`, `basis` citing Core 06 §5.3 / Core 07 §7.3 or §7.4), and a `decision_narrative` block carrying VERBATIM the texts currently in `decision_engine.py` (steel: headline "SIMULATED ADVANCE — brownfield specification upgrade", route label, rationale, the three conditions and three kill conditions from Core 07 §8; PP: headline "SIMULATED REJECT — no generic specification-adjusted gap", route label "No intervention", rationale, condition, kill condition) plus the INVESTIGATE fallback narrative used when §7.3 controls fail. No numeric value may change. `seed_basis` unchanged. Then one `scripts/build_manifests.py` run after full regression (Gate B incl. back-test PASS, threshold scan, pytest, smoke); `snapshot_manifest.json` may change only the two synthetic entries (+ `generated_on` if the date differs); `authority_hashes.json` must not change (only `generated_on` if any). ADR-006 → Accepted with final field names. Evidence policy 1.1.0 remains valid (extra fields allowed; `synthetic_inputs` unchanged as a mapping) — confirm `validate_synthetic_scenario` does not reject unknown top-level keys.
- Public snapshots, configs, `docs/core/**`, DOCX unchanged.

## Design constraints
- One generic `_simulate(public, scenario)` (or equivalent) replaces the two ID-specific functions. Selection: if `synthetic_inputs.equivalence.domestic_grade_equivalent` is true and `qualified_available_kt >= demand.target_spec_demand_kt` → REJECT route 0 (§7.4); else compute gap = target − effective capacity and apply the §7.3 conjunction (gap > 0; `route_publishable`; `d_star <= capability.route_bands.incremental_upgrade_max`; economics passes; ΔNV positive; `competition_warning` false) → ADVANCE route 5; else INVESTIGATE (route None). Economics/competition blocks: when `economics.cash_flows_without_support` exists compute S\*/ΔNV/ratio as today; when only `economics.support_required`/`reason` exists (PP) report `passes False`, `minimum_effective_support_m 0.0`, reason, and competition `passes_default_warning True` with the existing finding text moved to the scenario narrative or kept as engine text (state which — no invention).
- Runtime back-test: after selection, compare (state, route_code) with `ground_truth`; on mismatch raise `EvidenceIntegrityError` (fail closed, 422) and include `integrity.ground_truth_backtest` {expected, actual, match} on success. Gate B (`validate_scenarios.py`) adds the same back-test per scenario by running the engine; a mismatch is FAIL.
- Synthetic rule rows: `rules.evaluate_simulated_rules(scenario, public_case, capacity, thresholds)` returning R6/R7/R8 rows with `synthetic_flag: true`, `evidence_class: "D"`, `display_label`, `basis: "synthetic"`, and the same `_rule` schema; appended to `rules` in simulated mode only. Public-mode leakage test: no rule row has `synthetic_flag` true. UI: rule ledger renders synthetic rows with `.synthetic-row` and a SYNTHETIC chip (existing classes); dossier `gap_diagnosis` or a new `simulated_rules` block lists them; HTML prints them under the disclosure.
- Keep response keys used by the frontend and golden tests: `capacity.effective_qualified_capacity_kt` (steel), `capacity.specification_adjusted_gap_kt` (both), `capacity.target_spec_demand_kt`; PP keeps `formula_capacity_kt`, `qualified_available_kt`, `domestic_grade_equivalent`. Additive changes only.
- No new numeric literals in comparisons (threshold guard); read `thresholds["rules"]["R6"/"R7"/"R8"]`.
- Frontend contract tests (TL-07): assert `renderIntegrityBanner` renders both `real_state` and `active_state`; assert a `data-dossier-html` action exists in `renderDecisionActions`.
- Execution conventions as before; the shell channel is intermittent — the Implementer must use the detached-script + log-read protocol for every command. Commit/push/PR/merge Supervisor-controlled.
