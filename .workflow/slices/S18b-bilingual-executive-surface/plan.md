# S18b bilingual executive surface implementation plan

> For agentic workers: use the installed executing-plans or subagent-driven-development skill after independent plan approval. Root coordinates; a distinct implementation subagent writes; a separate reviewer approves the exact candidate. Do not make task-level source commits before that approval. Generic skill defaults yield to the owner's overnight workflow.

**Goal:** deliver the governed eight-step AR/EN executive journey, analyst evidence anchors and computed integrity, with reviewed visual proof and unchanged industrial decisions.

**Architecture:** existing SPA fallback selects a dedicated executive module; executive summary/case contracts own calculated content, while selected-case analysis responses supply only existing localized detail and passports. A fixed registry renders approved components and a context epoch prevents stale branch/case/locale content.

**Tech stack:** existing FastAPI, vanilla named-export ES modules, YAML bilingual catalogue, Noto fonts/tokens, pytest/Playwright/axe, uv and pinned canonical Chromium image. No dependency addition, CDN, new framework or runtime external HTTP source.

**Spec:** sibling `S18B-EXPERIENCE-DESIGN.md`, current `.workflow/slices/S18-executive-mode/design.md` as amended explicitly here, governing Core01/03/04/09 and methodology §§1,8,9,10,15.

Status: PROPOSED v3; addresses plan-review findings on live successor pins, capture-helper provenance and strict graph identity geometry. Previous v1/v2 retained in revisions/. No source changes authorized by this file alone. Coordinator records the separate review and delegated acceptance outside the candidate, then copies exact reviewed plan/design bytes into the new slice record. Parent S18a approved packet remains immutable.

## Global constraints

- All11 real decisions, simulated outcomes, route selection and frozen scenario/snapshot/golden bytes remain unchanged.
- Public evidence and Class-D simulation have incompatible display boundaries; both policy labels appear on every simulated result component.
- No threshold literals, unsupported metrics, recomputed route selection, synthesized need codes or EVSI dataset inference.
- Missing economics/route/EVSI stays null with typed availability; never format missing as zero.
- Executive modules each <200 lines, Python modules <=500; all CSS values use tokens; existing test scanners remain active.
- Source branch `slice/s18b-bilingual-executive-surface` is created in a new isolated worktree only after S18a merge and required target-branch CI; preserve main/S18 uncommitted work and alternate indexes.
- Single writer; one independent plan-review agent and a distinct exact-candidate implementation-review agent, both explicitly same-model Codex tonight. No attribution to Claude.
- Generated artifacts require the separately recorded operation bindings in Task6; no generator is a generic repair command.
- No S19/S20/S21/Aura mutation inside this UI candidate. Aura can consume the final accepted canonical projection in its separate reviewed operator packet.

## Review focus

1. Case A response arriving after case B or failed locale switch must never restore wrong identity/branch; tested in Task2.
2. Missing/contradictory/foreign claim references must never create a source link or hide disagreement; tested Task3.
3. Available numeric zero differs from unavailable EVSI/economics, especially PP/S15 cases; tested Task2/4.
4. Arabic fallback must not silently display English generated narrative as Arabic content; tested Task4.
5. Regenerated graph/canonical captures can change provenance identities without changing decisions; Task6 binds all inputs before writing and proves semantic equivalence.

## Explicit amendment decisions requiring reviewer agreement

A1. Permit **selected-case only** analysis reads for presentation detail absent from executive API. Counts/integrity/claim membership/summary EVSI still come solely from executive API; no every-case browser aggregation.

A2. Supersede only S18b's all112-old-images-identical promise with exact old path/matrix/producer/tolerance/budget preservation and reviewed diffs confined to declared additions, authority labels and run IDs. Exactly16 new images,128 total. S18a historical receipt/AM3 exception stays unchanged.

A3. S18b Core03/Core09/UI catalogue are graph inputs. Add a new write-once graph projection + snapshot/authority manifests before one canonical visual allocation. This closes the original S18b design's omission of unavoidable graph provenance closure. Node/edge semantics/counts expected unchanged925/1045; deviations stop.

A4. Correct KL132's Core09 expectation and the exact test pin **before** graph identity binding, not after. Preserve AM3/AM4 historical wording.

A5. Explicitly track all AM4§5 residuals using the disposition table below. This frontend candidate does not silently claim API schema/model robustness fixes. A later contract follow-up has its own plan/review/red-to-green; current failure exposed by a required S18b gate is blocking and returns for an amendment.

## Permitted source and test boundaries

Create under `src/ior_mvp/static/modules/executive/` only these modules:

- `index.js`: route bootstrap, start/retry lifecycle.
- `context.js`: executive state/epochs, URL parsing and history.
- `data.js`: same-origin API fetch and identity/decision/claim join validation.
- `registry.js`: fixed step/component descriptor map.
- `render.js`: shell, two decisions, active-step region and status.
- `steps.js`: SIGNAL, FALSE_POSITIVE_CONTROLS, PUBLIC_CONCLUSION, MISSING_MINISTRY_FACTS.
- `simulation.js`: SIMULATED_EVIDENCE, INTERVENTION, CONDITIONS_AND_KILL.
- `routes.js`: route0–8 table and expandable existing reasons.
- `summary.js`: four vectors, public dataset counts and separate EVSI summary.
- `evidence.js`: claim links, source region and return-focus lifecycle.
- `labels.js`: exhaustive executive step/vector/value/dataset/route keys.

Create `static/modules/claim-links.js` for common claim-to-evidence anchors and `static/css/executive.css` for scoped executive styling. Modify only these existing product files: `static/app.js`, `static/index.html`, `static/styles.css`, `static/css/tokens.css`, `static/modules/{api,state,events,workspace,portfolio}.js`, `static/modules/renderers/{decision,rules,evidence}.js`. If events.js would exceed200 lines, create `static/modules/analyst-navigation.js` to own new query/deep-link and anchor event behavior. No graph renderer/style change; existing graph open API reused through analyst context.

Modify catalogue `config/ui_strings.v1.yaml`1.5.0→1.6.0, adding exact EN/AR keys only. No evidence_policy/threshold/profile/narrative change. If an absent governed narrative blocks Arabic parity, submit an exact-key catalogue amendment; don't invent translation in the browser.

Create `browser_tests/executive_pages.py`, `test_executive.py`, `test_executive_states.py`, `test_executive_accessibility.py`, `test_executive_races.py`; create `tests/test_executive_frontend_contract.py`. Modify `browser_tests/{harness,test_accessibility,test_journeys,test_visual_baselines,visual_baselines,parity_grammar}.py` only for added navigation/claim control identities, source-language classification and 16 executive captures; preserve existing assertions except exact expected new controls/matrix. Modify `tests/{test_ui_catalogue,test_ui_tokens,test_static_frontend,test_visual_baseline_contract,test_frozen_public_evidence_pins,test_executive_isolation,test_s17_status_docs}.py` for legitimate new catalogue/registry/matrix and KL132 contract pins. In test_s17_status_docs.py change only the three live-successor assertions (snapshot count726, visual count128, exact new S18b change_ref) while preserving every historical S17 assertion. Snapshot count is an observed final exact value; a different count requires diagnosis/review, not forced manifest edits.

Documentation: Core03§7; Core09 new S18b acceptance and KL132 historical correction; Core02 methodology implementation mapping (S18b only). The original S18b parent allowed Core03/09, so Core02 mapping is an explicit necessary documentation expansion in this reviewed proposal. `docs/implementation/{UX_GENUI_DEMO_SPEC,API_REFERENCE}.md`, `docs/{BUILD_ROADMAP,BUILD_PROGRESS,REQUIREMENTS_TRACEABILITY,ARCHITECTURE_DECISIONS,KNOWN_LIMITATIONS}.md`, README's executive entry instructions, `.workflow/state.json`, new `.workflow/slices/S18b-bilingual-executive-surface/{persona,context,design,plan,plan_review,implementation_log,test_evidence,implementation_review,reviewer_findings,pr_record,completion}.md` as truthful stage records. Coordinator owns control-record finalization.

Generated paths, and nothing else: `data/graph/current.json`; two files `data/graph/projections/<bound-new-id>/{projection,manifest}.json`; `data/manifests/snapshot_manifest.json`; `docs/authority/authority_hashes.json` and generated Manifest§11; baseline112 existing paths+16 explicit new executive paths and baseline manifests; exact visual-tree/count pin. No pre-existing projection file, public/synthetic/golden/raw/document/entity/screening artifact changes.

## Task1 — route shell and governed vocabulary

Consumes `state.ui`, `loadLocale`, `t`, existing DOM/formatters and exact executive step/vector enums. Produces `initExecutive()` named export; path bootstrap dispatches only `/executive` to it and keeps normal analyst init unchanged. Query defaults come from summary opportunity order, not product-ID dispatch.

- [ ] Copy reviewed plan/design to new slice record; freeze worktree/base/control ownership evidence.
- [ ] Add failing direct-load test at `/executive?opportunity=SAU-H0-721049&step=SIGNAL&locale=ar`: assert one executive main, html ar/rtl, eight ordered step controls, explicit Analyst link, absence of analyst-only mode-toggle. Before implementation this fails because SPA currently renders analyst.
- [ ] Add bilingual keys for all11 fixed executive components, statuses, errors/retry, eight steps/four vectors, six dataset kinds plus UNMAPPED, nine routes and all displayed metrics. Preserve both synthetic labels exclusively in evidence_policy response.
- [ ] Build shell using existing token colors and fonts; append token values only when needed by executive scoped CSS. Add one explicit Executive anchor to analyst nav, preserving current case/locale query. Native links must work without click-event interception.
- [ ] Require source scanner/catalogue parity/ES module gates and direct-load test GREEN. No canonical generation yet.

Contract pattern:
```js
export async function init() {
  await loadLocale(resolveInitialLocale());
  if (window.location.pathname === "/executive") {
    const { initExecutive } = await import("./modules/executive/index.js");
    await initExecutive();
  } else {
    await initAnalyst(); // extracted existing initialization, same behavior
  }
}
```
`initAnalyst()` is a named function in `static/app.js`; it contains existing bind/load calls and no new domain logic.

## Task2 — atomic presentation context and eight steps

Interfaces in `context.js`:
`readExecutiveLocation(summary) -> {opportunityId, stepId, locale, invalidOpportunity}`;
`nextExecutiveEpoch() -> integer`;
`isCurrentExecutiveEpoch(epoch) -> boolean`;
`writeExecutiveLocation(selection, {replace})`.

Interface in `data.js`: `loadExecutiveContext(opportunityId, summary) -> Promise<{summary, executiveCase, publicAnalysis, simulatedAnalysis}>`; simulatedAnalysis may be null only if executive branch unavailable. Summary fetched once per route session/retry; fetch executive case and public analysis together, then fetch simulation only when availability says it exists. Reject identity/snapshot/scenario mismatch, real_decision inequality, mismatched state/route and unknown claim/evidence references. Scalar/null formatting reuses availability; no domain derivation.

- [ ] RED tests for all11 case IDs in both locales, exact eight steps/four vectors, public/simulated state/route equality against API fixtures. Use actual responses as oracle, not duplicated frontend calculations.
- [ ] RED delayed A→B response race, locale-fetch failure and browser Back/Forward. Response-order fixture returns actual frozen responses with controlled network delay; never modify canonical evidence.
- [ ] Implement fixed registry descriptors from known step IDs. The renderer rejects an unknown component type with an error status; never interpolates arbitrary HTML/type/key.
- [ ] Implement the eight step contracts in design table. Preserve engine order of route rows; show9 routes, precedence and status per branch. Collapse long exact reasons with native details; avoid flattening them into green/red only.
- [ ] Use existing localized_narrative/narrativeEntry and route.localized_reasons. Public/comparison header links to decision.public; simulated to decision.simulated. Missing public preferred route remains NOT_CALCULABLE even when real route0 is valid.
- [ ] GREEN complete step tests, branch warnings and no UI/domain arithmetic tests. Explicit intervention checks: PP support is0; PP absent NPV is null display, S15 EVSI rows UNAVAILABLE, steel linked contradicted claims visibly CONTRADICTED.

Behavioral test shape (adapt fixture imports, retain these assertions):
```python
@pytest.mark.parametrize("locale", ["en", "ar"])
def test_all_steps_preserve_real_decision(browser_session, locale):
    page = browser_session.page
    page.goto(f"/executive?opportunity=SAU-H0-721049&locale={locale}")
    for step_id in ("SIGNAL", "FALSE_POSITIVE_CONTROLS", "PUBLIC_CONCLUSION",
                    "MISSING_MINISTRY_FACTS", "SIMULATED_EVIDENCE",
                    "ROUTE_COMPARISON", "INTERVENTION", "CONDITIONS_AND_KILL"):
        page.locator(f'[data-executive-step="{step_id}"]').click()
        expect(page.locator('[data-branch="PUBLIC"]')).to_have_attribute("data-state", "INVESTIGATE")
        expect(page.locator('[data-branch="SIMULATED"]')).to_have_attribute("data-state", "ADVANCE")
        expect(page.locator('[data-active-step]')).to_have_attribute("data-active-step", step_id)
```
Use absolute harness base_url when required; implementation may adapt selector details once in executive_pages.py without relaxing assertions.

## Task3 — claim-to-source navigation and analyst integration

Interfaces: `claimLink(claimId, context) -> escapedHtml`; `evidenceAnchor(branch, opportunityId, evidenceId) -> collision-free id`; `openClaimEvidence(claimId, trigger)` validates claim before rendering exact rows; `closeClaimEvidence()` restores trigger focus if still attached. Encode each segment with explicit boundaries; no raw CSS selector concatenation from evidence IDs.

- [ ] RED source drill tests on metric.trade, rule.R1-F/R4-F/R4-D, decision.public and decision.simulated. Assert returned IDs exactly, expected `document.activeElement`, current locale, contradiction retention and public absence of synthetic rows.
- [ ] RED malformed/foreign missing-ID response fixture: no invented source link or first-row fallback; unresolved claim shows actual missing_need_codes. For broad R12 residual explicitly label “case-wide unmet needs,” not rule-specific proof.
- [ ] Add claim links only after executiveCase/current analysis identity matches; matching evidence IDs resolve to current public or current-scenario full passports from selected analyses. A simulated claim may retain linked public rows but each row preserves its actual branch/class/source.
- [ ] Reuse safeExternalUrl and existing passport rendering for full source/date/period/contradiction/address where stored. Do not copy a foreign aluminium scenario row into local claim context; graph link explains dependent-case traversal separately.
- [ ] Analyst load adds executive-case request guarded by existing context epoch; links bind metric cards/rule rows/decision narrative without changing values/order. Evidence rows receive stable `tabindex=-1` IDs. Source endpoint failure displays source linkage unavailable; never old case links.
- [ ] Replace portfolio `integer(0)` with executive summary computed integrity violation_count; expose PASS/FAIL/unavailable as distinct labeled states. Fetch summary once, not every case. IntegrityFAIL response fixture must show nonzero count and affected case.
- [ ] Add validated analyst deep-link parsing in new analyst-navigation module for opportunity/mode/graphView; explicit graph link opens existing component only after matching analysis. Unknown values ignored/rejected visibly as appropriate; no arbitrary graph query.
- [ ] GREEN existing analyst journey tests and new anchor tests. Update keyboard order with precise new links and preserve every prior control's relative order; do not replace exact assertions with a loose “some controls exist.”

## Task4 — accessible bilingual state and rendered experience proof

- [ ] RED fixtures: initial loading, summary transport error/retry, selected detail422, not-found query, no simulation, null economics, EVSI unavailable, UNMAPPED dataset, zero-count dataset, contradicted claim, unavailable graph, malicious text/URL, all required stale races.
- [ ] Render localized actionable states, source verbatim language labels and technical isolation. For UI-controlled prose require Arabic catalogue/narrative coverage; English exact source excerpts use explicit source caption/language rather than disguised code tokens. No custom translation of source spans.
- [ ] Playwright axes: 11cases×2locales for decisions; all8steps×2locales on steel; PP rejection+S15 unavailable EVSI; keyboard/focus/axe at1440/1024 and responsive no-page-overflow390/1920/2560. Test escape/return source navigation and reduced-motion state. Use harness console/network/axe guards without exemptions for unexplained errors.
- [ ] Run complete `make e2e-functional` before any baseline update. Preserve expected provenance/integrity failures separately until authorized generation; a new unrelated test failure requires investigation/reviewed correction.
- [ ] Manually inspect actual rendered screenshots: all8steps EN/AR at1440, route/unlocks/conditions at1024+390, four failure states EN/AR; inspect both full journeys using browser controls. Retain screenshots, URLs, state/case/locale, observer notes and any correction diff. Green tests alone are insufficient.
- [ ] Reviewer checks usability: public/simulation boundary in first viewport, legible Arabic wrapping/IDs, all9 routes discoverable, source return focus, zero/unavailable distinction, no exaggerated unsupported claims. Fix valid findings before generation.

## Task5 — records and explicit residual handling

- [ ] Update Core03 fixed registry and data-flow explanation, Core02 mapping, Core09 S18b test/visual contract and historical KL132 sentence. Add new ADR after checking existing highest ID (expected next ADR029; never overwrite an existing ADR).
- [ ] Record A1–A5 amendment rationale, authority classification, all11 unchanged outcomes and generation operation requirement. This is governed implementation/UI-contract work under existing owner Core-v2 delegation, no methodology DOCX/threshold/evidence change.
- [ ] Update README/UX/API instructions for direct executive URL, source drill, loss of external graph availability, public/simulated comparison and honest EVSI. Do not retrospectively change S18a receipt or earlier approvals.
- [ ] Add the residual table below verbatim in substance to KNOWN_LIMITATIONS/new follow-up record; final report distinguishes resolved vs open, never marks all AM4 observations “fixed.”
- [ ] Before freezing, fill exact achieved test/source facts in non-hashed controls; no final candidate tree may embed its own OID. Later review/CI delivery receipts external, followed by separately reviewed post-merge docs-only updates if needed.

## Task6 — graph, authority and canonical visual operation packet

This task is coordinator-owned mechanical generation after separate operation review; implementer supplies pre-generation source and diagnostics then pauses. No automatic retries or output-identity forcing.

1. Snapshot all approved predecessor hashes, ordinary/alternate indexes, frozen public/synthetic/golden trees, baseline112 image manifest and historic projections. Run `discover_inputs()` and visual `_source_hashes()` to identify exact changed paths. Allowed graph deltas: Core02/03/09/UI catalogue only in this plan (verify no accidental top-level source changes). Graph semantics925/1045 expected unchanged; engine run IDs/input identities may change.
2. Finish ALL graph-hashed files plus visual-hashed static/catalogue/test files. Run focused/full pre-generation regressions; preserve expected stale Core/graph/visual pin failures with names and zero unexplained failures. Do not weaken pins to run green.
3. Read-only `build_repository_projection()` emits diagnostic identity; record full input manifest, normalized semantic comparison, counts and old/new engine run IDs. Coordinator and separate reviewer bind that exact emitted identity and authorize one builder invocation in additive owner-control operation record.
4. Generate write-once graph once. Old projection directories remain byte-identical; pointer new ID equals bound identity. Stop on count/semantic mismatch; no Aura load here.
5. Run existing manifest builder once after docs and graph are final. Expected snapshot inventory increases724→726 for two new graph files, old rows unchanged except current-pointer; authority20 rows, changed hashes only Core02/03/09 and ui_strings. Expectations are audited, never forced. Mirror generated Manifest table. Immediate integrity, full reconstruction, graph validate/build--check; post-generation discovered inputs must exactly equal recorded inputs.
6. Canonical visual update authorized separately with a new S18b change reference. Preserve image/chromium/fonts/tolerance/budgets. Before any graph/visual binding, extend visual_baselines._source_hashes to include executive_pages.py and every new helper imported transitively by test_visual_baselines for executive captures; independently assert those source entries in test_visual_baseline_contract. Do not hash generated manifests or create a self-hashing cycle. Add four explicit journey-j scene names from parent design and no others. Single invocation captures128 images after functional proof. Record all112 before/after hashes,16 additions, pixel diffs and detailed manual review of every changed old path; unchanged affected-neutral paths must be exact. Stop on unexplained pixels. Use existing approved canonical image, not a new browser version.
7. Measure graph run-ID-only effects on the same finalized S18b layout: capture untouched candidate DOM twice (repeat determinism), then an identical layout with only the precisely bound new run ID rewritten to its old value, and a round-trip rewrite back. Bind exactly12 relationship rows and5 run-ID rows in each locale, with exact normalized ordered tuples. Each forward rewrite changes exactly5 text nodes, one run-ID token per node, and no other text node; the round trip restores every recorded field. Retain all R1 row/text/section/button boxes, line bands, clipping metrics and full producer identities. At0.01 precision, section/button boxes, line bands and clipping must remain unchanged; allow only inherited locale-specific span/small width and shared-edge changes within those5 rows. Untouched repeats must match every recorded field. Any violation stops for a separately reviewed operation amendment; no waiver for identifier-induced wrapping or clipping. Separately compare S18a and S18b full-screen images against declared UI changes, without asserting absolute cross-layout graph geometry equality. The controlled rewrite diagnoses ID effects and never alters production/canonical screenshots.
8. New temporary alternate index computes baseline subtree OID; update only that frozen pin and entry count128. Preserve all pre-existing alternate indexes. No graph/visual-source file may change now; required changes reopen operation planning, and any retry needs an explicit newly reviewed allocation.
9. Freeze exact candidate including final slice/source docs. Materialize clean proof root(s), ensure HEAD tree matches, no tracked drift. Preserve pre-generation and final diagnostics as separately hash-bound records.

## Task7 — exact candidate gates, independent review and delivery

Commands use `PYTHONPATH=src`, existing locked uv extras and fresh isolated local graph credentials/container/network/volume; never inherit Aura environment.

- [ ] `uv run --locked --extra dev pytest -q tests/test_executive_frontend_contract.py tests/test_executive_isolation.py tests/test_ui_catalogue.py tests/test_ui_tokens.py tests/test_static_frontend.py tests/test_visual_baseline_contract.py tests/test_frozen_public_evidence_pins.py`.
- [ ] `uv run --locked --extra dev python scripts/verify_integrity.py`.
- [ ] `uv run --locked --extra dev python scripts/validate_scenarios.py`.
- [ ] `uv run --locked --extra dev python scripts/reconstruct_snapshot.py --all`.
- [ ] `uv run --locked --extra dev pytest -q` and `uv run --locked --extra dev python scripts/demo_smoke.py`; explicit all11 states/routes/real-decision byte equality and no synthetic leakage.
- [ ] `uv run --locked --extra dev --extra graph python -m ior_mvp.graph validate` and `build --check`.
- [ ] Prohibited/secret, threshold, UI, ES-module and compile scans from existing Makefile, no relaxed exclusions.
- [ ] Complete `make ci` on exact candidate in fresh isolated proof root; second different-path clean root `make ci` for portability (matching S18a proof discipline). Each includes live local graph load925/1045, reload0/0, verify and real graphUI/stopped-service checks, complete browser functional and compare-only visual suite. Inspect results, not just return code.
- [ ] Writer pauses. Separate reviewer receives exact tree/archive, plan/amendment hashes, full diff, all red/green evidence, source/generator/canonical review, screenshots, residuals and proof-root identities. Explicit APPROVE/REJECT/BLOCKED with precise evidence and no self-approval.
- [ ] After APPROVE and coordinator delegated acceptance, stage approved paths only; staged tree==reviewed tree, committed tree==reviewed tree; preserve earlier indexes. Push task branch/open PR with full receiptSHA256, scope/amendments, code/UI/data impact, resolved findings, tests, deferred items, same-model review disclosure and screenshot references.
- [ ] Inspect actual hosted checks/logs on exact PR head, then obtain exact-head reviewer approval. If CI correction changes source, rebuild candidate/run applicable full gates/review; never transfer approval to changed tree. Merge only matching head/checks/protections, verify target-branch CI, publish additive durable receipt/reference record. No release tag; final independent cross-model release approval remains pending tomorrow.

## AM4§5 residual disposition and next contract follow-up

| Observation | S18b UI disposition | Required follow-up / acceptance criterion |
|---|---|---|
| Case EVSI lacks explicit availability marker | Render availability from summary row joined by exact opportunity+scenario; null step scalar remains not calculable. Do not claim API marker added. | Separately reviewed additive ExecutiveCase field/schema change using existing SyntheticEvsiCase; all11 equality,7available/4unavailable, absent simulation, zero EVSI and typed invalid-input tests; Core04/09/schema docs before graph generation. |
| Untyped malformed paths including synthetic_inputs nonmapping | Current frozen valid inputs consumed; UI presents request failure without stale content. No endpoint robustness claim. | Reproduce concrete injections at service boundary (`trade=[]`,missing year/R11, malformed EVSI numeric/key, synthetic_inputs scalar) with raise_server_exceptions=False; exact typed422/EXECUTIVE_INTEGRITY_ERROR; introduce narrow validated extraction, not broad catch-all. Test each original defect RED. |
| EvidenceReference public partition permits synthetic status/source when flagfalse | Renderer validates branch consistency before source exposure as defense; this is not model closure. | Model rejects flagfalse with statusSYNTHETIC/sourceDEMO_GENERATOR/scenario/labels; preserve legitimate public D proxies. Model+API injection fixtures prove no laundering. |
| Trivial rule.R4 and broader validators | Existing AM4 R4-F/R4-D tests retained; frontend drills exact delivered IDs. | Replace nonexistent-R4-only assertion with exact R4-F/R4-D supports; mutation tests for duplicate steps/vectors/IDs, missing refs, branch mismatches, nonfinite numeric EVSI, count/ID uniqueness. Don't mirror implementation blindly. |
| Overbroad R12 needs attached to unresolved R6/R7/R8/R10 | Explicit “case-wide unmet needs” label; no claim that every need supports each rule. | Separate authority-reviewed rule-specific structured mapping only where source contract establishes relation; otherwise keep honest broad context. No parsing prose. |
| Single missing simulated suffix | Keep fail-closed behavior; no waiver for absent evidence row. | Mutation test removing one required suffix from each supporting family; distinguish truly optional EVSI absent from required decision provenance. Relaxation requires exact contract ruling. |
| KL132 historical false byte-equality sentence/pin | RESOLVE in this slice before new graph generation, with exact historic110/112 statement and accepted two-image history. | Review new wording/test, preserve approved prior packet, operation proof passes. |
| KL85/KL107 routed whole analyst shell | Remains OPEN. Executive route added; screening remains analyst section06. | No broader workspace rewrite within S18b; S22 assesses necessity/owner disposition. |

This follow-up is dependency-ready after S18a, but should be a distinct bounded API plan and writer/reviewer cycle if root elects to execute it before S18b. It must not silently expand this frontend plan. The overnight mandate authorizes such reviewed work; it does not itself supply API field definitions or amend frozen contracts.

## Provenance and self-audit

Facts: current S18 design/AM4, Core files, actual executable API/model/static code, graph discover_inputs, baseline source discovery, Makefile and tests were read. A read-only live Python analysis probe verified that selected analyses already contain localized narratives/route rows and current executive projection omits those fields. No browser screenshots, candidate gates or Aura operations were run by the planner. S18a receipt/gate counts are prior implementer evidence, not my independent rerun. Exact source paths/authority hashes are captured in `PLANNING-SOURCES.json`.

Assumptions: source remains at approved S18a until its delivery, validated local data is available, no concurrent writer in the future worktree. Coordinator must bind/recheck actual merged base and source inventory. Risks: analyst-control expansion affects keyboard/visual contracts; full graph/visual regeneration unavoidable; source-only English EVSI next_fact must be visibly attributed; API robustness residuals not all closed in frontend. Requirements coverage: eight steps, both branches, vectors/counts/EVSI/integrity/claims, all11 cases, bilingual/RTL/accessibility/states, graph navigation, preservation, documentation, generation and exact delivery covered. Muhasabah PASS for proposed plan; independent review and implementation still required.
