# S18b AM5 — correction of source/UI review findings F1–F3

> For agentic workers: use superpowers:executing-plans after a different plan reviewer approves these exact bytes and root records delegated acceptance. Writer remains paused. This author does not approve implementation or its own plan.

**Goal:** Make computed executive integrity visible, reject conflicting analyst decision/source joins, and render the existing screening reviewer status faithfully in both locales.
**Architecture:** Correct three existing presentation/join boundaries without changing API models, engine decisions, stored evidence, parity grammar or module inventory. Preserve the accepted parent and AM1–AM4 packets; append this amendment and reviewed findings to the existing slice record.
**Tech stack:** Existing vanilla ES modules, governed bilingual catalogue, Python/Node behavioral tests and Playwright/axe.
**Spec:** Accepted S18B design/plan, Core03§7.1, Core04 executive projection contract, Core09§12, and independent pre-generation R1 REJECT229c695cdf41a235c2e8bf53285007c0deec827b70ef4d66a8f69fad28a26528. This addresses only F1–F3; the early review was not an exhaustive UI approval.

## Preparation, authority and bound starting state

Read/reused full AGENTS, authority manifest and original DOCX5717cbd42acc9947ce5e450013719275acb7ed1470847b21fb2cc547c8ac4ce9, configs/golden invariants, relevant Core and five-control-document preparation. Refreshed authority rule, parent design/plan and accepted amendment chain, current Core03/04/09, actual render/join/caller/model/tests, reviewer probes and writer checkpoint. Owner broad mandate authorizes reviewed bounded correction, not domain changes or self-approval.

After reading adopted API-contract engineer and bilingual-accessibility architect personas: the issues are integrity disclosure, cross-response identity and faithful enum presentation. Actually applied writing-plans, receiving-code-review, systematic-debugging, frontend-design-pro (existing Noto/token/hierarchy/accessibility constraints override its decorative defaults), verification-before-completion, github-flow, Sanad, al-Muhasibi and Muhasabah. No additional installation, new visual framework or decorative image.

Starting source W=/home/barami/projects/ior-worktrees/s18b at commit306a63b4d24266a63b62dd3835c6585ba328bbcd, checkpoint AM4-FULL-DIAGNOSTIC-START.json c08e1021b1e58dfc54f02382f611ca5c2b122a52fcf3d9b1cb5de78542f87347. Planner independently verified all1678 checkpoint files and all27 reviewer evidence entries. Actual inventory is50 JS modules including app.js,23 browser files,76 named browser tests. No new module/file/browser test name is needed.

The complete retained ExecutiveSummary FAIL fixture validates with four ordered checks and actual27 violations; mismatched ExecutiveCase still validates its own schema. Therefore F1/F2 require visible presentation and cross-response consistency, not schema weakening. Eight actual screening passports have status unconfirmed_by_responsible_authority, copied from the stored reviewer_status by screening/snapshot.py. Existing governed copy is the accurate shared meaning.

The full unmodified functional diagnostic has now completed:7failed,613passed,4deselected,1error in1541.43s; actual make exit2 and inner pytest exit1. Planner read the final receipt83bf41ecb05a5a89ac3ee18fa27c13ae94646419d8065a449ac7145cdf2d5cff and actual logb4639381dce9d3aff9d652413abdd0ed2cdcdf8ab8c83011a3bc5b4a71d08d0c. All seven failures have exactly eight F3 label leaks; the six injection tests fail their clean prerequisite before injection. The sole teardown is graph_visual_preflight_root:76 actual96 versus112, within the precise AM4v2 pending-generation classification. No additional failure class is being authorized. F1/F2 remain independent blockers despite this suite not detecting them. The writer reports all1678 hashes/pathset/index preserved and owned processes/port closed; those cleanup claims are attributed, not independently re-signalled by this planner.

## Exact product/test/file boundaries

| File | Permitted correction |
| --- | --- |
| src/ior_mvp/static/modules/executive/render.js | Dedicated integrity region, renderer, journey/failure/clear integration; no domain calculation or step registry change |
| src/ior_mvp/static/modules/executive/index.js | Clear stale summary on a new summary request; integrate summary failure with distinct integrity unavailable state, preserving epochs/history/retry/locale behavior |
| src/ior_mvp/static/css/executive.css | Only scoped integrity notice layout/status styles using existing tokens, logical properties and readable wrapping |
| src/ior_mvp/static/modules/claim-links.js | Selected public/active/simulation state/route/scenario consistency before analyst claim context; existing evidence/claim checks preserved |
| **src/ior_mvp/static/modules/screening/evidence.js** | **Newly admitted source boundary:** exact stored reviewer-status presentation in passportCard only; preserve every other field/anchor/coverage behavior |
| config/ui_strings.v1.yaml | Exactly the six EN/AR pairs in CATALOGUE-ADDITIONS.json under strings; existing metadata/locales/version1.6.0/date and all prior values unchanged |
| tests/test_executive_frontend_contract.py | F1 actual-renderer tests, F2 all11/negative joins, F3 actual-screening-renderer/parity tests described below; existing assertions retained |
| tests/test_ui_catalogue.py | Add only test_am5_integrity_catalogue_additions_are_exact_and_valid: literal six-key/value EN/AR table from CATALOGUE-ADDITIONS.json and unchanged complete validator; previous key/version/malformed tests preserved |
| browser_tests/test_executive_states.py | Extend only three existing named functions listed below; preserve their prior cases/HTTP collector assertions |
| **browser_tests/test_screening.py** | **Expanded test permission:** extend existing journey and clean Arabic parity test bodies for the eight status labels/raw identities; injected-English oracle remains unchanged |

No change to workspace.js is necessary: it already renders null claim context as honest source unavailable. If actual implementation shows otherwise, return concrete evidence for a separate boundary decision. No changes to data.js, models/API/engine/source snapshots, graph source, dom.js, pages.py, parity_grammar.py, label normalization, failure collectors, source-language grammar, tokens/fonts, baseline code, inventory checker expectations or Makefile. No extra browser files/names or JS modules; preserve50/23/76 exactly.

Documentation within the already permitted parent set: append an ADR-030 AM5 subsection in docs/ARCHITECTURE_DECISIONS.md; append focused implementation/test requirements to Core03§7.1 and Core09§12 only; update truthful S18b entries in docs/BUILD_PROGRESS.md and docs/KNOWN_LIMITATIONS.md and existing implementation_log/test_evidence/reviewer_findings/plan_review. Add .workflow/slices/S18b-bilingual-executive-surface/plan-amendment-5.md byte-identical to the approved PLAN.md. Existing plan/design/amendment bytes and historical approvals remain unchanged. No completion/approval claim before actual gates/review.

## Task1 — F1: persistent, accurate executive integrity notice

### Behavior

Add #executive-integrity immediately after loading/error status and before #executive-comparison in the executive main. Render from the current context.summary.integrity, never a calculated client count, a literal zero, analyst KPI, or prior case response.

- PASS: compact visible notice using existing Computed integrity / PASS copy, actual violation_count0, with no failure wording. No new focusable control is necessary.
- FAIL: prominent text-labelled notice with role=alert and existing danger color/border tokens. Show actual total27 in the negative fixture, all four ordered named checks with each actual status/count and every exact affected ID. Failed check names and IDs must be immediately visible, not collapsed. Preserve order/multiplicity; do not replace a supplied count with number of affected IDs.
- Display check names through a literal four-entry mapping to the new catalogue keys. Preserve raw check_id in data-integrity-check attributes, not visible pseudo-code that would collide with English catalogue normalization. Affected IDs and numeric counts use existing escaped/isolated technical rendering.
- UNAVAILABLE: absent/unretrievable integrity produces localized unavailable, never PASS0 or computed FAIL. This is a transport/defensive display state, not a new IntegritySummary enum. A real summary503 retains the existing error/Retry and clears old case panels/claims; on retry, newly fetched summary supplies the notice.
- Loading: clear stale integrity with stale context. Reset executive.summary before a new summary request so a failed retry cannot display the previous PASS. Successful context reuse on ordinary step/locale changes keeps the current bound summary; latest-epoch protection remains intact.
- Valid FAIL is a notice. Keep all eight steps, public/simulation states/routes, source links and navigation usable; do not invent global journey disablement or revise the API result.
- No passive focus stealing, trapping, extra tab stops or color-only meaning. At390/1024/1440 the notice wraps within the page, technical IDs retain LTR isolation and Arabic labels remain genuine Arabic.

Concrete renderer contract: a local/exported renderIntegrity(integrity) in existing render.js updates only #executive-integrity. renderJourney invokes it with context.summary.integrity; renderFailure invokes it from the current summary if available, otherwise null. clearContextDisplay also empties the new region. index.start invalidates the old summary at request start. Neither module changes computed decisions.

### Exact regression obligations

1. Add unit test test_executive_integrity_notice_preserves_computed_checks_and_states in test_executive_frontend_contract.py, parametrized EN/AR and PASS/FAIL/unavailable. Invoke actual render.js with the complete retained fixture/DOM sink and current locale bundle. First assert region presence and actual state/count; then exact names/affected IDs, input JSON unchanged and unchanged decision panels. PASS0 differs from unavailable. For unavailable pass null directly to the renderer and label this as defensive presentation input, not a schema-valid API summary.
2. Before any product patch, run F1 FAIL test against rejected source using a copied test and unchanged complete schema-valid fixture. It must fail on missing integrity output, not a new-key lookup/ImportError/shortened-check fixture. Preserve test order so region absence is the first defect assertion.
3. Extend existing browser test test_analyst_uses_computed_integrity_failure_and_affected_case with surface analyst/executive, locale EN/AR and PASS0/FAIL27. Preserve its analyst KPI assertions, upgrading the old invalid shortened fixture to a complete actual summary: change only overall status/count and REAL_DECISION_EQUALITY status/count/affected_ids. Call ExecutiveSummary.model_validate before route fulfillment; retain all four ordered checks.
4. Executive branch must inspect rendered visible named check rows, exact27 and affected steel ID, role/notice position and unchanged steel public INVESTIGATE/null plus simulated ADVANCE/5. Traverse all eight steps with the notice retained; exercise a native next/back action and source drill/Escape return focus. Check full-page axe/no overflow at390,1024,1440 in both locales, and full executive-subtree Arabic parity with unchanged scanner. PASS control must visibly show0 rather than unavailable and have no failure alert.
5. Extend existing test_transport_failures_are_explicit_without_stale_sources summary case with both initial failure and a retry after a successful summary, proving unavailable without stale PASS/count. Preserve exact503/422 collector records and every existing locale/detail/analyst transport case. If local EN/AR parameterization is added, keep the same function name and assert each locale's actual governed copy; no generic collector exemption.

## Task2 — F2: strict analyst response identity

Update analystClaimContext inside claim-links.js before constructing/exposing a context. Return null on mismatch; do not mutate input, throw a new user-facing exception, infer a route, or fetch another case.

Common boundary: same existing exact opportunity/snapshot; analysis.mode is public or simulated; the public projection is available/PUBLIC/non-synthetic without scenario/Class-D metadata and matches analysis.real_decision state AND route using strict equality. Preserve null versus real0; never use ||, Boolean, numeric coercion or a truthiness test for route equality.

Public analysis: active_decision state/route matches real_decision; simulation_decision and simulation_scenario are absent/null as supplied by the real public API. An available simulated branch in ExecutiveCase is allowed but is not joined/exposed in public mode; do not fetch hypothetical evidence or compare that branch to the public result.

Simulated analysis: require an actual simulation_decision and simulation_scenario, an AVAILABLE/SIMULATED executive projection with synthetic_flag true, exact current scenario ID, ClassD/source DEMO_GENERATOR and both existing policy labels matching the analysis scenario. Its state/route matches simulation_decision; active_decision state/route matches that same simulation result. Public projection must still match real_decision. A simulated analysis with unavailable/missing executive simulation, missing scenario, foreign scenario or conflicting active state/route is inconsistent and yields null. The real analyze_simulated raises when no scenario exists; do not invent a successful simulated response without a scenario.

All prior public/synthetic evidence membership, sources/status/classes, scenario-qualified claims and stale epoch guards remain active. Do not broaden this to a general malformed-input overhaul or change deferred backend error handling/model partition scope.

Regression in existing unit surface:
- Extend test_all_loaded_api_contexts_join_without_changing_public_decisions to call actual analystClaimContext for all11 public and all11 simulated analyses, preserving its current executive-context test and no-input-mutation proof. Expected IDs/counts are independently literal11/22, not derived by silently dropping rejected cases.
- Add test_analyst_decision_join_rejects_conflicting_projection with isolated mutations: public state only; public route null→0; public route0→null on polypropylene; simulation state; simulation route; active state/route in each mode; foreign/missing scenario; unavailable simulation while actual simulated analysis exists; policy-label conflict. Assert null and no changed input. Public state/route ExecutiveCase fixtures remain complete and validate through unchanged ExecutiveCase.model_validate. Malformed/absent analysis variants are explicitly negative transport-boundary fixtures, not claimed model-valid.
- Valid controls include steel public null, polypropylene public route0 and simulated route0, all11 both modes, and public analysis with an unavailable synthetic branch built through the existing complete model-valid unavailable fixture pattern. Valid public mode must not need simulated analysis or simulated sources.
- Capture original RED for public-state and route-only conflicts using actual rejected JS function; retain the independent reviewer4fail/1pass history. GREEN reruns all previous executive joins and source/metadata negatives too.

Extend existing browser test test_inconsistent_join_never_exposes_a_claim without removing its four original executive mutation cases. Add analyst-public and analyst-simulated cases for independent public state/route mismatch, active state/route mismatch, and simulated decision/scenario mismatch. Use route-intercepted complete stored fixtures; validate ExecutiveCase where applicable. Assert the analyst analysis still renders its genuine state, but there are zero claim buttons, an honest localized source-unavailable message, and an empty claim source panel. Select the next valid case and prove source links recover with correct passport IDs. No stale links from the rejected case survive. Existing late-analyst/case/locale races run unchanged.

## Task3 — F3: exact screening reviewer status localization

Newly permit passportCard's status value in screening/evidence.js to recognize only the existing exact stored enum unconfirmed_by_responsible_authority and render escaped t("graph.reviewer.unconfirmed_by_responsible_authority"). Reuse exact existing EN “Unconfirmed by responsible authority” / AR “غير مؤكد من الجهة المسؤولة”. Do not add another status meaning, call it confirmed, change source class/coverage or change records.

Wrap the visible value with an exact escaped data-screening-reviewer-status attribute containing the unchanged underlying status. That attribute provides machine/source traceability without presenting UI prose as a technical code. For other values retain the existing escape/technical behavior; this correction does not claim a new complete reviewer-status taxonomy. No blanket source island, technical-class exception, catalogue-key rename, normalization change or unrelated graph-passport change.

- Add test_screening_reviewer_status_uses_governed_copy_and_preserves_records to existing test_executive_frontend_contract.py as a shared evidence-presentation integration regression. Execute actual screening renderer against actual stored screening/evidence API fixture; assert exactly8 statuses, exact raw underlying status attribute, exact locale text, unchanged source JSON/IDs/coverage, no raw token masquerading as visible technical code. Use actual unchanged Arabic label_leaks/classification on renderer HTML, with isolated PROJECT_ROOT assertion. Preserve hostile escaping by mutating a copied status/ID and showing it cannot execute or break attributes; do not claim unknown prose passes parity.
- Original RED: unchanged source+current catalogue produces8 leaks and/or raw visible tokens. Preserve the reviewer's corrected1failed run, and rerun the integrated intended regression before source edit. Its earlier primary-root false pass remains historical, never substitute for RED.
- Extend existing test_screening_journey_queue_record_passport_anchor_back: remove only passport.status from its raw-visible-value loop and replace it with stronger per-card checks over all8 actual rows: exact raw data attribute, exact governed localized visible status, no visible raw token. Retain source_id/stage/evidence_class/date/completeness/query/run/coverage/endpoint checks and the whole queue→record→passport→back focus journey.
- Extend existing test_screening_surface_has_no_english_catalogue_prose_in_arabic to assert the8 statuses actually appear with governed Arabic before its unchanged whole-screen summary/queue/record parity checks. Keep expected_source_spans=0 and every allowed-class/island/Latin/label assertion.
- Run test_arabic_parity_predicate_fails_on_injected_english entirely unchanged: clean screen must pass; all six original hostile controls must still fail for their intended reason. Re-run existing EN/AR screening RTL/glyph, keyboard and full-page axe tests unchanged.

## Task4 — exact revalidation, review and generation consequences

- [ ] Root records separate plan approval/acceptance, preserves rejected checkpoint/full diagnostic, reconfirms writer pause and1678 source hashes/index, then resumes sole writer for this exact scope. Final full-run failures are classified separately in DIAGNOSTIC-STATUS.json; unrelated findings are not approved by this amendment.
- [ ] Write meaningful regression tests first and retain each original RED above in an isolated copy of the rejected checkpoint. Use explicit copied PROJECT_ROOT/PYTHONPATH/pytest config so the primary checkout cannot supply code/catalogue accidentally. All expected failures name the actual defect. Implement F1–F3 within finite paths, then GREEN those exact regressions.
- [ ] Run full tests/test_executive_frontend_contract.py, tests/test_ui_catalogue.py, tests/test_static_frontend.py, tests/test_es_modules.py and tests/test_browser_harness_contract.py; actual unchanged UI/ES/threshold/prohibited scans and catalogue validator. No unused catalogue key or grammar change is permitted. Preserve50/23/76 and all original test members.
- [ ] Run entire existing executive states/accessibility/races and screening browser files, all locales/modes/negative collectors, plus complete unmodified make e2e-functional. Preserve full logs and actual failure classification. No new filter/skip/xfail or swallowed browser error.
- [ ] Complete manual EN/AR FAIL27/PASS0/unavailable screenshots at390/1024/1440; inspect actual notice visibility/names/count/IDs, Arabic layout/technical direction, all steps, public/simulation distinction, native keyboard/source return. Inspect screening's real8 passports and full queue/record/back journey. All11 valid join controls and existing null/zero/absentEVSI/contradiction tests remain required.
- [ ] Run full pre-generation pytest and diagnostics. Only the ten exact stale output failures and precise retained96-v112 teardown in accepted AM4v2 may remain, with actual cause matched; report nonzero honestly. Actual16 graph/provisional112 resource proof remains mandatory. No exception covers F1/F2/F3 or another unclassified failure.
- [ ] Pause writer with fresh complete source/evidence checkpoint. A different implementation reviewer re-inspects actual candidate, all changed tests/RED/GREEN, actual rendered journeys and source traceability before source/UI readiness. This amendment's review is not implementation approval.
- [ ] Rebind Task6/7 operation draft after this correction. UI catalogue and optional Core03/09 appendix are already permitted graph inputs; config.py exact AM1 postimage remains unchanged. Static JS/CSS changes are included by actual visual discovery; no discovery override, new graph semantics or inventory pin change. Expected925/1045, snapshot726/authority20 remain actual verification obligations, not forced values.
- [ ] The old-image change allowance is narrowly extended for the screening reviewer-status cell's faithful bilingual wording and consequent local wrapping on existing journey-f screening images where that cell is present. Preserve all112 paths and inspect every changed old image; do not generalize to unrelated screening/UI drift. F1 appears in the16 new executive images. F2 has no authorized ordinary valid-case visual change.
- [ ] Separately reviewed graph→manifest→canonical128 operation, full pinned quality100/per-image600KiB/aggregate16MiB, exact decoded-RGB/strict R1 proof, **two full clean-root make ci**, final exact-tree review and hosted exact-head delivery remain unchanged. No generation, staging or service permission comes from this plan.

## Review focus and stop conditions

Reviewer must check completeness/validity of negative fixtures; notice visibility rather than hidden attributes alone; true27 versus one affected ID; PASS0 versus unavailable; strict null/0 routes; public vs simulated dependency distinction; all11 valid controls; eight real screening rows and unchanged hostile parity oracles; no inventory loophole; no unrelated failure reclassification. Stop for an extra product/test path, schema/domain input change, unknown status taxonomy requirement, new browser name/module, unexpected full-run class, unexplained visual pixel, or changed frozen byte.

## Sanad and Muhasabah

Direct planner evidence: all1678 source checkpoint hashes,27 review evidence hashes, complete fixture validation using existing models, in-memory exact six-key addition accepted by unchanged catalogue validator, actual50/23/76 AST/file counts, source/caller/guard reads, and direct inspection of the full diagnostic receipt and each failure class in its actual log. Renderer RED behavior is attributed to the independent review, not rerun by this planner. Runtime counts and cleanup originate in the writer's retained run; no planner browser rerun occurred.

One planning diagnostic initially inserted proposed strings into the in-memory locale metadata map; the unchanged validator correctly rejected it. Corrected insertion targets only strings.en/ar and passed, with metadata/locales/all existing values equal. Both attempts are disclosed in PLANNING-DIAGNOSIS.json; no product bytes changed.

Assumptions: source remains at the verified pause until root-authorized resumption; valid production APIs retain their existing typed contracts. Risks: stale integrity on retry, accidental public/simulated linkage, localization affecting other scanner callers, and source changes invalidating generation bindings. Requirements are mapped to exact tasks and regressions. Unverified: implementation GREEN, actual rendered correction quality, final unmodified diagnostics, complete independent UI review, generation, full gates/delivery. No source/index/graph/service mutation or self-approval. Muhasabah PASS for a bounded, evidence-linked correction proposal only.
