# S18b AM4 quality-closure implementation plan — v2

> **For agentic workers:** Use the existing separately assigned implementer and executing-plans skill, task by task. This planner cannot approve this packet or its implementation. A different strict reviewer and coordinator acceptance are required first.

**Goal:** Resolve the retained no-candidate fixture, visual resource, narrow accessibility and graph-passport presentation failures, and explain existing simulated capability states without altering domain results.

**Architecture:** Preserve the API and graph/evidence resolution. Correct the exact negative browser test, one lossless encoder parameter, two existing scroll-region wrappers, and shared passport presentation. Reuse existing graph/analyst/executive modules, tokens and policy-owned disclosure. No new production module or browser test file.

**Tech stack:** Existing ES modules, token CSS, bilingual YAML catalogue, Python/pytest/Playwright, pinned Chromium/Pillow producer.

**Spec:** `EXPERIENCE-AND-CATALOGUE.md`, parent S18b plan `ce2786284e0fafc4bcf89d66eed748362d0b2437c696af193b6471a8be772164`, accepted AM1, AM2v2 and AM3. AM2v2 plan `013a032a2fcaaaa354d1abd970c7d7fbbff67cb8fdd29c0801c4eaf59aefea08`; AM3 plan `34b000f64da574ab96a4e17b1a9203aaad086362f34820d29dae34ae471d92a0` and delegated acceptance `20260924-s18b-am3-plan-acceptance-1.json`. This is S18b AM4, distinct from historical S18a AM-4, whose approved bytes remain immutable.

**Revision status:** PROPOSED v2, correcting only independent finding AM4-P1 in the pre-generation verification sequence. Original v1 plan `78a94ec7cdfd2461fd4e3588f88f151f2b935ccc9156f977fa515f7b22f2c088` and its 45-entry packet remain immutable. Effective R1 verdict is the additive superseding REJECT `76f8f24145896a99d960f5405d81aa45a215291b050605f39354d7313ba0678d`; the earlier prematurely written APPROVE was not accepted by root and does not authorize work. Companion design/catalogue bytes, implementation boundaries, copy, grammar, all behavior tests and final gates are unchanged. Another reviewer must approve v2.

## Preparation, authority and evidence

Read AGENTS, manifest and original DOCX in authority order; reuse prior full governing preparation. Refresh Core03 §14, Core04 evidence statuses, Core07 §4.2, Core09 §2.7, UX §§8–12, roadmap and current handoff/accepted amendments. Planner adopted bilingual accessibility engineer and reproducible-test planner personas after these reads. Invoked writing-plans, systematic-debugging, frontend-design-pro, test-driven-development, verification-before-completion, github-flow, Sanad, al-Muhasibi and Muhasabah. Existing governed visual design/font choices override generic design-skill suggestions. Owner's recorded overnight/blocker delegation authorizes independently reviewed routine amendments, not self-approval, domain or security waivers.

Direct evidence: read the exact source, invoked actual `renderEvidenceResult` with the existing foil shared-enabler record, inspected the four copied rendered screens, extracted DOCX §6.4, and inspected current grammar. `DIRECT-DIAGNOSIS.json` confirms unannotated title/status/supports and rejected compound identifiers. Source reads are hashed in `SOURCE-READ-RECEIPT.json`; they are selected-file observations of an active writer's worktree, not a claim that the whole candidate was frozen. Refresh hashes at implementation and preserve any accepted intervening edits.

Attributed retained evidence: the two real fixture 404 teardown errors, graph-budget preflight and narrow axe logs are the writer's completed runs; eight-image encoding experiment is the coordinator's read-only local diagnostic. Copied input receipts retain that provenance and their noncanonical-runtime limitations. The aggregate old96+new16 cohort is **16,802,282 bytes**, 25,066 over the unchanged 16,777,216 limit. The coordinator's biased eight-image sample reduced 1,527,086 to 980,452 bytes with identical decoded RGB; it does not prove the final128 budget. Pillow's official [WebP documentation](https://pillow.readthedocs.io/en/stable/handbook/image-file-formats.html#webp) describes lossless quality as compression effort, default80 and maximum100. All final canonical proofs remain pending.

## Global constraints and stop conditions

- Immutable public decisions, real/simulation isolation, 11 cases, nine routes, Class-D simulation, thresholds, snapshot/golden/evidence bytes and graph semantics remain unchanged. No API/service/model/loader/data/engine correction is authorized here.
- No fabricated executive endpoint response; no shared failure-collector suppression, blanket network ignore, axe exclusion or whole-passport parity exemption. Original browser assertions and negative tests remain.
- Exact module inventory50, browser files23, named browser functions76 remain. Extend existing named browser tests or add parameter rows; do not append a new browser function without a separate exact inventory amendment. Unit-test functions are not subject to that browser inventory.
- All128 visual paths, producer digest, browser revision, fonts, dimensions, RGB/lossless contract, 600KiB per-image,16MiB aggregate and fixed comparison tolerances remain. No convenient cropping, content omission, smaller fonts or lossy conversion.
- Graph layout/nodes/edges,720px diagram, request epochs, scope/context validation, cached-evidence matching and safe URL policy remain.
- Preserve all previous packet/approval/owner attribution and frozen bytes externally. No source staging/commit/generation in this amendment's implementation steps. Task6 remains a separately reviewed operation, followed by full exact-candidate gates/review.
- Stop for a required path outside the finite table, source mismatch not explained by an accepted intervening amendment, unexpected behavioral failure, lossless mismatch, unexplained UI drift or final fixed-budget failure. Return concrete evidence for review; do not recategorize a failure as success.

## Exact file boundaries

The union below is finite. Existing parent permissions do not imply permission for other behavior within these files.

| File | Authorized delta |
|---|---|
| `browser_tests/test_screening.py` | Only existing no-candidate test's context/collector, exact real404 oracle, retained assertions and source-unavailable checks |
| `browser_tests/visual_baselines.py` | `quality=100` in `write_lossless_webp`; all existing normalization, method6, exactTrue, publication and budgets unchanged |
| `src/ior_mvp/static/modules/renderers/evidence.js` | Existing ledger wrapper's stable ID, role, tabindex and localized accessible name only |
| `src/ior_mvp/static/modules/methodology.js` | Existing table wrapper's stable ID, role, tabindex and localized accessible name only; this newly admits the file |
| `browser_tests/test_accessibility.py` | Exactly two named Tab stops in existing literal sequence; all old order/focus/wrap assertions retained |
| `src/ior_mvp/static/modules/graph/passports.js` | Typed fields, source language/caption markup, technical values, status mappings and escaped template placeholders; existing anchors, record ordering and URL guard retained |
| `src/ior_mvp/static/modules/graph/render.js` | Reuse graph disclosure renderer for actual two policy labels; graph control classes if needed; provenance/evidence IDs remain typed |
| `src/ior_mvp/static/css/graph.css` | Local graph toggle/retry/selector and passport presentation/focus/identifier-row styling using existing tokens; no diagram geometry changes |
| `src/ior_mvp/static/modules/executive/evidence.js` | Remove duplicated broad Latin-field rewrapping after shared passport correction; retain anchor remapping, branch markers, focus, graph link and policy warnings |
| `src/ior_mvp/static/modules/executive/simulation.js` | Capability-state legend and visible label alongside original state only |
| `config/ui_strings.v1.yaml` | Exactly15 new key pairs in companion spec, still pre-generation UI1.6; no existing wording/policy-label duplication/version bump |
| `browser_tests/parity_grammar.py` | Exactly two technical productions specified below; every old production/normalization/prose/leak rule unchanged |
| `tests/test_parity_grammar.py` | Positives and meaningful negative boundaries for those productions, preserve all old cases |
| `tests/test_visual_baseline_contract.py` | Exact source-binding assertions for all affected capture helpers; no Pillow import/default-dev dependency, budget or tolerance weakening |
| `browser_tests/test_visual_baselines.py` | Private encoder property helper called from existing `test_governed_visual_baselines_match`; no new named browser function |
| `tests/test_s17_graph_frontend.py` | Actual shared passport/disclosure output and safe escaping/status/source/technical regressions |
| `tests/test_executive_frontend_contract.py` | Shared passport caller/legend contract regression; no decision-oracle relaxation |
| `browser_tests/test_graph.py`, `browser_tests/graph_pages.py` | Extend existing named functions to full selected-passport parity, disclosure, keyboard and page axe plus negative controls |
| `browser_tests/test_executive.py`, `browser_tests/test_executive_accessibility.py` | Extend existing claim/legend/parity functions across both locales, meaningful source and unknown-state cases; retain all old assertions |

Additive records only: `.workflow/slices/S18b-bilingual-executive-surface/plan-amendment-4.md` (byte-identical plan), companion experience/copy under that slice, existing plan_review/implementation_log/test_evidence/reviewer_findings records; `docs/ARCHITECTURE_DECISIONS.md`, `docs/BUILD_PROGRESS.md`, `docs/BUILD_ROADMAP.md`, `docs/REQUIREMENTS_TRACEABILITY.md`, `docs/KNOWN_LIMITATIONS.md`, `docs/implementation/UX_GENUI_DEMO_SPEC.md`, and relevant already-permitted Core03/Core09 paragraphs. Core07/DOCX are read-only sources, not edits. Core02's existing presentation mapping may gain only an AM4 reference. Already approved parent Task6 outputs remain separately governed, not additional AM4 write permission. Do not change `browser_tests/pages.py`, the failure collector/harness, fixtures, snapshots, `config/evidence_policy.v1.yaml`, fonts or dependency locks.

## Review focus

1. A second unexpected console/network/page failure must still fail the negative fixture (Task1).
2. Source prose and company names must never become technical tokens or fabricated Arabic translations; genuine Arabic and hostile strings stay faithful (Task4).
3. Compound IDs, period strings and zero-valued page/line addresses must retain exact characters, order and source matching (Task4).
4. Focusable analyst wrappers must work with actual Tab/Arrow keys and not hide a full-page axe problem (Task2).
5. Encoder byte changes must not conceal UI pixel drift or rely on a biased sample for the full fixed budget (Task3/6).
6. A simulated state0 must not read as actual Ministry confirmation; U/unknown must never imply zero distance (Task5).

## Task1 — Assert the artificial fixture's real absence honestly

**Interfaces:** Keep the exact existing test name and three existing analyst fixture routes. Consume the actual request to `/api/executive/opportunities/FIX-PUBLIC-NO-CANDIDATE`; no interception of that endpoint.

- [ ] Preserve the original EN/AR RED log/error. In a disposable retained-source proof copy show the original test fails from the real404/console events, not setup/collection.
- [ ] Adapt the supplied patch, with explicit `try/finally: context.close()` cleanup. Attach unchanged `BrowserFailureCollector` to context and page before navigation. Capture the natural executive response with an exact URL/method predicate using `page.expect_response` around `page.goto`, rather than making an extra request.
- [ ] Assert `response.status == 404` and exact parsed body `{"detail":{"code":"EXECUTIVE_OPPORTUNITY_NOT_FOUND","message":"FIX-PUBLIC-NO-CANDIDATE"}}`. Retain the exact frozen fixture JSON hash and all existing disposition/headline/`None|null` assertions. Assert no `[data-claim-id]`, visible `.claim-unavailable`, and empty `#analyst-claim-sources`.
- [ ] After context closure assert exact set equality with the two records below. Collector already deduplicates records; do not add filtering or mutation of collected records.

```python
expected_errors = {
    BrowserFailure(category="app-http-error", method="GET", status=404,
      url=f"{app_server.base_url}/api/executive/opportunities/{opportunity_id}",
      detail="app response status is at least 400"),
    BrowserFailure(category="console-error",
      detail="Failed to load resource: the server responded with a status of 404 (Not Found)"),
}
assert set(collector.records) == expected_errors
```

- [ ] Run unchanged named test for EN and AR. Retain a disposable negative mutation that adds a distinct console error/pageerror and one that changes expected status/body; the full test must reject both. Removing a required response/record must also fail. Do not count the old ImportError or missing dependency as RED.

## Task2 — Give actual analyst scroll regions names and keyboard access

- [ ] Retain390px RED full-document `scrollable-region-focusable` evidence from both locales and add keyboard assertions to existing `test_graph_scroll_region_reaches_both_keyboard_endpoints` before changing wrappers.
- [ ] Change only these start tags, preserving existing contents/classes and escaping:

```html
<div class="card-body card-body-scroll" id="evidence-scroll-region"
 role="region" tabindex="0" aria-label="${escapeHtml(t('evidence.title'))}">
<div class="methodology-table-wrap" id="methodology-scroll-region"
 role="region" tabindex="0" aria-label="${escapeHtml(t('rules.title'))}">
```

- [ ] Keep both native regions keyboard reachable at every width; existing global `:focus-visible` supplies token styling. Extend the literal Tab sequence in `test_keyboard_tab_order_reaches_every_interactive_control_with_visible_focus`: insert `id:evidence-scroll-region` after the last economics/claim control and before the dossier action; insert `id:methodology-scroll-region` after `id:graph-toggle` and before screening queues. Confirm against actual DOM; a different location needs diagnosis, never derive expected order from actual DOM. Preserve every old stop, equality, focus signature and wrap check.
- [ ] At390px, both modes/locales, Tab to each wrapper and assert the localized accessible name and visible focus; use actual Arrow keys to move horizontally in overflowing tables, record nonzero range/change and both ends. Check both Arrow directions without assuming RTL scrollLeft sign. Where a table does not overflow, assert zero range and full containment, not fake movement. Preserve graph's existing720px two-endpoint keyboard proof and complete native node/edge reachability.
- [ ] Run full-page axe unchanged at390/1024/1440 after loaded analyst graph and selected passport. No `include`, `exclude`, ignored violation or hidden content substitution. Run old keyboard test across all existing parameter rows after the two-stop update.

## Task3 — Improve compression effort while preserving every pixel

- [ ] Retain original112 approved WebPs, manifest and hashes externally before any future generation. The root's112 diagnostic cohort (old96 plus fresh16 graph captures) is a separate failure cohort; never call it final128.
- [ ] Add exactly `quality=100` to the existing normalized-RGB WebP save call. Keep `lossless=True, method=6, exact=True`, normalization, alpha compositing, producer and every resource/comparison guard unchanged.
- [ ] In existing `browser_tests/test_visual_baselines.py`, add private `_check_lossless_encoder(tmp_path)` and invoke it at the start of the existing `test_governed_visual_baselines_match` (add `tmp_path` fixture argument; no new test function). It exercises the actual writer on RGB, RGBA with varying alpha and palette input: outputVP8L, decoded opaqueRGB equals `normalized_rgb(input)` exactly, dimensions exact, repeat output bytes equal. Compare the writer output to an independently encoded exact `lossless=True, quality=100, method=6, exact=True` reference for a deterministic nontrivial RGB pattern, retaining a pattern that actually distinguishes80/100 as meaningful RED. This alone is not full resource proof. Run the private helper independently in the existing e2e environment before generation and retain its result; the normal visual test calls it again on the final candidate. Pillow is an e2e-only dependency in pyproject.toml: do not import it into the default-dev unit suite, add skips or change dependencies.
- [ ] Preserve original budget RED. In the existing canonical producer, later re-encode every image of the retained112 failure cohort to an external proof directory at80 and100, prove80 repeats baseline diagnostic bytes where that producer matches, compare decoded RGB byte-for-byte at100, and record per-file and total sizes/time/library versions. A producer mismatch with the local diagnostic must be disclosed, not forced; the raw original failure bytes remain evidence.
- [ ] Task6 operation packet must encode each **final128 capture's same normalized RGB** at80 and100 externally, prove exact pixels and repeated100 byte equality, and enforce final100 file/aggregate budgets. Do not assert every file is smaller or extrapolate the sample. Preserve both encodings and RGB hashes for all128.
- [ ] This explicitly amends only unaffected-image **encoded-byte** preservation in the parent visual plan: unchanged content must have exact decoded-RGB equality against original112, even if compression bytes differ. Actual UI pixel changes remain limited to parent/AM2/AM4 approved surfaces and require declared affected paths, region-level metrics/screenshots and independent review. Run-ID controlled geometry/R1 remains separate and unchanged. No reencoding before separately accepted Task6.

## Task4 — Make selected graph passports bilingual, faithful and readable

**Interfaces:** `renderEvidenceResult(result, mode)` signature, stored record ordering, opportunity qualification, exact evidence-ID lookup, safeExternalUrl and anchors stay. Shared rendering applies to graph and executive claim panels.

- [ ] Add failing actual-renderer tests before edits. Use existing public records and foil synthetic `SYN-MINISTRY-ALU-FOIL-001::shared_enabler`; assert raw value fidelity, typedtechnical spans, English captions, Arabic original text and status localization. The current renderer's unannotated `<h5>Shared Enabler</h5>`, raw `synthetic`, and raw supports are direct RED witnesses, bound in this packet.
- [ ] Implement the field-by-field contract and exact new catalogue keys in the companion spec. Escape each value before HTML interpolation. Known status strings use a fixed mapping of exactly the seven Core04 values; unknown strings stay marked source text, never silently mapped to observed/unresolved. Only the exact stored reviewer status `unconfirmed_by_responsible_authority` has a localized label; arbitrary review prose remains source text. Do not manufacture review approval.
- [ ] English source prose uses individual `lang=en dir=ltr .source-language-island` spans with visible adjacent Arabic `.source-language-caption` using the existing exact caption. Title link and h5 each retain complete text; captions for links are visible siblings with stable IDs and `aria-describedby`. Arabic-only source spans retain `lang=ar dir=rtl` without an English caption. For mixed Arabic/Latin text, preserve all characters and split source runs so Arabic remains RTL and Latin runs have source annotations/captions; no transliteration, guessed Arabic name or whole-card LTR wrapper. Tests must compare original text in value spans excluding captions.
- [ ] Use one technical token per full technical value. For the two actually rejected syntactic types, add only the following explicit technical grammar productions before generic classes:

```python
("synthetic_evidence_id", re.compile(
    r"SYN-MINISTRY-[A-Z0-9]+(?:-[A-Z0-9]+)*-\d{3}"
    r"::[a-z][a-z0-9]*(?:_[a-z0-9]+)*")),
("period_years", re.compile(r"\d{4}(?:/\d{4})+")),
```

These classify typography, not graph/evidence authority. Stored resolution still enforces actual IDs and partitions. Preserve every old grammar and catalogue-label leak rule. Negative tests include whitespace/prose, `Hadeed`, `Universal Metal Coating Company`, missing/extra separators, invalid prefix, empty suffix, hyphenated/prose suffix, slash prose and dates with letters. Verify unknown forged references remain unresolved even if their syntax is technical. English labels must still fail the existing leak oracle.

- [ ] Replace the combined identity paragraph with separate labelled opportunity/evidence rows, using existing `workspace.opportunity` and `graph.provenance_id` labels respectively. Both keys were verified in the current catalogue; no extra key is admitted. Preserve complete IDs and visual order at390px; no truncation, source misclassification or external bidi control insertion.
- [ ] Render unresolved-ID and document-address sentences by splitting the trusted catalogue template at its exact named placeholders and inserting escaped technical tokens. Do not escape complete HTML again or substitute unescaped values into templates. Preserve zero-valued page/line indexes with `??`, string addresses as original prose, missing values as localized unavailable.
- [ ] Keep source URL text as an isolated URL only after unchanged safeExternalUrl validation, external `noopener noreferrer`, and original valid href. Invalid schemes have no link. Add malicious quote/tag/ampersand, `javascript:`, source-title HTML, mixed Arabic source, null/empty/array and contradiction cases; no injected nodes/scripts or lost text.
- [ ] Export one compact `renderGraphDisclosure(labels)` helper from existing passports module if needed. Graph render and passport synthetic rows use both exact labels from validated policy metadata (a legacy synthetic record with only the singular English label uses the already validated `state.ui.synthetic_labels` pair after checking the English value agrees), local classnames and visible Arabic source-caption; never add their wording to the UI catalogue or translate it. Keep warning distinction and actualClassD. Executive may keep its existing policyLabels helper but must not duplicate raw reclassification. Remove executive/evidence.js's broad `h5,dd,[data-passport-ref]` rewrap and synthetic-ID-as-source hack; retain article IDs, data-evidence-branch, anchor remapping, Escape/focus and branch isolation. Re-run all existing executive source/parity tests.
- [ ] Polish only the graph toggle/retry/select and passport block: existing navy/teal surface, token border/radius/padding, inherit fonts, logical wrap and visible focus, adequate native target size through padding, no browser-default grey stacked-word button. Keep native controls, hidden/expanded semantics, DOM handler selectors and selection focus behavior; no replacement custom select or global CSS change. Retain720px diagram and fixed geometry.
- [ ] Extend existing graph selection/passport, related-evidence, keyboard, hostile-name and all-view-state tests. Exercise all four views, public/simulated, EN/AR at390/1024/1440; select available actual nodes/edges until the union of stored, unresolved and document-address outcomes is exercised. Empty/unavailable/loading/error/invalid/retry states retain their distinct behavior; never fabricate a nonempty view to claim all axes selected. Existing explicitly labelled test-only route-blocking fixture stays syntheticClassD and separate from real-source claims.
- [ ] **After each passport settles**, run unchanged Arabic parity on full `#graph-view` (not native-only), plus full-page axe, focus/keyboard and containment; EN checks exact original text and technical direction. Preserve the AM2 preselection six-island adjacency oracle. For postselection expected source counts compute an independently authored fixture-level expectation from the fixed field-by-field spec, recording title link/title/body/warnings and genuineArabic exclusions; never read the renderer's current span count to create its own expected count. Keep exact canonical fixture counts as literals in test cases after independent evidence inspection.
- [ ] Require removal mutations for source annotation/caption, raw English status, misclassified company as code, wrong direction on a compositeID, unsafe URL and missing policy warning to fail the strict oracle. Retain AM2's five source-disclosure negative controls. Test actual Tab/Enter selection, passport anchor focus and Escape/close restoration in both shared callers.

## Task5 — Explain existing capability states in their simulation boundary

- [ ] Before change, strengthen existing journey/unit tests to require the five exact meanings and state0–3/U labels. Preserve actual nine dimension IDs/order and each original numeric state; unknown has U plus existing unavailable semantics and never0.
- [ ] In `capabilityDetail`, add a compact native `<details>` legend with localized summary, definition-list rows for0/1/2/3/U and the exact companion text. Each displayed dimension shows its original numeric code and corresponding short/full localized meaning; unknown retains explicit unavailability. Do not change `known`, state, D*, K/U, route band, control_message or API. Keep the source control_message separately annotated as before. The legend note explicitly identifies simulated states and lack of Ministry verification.
- [ ] Extend the existing journey test to both locales and original API equality, and unit fixture rows covering0/1/2/3/unknown with unknown penalty/output untouched. Assert the definition under a synthetic warning; removing the warning or treating unknown as0 must fail. All8-step screenshots are re-inspected at390/1024/1440; keyboard and parity include expanded legend. Parent step count8 and tabrail remain unchanged.

## Task6 — Documentation, source binding and handoff to unchanged gates

- [ ] Append ADR below before any hashed generation; update precise Core03 passport/accessibility and Core09 encoding/parity acceptance paragraphs, UX instructions and test traceability. State real failure/correction history and pending results, never preclaim candidate approval.
- [ ] Verify actual graph input discovery: admitted static/browser/test files add **no new graph engine input**; existing configUI and Core02/03/09 changes do change the already-approved graph closure. No change to counts925/1045, output schema, graph selection or hash algorithm. All pre-generation source changes must finish before final input binding.
- [ ] Verify actual visual source discovery includes affected staticJS/CSS, UI catalogue, `browser_tests/visual_baselines.py`, graph_pages/test_graph, parity_grammar and existing transitive executive capture helpers. Add exact source-binding assertions within the listed visual-contract file; if actual discovery omits a capture dependency, report its exact path for separately reviewed discovery addition rather than hiding the omission. Functional-only test changes remain bound by candidate tree and need not be falsely declared capture inputs.
- [ ] Keep exact50 modules,23files,76real named browser functions. Run literal inventory contracts and full focused suite, retaining original-defect RED and final GREEN. Run the full **unmodified** `make e2e-functional` with all collectors, full-page axe and graph preflight enabled; retain its exact command, exit status, full log and run summary. Before generation, the sole permitted functional nonzero cause is the exact `graph_visual_preflight_root` teardown predicate `assert len(retained) == 112` observing96, because inherited112 baselines contain16graph+96non-graph. All actual test-body/collector/axe assertions must pass. Match the fresh traceback and file inventory to this predicate; any additional/different failure blocks readiness. Report the full command as **nonzero / pending generated baseline inventory**, never GREEN. Do not skip, xfail, filter, edit the fixture, derive its expected count dynamically, or populate canonical baselines to force this diagnostic to pass. Execute the external resource checks below because this predicate aborts before later budget assertions.
- [ ] Run full `pytest -q` and retain its complete result. Exactly the ten already observed pre-generation graph/visual/status failures listed in `PRE-GENERATION-FAILURES.json` may remain attributed to pending Task6 output, with current tracebacks checked against the original causes. The precisely identified functional teardown is a separate pending-output item, not an eleventh pytest case. No extra failure is allowed into source approval and no pending item is waived for the final candidate.
- [ ] **Pause the writer before any costly generation.** Freeze an external exact source/evidence snapshot, full source diff, source hashes, RED/GREEN logs, full nonzero diagnostic and its precise pending-output attribution, independently completed resource checks, all collectors, rendered screens and per-axis UI checks. A different implementation reviewer must inspect the actual source and UI, including all selected passports and expanded legend, and issue an evidence-backed source-readiness verdict. This preliminary verdict may accept only the ten matched stale-output unit cases and the one exact retained-count teardown as pending generation; it is not final candidate approval or a full-functional PASS. Correct every other finding before Task6.
- [ ] Submit the complete exact Task6 packet to the separate operation reviewer: source freeze, permitted final diff, graph input closure, old/new semantic comparison, 112 prior baseline archive,128 final matrix and encoding controls, fixed budgets and declared UI drift. No generation approval is implied by accepting this plan.
- [ ] After accepted generation, run the **full unmodified functional suite to exit0**, with zero test/collector/axe/teardown failures and actual128 baselines:112retained+16graph. The unchanged fixture must execute its entire16-capture matrix/per-image/aggregate checks and publish its normal summary. Run every original gate: canonical graph/manifests reconstruction, all11 public outcomes/isolation, fullpytest with zero failures, integrity, smoke, live local graph proof, all128 compare-only e2e and final128 budget, unchanged strict same-layout R1 with12/5 rows and five-token mutation/repeats, and two clean-root makeci runs. Preserve producer/source/tree identities, separate projects and old evidence. Every formerly pending failure must now pass; any failure follows the reviewed correction workflow.
- [ ] Pause writer; separate exact-candidate implementation reviewer inspects actual screens/allaxes and test changes. Only exact approved candidate can enter normal staged-tree/commit/tree equality and PR workflow. Required hosted checks and final exact-head independent review remain mandatory before coordinator merge; no tag/final Claude acceptance.

### Exact pre-generation resource diagnostic for AM4-P1

The root's direct observation `34d0331a9b55dfc921ecd716586d964ba9cf052e69a4571751a9ceb6470a5e47` and independent reviewer evidence `a9b8da17f6f3587be610956da83cec4a99d3df706fafd7cd4086488eb013ad3c` bind inherited112 =16graph+96non-graph. The failure is before replacement-screen equality, per-image/aggregate checks and receipt publication. The following external diagnostic replaces no test and grants no canonical generation permission:

- [ ] Bind the diagnostic command's actual source/encoder/producer identities, original baseline manifest/hashes and output directory; require the canonical112 files and manifest unchanged before/after. Keep the original failure cohort and encoding sample as history. Use only the **actual16 fresh graph captures from this full functional run**, not an earlier run or fabricated files.
- [ ] Require exactly the literal Cartesian matrix: locales `en, ar`; viewports `desktop-1440x900`=1440×900 and `tablet-1024x768`=1024×768; screens `journey-i-graph-adjacency`, `journey-i-graph-route-blocking`, `journey-i-graph-shared-enabler`, `journey-i-graph-evidence-to-change`. Reject missing, duplicate or extra captures; bind path, source run, SHA-256, bytes, dimensions, lossless VP8L and decoded opaqueRGB for every file. Verify each capture≤600KiB. Do not assume these checks ran after the earlier fixture assertion failed.
- [ ] Independently enumerate the immutable inherited112 baselines and verify exactly16 graph and96 non-graph identities against their archived hashes. The **provisional112-image cohort** is the exact96 inherited non-graph files plus these16 replacement captures, with disjoint paths and no old graph counted twice. Preserve all individual resource checks and explicitly measure `sum(actual96_sizes) + sum(actual16_capture_sizes)` against16,777,216 bytes; record both subtotals, total and margin. A failure is an explicit resource blocker requiring the existing review path, not a pending-count exception. Do not re-encode or mutate inherited baseline files to make this arithmetic pass; their separately accepted Task6 encoding proof remains later.
- [ ] Record the final planned128 path ledger as96 inherited non-graph +16 graph replacements +16 new executive paths under the parent plan. The16 executive slots remain **not yet canonical / size unverified** here, never zero bytes or counted as budget PASS. No extra executive-capture machinery is required by this correction. This provisional112 measurement is not proof of final128 content, producer equivalence or aggregate budget. The full128 same-normalized-RGB80/100 and repeated100 encoding proof, fixed per-file/aggregate limits and independent declared-pixel review remain mandatory in the separately approved Task6 operation.

This is file/receipt inspection in the existing proof environment, with Pillow already confined to the e2e environment. It adds no product/test/checker path, phase-aware test framework, baseline regeneration, tolerance, budget or dependency change. Task3's complete pinned-producer encoding requirements remain unchanged.

### Additive ADR text

S18b AM4 repairs four demonstrated quality gaps while preserving public decisions, source evidence and strict acceptance. The artificial analyst fixture asserts its real optional executive404 through exact expected events and body. Existing analyst table wrappers receive named keyboard scroll regions. A shared typed passport renderer localizes declared evidence states, preserves source prose with captions and exact technical identifiers, and retains both synthetic warnings; a methodology-sourced capability legend explains existing simulated states. Lossless WebP compression effort100 is admitted solely to reduce encoded size without altering RGB. Historical baseline bytes are archived; encoding-only differences require exact decoded equality. UI pixel changes remain explicitly bounded and independently reviewed. Fixed600KiB/16MiB,128images, producer/fonts/tolerances, graph925/1045 semantics, strictR1 and two-clean-rootCI are unchanged. AM4-P1 records the exact pre-generation retained-count teardown as pending generated output, with an independent actual16-capture/provisional112 resource diagnostic; no functional command is falsely called GREEN. Final128/112 full-functional exit0 remains mandatory. Generation remains separately reviewed and all success claims await exact-candidate evidence.

## Sanad and Muhasabah

**Provenance:** Direct source/screen/DOCX/Node observations are in this packet. Root/implementer experiment results retain their attribution. Pillow compression semantics use official documentation, not an inferred lossy-quality analogy. The current source is mutable under the sole writer; selected hashes do not certify a whole tree. No source/index/services or canonical evidence were changed by this planner. After the coordinator quiet-window checkpoint, preparation resumed with file reads/writes only; no application/browser/container/network/service launch occurred.

**Assumptions/risks:** Final128 effort100 may still exceed the fixed budget; this is a stop, not a reason to relax it. Source-language segmentation must preserve mixed text and captions without damaging links/IDs; final DOM, native focus and actual all-view tests are required. Caller changes may alter executive passport geometry; reviewed screenshots and parent visual/R1 controls apply. Canonical fixture postselection counts must be independently fixed, not self-derived.

**Unverified:** No corrected implementation, full DOM/browser GREEN, pinned-producer full128 proof, generation, exact candidate, CI or deployment is claimed. Diagnostic-script preparation briefly referenced a nonexistent policy `display_labels` key; it was corrected to the actual `display_label`/`display_label_ar` keys and the final direct probe reran successfully. No product behavior was touched.

**Muhasabah: PASS for a bounded planning proposal, not implementation or approval.** Evidence, requirements, assumptions and risks are explicit. This planner is the author; another agent must approve/reject the exact packet. Tonight's review is same-model Codex, with Claude's later independent review pending.
