# S18b test evidence

Implementation in progress. No frontend test, screenshot or candidate gate pass is preclaimed. External command logs and observed RED/GREEN results will be linked before handoff.

Task1 RED: browser_tests/test_executive.py direct Arabic route failed because no executive-ready/main existed (1failed). First GREEN attempt is FAILED, not passed:1failed/1error after catalogue API500. Task2 initial batch is BLOCKED BY THE SAME PREREQUISITE (24failed,2errors,3deselected); repeat meaningful RED after independently reviewed validator amendment, before Task2 behavior implementation. External logs are in overnight-2026-09-23/s18b/implementation. No browser UX or complete gate pass claimed.

## Retained development results after AM1

External evidence root: `.autonomous-workflow/overnight-2026-09-23/s18b/implementation/` in the primary checkout. `am1-red.log`:4failed/6passed; `am1-green-r2.log`:65passed; `am1-shell-green.log`:1passed. `task2-red-r2.log`:24failed; `task2-green-r2.log`:29passed. `task3-red.log`:6failed; `task3-analyst-red-r2.log`:2failed; `task3-green-r3.log`:41passed. `task4-r5.log`:47passed; `task4-unit-green.log`:74passed. Later source additions require reruns. All failures and interim runs remain retained. Browser artifacts are disk-backed under `/home/barami/.cache/industrial-opportunity-resolution-mvp/overnight-2026-09-23/s18b-proofs/`. No canonical graph, manifest or image generation has run in this worktree.


## Later pre-generation diagnostics (not final candidate gates)

| Evidence log | Observed result | Scope/limitation |
| --- | --- | --- |
| `task4-economics-parity-green-r3.log` |3passed | Route economics and whole-main Arabic source parity; preceding RED/failed attempts retained |
| `task4-races-final-r2.log` |4passed | Actual delayed locale/current-case and serialized analyst source transitions |
| `task5-focused-green.log` |77passed | Frontend catalogue/static/isolation checkpoint |
| `task5-full-pytest-pre-generation-r1.log` |21failed/3238passed | Ten AM3 pins, one AM2 pin and ten graph/visual/status stale expectations; not a pass |
| `task4-full-functional-r1.log` |583passed/4deselected/3errors | Two artificial-case404 teardowns and graph aggregate budget overage; collection predates two later race tests |
| `am2-literal-name-red.log` |1failed | Canonical companies incorrectly displayed `UNAVAILABLE` |
| `am2-containment-red.log` |4failed | Both locales and steel/aluminium390px panel overflow, despite no document overflow |
| `am2-focused-unit-r2.log` |85passed | Graph/helper geometry, catalogue, tokens and static checks |
| `am2-focused-graph-r2.log` |34passed/6deselected | Four visual-budget cases await governed generation/repair; two390px full-page axe cases remain diagnosed, not waived |
| `am2-states-mutations-r1.log` |3passed | Five disclosure mutations; four views×two modes×six loading/absence/error/retry states in EN/AR390px |
| `task5-empty-explicit-red.log` |1failed | Empty summary with explicit case URL showed wrong state |
| `task5-empty-explicit-green.log` |5passed | Empty/explicit-empty/not-found/zero/unavailable distinctions |
| `am3-focused-green.log` |39passed | All prescribed actual authority/module/harness/API tests |
| `am3-negative-controls.log` |4inventory and8adapter mutations rejected | Isolated restored copy; writer rerun of attributed reviewer diagnostic |
| `am3-no-candidate-known-red.log` |2passed/2teardown errors | Exact absent artificial-case404 remains for separate amendment |

Manual evidence: `manual-final-r1` contains48 executive states/96 noncanonical PNGs, `manual-empty-corrected-r1` supersedes only the mistaken empty-state observations, and `am2-manual-r1` contains12 graph contexts/48 PNGs. Actual selected screenshots have been inspected at native390 and wider sizes, including all8 executive steps in both locales. The capture records distinguish artifact-backed graph fixtures from application API evidence; none proves live Neo4j/Aura. Full candidate/128canonical-image gates, independent implementation review and delivery remain pending.

After AM3, the complete pre-generation pytest diagnostic (`task5-full-pytest-pre-generation-r2.log`) recorded3251passed/10failed/one inherited warning in100.81s. All remaining failures concern pending graph reconstruction/manifest provenance, live status pins or the128-image successor. Source/product tests were stable during this run. These expected stale outputs remain failures until the separately approved generation sequence and exact-candidate gates; they are not skipped or treated as passed.


## AM4 focused evidence and unchanged pending gates

External log directory remains `overnight-2026-09-23/s18b/implementation` in the primary recovery root. All originals are retained, including failures and exact commands in the resumption/proof receipts.

| Evidence | Actual result | Limit |
|---|---|---|
| `am4-task1-original-red.log` / `am4-task1-green.log` |2 passed/2 teardown errors →2 passed | Real404 preserved; six external negative controls reject |
| `am4-task2-red-actual.log` / `am4-task2-live-green.log` |4 missing-region failures →20 passed | Reviewed endpoint correction, original graph oracle unchanged |
| `am4-task3-encoder-red.log` / `am4-task3-encoder-green.log` | Default-effort byte mismatch → property PASS | Local producer only; pinned final128 equality/repeat/budgets pending |
| `am4-task4-unit-key-corrected-r2.log` |67 passed | Exact original Task4 invocation, after typed-key RED and6 focused PASS |
| `am4-selected-passports-r1.log` / `am4-selected-passports-r2.log` |3 passed/1 failed count oracle →4 passed | R2 table uses raw fields and typed-absence contract |
| `am4-passport-matrix-and-mutations-r1.log` |13 passed | Twelve EN/AR390/1024/1440 case rows, each both modes/all views, plus five old/seven new mutation controls |
| `am4-executive-passports-r2.log` |13 passed | Shared stored references, native link/return focus and complete AR subtree |
| `am4-task5-unit-red.log` / `am4-task5-browser-red.log` |2 +2 failed | Absent capability legend before implementation |
| `am4-task5-unit-green-r2.log` |105 passed | Includes full catalogue usage without unused-key relaxation |
| `am4-task5-browser-all-r1.log` |56 passed | Expanded legend, original API equality, all steps and narrow/wide accessibility |
| `AM4-CAPABILITY-MUTATION-PROOF.json` |Control2 PASS; unknown→0 and missing warnings each2 FAIL | Isolated copied real modules, source unchanged |
| `am4-source-binding-green.log` |2 passed | Executes actual source discovery without importing Pillow into default tests |

Full pre-generation pytest and unmodified make e2e-functional must still be retained against frozen inputs. Only the approved ten cause-matched stale generated-output pytest cases and exact96-v112 retained-count teardown may remain pending. Separate actual16/provisional112 resource proof is required because that teardown aborts later budget checks. No final candidate/full-functional/128image/budget/two-rootCI approval is claimed here. The final exact external receipt and reviewer record carry actual results without embedding a self-referential tree identity.


AM4 D* RTL follow-up: `am4-distance-red.log` reports1 failed/1 passed, with measured D.left1061.765625 > star.left1054.046875 in Arabic. `am4-distance-green.log` passes3 journey/parity tests after existing directional isolation; the regression still measures actual glyph order, not DOM text order. Root original screenshot/source evidence is `s18b/root-visual-observation-am4-r1.json`, SHA-256235c81cee81bead9654e58cbc935534b4ed73f836534f35c8d92737ae02631c5.

## AM5 / F4–F6 focused results and final diagnostic boundary

Evidence remains in the external overnight `s18b/implementation/` directory and disk-backed `s18b-proofs/`; the delivery workflow must publish the final receipt and checksum references durably.

| Retained evidence | Actual result | Meaning |
|---|---|---|
| `am5-original-red-r1.log` |26 failed,6 passed | Actual rejected product: missing integrity, inconsistent joins, raw statuses and missing vector links |
| `am5-focused-units-r2.log` |139 passed | Current contract/catalogue/static/module/harness checkpoint before final literal grammar correction |
| `am5-targeted-browser-r1.log` |93 passed,1 failed | Actual hidden vector caller; failure retained and separately corrected |
| `f5-original-red-r2.log` |2 failed | Both locales reject real builder-valid unavailable simulation on rejected source |
| `f5-exact-grammar-red.log` / `f5-exact-grammar-green.log` |1 failed →112 passed | Two finite claim tokens plus unchanged negative controls |
| `f4-vector-word-red.log` |1 failed | Actual word split on pre-fix CSS with new exact regression |
| `f4-race-caller-r1.log` |1 failed | Original delayed-locale caller hit the hidden vector claim |
| `am5-complete-focused-browser-r2.log` |151 passed | Full five-file focused browser run; includes corrected callers, vector word ranges, true absence, integrity, joins and screening |
| `f6-scenes-original-red-r1.log` |16 failed scenes | Strong semantic visibility oracle detects original chrome-only capture preparation |
| `f6-scenes-corrected-green-r1.log` |15 passed,1 failed | Filename does not mean pass; subpixel heading clipping retained |
| `f6-scenes-corrected-r2.log` |16 passed scenes | Entire required content boxes and API contexts verified; local diagnostic captures only |

The final source checkpoint and subsequent full pytest, unmodified make e2e-functional, manual inspection and actual16/provisional112 resource outcomes belong to the additive external receipt. Only the ten precisely cause-matched stale generated-output tests and exact graph preflight96-versus112 teardown may remain pending before generation. Nonzero commands remain failures. Final128 canonical resource/compare-only proof, strictR1, two clean-root CI runs, hosted checks and independent exact-head approval remain required; none is inferred from these focused results.

## Actual source, generation and canonical phase evidence — 2026-09-24

Separate source/UI R2 approved the frozen subject after82 independent focused tests and bounded browser/image review. Retained full pre-generation diagnostics remain **3,318 passed/10 failed/1 warning, exit1**, and **661 functional passed/4 deselected/1 exact retained-count teardown, make exit2**; neither is a final green gate.

Approved read-only proof establishes complete unnormalized real-decision equality across six observations per case for all11, full public repeat and unchanged normalized925/1045 graph. Only33 catalogue-version and6 authorized simulated route8 graph-reference leaves differ in remaining analyses. Graph-property, duplicate-edge and real-decision-headline controls reject. Steel/public remains INVESTIGATE; PP/public+simulated remains REJECT/0. One graph/manifest invocation each then passed integrity, scenarios, full reconstruction, graph validation and build-check. Existing PDF stderr warnings remain retained.

The single pinned canonical runner passed **4 visual tests/661 deselected**. Final128 totals **11,666,326 bytes**, maximum **153,100**;112 old paths compare as96 pixel-changed and16 exact-RGB encoding-only, with16 executive additions. Manifest SHA-256 is `c5bded34d25b3b8a6f48e949982b11251c22ada4e90bf3e1c6ffcf7be3a3154b`, change_ref `S18B-BILINGUAL-EXECUTIVE-SURFACE-1`. Actual256 retained80/100 encodings match frozen hash/size/dimensions/RGB results. Separate original112 proof reproduces112/112 quality80 bytes at16,802,282: the unchanged16,777,216 budget remains **FAIL by25,066**. Quality100 totals11,027,172 with identical pixels/dimensions. These populations and results are not conflated.

Separate canonical-result review `3881ff0c41e7e0d308981e35a01f1397ca4f61d0f44bfc27471899c5e53582ef` and delegated acceptance `116a8db6e7632451ec8492cf75df194017cfe537fde1ca9b28966aa809cd8c4a` accept the completed operation and supplements. All final-candidate/R1/two-rootCI/hosted gates remain pending. Scoped Arabic review leaves AR-V01 open; required S19 original-defect RED→GREEN is future evidence, not executed tests. [pr_record.md](pr_record.md) binds receipts, actual reviewer attribution and eventual durable delivery publication.

The mechanical frozen-baseline pin is `8a4a6650c710fad02b4690c9ba85a1597ed4118a`. The executor changed only that subtree OID; the expected count remains 128. Its pin receipt records the actual update, not a final-candidate test result.

## Actual failed final gates and correction proof — 2026-09-24

Retain failed tree `1345ce5c8b53927825d1b6791c838968af2e9090` and proof commit `5a964b0ad3c675cc20c650651f62878b1fe6eb21` as REJECTED/INCOMPLETE. Full pytest produced **3,326 passed/2 failed/1 warning, exit1**: two stale724 assertions against the correctly approved726 manifest. Strict R1 also exited1: EN rows2/3 `small.right` moved+2.56px; AR rows2/3 `small.left` moved−2.56/−2.57px. N=N2=N3 and the other required fields were preserved; this was a genuine product failure, not a waived comparator result.

Reviewed correction evidence: original-CSS RED **2 expected failures/10 deselected**; corrected-CSS GREEN **2 passed/10 deselected**; existing named browser matrix **12 passed**; focused count tests **2 passed**. Independent review rederived raw geometry/restoration and inspected all16 trace-served CSS bindings; these host Chromium results do not replace canonical-container, strict R1 or full CI proof. The actual source adoption preserved all graph228 hashes, while CSS/test_graph changed within visual103. Full `make e2e-functional` under `s18b/ops-product-adoption-r1` exited0:661 passed/4 deselected in1,682.73 pytest seconds (1,692.692 command seconds). Canonical R2 stock capture exited0:4 passed/661 deselected in394.03 pytest seconds. Its128 images total11,666,814 bytes, maximum153,100;124 are byte-identical to R1 and four EN/AR desktop adjacency/shared-enabler pairs are pixel-changed and independently viewed at native resolution. Prior inspections are reused only for the124 exact-byte images. Finite codec execution exited0 with128 rows/256 retained encodings, exact decoded RGB and canonical repeat100 bytes. Visual manifest is `a4e72a65002bc8e6aede62a739a4e3ab9c67729791e376d5ff993725527bb6ce`; mechanical baseline subtree pin is `fe06e710d6180ca3261850182c4d9299b75dff4c`, expected count128 unchanged. Completed-result review and delegated acceptance are separate from the receipt's preserved historical review-pending wording. Historical canonical R1, original112 quality80 budget FAIL and earlier codec approvals remain unchanged in scope. Every new exact-candidate Task7/R1/two-root/hosted/review gate remains required. [Records and full hashes](pr_record.md).
