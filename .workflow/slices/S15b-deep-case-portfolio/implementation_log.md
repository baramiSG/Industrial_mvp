# S15b implementation log

## Candidate and method

Implementation remained on `slice/s15b-deep-case-portfolio-scenarios-and-goldens` at base `f671f36`. AM1's owner-provisioned one-LF paths were substantively authored through native existing-file edits. The four public snapshots were produced by the approved builder after two separate scratch builds byte-compared equal. No file-creation capability is claimed.

## T1–T2

Review-check behavior began with five failing assertions and reached five passing assertions. The utility now inventories content/modes, checks scope and preservation, validates frozen byte/tree pins, compares candidate/mirror/tree content, compares safe Git controls and enforces visual-region drift.

The initial portfolio RED showed seven rather than eleven cases and four missing snapshots. Four builder-derived PublicSnapshot 2.2.0 records and four project entries made the public suite green. Scenario authorship then exposed the approved no-EVSI/runtime contradiction. Product work stopped without altering data.

AM2 classified S15B-EVSI-01 VALID. New absence-spy, supplied-invalid and valid-compatibility tests produced eight implementation failures plus one corrected test-oracle mismatch (`next_fact` is present with null). The narrow `simulation.simulate` key-presence branch then passed 28 focused and 153 compatibility tests. All eleven scenarios validate and back-test. Computed S15b outcomes are ADVANCE/1, REJECT/0 EX-03, ADVANCE/2 and REJECT/0 EX-01; public results remain INVESTIGATE/null.

## T3

A manifest-verified `case_selection_view` adapter and `/api/case-selection` expose only the pinned public selection. The response carries the selection/rule/reference/input identities and both complete profile projections. Unsafe references, wrong selection sets, hash mismatch and API integrity failure are tested. UI catalogue 1.4.0, a bilingual selection section, native details, retry-safe loading and S15-only dossier 1.3 references were added.

AM3 admitted two stale contract tests after the initial out-of-scope edits had been restored. Registry tests observed 3 failed/8 passed, followed by 11 passed after exactly two literals. Contract tests observed 6 failed/27 passed, followed by 33 passed after exact D1–D3.

The owner-provisioned Chromium runtime passed preflight. The three named S15b browser behaviors passed 11/11. The first full functional run passed 482/498 and failed exactly 16 keyboard matrix nodes because the existing focus inventory omitted native summary controls. Stable selection-summary identities, summary inventory and the accessibility order oracle were added. The subsequent full nonvisual gate passed 498/498 with zero skips.

## T4 and preservation

Core 01/02/03/04/07/09, README, API reference, ADR-025, route matrix, requirements, roadmap, progress and limitations were updated. KL-124 records undemonstrated MONITOR; KL-125 records hypothetical Class-D analyst-draft facts and null aggregate EVSI.

Transient deviations are retained: initial provisional edits to two then-unapproved tests and `static/app.js` were restored before AM3; the later green contract scratch path was reused once for an additional static regression run rather than remaining unused. No failure evidence, product data, source authority, secret or deferred generated artifact was overwritten. The owner reconciled the logical clean index and retained raw hash `19a671…`; this implementation did not reset or stage it.

Generation counters remained visual 0 and manifest 0 at T4.

## Coordinated completion artifacts

Owner reconciliation confirmed `HEAD`, fetched `origin/main` and `FETCH_HEAD` all remained `f671f36`, so no rebase or integration edit was required. Final graph inputs were frozen before T5. Separate `graph-refresh-a` and `graph-refresh-b` builds produced the same `GRAPH-SAU-2026-09-12-e651e7065377` identity and byte-identical pointer, manifest and projection. The final direct write contains 922 nodes and 1,041 edges; graph validation, rebuild-check and 42 graph tests pass, including eleven public inputs, eleven scenario inputs and public/Class-D partition coverage. Both prior projection directories remain unchanged.

Visual source now defines 24 screens across two locales and two viewports: the 19 retained screens plus four S15b public workspaces and one selection/exclusions screen, for 96 future canonical entries and a 16 MiB aggregate budget. The matrix contract produced the expected pre-generation RED at 76 versus 96. Diagnostic capture used the preserved base and candidate servers across all four locale/viewport pairs. The first diagnostic attempt failed `NAVIGATION_SCROLL_NOT_SETTLED`; using the existing governed `open_screening` wait path passed without weakening the anchor. Off-viewport dossier coordinates were excluded. `visual-regions.json` contains only ordered, clipped, positive-area portfolio and workspace rectangles; screening and dossier regions are intentionally absent. Bounds regressions produced RED 3/11 then GREEN 14. The current unchanged 76-image B/W comparison passes the region parser.

The scratch-only Compose subject contains only `industrial-mvp-neo4j`, the pinned S16a image/digest and secret-staging/healthcheck semantics, loopback ports 7475/7688, and run-owned volume/network. It was not started and no credential value was read or written by this seat.

A pre-generation completeness pass added the four S15b IDs to the already-approved performance tuple without changing its threshold, five-sample method, mode/suffix matrix or screening test; the complete module passes 91 tests. Coverage diagnostics then measured all 36 old workspace image identities individually and checked all 16 old dossier plus all 16 old screening identities. Two retained diagnostic attempts failed because a redundant cleanup click requested a summary-back control after the next-run reset made it unnecessary; removing only that diagnostic cleanup produced a complete pass. A final workspace-only pass waited for the actual mode/state and catalogue version before measurement, replacing the earlier extrapolated steel geometry with per-case/mode rectangles. The final region subject has 44 mappings: eight portfolio images and all 36 old workspaces. Every dossier and screening check reports null visible version geometry, so none receives a mask. Failed and superseded diagnostics remain preserved under distinct attempt directories.

## Pre-generation correction round 1

S15B-PREGEN-F01 was classified VALID. The profile heading had converted `pharma_api` to `pharma api` inside the Arabic catalogue sentence. A two-locale real-browser assertion failed 2/2 before correction. The renderer now keeps catalogue text escaped and replaces a fixed interpolation marker with `technical(profile.profile)`, yielding a raw `pharma_api` LTR technical token. The same test passed 2/2 and refreshed EN/AR selection screenshots.

S15B-PREGEN-F02 was classified PARTIALLY_VALID. The reviewer and owner correctly showed that attempt4's steel-simulated diagnostics were in the wrong active state and unsettled; its four region mappings were invalid. New diagnostics require the exact selected ID, active mode, expected active state, two synthetic labels, `DEMO_GENERATOR`, catalogue version, loaded fonts and three stable layout frames before capture. All 36 before/after workspace identities then passed with matching state-bound geometry. Four steel-simulated mappings were corrected. Other reviewer-noted 1px differences remain because the approved region is the minimal measured before/after union; a new verifier compares every workspace mapping with that union and proves 36 workspace plus 44 total regions. Attempt4 remains cited as rejected evidence.

Correction verification passed 15 review-helper tests, 67 focused static/API tests, UI/ES checks, graph build-check/validation at unchanged `e651e7065377`, and the full 498-node nonvisual Chromium gate with zero failures/skips. Canonical visual and manifest counters remained zero at pre-generation re-review.

## T6 single canonical launch and verifier completion

The retained reviewer approved the corrected region/render/Compose subjects and the owner released exactly one V10 invocation. Before launch, the approved hashes matched, B was clean with 76 entries, `.candidate`/`.previous` and the canonical artifact mount were empty, image id `2cc7…` matched, and the target index remained `19a671…`. The single container launch asserted canonical Chromium and exited successfully with 4/4 visual capture nodes. It generated 96 lossless RGB entries: 76 retained paths, 20 additions, no removals, 14,724,616 aggregate bytes, maximum 230,978 bytes, all uid1000. Manifest SHA-256 is `4fd90577…`; the computed baseline tree OID is `0d2e0fe9…`. Three tree literals and `VISUAL_BASELINE_ENTRIES=96` were updated as authorized.

The original read-only verifier was completed test-first. RED was 2 failed/15 passed for missing canonical C and actual-byte tree computation; GREEN reached 17 passed, followed by 38 combined verifier/visual-contract passes. It now emits full inventory/modes/hashes/records/target evidence, actual-byte Git tree OIDs, stronger data preservation, full mirror/tree mode checks and bounded effective control identities. A subsequent RED exposed that RGBA `getbbox()` concealed RGB drift; changing the verifier to compare RGB produced 18 passing tests.

The initial RGB V16 report was then investigated under S15B-T6-MASK-01. A positive inside-region regression proved the one-bit mask with fill `1` left the inverted mask white inside and outside; its Arabic portfolio failure was therefore INVALID as a region result. An 8-bit `L` mask with white `255` corrected the application: inside-region RGB change passes and outside-region change fails. The full corrected comparison reports 52 residual old images outside approved regions: all 36 workspaces and all 16 screening images; all 16 dossiers are identical.

Read-only residual reports quantify bbox, changed pixels, maximum delta and governed metrics for every residual. Desktop workspaces and screening images contain substantial high-delta changes; some tablet screening pairs contain only one-level noise, while other tablet/Arabic images also fail tolerance. Fresh B/W state-bound captures reproduce the same residual metrics, excluding image/container provenance as the sole cause. Layout diagnostics show identical selected case/mode/state, loaded fonts, viewport geometry and visible workspace rectangle, but adding the portfolio/selection content changes absolute section position and its fractional viewport phase: representative EN desktop workspace top is `94.625` before and `94.859375` after, with scrollY `2446` versus `3999`. Normalized workspace DOM also differs because the select contains seven versus eleven options. These long-page anchor/absolute-layout changes explain widespread raster movement despite unchanged visible decision semantics; screening shows the same long-page pattern, whereas standalone dossier pages remain pixel-identical.

The generated files and pins are preserved. V10 count is 1 and exhausted; manifest count remains 0. No region widening, capture change, second generation, restoration or T7 action occurred. The ordinary index remains logically staged-empty at owner-reconciled raw SHA-256 `4d7fa3…`.

## T6/T7 accepted artifact chain

AM4 replaced only the extra S15b zero-RGB condition for 52 registered workspace/screening derivatives. Its owner-authored conjunctive verifier, exact state/DOM/style/source/runtime/image-linkage checks and Pillow dependency correction passed retained independent review; S15B-AM4-OWNER-F01 through F04 are closed. The original raw drift remains recorded against the retained parent.

Independent review then found S15B-UI-HHI-01: string `NOT_CALCULABLE` rendered as localized not-a-number. The exact local finite-number guard, executable EN/AR renderer tests and four-case bilingual browser assertions were approved. Functional proof is 68 focused nodes and all three named S15b browser tests; zero and valid numeric HHI remain numeric and public decisions remain INVESTIGATE.

The owner preserved the 1,479-file parent, released one corrective canonical update, and accepted the resulting eight desktop HHI-card changes after viewing them and rerunning the strict comparator: 96 common, 8 changed, 88 unchanged, 0 outside, 0 added/removed. Canonical count is 2 and exhausted. The accepted visual manifest is `ed998bb8…`, baseline tree `3698f644…`, 96 lossless RGB images, 14,718,710 bytes, maximum 230,978 bytes, all uid1000. Twenty-two focused image/provenance checks and four ordinary visual nodes pass.

T7 was run exactly once by the owner (`scripts/build_manifests.py`, command `d4b4e2`, exit 0). Snapshot-manifest entries increased 703 to 713 through exactly four public, four synthetic and two graph additions; 702 old rows are identical and only the existing graph pointer row changed. The 20-entry authority table contains exactly the seven approved changed rows. Post-manifest integrity, preservation, 11-scenario validation, reconstruction and graph build-check all pass. No implementation review, materialization, commit or delivery is claimed here.
