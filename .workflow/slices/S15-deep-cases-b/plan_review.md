# Plan review — S15 decomposition-1 and plan-1-s15a (attempt 1)

**Subjects:** `.autonomous-workflow/plans/s15-deep-cases-b/cycle-1/decomposition-1.json` (SHA-256 `ea01de65…`) and `plan-1-s15a.json` (`819e08fd…`), authored by `planner-fable` against `main` `ec859f7` (in-flight hashes re-verified by the planner against the merged bytes); mirrors `decomposition.md`, `plan.md` in this folder.
**Supervisor (owner lead agent) material review — 2026-09-13:** no B1–B7 finding. Independent review by `reviewer-grok` follows; `PLAN_APPROVED` only on its APPROVE.

## Supervisor checks

- B1: no authored public field; capability `U` = unverified within the searched sources (precision rule); no forced outcome (public engine expected INVESTIGATE; stop otherwise); MONITOR not manufactured (OD-4 no waiver — recorded UNDEMONSTRATED under R-5); residual exclusions only from stored WCO text with HS revision and address; `SERIES_GAP_YEARS` typed (missing ≠ zero); families 1.2.0 and sources 1.5.0 owner-ruled with methodology-cited basis; history retention keeps the S14 selection record byte-reproducible (fifth coupling); one manifest run per child; frozen roots untouched in s15a.
- B2: candidates computed from the frozen snapshots under S14-CS-1.1 in variants A/B/C with the coverage-key sensitivity, the snapshot-wide MONITOR search (4,873/4,996 material triggers), provisional briefs run through the public engine and five scenario dry runs through the real validators/selector — all recorded as provisional until stored text/`M15`.
- B3: exact oracles (pinned selection tests on doubles and the real run twice; `CASE SELECTION RECONSTRUCTION PASS (2 records)`; WCO index hrefs re-read verbatim; window budgets; manifest oracle; portability; `make ci`).
- B4: Manifest §7 mapping complete (families 1.2.0 + history 1.1.0; sources 1.5.0; Core 02/04/05/09 for s15a; Core 07/09 and the governed oracles for s15b); exactly one run per child.
- B5: RED-first on doubles before real runs; acquisition of producer/register documents AFTER the selection run so acquired documents never re-order the selection (T5 → T6); WCO first.
- B6: robots/terms first; UNAVAILABLE honest; Comtrade V3 ≤ 2 HTTP per unit; no key in artifacts; no Etimad purchase.
- B7: OUT respected — no portfolio/scenario/golden/visual change in s15a; no route 8; boundary sets shown, not selected.

## Owner rulings (`.autonomous-workflow/owner-decisions/20260913-s15-plan-1-rulings.md`)

OD-1…OD-3, OD-5…OD-11 ACCEPTED as proposed (OD-3 provisional until stored text; OD-6 budget raise to 16 MiB as a governed oracle change in s15b; OD-9 plus a visible backlog item for a user-facing selection/exclusions panel). OD-4: NO WAIVER — MONITOR recorded UNDEMONSTRATED and returned to the owner under R-5.

## IAC (transferred to the implementer)

- IAC-1 Bytecode isolation; isolated worktree from current main; prepared against `ec859f7`/`1289e31`, integrated onto `M15` (s14b merged) before delivery with the reviewer assessing the integration delta.
- IAC-2 Every exclusion row: rule version, HS revision, verbatim WCO text with stored address, group structure, judgment reason; every "not found" statement names sources, dates, filters and access limitations.
- IAC-3 Selection run twice, byte-identical, from hashed inputs; never a hand edit; acquisition after the run.
- IAC-4 Comtrade windows: TERMS + 1 data request per unit; key in-shell only; observed semantics; the AM-3 predicates; honest MISSING/OBSERVED/ZERO.
- IAC-5 `make ci`, portability copy and the CI-equivalent scratch-clone gate before hand-off; canonical identity with trailing LF and the full base sha.

### Independent plan review — `reviewer-grok`, 2026-09-13: APPROVE, no findings — PLAN_APPROVED

Recomputed on `main` `1289e31`: the fifth coupling (live families sha equals the S14 record; the S14 pin uses the live path), variants A/B/C (B: 294110/294120, 310430/310510), the typed `SERIES_GAP_YEARS` and viability exclusions, the H6-only MONITOR search (4,873/4,996 material; in-scope free viable lines 293723 and 310560 only — OD-4 stands), the four scenario designs on provisional snapshots (ADVANCE route 1; ADVANCE route 2; REJECT EX-03; REJECT EX-01; Gate B PASS; back-tests match), "no governed input references 1.4.0's hash" TRUE (no 1.4.0 history needed), stored WCO hrefs are numbered 0739/1572/1576 (verbatim index re-read required). Binding IACs: stored-text identity rows; S14 pin re-pointed to the recorded families identity by content hash; history rule extended to CASE-SELECTION inputs; s14b 2.2.0 proof after M15 without editing pinned modules; `files.modify` controls `cli.py` (selection subcommands only); 310530 is a DD-2 exclusion; precise seed_basis wording; W-P15 per-unit pattern; canonical identity + scratch-clone `CI=1`. Record: `.autonomous-workflow/evidence/s15-deep-cases-b/plan-1-review.json`.

### Post-approval owner rulings applied at integration

OD-12 registered exactly three document sources. OD-13/OD-14 govern
same-day scoped partner identity and disjoint recorded units. OD-15 records W1
`697322e`, W1' `7d4853e8d979d86a5fa932fbd4468bceacfb63b4`, M15
`6dc966a9f210b47a94b96aaeffadcbcc6642415f` and the owner-resolved
five-snapshot/nine-brief integration count. OD-16 makes recorded-input
`reconstruct-selection`, not a post-W-A15b current-state select, the T11
identity oracle. These rulings do not constitute implementation approval; the
independent reviewer must assess both integration resolutions.
