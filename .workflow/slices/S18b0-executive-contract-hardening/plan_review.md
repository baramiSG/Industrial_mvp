# S18b0 independent plan review

Verdict: **APPROVE** for exact plan SHA-256 `00dc585a6f909b291195faceb7bc94b5decbd6cccccf8182b7039557d42bc67e` and bound publication manifest SHA-256 `45359ab136094d6ddb14bb50ab58fa810ced7e8f76475b72072c24a278c175ef`. Reviewer: `/root/s18_followup_plan_review`, independent same-model Codex plan reviewer. This is not implementation approval, cross-model review, or Claude approval.

## Basis

The separate follow-up preserves S18a's approved tree and packet. Production changes are confined to executive projection validation and model provenance; no Core, configuration, domain engine, graph, visual source, baseline, manifest or frozen evidence edits are authorized. The plan requires proving that intersection against actual discovery before writing, with a reviewed amendment for drift or a new required path.

The proposed validation addresses concrete operations visible in the current source: empty/malformed latest-trade `max`, unchecked required R11 `next`, coercing/indexed EVSI extraction, and unchecked dependent scenario mappings. Existing `executive/api.py` already maps ExecutiveIntegrityError and Pydantic ValidationError to the governed422; it remains unchanged. Guards belong at the failing extraction boundary, with no broad exception catch or success fallback.

EvidenceReference's existing public branch rejects scenario/label metadata but currently permits status=synthetic or source=DEMO_GENERATOR with flagfalse. The plan closes those two markers while explicitly preserving legitimate public ClassD proxies. Supplied invalid EVSI cannot become unavailable or zero; absent optional EVSI remains valid and zero remains available. The tests require True/False, string and nonfinite cases, immediate/model/API boundaries, original-defect RED evidence, cache isolation, unchanged AM4 assertions and all11 valid response/outcome byte equality.

Coverage distinguishes newly repaired defects from existing correct validators. Current R4-F/R4-D coverage is retained; single missing referenced rows fail closed without requiring every optional suffix. Explicit case-level EVSI availability and broad case-wide R12 needs remain open with precise follow-up criteria. No claim that all malformed inputs are covered is permitted.

The verification sequence requires focused RED/GREEN, one exact clean-root full makeci, separate-path full pytest/integrity/reconstruction/e2e, unchanged frozen/provenance hashes and independent implementation review, then hosted checks and exact-head approval before delegated merge. It does not borrow AM4's implementation approval for new code.

## Durable publication scope

I independently verified all54 exported records in the bound coordinator manifest: matching digest and size, unique relative destination paths, total1,940,576 content bytes; the manifest itself is a separate55th immutable file. Automated credential-pattern checks across those exported bytes found zero alerts for private-key delimiters, GitHub token formats, long API tokens and assignment-like credential values. This is a scoped publication review, not a general security certification; the plan still requires final secret/prohibited-file review before staging.

Publication includes the byte-preserved full AM4 receipt, original checksum inventory, exact candidate inventory/diff, complete gate logs/results, raw bilingual R1 measurements and authenticated Claude text. The new README/checksum/delivery_record wrapper must resolve renamed `.log.txt` paths and preserve the receipt's historical local references without editing its bytes. The original856-file bundle is explicitly retained locally; this selected durable subset must never be represented as the entire bundle. Candidate archives, Git indexes, credentials, owner source logs and bulk runtime paths are excluded. Existing S18a files remain untouched; new evidence records are additive under their explicit paths.

## Preparation and provenance

Reviewed AGENTS/Manifest; authoritative methodology DOCX evidence, AI, intervention and dossier requirements; Core04§15 and Core09§11; relevant configuration/goldens; S18 plans/amendments, AM4 handoff/receipt and Claude's supplied verdict; owner overnight delegation; actual executive service/case/provenance/model/API code and tests. Personas chosen after reading: API-contract reviewer and evidence-provenance/build auditor. Skills read/applied: task-standards, project-orientation, strict-reviewer, github-flow, sanad-provenance, al-muhasibi and muhasabah-gate.

Direct evidence: source inspection establishes the operations/invariants above; my publication verification recomputed file hashes/sizes and scanned bytes without printing credential-like contents. The planner's read-only diagnostic file supplies exact prior exception observations; I did not independently rerun its probes or any product tests. Task1 must supply original endpoint-failure evidence before a fix is claimed. S18a tests/gates remain attributed to its receipt and prior reviewer.

## Self-audit

Muhasabah: **PASS for plan approval**. Requirements, permitted paths, false-positive protections, source immutability, independent roles, test evidence, delivery gates and open residuals are explicit. Assumptions: the coordinator binds the actual merged S18a base and confirms the writer lock; valid current values are unchanged by stricter validation. Risks: overrestricting valid proxies/optional data and incomplete publication provenance are covered by positive controls, canonical output comparison and hash-preserving publication. No implementation, test/gate completion, Aura result or final release acceptance is claimed. I changed only this external review report.
