# S18b0 context and boundaries

Base: delivered S18a merge `b4a00adb685c61c4d50c24e94647fbd24e599096`,
tree `1364789a964f96f6ebabf8eae1d239a5bad0b15e`. PR37 and main verification
are recorded in the additive S18a delivery record. Source branch:
`slice/s18b0-executive-contract-hardening`, isolated worktree
`/home/barami/projects/ior-worktrees/s18b0`.

This child follows the exact approved `plan.md`. It repairs enumerated
malformed executive projection paths and model partition markers while
preserving every valid response, schema and engine decision. The coordinator
owns control-document/state changes and the finite immutable S18a evidence
publication; the implementation author owns only the permitted product/tests
and these new slice notes.

Relevant authority: governing methodology evidence/AI/governance/dossier
sections, Core04 sections14–15, Core09 section11, evidence policy and the
approved S18a amendments. Upstream frozen inputs are valid today; injected
negative fixtures exercise the projection boundary directly and do not claim
these malformed inputs are present in official or frozen evidence.

There is no Core/config/graph/static/browser/baseline/golden change or generator
allocation. No executive API handler edit, schema expansion or dependency
change is permitted. The graph/Aura operation is a separate task and no graph
service or Aura environment was used for this child implementation.

Explicit residuals: case-level EVSI availability marker remains open, with an
exact summary-row join proposed for frontend presentation; broad R12 needs
remain case-wide; unenumerated malformed paths remain unverified; KL-132 needs
its separately authorized Core/generation follow-up. Existing meaningful R4-F
and R4-D mapping assertions remain byte-identical.

Sanad: base and file-boundary identity directly verified; delivery CI is
attributed to the coordinator's linked delivery record. Muhasabah: scope and
remaining uncertainty stated; no broader closure or release approval claimed.
