# Known Limitations

Updated by the Supervisor at every slice completion. Items marked *pre-existing* were present in the v0.1.0 package before this build.

## Open (to be closed by a roadmap slice)

| ID | Limitation | Source of requirement | Closing slice |
|---|---|---|---|
| KL-33 | The Decision Dossier "Supply conclusion" block renders the `domestic_capability` object as a raw JSON source-language island instead of the methodology §15 structured supply conclusion (named producers, effective qualified capacity, utilisation, target specifications, planned changes). Pre-existing since S07; unchanged by S08 but more visible with the richer v2 object. | Methodology §15; Core 04 §9 dossier contract. | S19 — bilingual dossier redesign renders every §15 block structurally. |
| KL-35 | Per-rule `may_support_advance` is configured only for `R1_D` today; restricting further rules requires a Manifest §7.3 operating-configuration change. | Manifest §7.3; thresholds 1.2.0. | Owner decision at next roadmap review. |
| KL-36 | PublicSnapshot 2.1.0 validation requires numeric non-empty `downside_cash_flows_m_sar` and `hurdle_rate` per route-evidence record, so a governed snapshot cannot express present route economics as UNAVAILABLE although Core 07 §5.2 permits missing values; the engine path is proven through mocked-loader API and compute-level tests with the validator boundary unchanged. | Owner F-01 (2026-09-03); Core 07 §5.2. | Unassigned — would require Manifest §7.4 Core 04/07 change. |

## Closed by completion slices

| ID | Limitation | Resolution | Evidence | Closure |
|---|---|---|---|---|
| KL-01 | Threshold literals `0.40`, `1.25`, `50` embedded in engine code (*pre-existing*). | Config-sourced predicates (`rules.py`, `decision_engine.py`), thresholds 1.1.0 R11 key, `scripts/check_threshold_literals.py` guard in CI and `make ci`. | `.workflow/slices/S02-threshold-governance/test_evidence.md`; CI run 33573669072 on PR #2 — 4/4 pass. | Squash merge of S02 (PR #2, `c438370`). |
| KL-02 | R3 compared `top_two_value_share` with the largest-supplier threshold (*pre-existing*). | `rules.r3_fires` uses `largest_supplier_share` only; absent → `NOT_CALCULABLE` in metrics. | Same evidence as KL-01; `test_r3_does_not_substitute_top_two_share_for_largest_supplier`. | Squash merge of S02 (PR #2, `c438370`). |
| KL-03 | No threshold boundary tests (*pre-existing*). | `tests/test_threshold_boundaries.py` (R1-D, R2, R3, R11, Kmin, D\* bands, competition; below/equal/above). | Same evidence as KL-01. | Squash merge of S02 (PR #2, `c438370`). |
| KL-04 | Evidence policy did not require `display_label`; a scenario missing it raised `KeyError` (*pre-existing*). | `evidence_policy.v1.yaml` 1.1.0 requires all Core 06 §4 fields plus class/source/label; `validate_synthetic_scenario` raises typed `EvidenceIntegrityError`; repository loader delegates. | `.workflow/slices/S03-evidence-isolation-hardening/test_evidence.md`; CI run 33579923763 on PR #3 — 4/4 pass. | Squash merge of S03 (PR #3, `ddf905d`). |
| KL-05 | No validator reconciled synthetic scenarios to public marginals (*pre-existing*). | `evidence.reconcile_synthetic_scenario` (demand, nameplate, factors, qualified availability; INFORMATIONAL demand layers; NOT_APPLICABLE allocation) blocks simulation on FAIL; `scripts/validate_scenarios.py` Gate B in CI and `make ci`. | Same evidence as KL-04; Gate B `SCENARIO VALIDATION PASS (2 scenarios)`. | Squash merge of S03 (PR #3, `ddf905d`). |
| KL-06 | Analysis response did not expose methodology/config versions per case (*pre-existing*). | `config.authority_summary` in every analysis, banner and dossier (FR-001). | Same evidence as KL-04; `tests/test_authority_disclosure.py`. | Squash merge of S03 (PR #3, `ddf905d`). |
| KL-07 | Simulated ledger repeated the public R6/R7/R8 `DISABLED` rows instead of re-evaluating with labelled synthetic inputs (*pre-existing*). | `rules.evaluate_simulated_rules` appends Class-D labelled R6/R7/R8 rows in simulated mode (FULL/DEGRADED/DISABLED with `NOT_CALCULABLE` metrics); public ledger carries none; UI and dossier disclose them. | `.workflow/slices/S04-simulation-fidelity/test_evidence.md`; CI run 33584437086 on PR #4 — 4/4 pass. | Squash merge of S04 (PR #4, `98c1a40`). |
| KL-08 | Steel/PP branch dispatch and decision narrative hard-coded in engine code; scenarios carried no ground truth (*pre-existing*). | Generic `_simulate` (Core 07 §7.4 → §7.3 → INVESTIGATE); scenarios 1.1.0 carry `ground_truth` and `decision_narrative`; runtime and Gate B back-test fail closed. | Same evidence as KL-07; Gate B back-tests PASS for both scenarios. | Squash merge of S04 (PR #4, `98c1a40`). |
| KL-09 | No CI workflow; proof commands run manually only (*pre-existing*). | `.github/workflows/ci.yml` (uv 3.12/3.14, pip 3.12, Docker build) running scan → compile → node → integrity → pytest → smoke; local `make ci`. | `.workflow/slices/S01-ci-and-toolchain/test_evidence.md`; CI run 33569855956 on PR #1 — all four jobs pass. | Effective on squash merge of S01 (PR #1). |

## Closed by milestone v0.3.0 slices

| ID | Limitation | Resolution | Evidence | Closure |
|---|---|---|---|---|
| KL-22 | No Playwright or real-browser interaction/visual test was part of v0.2.0 final acceptance. | S06 adds 17 named Playwright tests / 62 Chromium nodes for journeys, keyboard/focus, axe WCAG 2.1 A/AA, RTL glyphs, four viewports, print/PDF, console/page/network/HTTP guardrails, and 40 non-oracle documentary references. | `.workflow/slices/S06-browser-acceptance-harness/test_evidence.md`; local `make e2e` observed 62 passed. | Closed on the squash merge of S06 (PR #8, `6d00e27`); PR CI run 33602331107 and default-branch run 33602662668 each ran `browser / Chromium / Python 3.12` green (62 passed); Grok re-review APPROVE zero unresolved. |
| KL-21 | Raw visual literals outside a complete token layer and one monolithic browser module. | S07 introduces an enforced token-only CSS layer, logical properties, 19 named-export ES modules at no more than 199 lines, recursive syntax checking, and bilingual browser regression. | `.workflow/slices/S07-bilingual-interface-foundation/test_evidence.md`; local implementation evidence only. | Closed on the squash merge of S07 (PR #9, `9f045a4`); PR CI run 33622991390 and default-branch run 33623528164 each ran all five jobs green (browser: 118 functional + 4 visual nodes); Grok APPROVE zero unresolved. |
| KL-31 | The demo topbar exposed FastAPI `/docs`, whose default Swagger UI loads public-CDN assets. | S07 removes the topbar link while retaining `/docs` as an engineer-only direct route; keyboard inventory remains 15 controls through the locale switch. | Static/API contracts and the 16-node bilingual keyboard matrix in `.workflow/slices/S07-bilingual-interface-foundation/test_evidence.md`. | Closed on the squash merge of S07 (PR #9, `9f045a4`); PR CI run 33622991390 and default-branch run 33623528164 each ran all five jobs green (browser: 118 functional + 4 visual nodes); Grok APPROVE zero unresolved. |
| KL-26 (formula/schema portion) | R5 lacked retained-import, net-exposure, apparent-consumption, and penetration computation. | S08 adds the four methodology §3.3 formulas with typed `UNAVAILABLE` inputs and per-measure `NOT_CALCULABLE` reasons. The two public fixtures still lack production/re-export inputs; that acquisition residual remains open for S12 and is not treated as zero. | `.workflow/slices/S08-snapshot-v2-computed-rules/test_evidence.md`; formula, rule, migration, golden, and scenario tests; local implementation evidence only. | Provisional S08 formula/schema closure; effective only after independent approval, PR merge, and green current-head/default-branch CI. |
| KL-30 | R1-D emitted a literal confidence cap instead of projecting `rules.R1_D.confidence_cap`. | S08 reads the configured cap for both metrics and decision effect; an injected non-C configuration test and recursive threshold scan reject hidden coupling. | `.workflow/slices/S08-snapshot-v2-computed-rules/test_evidence.md`; `tests/test_rules.py`; local implementation evidence only. | Closed on the squash merge of S08 (PR #10, `8b6d55c`); PR checks and default-branch run 33637594756 all five jobs green; Grok APPROVE zero unresolved. |
| KL-32 | Canonical visual updates wrote root-owned host paths. | S08 maps the canonical container to the host UID/GID, uses writable `/tmp` home/cache paths, asserts effective identity, and sweeps both writable mounts for host ownership after update. The S08 update produced 40 host-owned WebPs and host-owned manifests/artifacts. | `.workflow/slices/S08-snapshot-v2-computed-rules/test_evidence.md`; ownership unit tests and independent recursive `os.stat` evidence; local implementation evidence only. | Closed on the squash merge of S08 (PR #10, `8b6d55c`); PR checks and default-branch run 33637594756 all five jobs green; Grok APPROVE zero unresolved. |
| KL-20 | Only two of five methodology §6.3 sector profiles were implemented. | S09 adds all five profiles with nine weights, complete hard gates, Kmin/band validation and 82 profile tests. | `.workflow/slices/S09-public-decision-and-profiles/test_evidence.md`; PR #12 runs 33754472700 and 33755119710; Grok APPROVE zero findings. | Closed on the squash merge of S09 (PR #12, `66d4835`). |
| KL-23 (public branch) | Neither packaged golden produced `MONITOR`, leaving the generalized public branch unproved. | S09 adds a synthetic-free R3-only public fixture with a named trigger and route-0 `MONITOR`, while keeping both public goldens exact. | `tests/fixtures/public_decision/monitor-r3-only.json`; S09 test evidence; PR #12 and default-branch CI 5/5. | Public portion closed on the squash merge of S09 (PR #12, `66d4835`); simulated remainder stays open for S10. |
| KL-24 | Public route selection did not execute the complete route-hypothesis sequence. | S09 emits routes 0–8 as ordered hypotheses with lower-route precedence, maximum-unrounded-ΔNV selection, tie handling and route-8 `GRAPH_REQUIRED`; dedicated fixtures exercise the branches. | `tests/test_route_hypotheses.py`; S09 test evidence; PR #12 runs 33754472700 and 33755119710. | Closed on the squash merge of S09 (PR #12, `66d4835`); route-8 graph activation remains separately scoped to S16. |
| KL-23 (simulated branch) | Generalized simulation `MONITOR` and routes 0–7 were scenario-authored only. | S10 contract 2.0.0, `simulation.py`, ten fixture proofs, Gate B back-tests and frozen simulated exacts unchanged. | `tests/test_simulation_generalized.py`; S10-LOCAL and S10-CI. | Closed on the squash merge of S10 (PR #13, `a610b49`); PR run 33794229014 and default-branch run 33794894041 each passed 5/5. Public portion closed on S09. |
| KL-25 | R8 simulation was explicitly `DISABLED`/`NOT_CALCULABLE` because packaged scenarios lacked governed MES and related inputs. | Contract 2.0.0 exposes `minimum_efficient_scale_kt`; steel packaged scenario carries Class-D `50.0`; simulated R8 evaluates when inputs exist; boundary tests at 0.20/0.25. | `rules.evaluate_simulated_rules`; `tests/test_threshold_boundaries.py`; S10-LOCAL and S10-CI. | Closed on the squash merge of S10 (PR #13, `a610b49`); packaged residual for missing base-demand/probability blocks remains KL-37. |
| KL-27 | No expansion-assumption bypass existed; only the disclosed public nameplate ceiling applied. | Contract 2.0.0 `expansion_assumption` block with reconciliation check `expansion_assumption_disclosed_and_bounded`; structural bound without an authorized numeric multiple remains KL-38. | `scenario_contract.py`; `evidence.reconcile_synthetic_scenario`; S10-LOCAL and S10-CI. | Closed on the squash merge of S10 (PR #13, `a610b49`); PR and default-branch CI passed 5/5. |
| KL-28 | Synthetic scenarios contained no tariff-line or buyer-allocation blocks; reconciliation reported `NOT_APPLICABLE`. | Contract 2.0.0 governed `tariff_line_allocation` and `buyer_allocation` blocks; ten Gate B reconciliation checks including tariff-line and buyer totals. | `evidence.reconcile_synthetic_scenario`; `tests/test_scenario_validation.py`; S10-LOCAL and S10-CI. | Closed on the squash merge of S10 (PR #13, `a610b49`); PR and default-branch CI passed 5/5. |
| KL-34 | An admitted deep case with no fired candidate signal or unmatched residual branch failed closed with HTTP 422 rather than a screening disposition label. | Core 07 §7.6 now distinguishes no-trigger from other residuals: no trigger yields null formal state, `NO_CANDIDATE` and `NO_TRIGGER_FIRED`; other unmatched residuals still fail closed. | `tests/fixtures/public_decision/no-candidate-no-fired-signal.json`; public-decision, API, GenUI, dossier and browser fixture tests; S13b test evidence. | Closed in the S13b implementation candidate; delivery review, PR and merge remain separate gates. |

## Accepted for this MVP (recorded, not scheduled)

| ID | Limitation | Rationale |
|---|---|---|
| KL-37 | Packaged scenarios declare no base-demand, commitment-probability, production/retained-flow, tariff-line, or buyer-allocation blocks, so simulated R5/R8 remain DISABLED until S14/S15 fixtures or Ministry data supply them. | S10 records the limitation explicitly; engine semantics are ready when blocks appear. |
| KL-38 | Structural expansion bound uses disclosed public nameplate and declared `expansion_assumption.planned_nameplate_kt` without an authorized numeric multiple. | Reconciliation check `expansion_assumption_disclosed_and_bounded` enforces disclosure and ordering only. |
| KL-39 | Simulated MONITOR requires a declared `monitor_trigger`; otherwise validation fails closed with HTTP 422. | Prevents catalogue-only MONITOR shapes without a named observable trigger. |
| KL-40 | **Engine exercised; partner detail remains unavailable:** 1,471 R2-FULL survivors are emitted deterministically in lexicographic hashed batches. Historical `un_comtrade` W2 run `20260912T144127Z` recorded 59 `ENDPOINT_UNVERIFIED` units and zero requests; latest marker `fde161434c24999fd0049f9c5813f6743449fc4da0c317e696374abea0970860`. W0-ter still did not expose the all-partners token, so AM-2 prohibited a rerun. | Covered 0; uncovered 1,471 with `PARTNER_DETAIL_NOT_ACQUIRED`. Obtain an official, verbatim PARTNERS template/token before another bounded window. |
| KL-41 | BACI BULK is stored as raw evidence only; no analytical snapshot kind exists in S11 and BACI is never spliced into the reporter universe. | Core 05 §11 DD-22; `baci_cepii` in BuildReport.raw_only. |
| KL-42 | **Closed by OD-11/W1-ter:** official Basic Individual limits establish a non-paginated response below the 100K cap. Run `20260912T143742Z` passed every count/content/scope/classification predicate for all eight units and produced the 5,443-HS6 universe. | Universe snapshot is 24,195,845 bytes; latest-run selection supersedes the two incomplete historical runs without editing them. |
| KL-43 | Stored `zatca_tariff` pages were re-read and verified to resolve to `PageNotFound.aspx`; S13a W0 observed no documented tariff-tree data endpoint, so W3 did not run and no COMPLETE tariff snapshot exists. | Verify an official data endpoint before any new tariff acquisition; do not increase a request bound against the known page-not-found template. |
| KL-44 | `baci_cepii` latest run recorded `LICENSE_UNRECORDED` for 2024 BULK (`data/raw/baci_cepii/.../attempt.json`). | Terms capture did not complete under configured licence gate. |
| KL-45 | **Partially resolved in S13a:** the screening builder consumes the proven universe and governed entity inputs and emits 5,443 records; deep resolution remains S14. | S12 entity inputs are hashed but do not create capability without verified family links. |
| KL-46 | **Resolved as an isolation requirement in S13a:** the historical `wits_trade` universe run remains HTTP 403 / INCOMPLETE and is not substituted for UN Comtrade or spliced with another source. The complete WITS partner snapshot remains a frozen reconstruction oracle only. | Source-filtering, byte-identity and reconstruction checks preserve source boundaries. |
| KL-74 | **Closed:** W1-ter run `20260912T143742Z` accepted the real Saudi HS6 universe only after OD-11 mapped the official non-pagination cap to every DD-3 predicate. | Eight of eight units COMPLETE; 5,443 HS6, both flows, 2021–2024. |
| KL-75 | **Narrowed by S14a W-C:** UN Comtrade Basic Individual limits are observed (100K records/call, 500 calls/day, 5 calls/second). For HS 721061 only, omitting `partnerCode` returned one World and seven non-World H6 codes; this does not establish a general all-partners token or comma-separated `cmdCode` batching. | W-C stores the exact response and contract. No `all`/`ALL` token was tried and no claim is generalized beyond this unit. |
| KL-78 | W1-bis incident: eight uncommitted `attempt.json` files were edited after their run to copy metadata from immutable page contracts, violating write-once evidence history despite exact provenance. | OD-11 preserves both original/repaired SHA-256 sets in ADR-019 and the slice log, forbids further edits, supersedes the run with W1-ter and adds RawStore write-once tests. |
| KL-79 | A proven-universe screening cycle adds 52,798,074 bytes across 98 governed files even after common-field deduplication. | AM-2 per-file/total budgets pass; deterministic compression is a candidate future §7.4 change, not part of S13a. |
| KL-80 | W0-ter did not expose parameter text without sign-in. S14a OD-12 superseded that stance for one bounded 721061 response-row observation: omission returned partner codes and `partner2Code`, `motCode`, `customsCode` were each single-valued at their stored values. | The observed rows do not document general parameter semantics. V2 was not used because `partner2Code` was not multi-valued; other candidates remain not acquired. |
| KL-76 | Tariff-tree mapping remains unavailable, so R0 would be DEGRADED for valid HS6 records and cannot prove national-line identity. | W3 not opened; no tariff snapshot. |
| KL-77 | No stored span was asserted to establish a plant-family capability link. R9-S remains DISABLED without a verified Class-C link and unknown capability does not improve adjacency. | `data/screening/lists/plant-family-links-v1.json` contains zero entries. |
| KL-81 | Methodology §8.2(c) greenfield candidates are NOT_CALCULABLE at screening grain because R9-S assigns no D* and route 7 requires D* > 0.65. | The screening snapshot records `D_STAR_NOT_ASSIGNED_AT_SCREENING`; no screening result may invent D* or populate the greenfield queue. |
| KL-82 | Persistence-only CANDIDATEs are counted unqueued (135 in the current snapshot) with no invented materiality floor. | OD-4 preserves these R1-D-only records as candidates without assigning a named queue. |
| KL-83 | PR #21 hosted CI exposed machine-specific absolute input paths in the never-merged ScreeningSnapshot `311f105c4ccf`; reconstruction failed outside the originating checkout. | OD-15 supersedes it with portable snapshot `9b6b22032fd8`, requires repository-relative `data/`/`config/` manifest keys, and rejects absolute or parent-traversing screening input paths without fallback. |
| KL-52 | Duplicate `units` entries in `config/acquisition_sources.v1.yaml` are rejected by the write-once raw store at acquisition time rather than at configuration load (pre-existing S11 pattern, observed by the S12a independent reviewer). | Configuration is hash-pinned and reviewed under Manifest §7.3; fail-closed behaviour is preserved. Config-time validation may be added with a later acquisition slice. |
| KL-53 | S12a parser edge behaviours recorded by the independent reviewer as low severity: forbidden-key traversal does not descend YAML `!!omap` tuples (unreachable with the pinned configuration); `<th>` cell text is joined without a separator (specification-silent, covered by the bounded-guard clause); the cached kind-registry dict is shared-mutable (identical to S11). | Accepted without code change in S12a (`reviewer_findings.md`); none affects stored evidence, hashes or frozen outcomes. |
| KL-26 (data portion) | The engine exposes `NOT_CALCULABLE`, its reason, and the configured threshold. Gross imports are not coerced into apparent consumption. The §3.3 formulas and schema fields exist since S08; only the public inputs are `UNAVAILABLE` until S12 acquisition. |
| KL-29 | Successful config, public, synthetic, and golden loads are process-local `lru_cache` values. An approved on-disk correction is not observed until process restart or explicit internal cache clear. | The MVP uses immutable hashed local files and no write endpoint. The operator runbook requires restart after an approved artifact change; startup/restart is acceptance-tested. |
| KL-84 | The Decision Dossier rule ledger remains an English source-language island in Arabic output; S13b localizes the workspace, methodology and screening ledgers only. | The structural bilingual dossier redesign remains assigned to S19; no S13b label claims dossier-ledger parity. |
| KL-85 | UX-02 remains deferred: Screening is section 06 of the existing long single page rather than a routed multi-surface shell. | OD-3 retained the bounded S13b shell change. A routed-shell decision requires a later interface slice. |
| KL-86 | Screening records have 0/5,443 record-level `evidence_ids`. The eight UNIVERSE Class-B passports are delivered by `GET /api/screening/evidence`, rendered once in the persistent evidence area, and linked from records. Source access classification (`public_registered`) and the terms observation are not passport fields and the runtime does not import the acquisition registry. | The surface states this residual through `screening.evidence.passport_detail_not_exposed`; exposing registry-only metadata is a candidate later §7.2 change. |
| KL-87 | Screening adjacency is shown only through governed R9-S status/metrics; the real plant-family link list is empty, so capability remains unknown and never improves adjacency. | This preserves KL-77 and the unknown-capability penalty; S13b adds no plant or capability evidence. |
| KL-88 | The S13b canonical regeneration changed 24 of the 40 pre-existing visual entries: eight sidebar-only and sixteen sidebar plus integrity-authority; all sixteen dossier entries were byte-identical. | Measured masks are confined to `aside.sidebar` ∪ `.integrity-authority`; the owner accepted the 56-entry set in OD-15. |
| KL-89 | The Arabic technical-value grammar can admit ALL-CAPS English words as `code_token`, snake/dotted lowercase English as `catalogue_key`, and Title-Case-plus-digit strings as `governed_id`. | The grammar is only a technical filter. Content parity also requires EN-template equality and `label_leaks == []` against both governed catalogues; browser evidence records island counts by class. |

## S14a evidence-child limitations (open, not fabricated)

| ID | Limitation | Evidence / boundary |
|---|---|---|
| KL-90 | Public MONITOR is not reachable for the five selected S14 cases because each H6 2022–2024 series fires material R1-D. | Engine proof records INVESTIGATE/null for all five. No input or threshold is changed to manufacture MONITOR; the generalized branch remains covered by S09/S10 fixtures. |
| KL-91 | Public REJECT is not supported for the five selected S14 cases. | None establishes a satisfied hard exclusion or generic-capacity contradiction. Simulated outcomes belong to S14b and must be derived by computation. |
| KL-92 | The 2021 H5 row is excluded from every S14 brief; only H6 2022–2024 is projected. | `LATEST_REVISION_ONLY` prevents an ungoverned H5/H6 splice. R1-D still fires on three positive observed years. |
| KL-93 | Target-specification demand remains Class E / `UNAVAILABLE`. | Owner PR-2 keeps Etimad specification booklets behind supplier login/purchase outside S14a; no public booklet span is fabricated. |
| KL-94 | HS 721070 PPGI is not selected despite a stored UNICOIL disclosure because its 2024 universe import net weight is unavailable. | OD-2's ruled viability key excludes 721070, 721030 and 721050; no quantity is inferred from value. |
| KL-95 | The first WCO index consultation returned 200 but the in-memory predicate emitted no chapter hrefs. | A second bounded, disclosed index read produced verbatim Chapter 39/72/76 hrefs; no filename pattern was guessed. This is a recorded deviation from the planned single index read. |
| KL-96 | Tasnee and SPIMACO robots endpoints fail certificate-chain validation from this environment. | W-T made one verified request each with the system CA paths and recorded `CERTIFICATE_VERIFY_FAILED`; verification was not disabled and no document request followed. |
| KL-97 | Partner detail is OBSERVED from WITS for 392010/721012/760429/760711 (42/12/43/27 non-World rows) and from corrected UN Comtrade V3 for 721061 (7 non-World rows). | V3 returned unique partner descriptions and values reconciling exactly to the stored universe aggregate. The WITS HTTP-200 `Error.aspx`, original V1 and OD-18 untransmitted-variant repeat remain visible as superseded attempts. |
| KL-98 | Capability remains mostly unknown. HS 392010 and both steel/foil cases have no target-specific producer span; 760429 has only broad Class-C extrusion/profile evidence. | Dimensions stay `U` and profile gates `UNAVAILABLE` except 760429 core process state 0. ALUPCO/TALCO site labels lack a same-document company span within the frozen two-line entity window and are omitted from mentions-v2. |
| KL-99 | OD-11 excludes residual subheading 3921.90 from S14 selection because it does not resolve a product-specific decision object. | Hashed `identity_exclusions` row cites WCO Chapter 39 page 8 lines 36–41 and page 9 lines 3–4. The rule re-run selects 392010; runner-up is 391739. 760429 remains selected because the sibling 7604.21 separates hollow profiles. |
| KL-100 | **Closed by S14b:** version-gated PublicSnapshot 2.2.0 carries typed `partner_detail`; R3/R4-D, metrics and dossier distinguish OBSERVED, MISSING and ZERO. | Five builder-derived 2.2.0 snapshots, state-specific tests and the OD-16 second canonical regeneration; 2.1.0 records remain valid unchanged. |
| KL-101 | `PARTNER_TRADE_OBSERVED_ZERO` is producible only from a well-formed JSON provider envelope with clear error and `count == len(data) == 0`. | WITS HTML cannot distinguish a genuine zero from an unparsed page; its UNPARSED response remains MISSING/`FORMAT_NOT_PARSEABLE`. |
| KL-102 | The Comtrade partner snapshot is globally INCOMPLETE because 59 historical PARTNERS attempts remain `ENDPOINT_UNVERIFIED`, while its 721061 V3 unit is COMPLETE. | `PARTNERS-SAU-UN-COMTRADE-2026-09-13` uses the at-least-one-complete-with-exclusions policy, selects V3 and retains all historical exclusions and superseded runs. |
| KL-103 | The official v1 parameter table remains unobserved in this repository. | OD-16 states that `includeDesc` is documented; the corrected V3 rows establish operationally that `&includeDesc=true` populated all seven non-World partner descriptions. The first OD-18 command omitted it because of shell quoting and is not treated as an API outcome. |

## S14b portfolio-child limitations and carry-forward

| ID | Limitation | Evidence / boundary |
|---|---|---|
| KL-104 | MONITOR is not demonstrated by the five S14b cases. | Every public case fires material R1-D, so route-0 MONITOR's empty-material-trigger predicate is false; simulated ground truth also selects no MONITOR. The S09/S10 synthetic-free fixtures continue to prove the generalized branch. |
| KL-105 | Case projection `_scaled` rounds source values divided by 1,000,000 to six decimal places. Coarser three- or four-decimal projection breaks the 721061 partner/world reconciliation at the governed 1e-6 comparison. | `test_partner_scaling_six_decimals_preserves_721061_value_reconciliation`; six decimal places preserve the seven-partner sum within tolerance. This precision is a serialization constraint, not evidence of grade or quality. |
| KL-106 | `scripts/demo_smoke.py` and `scripts/final_acceptance.sh` still exercise the two original packaged cases rather than iterating all seven goldens. | S14b's full API, Gate B, pytest and browser matrices cover seven cases. The **S22 release-script backlog** item is to extend both release scripts to every golden before v0.3.0 final acceptance; S14b does not broaden release machinery. |
| KL-107 | KL-85 remains open: Screening is still section 06 in the long single-page shell, not a routed multi-surface experience. | OD-15 corrected the portfolio count/rule chip and Arabic decision-subject catalogue parity only; it did not authorize the deferred UX-02 shell redesign. |
| KL-108 | OD-15 truth/parity corrections required a second governed visual regeneration under OD-16. | Against W2, exactly eight portfolio WebPs changed inside the chip boxes and seven Arabic desktop public-workspace WebPs inside the decision-subject card; `DRIFT_FAILURES 0`, 76 manifest entries, 11,157,252 root bytes. |

## S12a institutional acquisition outcomes (open, not fabricated)

The 2026-09-11 bounded documentation consultations and operator invocations are recorded in `.workflow/slices/S12a-acquisition-framework-institutional-sources/implementation_log.md`. All five acquisition attempts made zero requests, stored no pages, have null observed response and INCOMPLETE coverage with ENDPOINT_UNVERIFIED; their sibling `coverage.json` matches embedded attempt coverage. Each kind build returns COVERAGE_INCOMPLETE, not FORMAT_NOT_PARSEABLE. These observations do not prove that the institutions lack public data. No institutional source-specific parser, normalized row or snapshot was invented.

| ID | Source / kind and limitation | Exact evidence / next requirement |
|---|---|---|
| KL-47 | `gastat` / production: public methodology/use-policy pages were consulted, but no dataset endpoint or unit contract was verified; the 2025 IPI reference year is not a physical production or retained-flow series. | `data/raw/gastat/e7cb218115c5b69b8341bbae20e35d056fd55e67283bea3b7c6c1b7695eec81d/20260911T215546Z/attempt.json`; YEARS=2025, MAX_REQUESTS=2. Verify actual dataset endpoint, units and applicable terms in a separately authorized run; R5 live inputs remain unavailable. |
| KL-48 | `ministry_of_industry` / directory: bounded official-site documentation checks timed out or failed connectivity; controlled data-sharing descriptions did not establish a public directory contract. | `data/raw/ministry_of_industry/b5fd807aa458a97d4eae2dbb072075614f7061e77d3e130ffea85f46fb22673f/20260911T215956Z/attempt.json`; period-free, MAX_REQUESTS=1. All seventeen source facts remain UNAVAILABLE; verify the directory contract without treating a licence as production. |
| KL-49 | `modon` / directory: official-site documentation checks failed certificate-chain verification; TLS verification was not disabled and historical extracts were not promoted to a current contract. | `data/raw/modon/94801a4f5e4b99c963baca7ca892d508b7531bc01853205c4b950ec2b0480135/20260911T215956Z/attempt.json`; period-free, MAX_REQUESTS=1. All seventeen source facts remain UNAVAILABLE; verify current directory access under normal TLS in a separately authorized window. |
| KL-50 | `saso_catalogue` / registry: official open-data references were consulted, but API documentation was unreachable and no metadata endpoint/unit was verified; applicability of portal versus source-specific reuse terms remains unresolved. | `data/raw/saso_catalogue/9529b34e0e2b6c2c39cdc9dfa404bf9b5b96c6f6b6de821a4281e928b8b07b49/20260911T220134Z/attempt.json`; period-free, MAX_REQUESTS=2. Access classification remains UNAVAILABLE; no paid standard or compliance fact acquired. |
| KL-51 | `saber_registry` / registry: platform registration/terms were observed (`public_registered`), but no public product/certificate enumeration endpoint, unit or API credential contract was verified. | `data/raw/saber_registry/e20dac0cf35c1b06bc91836d25f7e5ca0670fb5b764f4af5adfb90289aa51be3/20260911T220134Z/attempt.json`; period-free, MAX_REQUESTS=2. Credential sentinel remains UNAVAILABLE, not an invented bearer credential; registration never proves buyer qualification. |

The T7 live window is closed. DIRECTORY/REGISTRY text-only screening is a bounded field-label policy (Core 05 §10), not universal personal-data detection. Future PersonalDataFields/UninspectableTextPayload refusals would be OUT_OF_SCOPE_CONTENT with hash-only response metadata and no retained refused body; PDF/XLSX and non-UTF-8 bodies cannot enter the DD-15(b) retained-text parser path. None of those privacy refusals occurred in these zero-request runs. The pre-existing RunReport aggregate-count issue remains deferred without changing these emitted zero counts or asserting general equality between report counts and transport calls.

## S12b document acquisition outcomes (open, not fabricated)

The 2026-09-12 bounded T7 consultations and operator invocations are recorded in `.workflow/slices/S12b-document-store-disclosures-tenders/implementation_log.md`. `-v1`/`-v2` lists and their zero-request runs are retained as history. T7-v3 (slot 3, SR-06) built twelve COMPLETE DocumentRecords for `saso_documents` and `producer_unicoil`; five other document sources remain without records. The independent implementation review (S12B-IR3-F01/F02) led to the slot-4 correction round: the six SASO records were rebuilt from the declaration-corrected `saso_documents-v4` list against the same stored run (KL-55), the visual-order Arabic fact is disclosed (KL-64) and the RunReport aggregate-count artefact is named (KL-65). OD-13 then authorized slot 5 to rebuild all twelve records offline after correcting per-page hashes, physical-page preservation and latest-run supersession; raw runs and document ids remain unchanged.

| ID | Source / limitation | Exact evidence / next requirement |
|---|---|---|
| KL-54 | **Resolved (slot 3):** `producer_unicoil` — six COMPLETE records from list `producer_unicoil-v3` (SHA-256 `1115ff78937738b210d1725a6c550459dbdeca37a637759aab2892dd31f89d65`); run `20260912T053955Z`. Prior v2 `LICENSE_UNRECORDED` attempts retained under `20260912T052309Z`. | `data/documents/producer_unicoil/records/` (6 files); `data/raw/producer_unicoil/*/20260912T053955Z/`. |
| KL-55 | **Resolved (slot 3; records replaced slot 4 under OD-11 and rebuilt slot 5 under OD-13):** `saso_documents` — six COMPLETE records from run `20260912T053622Z` (acquired under list `saso_documents-v3`, SHA-256 `8a12aca74e38a53b8f3f83f8a148b92f653bce73c382260322e9ef837b1b26f6`; terms from SASO acceptable-use policy). The v3 entries declared `languages: ["en"]` for `/ar/` PDFs whose derived text is Arabic (S12B-IR3-F01); slot 4 rebuilt the records offline from corrected declaration list `saso_documents-v4` (SHA-256 `16d9e29bd86f720c38a29ffcd7a24e5140c94a2893226fac7f67183a3a4338bb`; same URLs, order and verbatim texts; `languages` `["ar","en"]` for E-001/E-003/E-004/E-006 and `["ar"]` for E-002/E-005). Slot 5 then rebuilt them again from the same raw run after OD-13 corrected physical-page preservation, newline-joined page hashes and superseded-run recording. The six `document_id`s remain unchanged; current `pages` reflect the corrected derivation contract. v1/v2/v3 lists and prior v2 attempts remain history. | `data/documents/saso_documents/records/` (6 files, `list_ref.list_id = saso_documents-v4`); `data/raw/saso_documents/*/20260912T053622Z/`; ADR-017 correction rounds. |
| KL-62 | `producer_unicoil`: publisher privacy policy (`https://www.unicoil.com.sa/privacy-policy/`, HTTP 200) states PDPL personal-data handling only; no document reuse or download licence terms were observed. Config follows ZATCA precedent: `access_classification: public_open`, `terms_reference: UNAVAILABLE`, `license_capture_required: false`. | Consultation 2026-09-12T05:35:10Z; ADR-017 T7-v3. |
| KL-56 | `producer_sabic`: landing page HTTP 200; v2 consultation found no qualifying PDF within the 16 MiB window (`producer_sabic-v2` entries `[]`). | `data/raw/producer_sabic/9b661d9033f799e8…/20260912T052310Z/attempt.json`; ENDPOINT_UNVERIFIED; navigate investor-relations/product pages in a new window. |
| KL-57 | `etimad_tenders`: portal HTTP 200; v2 list empty after public listing inspection (`etimad_tenders-v2`). | `data/raw/etimad_tenders/b10afba6…/20260912T052309Z/attempt.json`; ENDPOINT_UNVERIFIED. |
| KL-58 | `tadawul_disclosures`: `saudiexchange.sa` returned HTTP 403 on consultation; v2 list empty. Slot-1 recorded duplicate zero-request runs `20260912T050943Z` and `20260912T050946Z` three seconds apart without a logged reason (retained write-once evidence). | `data/raw/tadawul_disclosures/83faa784…/{20260912T050943Z,20260912T050946Z,20260912T052309Z}/attempt.json`; ENDPOINT_UNVERIFIED. |
| KL-59 | `producer_tasnee`: no TCP connection to `tasnee.com` during consultation (DNS resolves). | `data/raw/producer_tasnee/eb5a146f…/20260912T052310Z/attempt.json`; ENDPOINT_UNVERIFIED. |
| KL-60 | `producer_advanced_petrochemical`: no DNS resolution for `advancedpetrochem.com`. | `data/raw/producer_advanced_petrochemical/ab31790e…/20260912T052310Z/attempt.json`; ENDPOINT_UNVERIFIED. |
| KL-61 | Document text-layer derivation is verbatim-only: `pypdf==6.16.1` layout mode for PDF, stdlib HTML/plain parsers, `LINE_SEGMENTATION_V1`; no OCR, normalisation or runtime-image dependency. Documents without parseable text remain `RAW_ONLY` with `text_layer.status=UNAVAILABLE` / `FORMAT_NOT_PARSEABLE`. | Core 04 DocumentRecord 1.0.0; ADR-017 DD-4/DD-5/DD-21. Widening envelopes or adding OCR requires a governed change. |
| KL-63 | Real publisher PDFs acquired in T7-v3 may exhibit `pypdf` layout-mode spacing artefacts (extra whitespace between tokens, rotated-text warnings on some UNICOIL EPD PDFs; one `Rotated text discovered` warning was emitted during the slot-4 offline rebuild of the six SASO records). Derived lines are stored verbatim from layout extraction; no normalisation is applied. Current records re-derive byte-identically under the OD-13-corrected page contract. | Observed on live `saso_documents` and `producer_unicoil` records; ADR-017 OD-6/OD-13. |
| KL-64 | **Visual-order Arabic (S12B-IR3-F01, OD-12):** `PDF_TEXT_LAYER_PYPDF_LAYOUT 1.0.0` stores the PDF content-stream text order verbatim. The six SASO publisher PDFs paint Arabic in visual (left-to-right glyph) order, so the stored Arabic lines of `DOC-SASO-DOCUMENTS-641a518f410a-1bfd6143aaa9`, `DOC-SASO-DOCUMENTS-69ea78b9cd4e-000691d40661`, `DOC-SASO-DOCUMENTS-710bf26c140e-b25658901d54`, `DOC-SASO-DOCUMENTS-c778364b4087-b89ca084a45d`, `DOC-SASO-DOCUMENTS-d5e242b67294-9189ec73295b` and `DOC-SASO-DOCUMENTS-e57d73b6e5b7-0e0d00cb00cf` are visual order, not logical reading order — e.g. the authority-name cover line is stored as `ةدوجلاو سيياقلماو تافصاوملل ةيدوعسلا ةئيهلا` (character reversal reads `الهيئة السعودية للمواصفات…`; lam-alef ligature glyphs also surface as `امل`). `text_layer.status = COMPLETE` / `quality_summary = PASS` denote a complete extracted text layer only. Logical order is guaranteed only where the PDF's ToUnicode mapping yields it (CID fixture `test_double_arabic_cid.pdf`, `test_arabic_cid_pdf_preserves_logical_order_verbatim`). No bidi reordering or reshaping is applied (plan non-goal; arabic-reshaper/python-bidi not installed). | Searches for logical Arabic strings and bidi-aware rendering must not treat these lines as logical text; logical-order recovery and any Arabic normalisation belong to S20 normalisation, recorded as a versioned transformation. Pinned by `tests/test_acquisition_document_records.py`. |
| KL-65 | **RunReport aggregate-count artefact (S12B-IR3-F02; deferred from S11/S12a):** `RunReport.requests_made` for run `20260912T053622Z` is 27 and for `20260912T053955Z` is 21 because `pipeline._run_units` sums the per-unit `coverage.requests_made`, each of which echoes the cumulative shared `RequestBudget.used` at that unit's completion (SASO 2+3+4+5+6+7; UNICOIL 1+2+3+4+5+6). The stored page contracts prove 7 transport fetches for the SASO run (1 TERMS `text/html` + 6 `application/pdf`; `MAX_REQUESTS` 7) and 6 for the UNICOIL run (6 `application/pdf`; `MAX_REQUESTS` 6; `license_capture_required: false`). Neither run exceeded its budget; the figures are not 27/21 HTTP requests. The counting logic and emitted T7 coverage records are unchanged; slot 5 only fixes `pipeline.acquire_documents` to pass the DocumentList into minimum-request accounting. | `data/raw/saso_documents/*/20260912T053622Z/` (7 payloads), `data/raw/producer_unicoil/*/20260912T053955Z/` (6 payloads); pinned by `test_run_report_requests_made_is_aggregate_count_artefact_not_transport_count`. A governed coverage-count change remains deferred. |
| KL-66 | **HTTP-wrapped publisher PDF (S12B-IR5-A07):** `DOC-PRODUCER-UNICOIL-cec488bffcda-3017de71d962` stores publisher bytes beginning with an `HTTP/1.0 200` header block before `%PDF-1.4`. The no-sniffing policy preserves the payload exactly as received; `pypdf` parses the embedded PDF with an invalid-header warning and byte-exact reconstruction passes. | Consumers must not assume `%PDF-` starts at byte zero. The raw payload and derived record remain unchanged; ADR-017 records the accepted observation. |

The T7-v3 correction window closed 2026-09-12 05:40:21 UTC. Duplicate v2 acquire timestamps for `producer_unicoil` (`20260912T052305Z`/`052309Z`) and `saso_documents` (`052304Z`/`052309Z`) reflect a repeated Makefile invocation during the v2 window after the first pass logged `LICENSE_UNRECORDED`; both runs are retained. Duplicate slot-1 tadawul runs (`20260912T050943Z`/`050946Z`) remain cited in KL-58.

## S12c entity-resolution outcomes (open, not fabricated)

| ID | Limitation | Evidence / boundary |
|---|---|---|
| KL-67 | No deterministic identifier was observed in the acquired evidence; rank-1 identifier equality is proved only by doubles. No S12a institutional row exists, so no licence holder is minted. | Real artifact: `DETERMINISTIC_IDENTIFIER` 0 and `LICENCE_HOLDER` 0; `data/entities/resolution/ENTITIES-2026-09-12-a12e24c31b02.json`. |
| KL-68 | The two glyph-encoded UNICOIL verification statements have no readable governed name span and their passports remain `UNRESOLVED` / `NO_MENTION_RECORDED`. | `DOC-PRODUCER-UNICOIL-7d21f605fc4e-6eb00a1886d0` and `DOC-PRODUCER-UNICOIL-9cf950950e95-6217c780a89a`; deliberate omission in `mentions-v1`. |
| KL-69 | SASO is a standards authority outside the four entity namespaces; six document passports remain `UNRESOLVED` / `OUT_OF_SCOPE_ENTITY_TYPE`. Visual-order Arabic spans are retained, not reversed. | `DOC-SASO-DOCUMENTS-641a518f410a-1bfd6143aaa9`, `DOC-SASO-DOCUMENTS-69ea78b9cd4e-000691d40661`, `DOC-SASO-DOCUMENTS-710bf26c140e-b25658901d54`, `DOC-SASO-DOCUMENTS-c778364b4087-b89ca084a45d`, `DOC-SASO-DOCUMENTS-d5e242b67294-9189ec73295b`, `DOC-SASO-DOCUMENTS-e57d73b6e5b7-0e0d00cb00cf`; M-021…M-027. |
| KL-70 | Jurisdiction and Arabic primary names are `UNAVAILABLE` for all seven real entities because no cited span establishes them. | DD-1 observed-attribute rule; artifact entity rows. |
| KL-71 | Hadeed, SABIC, Advanced Petrochemical and Tasnee are known only by the short labels stored in frozen snapshots; no legal-name expansion is inferred. | M-030/M-033/M-034/M-035 and matching producer observations. |
| KL-72 | Pre-2004 ownership was not harvested: the 1997 founding-shareholder sentence was superseded for current ownership and contains a personal name. | OD-7 deliberate omission; one retained 2004 unnamed-owner record cites M-004/M-012. |
| KL-73 | M-009, M-017 and M-020 remain reviewer-pending rather than guessed matches. | `LOCALITY_VARIANT`, `SUBJECT_OUTSIDE_WINDOW` and `VARIANT_NAME_EQUALITY` respectively; no pending link carries an entity id. |

Future-use template copied verbatim from the approved privacy policy (not an actual T7 refusal; fill only from a future observed refusal record):

```text
<source/query_hash/run_id>: OUT_OF_SCOPE_CONTENT:UninspectableTextPayload. Response refused before storage by S12a text-only policy. No payload retained; DD-15(b) offline parsing unavailable. Evidence: <attempt path/status/body SHA-256/byte count>. Format/encoding support requires a separate governed change. Do not call this FORMAT_NOT_PARSEABLE; that code describes stored UNPARSED build refusal.
```
